# 2.7. Variants и состояния — 11:53

Закрываем пробел уроков 5–6: hover, disabled, loading, focus и размеры. Директива `component_variants`.

| Тайм-код | О чём |
| --- | --- |
| 0:00 | Зачем сейчас: по этим состояниям в четвёртом модуле будет собираться прототип — агент поймёт, что к чему. Прототипы в самой Figma при этом не нужны |
| 0:50 | **Два режима**: `component_variants <Component>` — точечно, или без аргумента — bulk по всем компонентам с неполной матрицей |
| 1:00 | В bulk агент читает `components.md` и **даже не заходит в Figma**, чтобы найти дырявые матрицы |
| 1:46 | Четыре фазы: read (каталог + сам компонент в Figma) → brief → апдейт ДС с добавлением недостающих семантических токенов → отрисовка вариаций |
| 2:40 | **Реалистичные ожидания**: 3 варианта × 4 состояния = 12 ячеек — стабильно, обычно без правок |
| 3:00 | Плывёт от 25 ячеек (5 вариантов × 5 состояний); директива сама предложит раздробить на итерации |
| 3:27 | Сложные анимации и кольцо загрузки здесь не отрисовываем — поведение опишем при прототипировании |
| 3:40 | В брифе агент делит рекомендации по приоритету: критические / низкий / skip — и сразу называет новые токены (`surface-action-primary-hover` и т.п.) |
| 5:14 | Финальный аудит директивы: ноль непривязанных обводок, заливок и текстов — всё через переменные и текстовые стили; отступы тоже проставлены токенами |
| 5:40 | Результат на Button: три типа (ghost, secondary, primary) × состояния (hover, disabled, loading, focus) = 15 ячеек |
| 6:56 | Card получила ось Type: static (не реагирует на hover) и interactive. Tabs — состояние hover, Dropdown и Select — error, hover, disabled |
| 7:40 | После появления новых компонентов достаточно сообщить о них — агент допишет `components.md`, и следующий прогон сам увидит дырявые матрицы |
| 8:37 | Как указать конкретный компонент: имя из `components.md` или Copy Link to Selection сразу после вызова директивы |
| 9:00 | **Размеры — третья ось той же матрицы**, задаётся на фазе брифа, отдельного механизма нет. Агент может сам решить, что размеры не нужны — тогда попросить явно |
| 9:40 | Размеры чаще нужны атомарным элементам (кнопки, иконки), реже карточкам, почти никогда tooltip и badge |
| 10:20 | **Когда через директиву**: работы много, но она банальная — состояния кнопок и инпутов, с этим справится и джун |
| 10:40 | **Когда руками**: один вариант, точечная правка одной ячейки — не тратить лимит MCP и не ждать |
| 11:00 | Анимации внутри Figma не делаем: агент не умеет создавать прототипы в Figma, и это не нужно — прототип будет в вебе |
| 11:20 | Домашка: прогнать минимум три компонента, добавить вариации по размеру или состоянию |
