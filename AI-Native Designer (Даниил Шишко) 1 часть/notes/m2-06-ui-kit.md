# 2.6. UI-кит — расширяем библиотеку — 13:32

Первое реальное применение контракта: нет компонента — сначала заводим его в ДС. Директива `grow_ui_kit`.

| Тайм-код | О чём |
| --- | --- |
| 0:00 | На входе — стартовые компоненты или каталог после сканирования; на выходе расширенный набор на той же ДС, без параллельных копий и прибитых цветов |
| 0:50 | **Пять фаз**: read → brief → md → canvas → write + audit |
| 0:50 | Read: агент читает `ds/components.md` и держит каталог в контексте. Без чтения каталога он дублирует то, что уже есть |
| 1:46 | Про свои ручные правки нужно сообщать, указывая конкретные фреймы — тогда он допишет их в `components.md` дешевле |
| 2:00 | Brief: агент сам предлагает, каких компонентов не хватает под функционал из wireframes, плюс список необязательных |
| 2:20 | Md-фаза: анатомия, вариации, состояния, отступы. Только потом `use_figma` на канвас — можно сразу указать секцию ссылкой |
| 2:40 | Аудит вынесен из автопрогона: он не всегда нужен, а ждать приходится |
| 3:26 | **Важная мысль урока**: не надо давить агента до талого. Правку высоты руками делаешь за 5 секунд, а спор с агентом съест время и забьёт контекст мусором. Для части задач уже есть готовые решения |
| 4:20 | Реалистичные ожидания: одиночные компоненты с прозрачной анатомией (tooltip, toast, breadcrumb, pagination) — каркас надёжно с первого прогона |
| 5:07 | Наборы со сложной матрицей (таблицы, организмы) — 70% каркас, 30% доводка руками, минут на 15 |
| 5:30 | Варианты начинают плыть после 7 и более, если они сильно разные; 2–3 варианта — стабильно |
| 5:50 | **Рычаги модификации директивы**: свой список базовых элементов вместо дефолтного; явное требование задавать spacing-токенами все паддинги и отступы |
| 6:47 | Флаги: `skip audit` для быстрого прогона, `grow_ui_kit one tooltip` для одного компонента, точки остановки между фазами — чтобы обсудить структуру с командой |
| 7:00 | Артефакт: агент разместил новые компоненты поверх существующих на той же странице — указание «работай на странице компонентов» он понял буквально |
| 7:30 | Разбор Table: две вариации (компактная и дефолтная), стили и скругления проставлены; ширины колонок правятся руками за минуту, дальше hug и новые строки работают |
| 8:27 | Дальше по компонентам: Tabs, Tooltip, Dropdown, Select, Toast, Breadcrumb, Pagination. **Сквозной дефект — обрезанная высота, лечится hug** |
| 10:13 | Для Breadcrumb и Pagination агент сам собрал демо-примеры использования |
| 10:40 | Демонстрация синхронизации: автор прервал прогон и сообщил, что развёл Foundations и Components по разным страницам — агент это учёл при размещении |
| 11:53 | Запись в каталог: тип, состояния, node id, краткое назначение («контекстное меню, кебаб-триггер, панель опций») |
| 12:20 | Отдельный аудит на консистентность запускать, только если видно расхождение с ДС; мелочи быстрее поправить самому |
| 12:40 | Домашка: прогнать `grow_ui_kit`, дать агенту самому оценить, чего не хватает, поправить руками или отправить на аудит, прислать ссылку на файл |
