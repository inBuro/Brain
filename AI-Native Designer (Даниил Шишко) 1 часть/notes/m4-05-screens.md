# 4.5. Сборка экранов — 6:33

Директива `screens` переносит макет в код как есть. Держимся статики: переходы и данные — следующий урок.

| Тайм-код | О чём |
| --- | --- |
| 0:00 | **От чего зависит результат**: агент не рисует экран заново, а переносит макет. Насколько аккуратно собран макет — настолько аккуратен код |
| 0:40 | Чистый макет (Auto Layout, компоненты базы) → гибкий экран, который тянется по ширине и ведёт себя предсказуемо |
| 1:00 | Макет «на глаз» с ручными отступами → тот же беспорядок, только теперь в коде |
| 1:16 | Можно попросить ревью перед переносом, но тогда результат разойдётся с макетом |
| 1:20 | **Наблюдение по курсовым**: у тех, кто собирает на больших готовых базах вроде shadcn, компоненты на экранах часто отвязаны. Правильнее поправить сам компонент в базе, чем отвязывать на экране |
| 2:00 | При отвязанных компонентах остаётся надеяться, что агент соотнесёт элемент с компонентом по имени |
| 2:29 | **Директива — постоянный инструмент**: понадобился новый экран — запускаешь снова со ссылкой на другой фрейм, она добавляет страницу и подключает к остальным |
| 3:39 | Результат: навигационная структура со всеми собранными экранами, модалки и их появление уже работают |
| 4:20 | Совет: собирать по одному экрану и проверять, а не все разом — тогда на следующем шаге ничего не поедет |
| 4:52 | **Почему не собрать сразу с переходами**: контекст про экраны, их взаимосвязи и переходы получается слишком объёмным, и что-то теряется |
| 5:20 | Общее правило автора: большая задача дробится на маленькие. Взаимосвязи строит отдельная директива, и при добавлении новых экранов её повторный вызов проверит, куда добавить переходы |
| 6:02 | Домашка: минимум один экран (лучше все три из второго модуля), скрин с видимой ссылкой в браузере |
