# 2.5. Ставим дизайн-систему — три сценария — 33:54

Самый плотный урок модуля. Две директивы: `ds_baseline` (сценарий A) и `ds_scan` (сценарии B и C).

| Тайм-код | О чём |
| --- | --- |
| 0:00 | Три сценария: A — ДС нет, ставим с нуля; B — берём готовую из Community; C — в компании уже есть своя |
| 1:00 | ДС в Figma = слой Variables + компоненты, которые на них ссылаются. Не три разрозненных артефакта, а одна структура |
| 1:43 | Слой 1, примитивы: палитра без смысла в продукте — Blue 500, Gray 100 |
| 1:50 | Слой 2, семантика: `surface-action-primary` ссылается на примитив через alias. **В семантике не пишем hex** — палитра почти не меняется, а семантику перепривязываем, ради этого она и существует |
| 3:00 | Слой 3, компонентные токены: нужен, только если у компонента своя тонкая логика; на стартовых семи компонентах его нет |
| 4:00 | Как выбрать сценарий — по тому, кто контролирует ДС |
| 5:08 | A подходит, когда кроме тебя с ней никто не работает: стартапы, микрокейсы, обучение. Плюс — нет лишних компонентов, агент не захлёбывается контекстом |
| 5:30 | B — когда работаете командой: если каждый плодит компоненты своим агентом, появляется рассинхрон, а бесшовно синхронизировать это практически невозможно |
| 6:53 | Что зашито в baseline: 1 акцентный цвет, 9 нейтралов, 4 функциональных (success, warning, error, info), типошкала на 9 размерах Inter, радиусы 0/4/8/16, отступы с шагом 4 и 8 |
| 7:30 | Фаза брифа: агент заполняет её сам из контекста проекта, тебе остаётся подтвердить |
| 8:47 | Артефакты: `ds/brand_brief.md` и `ds/foundation.md` с конкретными значениями |
| 9:30 | После перезапуска среды MCP отваливается — переподключение и повторная авторизация |
| 10:00 | Итог прогона: 49 примитивных переменных, 25 семантических, фундамент-секция и 7 базовых компонентов |
| 10:32 | **Ключевая механика — `ds/components.md` с node id.** Агент держит не саму ДС, а её компактную карту: имя, назначение, варианты, id. При сборке читает карту, решает и вызывает `use_figma` по конкретному id |
| 12:13 | Ещё карта учит агента образцу: видя структуру и привязки к семантике, он не может собрать новый компонент с рандомными отступами и прибитым цветом |
| 12:40 | Артефакт прогона: агент положил фундамент и компоненты в секцию к sitemap. Так быть не должно — перетащить руками **и обязательно сообщить агенту** |
| 13:54 | Что на странице Foundation: палитра, семантика (border-default, text-muted, surface-action-primary), типографика стилями heading/body/label, радиусы, спейсинги |
| 15:36 | Правки после прогона минимальные: где-то fill вместо фикса, где-то авто-спейсинг — вкусовщина. Отступы внутри кнопки тоже стоило бы задать переменными |
| 17:16 | Итог сценария A: три фазы — бриф из пяти вопросов → `foundation.md` (учитывает кириллицу, если сказать в брифе) → запись в Figma |
| 18:57 | **Библиотеки для сценария B**: shadcn/ui (веб, React, самая актуальная, есть свой MCP), Material 3 (кроссплатформенная, но очень большая — агенту тяжело), Carbon (B2B), Gravity UI (российская), Relume (блоки для лендингов) |
| 20:39 | Порядок: Community → Duplicate в свои драфты → **копировать ссылку именно на драфт**, не на исходник (к исходнику нет полного доступа) → запустить `ds_scan` |
| 21:30 | `ds_scan` создаёт `source.md`, `foundation.md`, `components.md`, `CONTRACT.md`, дополнение в `CLAUDE.md` |
| 22:20 | **Скан читает только верхний уровень страницы компонентов** — на ДС в 300 компонентов агент иначе захлёбывается |
| 23:00 | Где работает прилично: фундамент и токены, переменные Figma, простые компоненты с говорящими именами и заполненной матрицей вариантов, типовые экраны — 70–80% с первого раза |
| 24:06 | Где сыпется на корпоративных ДС: неочевидные имена (badge / chip / tag); старое вперемешку с новым — агент берёт первое попавшееся и не оценивает актуальность; паттерны композиции, которых нет в библиотеке как сущности; ДС, разбитая по нескольким файлам; фиксированные размеры вместо hug/fill |
| 26:00 | Лечение: гигиена в файле, описания у компонентов, архив отдельной страницей; можно комментировать компоненты прямо в `components.md` и завести `patterns.md` — хоть скринами, хоть словами |
| 27:33 | **Кириллица в чужой ДС**: проверить, что шрифт отображает символы; межстрочный выше на 3–5%; межбуквенный чуть больше — на крупных заголовках кириллица плотнее; русские слова длиннее английских на 15–20% |
| 28:20 | **`ds/CONTRACT.md` — то, без чего всё остальное декорация.** Компоненты только через UI-кит, цвета только через семантику, нет компонента — сначала заводишь его в ДС, нет токена — заводишь переменную с alias и дописываешь `foundation.md` |
| 29:14 | Механизм синхронизации: ручные правки нужно проговаривать агенту, он сверит и внесёт в `foundation.md`. `CLAUDE.md` держит архитектуру, чтобы новая сессия сразу понимала правила |
| 29:40 | Типовая ошибка сценария A: агент отходит от ДС и начинает изобретать, когда переполняется контекст — держать в узде |
| 30:30 | Ошибка сценария B: просишь поменять мелочь — он перекапывает всю систему. Формулировать точечно |
| 30:56 | Ошибка сценария C: закинуть ссылку на весь корпоративный файл. Скармливать изолированно и по частям, начиная с Foundation |
| 32:39 | Домашка: сценарий A по умолчанию; для B нужна подписка минимум max 5x; для C достаточно скормить foundations и несколько ключевых компонентов |
