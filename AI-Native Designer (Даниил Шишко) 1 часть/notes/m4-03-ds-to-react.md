# 4.3. Из дизайн-системы в React: проект и компонентная база — 7:28

Директива `react_base` превращает ДС в код. Зеркало держится на общих именах.

| Тайм-код | О чём |
| --- | --- |
| 0:00 | Источник истины один — дизайн-система: Figma плюс папка `ds/` с `foundation.md`, `components.md` и `CONTRACT.md` |
| 1:00 | Как ложится одно на другое: переменная Figma → токен с тем же именем, компонент Figma → React-компонент с теми же вариантами |
| 1:12 | Зачем: общее имя позволяет агенту быстро ориентироваться по всей базе |
| 1:30 | **Главный вопрос — поменял в Figma, что в коде?** Ничего. Автоматической синхронизации нет и быть не может, нужно сообщать агенту. Чем конкретнее скажешь, что изменил, тем быстрее |
| 2:00 | Работает и в обратную сторону: собрал компонент сразу в React — можно попросить добавить его в Figma |
| 2:10 | Признание автора: Figma почти перестал пользоваться, собирает продукты сразу в React с агентом |
| 2:24 | **Если база опенсорсная** (shadcn и подобные): у библиотеки есть готовая React-реализация — агент просто подключает её. Сообщить нужно только про свои кастомизации цветов и семантики |
| 3:20 | Если база своя — агент собирает компоненты сам по описанию из `ds/` |
| 3:35 | Запуск директивы `react_base`, дальше предлагается посмотреть результат на локалке |
| 3:50 | **Что такое локалка**: локальный сервер, который симулирует живой сайт только для тебя. Ссылку другому человеку переслать нельзя — у него её не будет |
| 4:47 | Результат прогона: страница с foundation, списком компонентов, базовыми ховер-эффектами |
| 5:00 | Что здесь ещё не сделано: логика, переключение страниц и табов, переходы по ссылкам — только компонентная база |
| 5:58 | **Приём**: `Ctrl+Shift+P` → Quick Open Browser Tab → New Integrated Browser Tab — прототип открывается прямо внутри VS Code, окна менять не нужно |
| 6:30 | Ограничение проверки: на одной странице не увидишь каждый компонент во всех состояниях, и разработчику такое не отдашь — для этого Storybook |
| 7:14 | Домашка: прогнать директиву, визуально проверить перенос, приложить скрин локалки |
