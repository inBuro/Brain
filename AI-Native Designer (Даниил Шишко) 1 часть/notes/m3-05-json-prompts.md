# 3.5. JSON-промпты для серий и воспроизводимости — 19:07

Перевод промпта из фразы в структуру. Стилевые поля фиксируем, меняем только объект — так держится серия.

| Тайм-код | О чём |
| --- | --- |
| 0:00 | План урока: директива `brand_visual_direction` → конструктор JSON → серия в Nano Banana → сравнение с обычной фразой |
| 0:50 | **Важная оговорка про модуль**: здесь не строгий пайплайн, как в прошлых модулях, а набор знаний под курсовой |
| 1:35 | Что даёт JSON: информация дробится на категории, отдельные куски меняются быстро и точечно |
| 1:50 | В конструкторе блоки можно добавлять, переименовывать и **менять порядок** — приоритет всегда у начала промпта |
| 2:30 | Кнопка «шпаргалка»: справочник по стилю, освещению, камере и объективу — главным параметрам стилистики |
| 3:12 | Готовый JSON копируется целиком в генератор |
| 3:30 | Запуск директивы: агент читает продукт и палитру и проводит бриф; можно приложить своё изображение-референс |
| 4:46 | Наблюдение автора: агент склонен подсыпать неон, а слово «неон» почти всегда даёт колхозный результат — промпт надо перечитывать |
| 5:20 | Результат директивы — `brand-direction.md` с готовым блоком style / mood / lighting / camera |
| 6:19 | Перенос блоков в JSON-конструктор один к одному, затем добавляется поле `object` — дальше меняется только оно |
| 7:00 | Генерация в Gemini: выбрать оплаченный аккаунт, режим создания изображений, **явно назвать модель** — по умолчанию он не всегда берёт нужную |
| 7:52 | Нюанс: после первой генерации в конкретной модели последующие идут в ней же |
| 8:30 | Честная проверка воспроизводимости — новый чат без контекста предыдущих генераций |
| 9:24 | Что совпадает от кадра к кадру: угол освещения, палитра, тень, текстура бумаги, контрастная типографика — серия держится |
| 10:57 | **AI-режим конструктора**: работает через OpenRouter, ключи бесплатные |
| 11:30 | Почему на OpenRouter есть бесплатные модели: провайдеры выкладывают новые модели на время, чтобы обкатать. Найденная сегодня бесплатная завтра может исчезнуть |
| 12:28 | Как выбирать модель: фильтр prompt pricing = free, читать описание. Под генерацию промпта берётся модель для агентских задач, а не для программирования |
| 13:58 | Получение API-ключа: **скопировать сразу**, второй раз показать его нельзя — иначе пересоздавать |
| 14:40 | Настройки режима: под какую нейросеть пишем, язык вывода, описание объекта, свой стилевой блок в конец |
| 15:30 | **Ограничение по символам — 2500 как верхняя планка.** Чем длиннее текст, тем меньшую его часть модель возьмёт: сфокусируется на начале, остальное может проигнорировать |
| 16:30 | Итог AI-режима: структура собирается сама, остаётся менять `object` |
| 17:04 | По формату ввода модель сама понимает, что нужно генерировать изображение — просить явно не обязательно |
| 17:40 | Разница подходов: JSON от директивы даёт максимально похожие кадры, AI-режим конструктора — ту же стилистику, но композиционно разнообразнее |
| 18:45 | Домашка: свой JSON-промпт для бренда и три разных объекта в одной стилистике |

Ссылка из урока: `fluid.fyi/prompt_constructor.html` — конструктор JSON-промптов.
