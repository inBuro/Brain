---
type: concept
project: Novation
created: 2026-04-27
updated: 2026-04-27
status: stable
tags: [lcxl, midi]
---

# Custom Modes Model

Launch Control XL MK3 хранит до **16 пользовательских режимов** (custom-modes 1..16) во внутренней памяти. Каждый режим — самостоятельный layout: какие физические энкодеры/слайдеры/кнопки шлют какие CC и ноты, какая подсветка.

## Как переключается режим

LCXL принимает и отдаёт смену режима как контрол-сообщение **CC30 на канале 7**. Значение CC30 = `5 + N` для custom-mode N (т.е. mode 1 → value 6, mode 11 → value 16, …) — но фактический маппинг в `XL_Performance` сделан под зарезервированную «дыру» под mixer-modes 11–14, см. [[Mode Encoding]].

Подсветка кнопок выбора режима в самом LCXL также реагирует на тот же CC30 — поэтому, если входящий CC30 не отфильтровать, можно случайно поймать петлю «устройство шлёт mode → LCXL подсвечивает → шлёт обратно». В патче эта петля закрывается фильтром `[sel 30 31]` в [[../entities/MIDI Passthrough]].

## Как используется в этом патче

| Диапазон | Слой | Где |
|---|---|---|
| 1–10 | инструменты | [[../entities/Instruments Layer]] |
| 11–14 | микшер | [[../entities/Mixer Layer]] |
| 15–16 | свободно (не используется) | — |

Cross-mode переход между двумя диапазонами реализован отдельно: [[../entities/CC47 Cross-Mode Transit]].
