# Карта скиллов

**Правило:** скиллы живут как группы-мультискиллы (9 на 2026-08-28). Триггерятся только роутеры групп (верхний уровень `.claude/skills/`); подскилл роутер читает как файл `<группа>/subskills/<имя>/SKILL.md` — вложенные SKILL.md системой не обнаруживаются, это фича архитектуры, не баг. Новый скилл вводится только через протокол онбординга (`meta/subskills/skill-onboarding/SKILL.md`). Исключения (не трогать): `Trading/` — отдельная память/репозиторий; вендорские паки (gstack, резюме-бандл, better-skills, awesome-ux-skills, MAX-MSP_CC_Framework в `Agents/m4l-master/skills/`) — внутренности не переписываются, только обвязка роутером. Официальные Anthropic-плагины (`frontend-design`, `code-review`, `code-simplifier`, `figma`, `superpowers` — `~/.claude/plugins/`) — отдельный, ещё более внешний слой: обновляются сами, не живут в `.claude/skills/`, группы лишь ссылаются на них по plugin-имени.

**Одна площадка.** 2026-08-27 все 8 групп перенесены в `~/.claude/skills/` — раньше `fadercraft-comment-miner`/`portfolio`/`dev-toolchain`/`meta` жили проектно внутри `~/Brain/.claude/skills/` (видны только при работе внутри Brain), это разводило пользователя между двумя местами и создавало путаницу «где что лежит». Функциональное деление осталось прежним — широкие/переиспользуемые группы (`design`, `frontend`, `ux-research`, `seo`) не завязаны на продукт, а `fadercraft-comment-miner`/`portfolio`/`dev-toolchain`/`meta` по содержанию всё ещё продукт- и Brain-специфичны — просто теперь это не отражено расположением папки, только описанием роутера.

## Пересборка архитектуры — и директива, и скилл

Инструмент, которым эта архитектура (включая эту карту) проектируется и пересобирается, раньше жил как top-level скилл `skills-architect`. 2026-08-27 владелец перевёл его в директиву `directives/directive_skills_architect.md` (слэш-команда `/skills-architect`) ради предсказуемого явного вызова. 2026-08-28 решил держать обе формы разом: скилл восстановлен в `~/.claude/skills/skills-architect/` (глобально, рядом со всеми остальными) и снова триггерится сам по description, директива остаётся для явного вызова по имени/слэш-команде. Содержание в обеих формах идентично — осознанный выбор двух входов в одну методологию, не рассинхрон. Подробности — в `directives/README.md` (раздел «Обратная миграция»).

## 9 операционных групп

**`design`** (глобальная, `~/.claude/skills/design/`) — 15 подскиллов: figma-mcp (Figma MCP setup+справочник), mobbin (Mobbin MCP UI-референсы), parity-check (React↔Figma read-only аудит, обобщён под любой проект), extract-design-system (дизайн-система из URL), image-to-design-system (дизайн-система из скриншота), landing-page-design (конверсия лендингов), prototype-upgrade (доведение сырого AI-прототипа до дизайн-системы, влит 2026-08-28), плюс вендор-пак «interfaces» (8 подскиллов, реархивирован и влит 2026-08-28 с русскими триггерами): better-interface, interface-review, better-colors, better-ui, better-typography, better-accessibility, better-layout, better-writing. UX/UI-решения, Figma, дизайн-системы как спецификация — не код. Широкая кросс-проектная — fadercraft/bifi/Dynamic Focus/GrooveMix и другие потребляют её, не дублируют. Исполняет `ux-ui-designer`.

**`frontend`** (глобальная, `~/.claude/skills/frontend/`) — 2 подскилла + 1 официальный Anthropic-плагин: impeccable (тяжёлый фронтенд-тулчейн, 20+ команд, Apache 2.0, «Based on Anthropic's frontend-design skill»), design-anti-slop (чистка AI-слопа по коду HTML/CSS/Tailwind/React + визуальный режим по скриншоту), плюс прямой вызов плагина `frontend-design:frontend-design` (`~/.claude/plugins/`, лёгкий вход в production-grade фронтенд, обновляется сам). Выделена 2026-08-15 из `design` — реальный код, а не дизайн-решение. Исполняет `frontend-designer`.

**`ux-research`** (глобальная, `~/.claude/skills/ux-research/`) — 1 подскилл-ось (frameworks): 18 канонических UX-фреймворков (вендор-пак awesome-ux-skills — research methods, personas, journey/empathy mapping, Double Diamond, Nielsen-эвристики, WCAG, cognitive load, persuasion, feature prioritization, AI-продукт-паттерны). Стратегия/методология/критика, не исполнение — пара к `design`+`frontend`, разведена намеренно. Все три группы вместе покрывает агент `ux-researcher` (read-only по природе — закрывает роль аудитора, отдельных `-auditor` субагентов нет); `design` дополнительно исполняет `ux-ui-designer`, `frontend` — `frontend-designer`.

**`seo`** (глобальная, `~/.claude/skills/seo/`) — 2 подскилла: seo-audit (технический SEO), ai-seo (AI-цитируемость/AEO). Общие для любого продукта, сейчас использует только Fadercraft.

**`fadercraft-comment-miner`** (`~/.claude/skills/fadercraft-comment-miner/`, продукт-специфичная) — 1 подскилл: fadercraft-youtube-outreach (майнинг+ответы на YouTube, `disable-model-invocation`). SEO и дизайн потребляет из глобальных групп, своих копий не держит.

**`portfolio`** (`~/.claude/skills/portfolio/`, продукт-специфичная) — 2 подскилла: portfolio-writer (канон написания кейсов in-buro.com), notion-review (цикл ревью кейса через Notion-комментарии; механика самого Notion 2026-08-27 вынесена в глобальный скилл `notion`, см. ниже). 2026-08-28 сужена — telegram-vacancy-mining и резюме-бандл выделены в отдельную группу `hiring` (см. ниже), portfolio теперь только про контент сайта.

**`hiring`** (`~/.claude/skills/hiring/`, продукт-специфичная, новая 2026-08-28) — 23 подскилла: telegram-vacancy-mining (майнинг вакансий из Telegram, физически перенесён из `portfolio`) + весь резюме/job-search бандл (resume-ats-optimizer — единственный, что реально вызывался; остальные 21 2026-08-27 были в `_archive/skills-retired-2026-08-27/` за неиспользование месяц, 2026-08-28 возвращены сюда подскиллами вместо плоских архивных файлов — решает саму проблему «висели незаметно»). Владеет всем механизмом поиска работы; portfolio владеет только контентом сайта — границы разведены явно.

**`dev-toolchain`** (`~/.claude/skills/dev-toolchain/`, продукт-специфичная) — 1 свой подскилл (directus-cms, CMS через официальный MCP). Раньше здесь же индексировался вендорский пак gstack (55 скиллов) — 2026-08-27 сам gstack (1.1 ГБ) удалён вместе с 54 дохлыми symlink-обёртками, 2026-08-28 индекс убран из роутера целиком (незачем маршрутизировать на несуществующее). Переустановить можно с `github.com/garrytan/gstack.git`, версия на момент удаления 1.55.1.0 — после переустановки индекс собирать заново через `meta/subskills/skill-onboarding`, не восстанавливать вслепую.

**`meta`** (`~/.claude/skills/meta/`, про обслуживание самой среды) — 2 подскилла: release-environment (сборка среды с нуля из директив/истории), skill-onboarding (протокол внедрения нового скилла).

## Отдельный глобальный скилл — `notion`

`~/.claude/skills/notion/SKILL.md` — механика работы с Notion через MCP (структура страниц, `allow_deleting_content`, грабли API), без группы-обёртки. Не вендорский пак (авторский канон, вынесен 2026-08-27 из `portfolio/subskills/notion`) и не над-групповой инструмент вроде бывшего skills-architect — просто знание общего назначения, которое не привязано ни к одному продукту и не заслуживает пока отдельной группы ради одного файла (аналогия — как отдельные скиллы резюме-бандла лежат вне группы `portfolio`, только индексируются её роутером). `portfolio/subskills/notion-review` (цикл ревью кейса) на него ссылается за механикой, не дублирует. Если появится второй скилл того же рода (Slack/Linear-механика и т.п.) — тогда решать вопрос группы отдельно.

## Вне периметра (не трогать)

- **M4L/Ableton** — не отдельная группа: уже цельная вендорская система `Agents/m4l-master/skills/MAX-MSP_CC_Framework/.claude/skills/` со своим роутером `max-router`, плюс агенты m4l-master/ableton-producer поверх неё.
- **Trading/** — отдельная память/репозиторий, полностью вне периметра.

## Счётчики (на 2026-08-27)

- Верхний уровень `~/.claude/skills/`: 9 операционных роутеров (`design`, `frontend`, `ux-research`, `seo`, `fadercraft-comment-miner`, `portfolio`, `dev-toolchain`, `meta`, `hiring`) + `notion` (отдельный, см. выше) + `skills-architect` (восстановлен 2026-08-28, см. раздел «Пересборка архитектуры» выше) + `_archive`. `resume-ats-optimizer` и весь резюме-бандл 2026-08-28 перестали быть отдельными top-level записями — теперь подскиллы `hiring`.
- Проектный верхний уровень `~/Brain/.claude/skills/`: пусто, только `_archive` (история ретирнутых скиллов самого Brain). Разделение глобальный/проектный по расположению папки упразднено 2026-08-27 — см. «Одна площадка» выше.
- Удалено: `market-news-analyst` (глобальный симлинк на Trading-скилл, не нужен).
- Перенесено из `directives/` в скиллы: `directus-cms` (был сиротой, не отмечен в архиве).
- Перенесено из agent-memory в скиллы: 18 UX-фреймворков `ux-researcher` (`.claude/agent-memory/ux-researcher/skills/` → `ux-research/subskills/frameworks/rules/awesome-ux-skills/`) — методология, не накопленные факты, один канон в группе, не в памяти агента.
- 2026-08-15: группа `design` (10 подскиллов) разведена на `design` (7, UX/UI-решения) + `frontend` (2 + плагин, код). Локальная копия `frontend-design` (дублировала официальный плагин `frontend-design@claude-plugins-official`) ушла на пенсию в `~/.claude/skills/_archive/skills-retired-2026-08-15/`. Новая персона `frontend-designer` (`~/Brain/agents/frontend-designer/`) — держатель группы `frontend`.
- 2026-08-27: `skills-architect` перешёл из скилла в директиву (`directives/directive_skills_architect.md`, `/skills-architect`) — владелец вызывает всегда явно, автовсплытие лишнее. Старый скилл в `.claude/skills/_archive/skills-retired-2026-08-27/skills-architect/`.
- 2026-08-27: обратная находка по тому же вопросу — директивная копия `meta/subskills/release-environment` (`directive_release_environment.md`) ретирнута в `directives/_archive/`. Проверка по логам сессий показала асимметрию, которую документация не отражала: скилл реально использовался (31 упоминание) и был более свежей версией, директива — 1 чтение за всю историю и отставание в содержании. Канон — только скилл. Урок для будущих пар скилл/директива: заявление «дублирование оправдано использованием» проверять логами (`grep` по имени в `~/.claude/projects/<проект>/*.jsonl`), не принимать на веру из файла.
- 2026-08-27: добавлен `portfolio/subskills/notion` — 3-й собственный подскилл группы `portfolio` (механика Notion-ревью на кейсе Beefy: гейт «сперва Notion, потом код», структура страниц, грабли API). Онбординг прогнан по протоколу `meta/subskills/skill-onboarding`; дублей не найдено, зона пересечения с `connect_notion_mcp.md` (директива подключения MCP, не механики работы) явная, конфликта нет.
- 2026-08-27 (в тот же день, второй проход по этому скиллу): прогнан через `directive_skills_architect.md` и разделён — содержимое оказалось смесью общей механики Notion (не портфолио-специфична) и Beefy-специфичного сценария ревью. Общая механика вынесена в отдельный глобальный скилл `notion` (см. раздел выше); портфолио-остаток переименован в `portfolio/subskills/notion-review` (только цикл ревью + Beefy-синхронизация с Figma), ссылается на глобальный канон вместо дублирования.
- 2026-08-27 (третий проход в тот же день): владелец перенёс оставшиеся 4 проектные группы (`fadercraft-comment-miner`, `portfolio`, `dev-toolchain`, `meta`) из `~/Brain/.claude/skills/` в `~/.claude/skills/` — разделение глобальный/проектный по расположению папки упразднено, все 8 групп физически в одном месте (см. «Одна площадка» выше). Заодно очищена площадка `~/.claude/skills/`: удалён вендорский `gstack` (1.1 ГБ) и 54 его дохлых symlink-обёртки, 21 из 22 резюме-бандла отправлены в `_archive/skills-retired-2026-08-27/` как ни разу не вызванные за месяц логов сессий (проверено `grep` по `Skill()`-вызовам в `~/.claude/projects/*/*.jsonl`).
- 2026-08-28: роутер `fadercraft` переименован в `fadercraft-comment-miner` — старое имя было неотличимо от названия продукта, новое отражает единственную функцию роутера (майнинг+ответы на YouTube).
- 2026-08-28: владелец передумал по вчерашнему переходу «skills-architect: скилл → директива» — решил держать обе формы разом. Скилл восстановлен из архива в `~/.claude/skills/skills-architect/` (глобально, снова триггерится сам по description), директива `directive_skills_architect.md`/`/skills-architect` остаётся для явного вызова. Содержание идентично в обеих формах — сознательный выбор владельца, не рассинхрон, который надо чинить.
- 2026-08-28: точечный прогон `directive_skills_architect.md` по скиллу `notion` (Фаза 0 — файловый снапшот в `_archive/pre-architect-2026-08-28/`, т.к. git был временно недоступен). Инвентаризация нашла дубль-канон: правило безопасного чтения Notion API-токена (3 инцидента утечки) жило приватно в `.claude/agent-memory/copywriter/notion_api_token_handling.md`, невидимо остальным агентам. Перенесено в `notion/SKILL.md` («Механика API»), copywriter-заметка заменена ссылкой на канон. Остальные соседи (`notion-review`, `connect_notion_mcp.md`, `directive_prd_notion.md`) проверены — пересечений не нашлось, уже разведены правильно.
- 2026-08-28: полный прогон директивы по всей `~/.claude/skills/` (свежий снапшот в `_archive/pre-architect-2026-08-28-full/`, инвентаризация параллельными read-only агентами по группам). Находки и фиксы:
  - `dev-toolchain` и `frontend`: оба ещё описывали `gstack` как живой вендор-пак (дедуп-вахта/индекс) — призрак после вчерашнего удаления, почищено.
  - `seo/subskills/ai-seo/SKILL.md`: битая ссылка на несуществующий `../../tools/REGISTRY.md`, удалена.
  - `portfolio/SKILL.md`: таблица-индекс ещё перечисляла все 22 резюме-скилла как живые — сведена к одному `resume-ats-optimizer`, 21 архивный перечислен списком с указанием, как вернуть при необходимости.
  - `prototype-upgrade` (был отдельным top-level скиллом): нашли баг обнаружения — SKILL.md лежал на уровень глубже нужного (`prototype-upgrade/prototype-upgrade/SKILL.md`) и никогда не подхватывался системой. По решению владельца влит в группу `design` как `subskills/prototype-upgrade` (баг обнаружения чинится тем же движением) — карта дублей с `extract-design-system` и `design-anti-slop` (группа `frontend`) зафиксирована в дедуп-вахте `design`, не сливать, только следить.
  - `design/subskills/better-skills` (вендор-пак «interfaces», 8 вложенных скиллов) — тем же днём отправлен в архив и возвращён обратно владельцем (замечен в utility учёта плотности вызовов скиллов), влит в `design` как 8 отдельных подскиллов с добавленными русскими триггерными фразами.
  - `frontend/SKILL.md`: добавлена пропущенная запись в дедуп-вахту — `impeccable` (встроенный anti-slop) vs `design-anti-slop`, роли разведены (тулчейн целиком vs точечный аудит), держать оба.
  - Низкоприоритетные находки (неоднозначная ссылка на design-anti-slop в `design/references/principles.md` P7, недокументированная зависимость `belt` CLI у `landing-page-design`, разночтения имён Figma MCP-тулов `mcp__figma__*` vs `mcp__claude_ai_Figma__*` в зависимости от способа подключения) — в план не взяты, оставлены на усмотрение владельца.
- 2026-08-28: новая группа `hiring` — владелец решил развести смешанный домен `portfolio` (контент сайта + сам поиск работы в одном роутере) на два по смыслу. `hiring` забрал `telegram-vacancy-mining` (был подскиллом `portfolio`) и весь резюме/job-search бандл — `resume-ats-optimizer` (был top-level) и все 21 архивных 2026-08-27 (возвращены как подскиллы, не top-level файлы — решает исходную проблему «висели незаметно»). `portfolio` сужен до `portfolio-writer` + `notion-review`, у обеих групп своя `references/principles.md` (P1/P2 про дедуп вакансий и честность резюме — в `hiring`; P «сперва канал переговоров» — остался в `portfolio`). Обновлены перекрёстные ссылки в `portfolio/CLAUDE.md` (рабочая область поиска работы).
- 2026-08-28: аудит обнаруживаемости (11 параллельных read-only агентов по всем группам) нашёл, что `hiring/career-changer-translator` — capability statement без узнаваемого симптома задачи («Translate skills from one industry to another» ≠ как реально формулируется просьба). По решению владельца — не переформулировка, а пенсия: ушёл в `_archive/skills-retired-2026-08-28/hiring-career-changer-translator/`, убран из таблицы маршрутизации и счётчика `hiring/SKILL.md` (23 → 22 подскилла).
- 2026-08-28: аудит обнаруживаемости также нашёл 27 description'ов только на английском (22 подскилла `hiring` — весь резюме/job-search бандл, 2 `seo`, 3 `design`) — при том что весь чат идёт по-русски ([CLAUDE.md:7](CLAUDE.md#L7)), эти description'ы не ловили русскую формулировку задачи напрямую, только через русский текст роутера. Владелец решил переводить не машинно (DeepL), а вручную под стандарт Б.1 — все 27 переписаны на русский, жаргон (ATS, LinkedIn, STAR, C-suite, DTCG и т.п.) оставлен латиницей по конвенции CV RU/EN parity. Тело файлов (7403+987+384 строк) сознательно НЕ переведено — оно читается агентом после того, как скилл уже найден, на обнаруживаемость не влияет, а машинный перевод инструкций для LLM рискует потерять точность формулировок без отдельной вычитки на голос.
- 2026-08-28: Волна 2 аудита — resume-* кластер в `hiring` (6 description'ов конкурировали друг с другом и с `resume-ats-optimizer` за одно смысловое пространство) получил явные различители: `cover-letter-generator` (hook/gaps, после job-description-analyzer), `resume-formatter` (layout, не keywords), `resume-tailor` (после job-description-analyzer), `tech-resume-optimizer` (GitHub/Projects/tech-стек), `resume-version-manager` (симптом «запутался в версиях»), `creative-portfolio-resume` (две версии для творческих ролей) — прочитаны тела файлов, различители взяты из реального содержимого, не выдуманы. Волна 3 (косметика) — `design/figma-mcp` и `meta/release-environment` переставлены на симптом-первым; `ux-research/frameworks` получил verb-триггеры; `frontend/impeccable` (вендорский тулчейн, Apache 2.0) — не переписан, только дописана одна граничная фраза снаружи (Инвариант 2); строка плагина `frontend-design` в роутере `frontend/SKILL.md` получила явное «без тулчейна/PRODUCT.md/npx».
- 2026-08-28: та же волна аудита — Волна 1 (низкий риск, готовые формулировки) применена: 1) роутеры `design`, `seo`, `dev-toolchain`, `ux-research`, `fadercraft-comment-miner` открывали description структурным «Роутер группы X (N подскиллов)» вместо симптома задачи — переставлены местами, счётчик из description убран (он и так есть в этой карте); `dev-toolchain` заодно вынес административную заметку про удаление gstack из description в конец, `ux-research` получил явную границу и с `frontend`, не только с `design`; 2) 3 weak description'а (`design/mobbin`, `hiring/academic-cv-builder`, `hiring/reference-list-builder`, `hiring/resume-section-builder`) переписаны с capability statement на симптом задачи; 3) `meta/skill-onboarding` — добавлено покрытие кейса «скилл создаёт сам агент по ходу работы», не только «пользователь просит»; `skills-architect` (и глобальный скилл, и `directive_skills_architect.md`) получил явную границу с `skill-onboarding` («не для точечного добавления одного скилла»).
