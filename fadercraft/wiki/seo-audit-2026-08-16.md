---
type: brief
project: Novation
created: 2026-08-16
updated: 2026-08-16
---

# SEO-аудит 2026-08-16 — бриф для агента

**Summary**: Внешняя проверка индексации `fadercraft.com` (по индексу Google + почте, без доступа к проду и репо лендинга) вскрыла: сайт перестроен, в индексе висит дубль-preview Cloudflare Pages, а roadmap местами отстал от реальности. Страница содержит находки и очередь задач для агента, включая ответ на исходный вопрос основателя про перевод страниц на FR/DE/en-GB. **Правка 2026-08-16 (постфактум):** проверено против локального GSC-монитора, который у той сессии не был виден — `/free-custom-modes` и сабмит sitemap оказались уже решены. **Правка 2 (тот же день, доступ к репо и живому проду уже был):** найден и задеплоен реальный баг (`/xl-updates` не было в sitemap — «unknown to Google»), preview-хосты получили явный `noindex` в коде, `/guide/…mk3-across-live-sets` оказался не новой регрессией, а тем же нерешённым с июля кейсом. См. §7.

**Sources**: индекс Google (поисковые запросы 2026-08-16), почта `sc-noreply@google.com` 2026-07-24, [[roadmap]], `wiki/log.md`, `~/.config/google/gsc-reports/latest.md` (постфактум).

**Last updated**: 2026-08-16

---

## 0. Как читать эту страницу

Всё ниже получено **без доступа к проду и к коду сайта**. Ограничения окружения на момент аудита:

- `fadercraft.com`, `maxforlive.com` и preview-хост `*.pages.dev` — заблокированы egress-политикой окружения.
- Репозиторий лендинга недоступен: `list_repos` отдаёт только `inBuro/Brain` и `inBuro/Chess-Demo`.
- В той сессии Google Search Console не читался — **но это ограничение конкретной сессии, не факт отсутствия доступа.** На машине уже есть рабочий локальный доступ к GSC API (`~/.config/google/gsc-service-account.json`, service account с правами Owner на `sc-domain:fadercraft.com`, хелпер `~/.config/google/gsc.sh`) и автономный launchd-монитор (`com.fadercraft.gsc-index-monitor`, раз в 2 дня пересабмичивает sitemap и проверяет 7 ключевых URL — отчёт лежит в `~/.config/google/gsc-reports/latest.md`). Читается это только при выключенной песочнице (ключ под `~/.config`), поэтому сессии с ограниченным доступом к файловой системе его не видят. Проверено 2026-08-16 постфактум — часть находок в §4 этой правки уже закрыта данными монитора.
- В подключённом почтовом ящике **нет ни одного письма Gumroad** — аккаунт продавца заведён на другую почту, данные о продажах недоступны.

Поэтому у каждого факта ниже проставлен статус: **✅ подтверждено** (виден в индексе Google), **❌ опровергнуто** (вики расходится с наблюдаемой реальностью), **❓ не проверяемо** (нужен доступ).

⚠️ Отдельная оговорка про метод: поисковая выдача **не является полным дампом индекса**. Отсутствие страницы в результатах — повод проверить, а не доказательство деиндексации.

---

## 1. Исходный вопрос основателя

> «Имеет ли смысл перевести страницы с гайдами на французский, uk English, немецкий, для лучшего индекса в этих странах?»

**Вердикт: нет, не сейчас. Вариант en-GB не имеет смысла и в перспективе.**

Аргументы:

1. **Аудитория ищет по-английски.** SEO-таргеты проекта (`docs/superpowers/specs/2026-05-01-fadercraft-launch-design.md:154`) — `launch control xl mk3 max for live`, `lcxl mk3 ableton mapping`. У этих терминов нет немецких/французских эквивалентов: «custom mode», «Max for Live», «sends», «encoder bank» немцы и французы гуглят как есть. Локализованная страница целится в запросы с околонулевой частотностью, а на английский запрос из Германии английская страница ранжируется и так.
2. **en-GB — это орфография, а не язык.** Бонуса в британской выдаче за неё нет; получается дубль контента, hreflang-развилка и canonical-риск при нулевом апсайде. Для UK работают ссылки и упоминания, не перевод.
3. **База не готова.** В индексе шесть из семи ключевых страниц, одна только что регрессировала в noindex (см. §3), содержимое `sitemap.xml` не сверено, плюс дубль всего сайта на preview-хосте. Локализация в такой конфигурации умножает проблему, а не решает её.
4. **Стоимость не разовая.** Копи живая (терминология менялась: `signals → sources`, `gumroad-description-sends-follower.md:113`). Каждая правка формулировки превращается в 4 правки на языках, которые никто не проверит. Протухший перевод хуже отсутствия перевода.
5. **LLM-поиск.** По нишевым техническим темам ассистенты цитируют преимущественно англоязычные источники.

**Условия для пересмотра** (проверяются по GSC, см. задачу D1):

- английский органик даёт стабильный поток (ориентир ≥1000 сессий/мес);
- в отчёте «Страны» по DE/FR видны **показы с нормальной позицией, но низким CTR** — только это доказывает языковой барьер, а не отсутствие видимости;
- есть страница-победитель, которую есть смысл клонировать (переводят выигравший контент, а не всё подряд).

Тогда переводить не сайт целиком, а одну-две страницы, руками, с hreflang. Список языков пересобрать по данным: по объёму Ableton-аудитории DE обычно интереснее FR, а ES/PT-BR/JP нередко крупнее обоих.

Отдельная история — **перевод Quickstart для покупателей**. Это не SEO, а качество продукта; окупается, когда появится заметная доля покупателей из конкретной страны.

---

## 2. ❌ Опровергнуто — [[roadmap]] расходится с реальностью

Roadmap отстаёт минимум на полтора месяца (шапка `updated: 2026-06-18`, самая свежая PM-запись внутри блока Phase 2 — 2026-06-30, последний коммит Brain — 2026-07-03) и по четырём пунктам прямо дезинформирует:

| Утверждение вики | Реальность |
|---|---|
| `roadmap.md:579` — «Деплой `/sends-follower` — БЛОКЕР LAUNCH-ЛИНКОВ, страница не задеплоена» | Страница живая и проиндексирована, в сниппете цена $14 и требования Live 11+ / M4L 8.5 |
| `roadmap.md:565` — «SEO-meta для роута не сделана» | Google отдаёт по-роутные title/description по всем новым маршрутам — постбилд отработал |
| Структура сайта = лендинг + `/free-custom-modes` + `/legal` | Фактически хаб + три продуктовые страницы: `/control-xl`, `/sends-follower`, `/dynamic-focus` |
| Продуктовая линейка = Control XL + Sends Follower | Есть третий продукт **Dynamic Focus, $19** — страница на сайте, девайс на maxforlive, исходники в `fadercraft/Dynamic Focus/` (файлы от 2026-06-28). **Уточнение (проверено постфактум 2026-08-16):** `roadmap.md` продукт знает — раздел Phase 3 обновлён 2026-08-08 с ценой, живым отзывом первого покупателя и статусом «запущен». Реально отсутствующее — не факт в roadmap, а собственная синтез-страница в `wiki/index.md` уровня [[Sends Follower]]/Control XL (задача E2). Тот же пробел, впрочем, и у Sends Follower — паттерн вики, не что-то уникальное для Dynamic Focus |

**Вывод для агента: roadmap нельзя использовать как источник статуса, пока не сверен.** Планирование по нему даёт ложные приоритеты.

---

## 3. ✅ Подтверждено

- **В индексе четыре страницы**: [`/`](https://fadercraft.com/), [`/control-xl`](https://fadercraft.com/control-xl), [`/sends-follower`](https://fadercraft.com/sends-follower), [`/dynamic-focus`](https://fadercraft.com/dynamic-focus) — с осмысленными по-роутными title/description, собранными из живой копи.
- **Preview-деплой Cloudflare Pages проиндексирован**: `3c812ca9.fadercraft-landing.pages.dev`. Всплыл в **трёх разных** запросах, в одном — **выше** канонической `/control-xl` по брендовому запросу. Заголовок там старый («Control XL · M4L device for LCXL MK3» против прод-версии «M4L device for Launch Control XL MK3 — Fadercraft») → проиндексирована протухшая сборка. Это полный дубль сайта на чужом хосте: размытие сигналов, каннибализация брендовой выдачи, риск отдать пользователю неактуальные цену и состав бандла.
- **GSC-ресурс существует и подтверждён как доменный.** Онбординг-письмо `sc-noreply@google.com` от **2026-07-24**: «Вы недавно подтвердили этот доменный ресурс». Владелец — ящик `YellowShoess@gmail.com`.
  - ⚠️ Расхождение дат: в roadmap верификация датирована 2026-06-10, письмо — 24 июля. Либо повторная верификация, либо вторая property. Проверить, в какой из них накоплена история.
- **Алертов от GSC нет.** За год ни одного письма об ошибках индексирования, ручных санкциях или Core Web Vitals.
- **maxforlive-профиль активен**, девайсов несколько, Dynamic Focus среди них.
  - ⚠️ Низкая достоверность: в выдаче мелькнуло имя «Default and Selection Mapping v1.2 by Fadercraft» — получено из пересказа сниппета, не с самой страницы. Проверить глазами.

**Добавлено постфактум 2026-08-16, из локального GSC-монитора** (`~/.config/google/gsc-reports/latest.md` — см. §0, это не была доступно исходной сессии):

- **`sitemap.xml` отправлен и принимается.** Автономный launchd-джоб пересабмичивает его каждые 2 дня (`PUT .../sitemaps/...` → `204`), последний раз 2026-08-14. Открытый пункт `roadmap.md:356` можно закрывать — вопрос «отправлен ли» отвечён; вопрос «актуален ли список URL внутри» остаётся (см. B1).
- **`/free-custom-modes` жив и в индексе**, стабильно, с ~2026-08-01 (три недели). Отсутствие в четырёх поисковых запросах было артефактом выборки, не деиндексацией.
- **6 из 7 отслеживаемых URL — «Submitted and indexed»** на 2026-08-14: `/`, `/control-xl`, `/sends-follower`, `/free-custom-modes`, `/dynamic-focus`, `/guide/…knob-jump-pickup-mode`.
- **`/guide/…mk3-across-live-sets` — не новая регрессия (поправка §7): это тот же нерешённый с июля кейс.** Прямой GSC URL Inspection (2026-08-16) показывает `Excluded by 'noindex' tag`, но `lastCrawlTime: 2026-07-04` — Google ни разу не пересканировал страницу за полтора месяца, хотя живые заголовки чистые уже давно (проверено curl'ом). Ручной Request Indexing отправлялся 2026-07-14 (см. память `reference_gsc_index_monitor`) и не подействовал. Монитор показывает «не в индексе» непрерывно с 1 августа — это застрявший старый краул, а не свежий сбой.

---

## 4. ❓ Не проверяемо без доступа

Ранее в этом списке были «отправлен ли sitemap» и «судьба `/free-custom-modes`» — оба сняты выше данными локального GSC-монитора, вопрос закрыт без доступа к репозиторию лендинга. Осталось непроверяемым без доступа к коду/проду:

1. **Содержимое `sitemap.xml`** — сам факт отправки подтверждён (см. §3), но актуален ли список `<loc>` (не тянет ли старые URL до реструктуризации) — неизвестно.
2. **`robots.txt`, canonical, `_headers`, `_redirects`** — в том числе есть ли защита preview-хостов от индексации и живы ли vanity-редиректы.
3. **Роуты `/sf`, `/update`, `/sends-follower/updates`** — существуют ли.
4. **Раскатан ли Control XL v1.1** (`version.json` → `latest`, «Last Updated» в листинге maxforlive 15522).
5. **Продажи и страны покупателей** — нечем проверить, есть ли вообще покупатели из DE/FR. Это ровно те данные, которые нужны для решения по локализации.
6. **KVR-листинг** (`roadmap.md:395`) — следов не нашёл, похоже не сделан. Доказательство слабое.

---

## 5. Очередь задач для агента

Порядок блоков — это порядок исполнения. Блок A гейтит B и C.

### A. Доступы (гейт, исполнитель — человек)

- [ ] **A1. Выдать один из двух доступов к коду/проду.** Без любого из них блоки B/C неисполнимы:
  1. установить GitHub App на репозиторий лендинга (`fadercraft-landing`) → проверка по коду;
  2. разрешить `fadercraft.com` и `maxforlive.com` в network policy окружения ([документация](https://code.claude.com/docs/en/claude-code-on-the-web)) → проверка живого прода.
  **Готово, когда:** агент может прочитать `robots.txt`/`_headers`/canonical любым из способов.
  ⚠️ **GSC-доступ сюда не входит — он уже есть локально и не требует новой выдачи.** `~/.config/google/gsc-service-account.json` + `~/.config/google/gsc.sh` (нужен выключенный sandbox), плюс готовый отчёт `~/.config/google/gsc-reports/latest.md`, обновляется раз в 2 дня автономно. См. §0 и §3 — часть выводов этого брифа уже получена оттуда постфактум.

### B. Сверка фактов (после A)

- [x] **B1. Инвентаризация `sitemap.xml` — сделано и пофикшено (§7, 2026-08-16).** Нашёлся мёртвый `/updates` и отсутствующий `/df-updates`; `/xl-updates` оказалась «unknown to Google». Исправлено и задеплоено.
- [x] **B2. ~~Расследовать регрессию~~ Причина названа (§7, 2026-08-16):** не регрессия — застрявший с 2026-07-04 краул, живые заголовки чистые. Не связано с C1 (preview-хосты — отдельный, тоже разобранный механизм). Осталось только вручную повторить Request Indexing (ссылка в §7).
- [ ] **B3. Аудит индексируемости по всем роутам** — `robots.txt`, `<link rel=canonical>`, `meta robots`, заголовки. Проверить, что canonical указывает на апекс, а не на pages.dev.
- [ ] **B4. Проверить роуты `/sf`, `/update`, `/sends-follower/updates`** и синхронность `_redirects` с [[outbound-links]].
- [ ] **B5. Статус Control XL v1.1** — `api/version.json` `latest`, версия и «Last Updated» в листинге maxforlive 15522, упоминание v1.1 в описании Gumroad.

### C. Фиксы

- [x] **C1. Закрыть preview-хосты от индексации — сделано (§7, 2026-08-16).** Cloudflare Pages уже отдавал `noindex` на preview по умолчанию (не был баг конфигурации); добавлен явный `X-Robots-Tag: noindex, nofollow` в `functions/_middleware.js` как страховка в коде. **GSC → Удаления не годится** — `pages.dev` не наш верифицированный ресурс, удалить оттуда URL нельзя; уже проиндексированная копия сойдёт из индекса сама на следующем краулинге Google (недели, не дни) либо через публичный инструмент «Remove outdated content» (нужен интерактивный вход, не автоматизируется отсюда).
- [x] **C2. Обновить `sitemap.xml` — сделано и задеплоено (§7).**
- [ ] **C3. Проверить, что три продуктовые страницы связаны внутренними ссылками** с хаба и футера — страница без входящих ссылок остаётся сиротой даже после индексации.

### D. Данные под решение о локализации

- [ ] **D1. Выгрузить GSC → Эффективность за 3 месяца**, вкладки «Страны» и «Запросы», CSV положить в `_Inbox/`.
  **Готово, когда:** по DE/FR/GB известны показы, CTR и средняя позиция.
- [ ] **D2. Свести страны покупателей** из Gumroad (нужен доступ к ящику продавца или CSV-экспорт).
- [ ] **D3. Вынести вердикт по локализации** по критериям из §1 и записать в эту страницу. **До закрытия D1 переводы не начинать.**

### E. Гигиена вики

- [ ] **E1. Привести [[roadmap]] в соответствие с фактами** — снять устаревшие блокеры Sends Follower, добавить роуты `/control-xl` и `/dynamic-focus`, завести пункт про preview-индексацию, обновить шапку `Last updated`.
- [ ] **E2. Завести синтез-страницу продукта Dynamic Focus** в вики и добавить в `index.md` — материал уже есть (roadmap Phase 3, `log.md`, `outbound-links.md`), не хватает только собранной страницы уровня [[Sends Follower]]. Не факт-гэп, а гэп синтеза.
- [ ] **E3. Разобраться с расхождением GSC-property** (10 июня против 24 июля) — назвать, какая property рабочая.
- [ ] **E4. Записать итоги в `wiki/log.md`** по формату журнала.

### Анти-лист (в этом заходе НЕ делать)

- ❌ Не переводить страницы на FR/DE/en-GB и не заводить hreflang — гейт D1.
- ❌ Не плодить новые страницы ради SEO, пока не закрыты C1–C3: на домене из четырёх страниц дубли и сироты дороже недостающего контента.
- ❌ Не править код лендинга вслепую по памяти вики — сверяться с фактическим состоянием (§2).
- ❌ Не подавать заявку на удаление в GSC для чего-либо, кроме preview-хоста.
- ❌ Не запрашивать доступ к GSC у пользователя — он уже есть локально (см. A1), спросить только про GitHub App/network policy.
- ❌ Не открывать заново вопрос про `/free-custom-modes` или сабмит sitemap — оба закрыты §3 данными монитора.

---

## 6. Приоритет одной строкой

~~`A1 → C1 → B1 → D1`~~ **A1/B1/B2/C1/C2 закрыты 2026-08-16 (§7).** Осталось: D1 (выгрузка GSC по странам под вердикт о локализации) и два ручных клика в Search Console.

## 7. Итог 2026-08-16 — что сделано после аудита

Доступ к репозиторию лендинга (`~/Projects/Projects/fadercraft/app`, `inBuro/fadercraft-landing`) и к живому проду оказался уже доступен напрямую в этой среде — A1 отпал сам, ни GitHub App, ни network policy не понадобились.

**Задеплоено на прод** (`inBuro/fadercraft-landing@6ce81c0`, проверено curl'ом после деплоя):
- `functions/_middleware.js` — `X-Robots-Tag: noindex, nofollow` на любой хост кроме `fadercraft.com`.
- `public/sitemap.xml` — убран мёртвый `/updates`, добавлены `/xl-updates` и `/df-updates`. Пересабмичен через GSC API (`204`).

**Главный результат:** `/xl-updates` была «URL is unknown to Google» — реальная, ранее не обнаруженная пропавшая страница, причина ровно в баге sitemap. Это и отвечает на исходное беспокойство фаундера.

**Дубль preview-хоста** не был багом конфигурации — Cloudflare Pages уже сам ставил `noindex` на preview. Код-фикс — страховка, не решение: уже проиндексированная старая копия уйдёт из выдачи сама на следующем краулинге (недели), автоматически ускорить это нельзя (`pages.dev` не наш верифицированный GSC-ресурс).

**Осталось вручную** (нет публичного API у Request Indexing): в Search Console → URL Inspection → «Запросить индексирование» для [`/xl-updates`](https://search.google.com/search-console/inspect?resource_id=sc-domain%3Afadercraft.com&id=https%3A%2F%2Ffadercraft.com%2Fxl-updates) и повторно для [`/guide/…mk3-across-live-sets`](https://search.google.com/search-console/inspect?resource_id=sc-domain%3Afadercraft.com&id=https%3A%2F%2Ffadercraft.com%2Fguide%2Flaunch-control-xl-mk3-across-live-sets) (висит с 4 июля, первый запрос от 14 июля не подействовал).

**Не сделано:** E1 (привести roadmap к фактам), E2 (синтез-страница Dynamic Focus), E3 (расхождение дат GSC-property), D1–D3 (данные и вердикт по локализации), остаток B3–B5 (canonical-аудит остальных роутов, `/sf`/`/update` роуты, статус Control XL v1.1).

## Related pages

- [[roadmap]]
- [[outbound-links]]
- [[external-links]]
- [[copy-inventory]]
