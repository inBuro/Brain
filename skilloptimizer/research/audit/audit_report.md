# UX-аудит: Skill Optimizer

> Методология: 10 эвристик Нильсена + когнитивный обход, `directive_ux_audit.md`. Аудит смотрит на продукт как на потенциально публичный (через призму брифа и `competitive_analysis_final.md`) — многие находки не мешают текущему единственному пользователю (он же автор, знает продукт наизусть), но станут реальным трением для нового человека.
>
> **Оговорка по методу:** шаг 1 (главный список) верифицирован живым скриншотом (`screenshots/01_main_list.png`), сделанным через реальный клик по иконке в мунбаре. Состояния Edit/Delete-confirm описаны по коду (`DescriptionPopover.swift`), который я знаю построчно из работы над ним в этой же сессии.
>
> **Находка №1 (попап описания отсоединяется от строки) — понижена в приоритете 2026-08-29.** Воспроизведена дважды через автоматизированный клик, включая один раз через настоящий синтетический mouse-клик (не AX-скриптинг) — скриншот-пруф в `screenshots/02_description_popover_detached.png`. Тем не менее владелец не встречает этого в реальном повседневном использовании и попросил не приоритизировать это дальше. Находка остаётся задокументированной (техническая причина правдоподобна — `.popover` внутри `MenuBarExtra(.window)`), но не в топе рекомендаций — см. обновлённую сводку.

## Когнитивный обход

**Шаг 1 — открыть мунбар.** Цель: увидеть плотность использования скиллов. Обнаружимость иконки — ок (стандартная позиция в строке меню). Связь «иконка → список активности» — не очевидна без знания контекста продукта (иконка `chart.bar.fill` не намекает на «Claude Code skills» специфично; для случайного человека это может быть любой чарт-трекер). Обратная связь — мгновенная (попап открывается сразу).

**Шаг 2 — прочитать список.** Цель: понять, что часто используется. Обнаружимость сортировки/таймфрейма — ок, но кнопка сортировки «321»/«ABC» требует наведения (tooltip) или знания конвенции, чтобы понять, что это переключатель, а не статичный лейбл. Связь «роутер без счётчика, но высоко в списке» → не очевидна (находка №4). Обратная связь на смену таймфрейма/сортировки — мгновенная, без анимации моргания (хорошо).

**Шаг 3 — открыть скилл.** Цель: посмотреть, почему скилл (не) срабатывает. Обнаружимость — chevron `›` есть, но семантически неоднозначен (находка №3). Связь «клик → появится инфо-попап» — при наблюдавшемся баге позиционирования (находка №1) эта связь буквально разрывается: результат клика визуально не связан с местом клика.

**Шаг 4 — отредактировать триггеры.** Цель: добавить/убрать фразу. Обнаружимость «+ Text-trigger» — хорошо (оранжевый акцентный цвет). Обратная связь на удаление триггера (✕) — операция происходит молча, без какого-либо подтверждения или последующего сообщения (находка №2). Обратная связь на сохранение через «+ Text-trigger» — по коду не появляется вообще (находка №5, самая серьёзная находка аудита).

**Шаг 5 — удалить скилл.** Цель: заархивировать неиспользуемый скилл. Обнаружимость — «Delete Skill» в углу, текст явный. Связь и обратная связь — хорошо: два шага подтверждения, `deleteError` показывает причину при неудаче.

## Эвристическая оценка

1. **Видимость состояния системы** — частично нарушена: «Saved» после сохранения триггера не успевает отрендериться (находка №5); ранжирование роутеров по максимуму среди детей не отражено видимым числом на самой строке роутера (находка №4).
2. **Соответствие реальному миру** — нарушена в одном месте: chevron `›` рядом с именем скилла в общем списке UI-конвенций обычно означает «раскрыть/свернуть», здесь означает «открыть попап с информацией», при этом сама иерархия роутер→подскиллы никогда не сворачивается (находка №3).
3. **Свобода и контроль** — хорошо для Delete Skill (двухшаговое подтверждение, Cancel есть); слабо для удаления отдельного триггера — нет отмены, нет подтверждения (находка №2).
4. **Консистентность и стандарты** — три асимметрии: (а) Delete Skill подтверждается, удаление триггера — нет; (б) успех сохранения не показывается, ошибка — показывается; (в) `.popover` в теории должен вести себя как системный поповер с якорем и стрелкой, но внутри `MenuBarExtra(.window)` этого не происходит (находка №1).
5. **Предотвращение ошибок** — Delete Skill соблюдает (архивация, не удаление; подтверждение); удаление триггера — нет (находка №2).
6. **Узнавание вместо запоминания** — в целом хорошо: вся нужная информация (счётчики, триггеры) видна на экране без необходимости что-то держать в памяти.
7. **Гибкость и эффективность** — есть быстрые действия (клик по `+N` для acknowledge), но нет шорткатов для частых power-user операций (например, массовая архивация нескольких найденных неиспользуемых скиллов сразу — только по одному).
8. **Минималистичный дизайн** — сильная сторона продукта: минимум хрома, текст вместо иконок там, где можно, эта работа уже проделана в проекте.
9. **Помощь с ошибками** — `deleteError` показывает `localizedDescription` — техническое сообщение файловой системы, не человекочитаемая рекомендация (например, «нет прав на запись» вместо «Permission denied»). Минорная находка, не включена отдельной строкой в таблицу ниже (cosmetic, не набрала веса).
10. **Помощь и документация** — отсутствует полностью: нет онбординга, нет объяснения в самом приложении, что такое «плотность скиллов», зачем сканируются файлы, что данные не покидают машину (находка №2 в контексте брифа/позиционирования из `competitive_analysis_final.md`).

## Все найденные проблемы

| # | Проблема | Эвристика | Severity | Экран | Рекомендация |
|---|---|---|---|---|---|
| 1 | Попап описания скилла (`​.popover` внутри `MenuBarExtra(.window)`) рендерится отсоединённым от вызвавшей строки — наблюдалось как плавающий блок текста в произвольном месте экрана, без стрелки-якоря. **Понижено 2026-08-29:** воспроизведено дважды в тестах (включая реальный mouse-клик, не только автоматизацию), но владелец не встречает этого в обычном использовании — не приоритизируется дальше, задокументировано на будущее | Консистентность (4), Видимость (1) | ~~Major (3)~~ **Held — не приоритет** | Попап описания скилла | Не действовать сейчас; если проявится в реальном использовании — тогда заменить `.popover` на кастомное overlay-позиционирование внутри `.window`-попапа |
| 2 | Удаление триггера (✕) происходит мгновенно, без подтверждения и без возможности отменить — прямая запись в `SKILL.md` | Контроль (3), Предотвращение ошибок (5), Консистентность (4) | **Major (3)** | Попап описания → Edit | Добавить хотя бы toast «Removed “X” · Undo» на несколько секунд, либо мелкое подтверждение как у Delete Skill |
| 3 | Chevron `›` у названия скилла визуально совпадает с конвенцией «раскрыть/свернуть», но не раскрывает/сворачивает — открывает информационный попап; при этом реальная иерархия роутер→подскиллы вообще не сворачивается | Соответствие реальному миру (2) | **Minor (2)** | Главный список | Сменить иконку на что-то однозначно «информационное» (`ⓘ` или подобное), оставив `›` только если появится настоящее сворачивание групп |
| 4 | У роутера без собственных прямых вызовов не показан вообще никакой счётчик — а порядок в списке при этом определяется максимумом среди его детей, что невидимо без разворачивания. Это уже дважды путало реального пользователя в этой сессии | Видимость состояния системы (1) | **Major (3)** | Главный список | Показывать бледным/приглушённым цветом счётчик = максимум среди детей рядом с именем роутера, а не полностью прятать число |
| 5 | Сообщение «Saved» после успешного сохранения триггера/команды технически устанавливается в состояние, но поле сразу же схлопывается (`isAdding = false` вызывается в той же синхронной ветке) — пользователь физически не может увидеть подтверждение успеха; при ошибке сообщение показывается корректно (асимметрия) | Видимость состояния системы (1) | **Major (3)** | Попап описания → «+ Text-trigger» / «+ /trigger» | Показать «Saved» не в схлопывающемся поле, а как отдельный transient-тост, либо задержать `isAdding = false` на ~1 секунду после показа «Saved» |
| 6 | В приложении нет вообще никакого объяснения назначения/приватности продукта — ни при первом запуске, ни где-либо в UI | Помощь и документация (10) | **Minor (2)** | Всё приложение | Не блокер для текущего личного использования; станет обязательным при публичном релизе — короткая строка в футере или отдельный `?`-попап |

## Сводка

- **Критические проблемы (severity 4):** нет.
- **Серьёзные проблемы (severity 3), статус на 2026-08-30:** #2 и #5 — закрыты (см. обновление ниже и `docs/fixes_pending.md`). #1 — закрыто (архитектурно). #4 — отклонено владельцем, больше не в скоупе. Из шести исходных находок серьёзного уровня в открытом состоянии не осталось ни одной.
- **Общая оценка.** Сильная сторона продукта — минимализм и приватность (уже отмечено эвристикой #8, и подтверждено конкурентным анализом: ни один из 8 исследованных конкурентов не даёт того же уровня privacy-by-default). Системная слабость — **асимметрия обратной связи**: везде, где действие может провалиться, есть явная сигнализация (`deleteError`, error-текст в `AppendField`), но там, где действие успешно — сигнал либо отсутствует, либо технически не успевает отрендериться. Это системный паттерн, не единичная случайность (находки №2 и №5 — одна и та же болезнь в двух местах).
- **Топ-5 рекомендаций:**
  1. Исправить невидимость «Saved» в `AppendField` — самая дешёвая по трудозатратам и самая объективно доказанная находка (баг в коде, не гипотеза).
  2. Добавить лёгкое подтверждение/undo на удаление триггера — привести к консистентности с Delete Skill.
  3. Показать приглушённый «максимум среди детей» счётчик у роутеров без собственных вызовов — недорогая правка, устраняющая уже дважды случившуюся путаницу.
  4. Перед публичным релизом — добавить минимальное объяснение privacy-модели в UI (перенять паттерн WakaTime/AgentPeek — не переносить их усложнённый онбординг, только сам факт объяснения «данные не покидают машину»).
  5. (Снято с приоритета) Позиционирование `.popover` внутри `MenuBarExtra(.window)` — задокументировано, но не в фокусе: владелец не наблюдает это в реальном использовании.

## Обновление 2026-08-30 — архитектура мунбара переписана

> **Метод этого обновления — не тот же, что у оригинального аудита.** Оригинал верифицирован живым
> скриншотом. Здесь — чтение `git diff` по несохранённым (uncommitted) правкам владельца, без пересборки и
> редеплоя приложения (по `feedback_skilloptimizer_deploy_gate` правке — `scripts/deploy.sh` только по
> прямой просьбе). Живой процесс `SkillOptimizer.app`, который сейчас запущен, старше этих правок — то, что
> ниже, не подтверждено визуально, только по коду. Переверифицировать скриншотом после следующего деплоя.

Пока писался этот документ, владелец переписал мунбар-механику: `MenuBarExtra` (SwiftUI) заменён на
нативный `NSStatusItem` + собственный `NSPanel` (`AppDelegate.swift`, `AppWindowController` —
переименованный `PinnedPanelController.swift`). Причина в коде задокументирована прямо в комментариях
файла: клики через `MenuBarExtra(.window)` иногда требовали двух-трёх нажатий, чтобы засчитаться за одно.

**Находка №1 — закрыта 2026-08-30 (решение владельца).** Причина находки была явно завязана на `.popover`
внутри `MenuBarExtra(.window)` — этого механизма в коде больше нет вообще. Новая панель позиционируется от
реального `button.window.frame` статус-айтема, попап описания скилла теперь открывается с
`arrowEdge: .leading` (было `.trailing`) с подобранным отступом строки. Владелец принял архитектурный фикс
как достаточный без отдельной визуальной переверификации — закрыто.

**Находка №4 — отклонена 2026-08-30 (решение владельца): счётчик не нужен.** Приглушённый «максимум среди
детей» у роутера без прямых вызовов сознательно не делается. Снято из Must (`prioritization.md`,
`mvp_scope.md`, `features_list.md`, PRD roadmap и Notion — обновлено).

**Находки №2 и №5 — закрыты 2026-08-30.** Планы фикса ниже применены в `DescriptionPopover.swift`. Полные
Job Story и сниппеты — `docs/fixes_pending.md` (там же и в Notion, статус закрыт).

*Находка №5 (Saved невидимо).* Причина в `AppendField.save()` (`DescriptionPopover.swift`): `isAdding = false`
вызывается в той же синхронной ветке, что и `status = "Saved"`, поэтому поле схлопывается раньше, чем успевает
отрендериться success-текст. Минимальный фикс — задержать схлопывание:
```swift
private func save() {
    let value = text.trimmingCharacters(in: .whitespacesAndNewlines)
    do {
        try onSave(value)
        status = "Saved"
        text = ""
        onSaved()
        DispatchQueue.main.asyncAfter(deadline: .now() + 1.2) {
            isAdding = false
            status = nil
        }
    } catch {
        status = "Error: \(error.localizedDescription)"
    }
}
```
Поле остаётся открытым с текстом «Saved» ~1.2с, потом само схлопывается — та же механика, что уже
используется для error-текста (который остаётся видимым, так как ветка `catch` не трогает `isAdding`).

*Находка №2 (удаление триггера без подтверждения).* Сейчас — мгновенный `try?` без подтверждения и без
обработки ошибки. Самый дешёвый фикс, консистентный с уже существующим паттерном в этом же файле
(`isConfirmingDelete` у Delete Skill) — тот же двухшаговый confirm на уровне фразы:
```swift
@State private var confirmingPhrase: String? = nil

// в строке фразы, вместо мгновенного xmark.circle.fill:
if confirmingPhrase == phrase {
    HStack(spacing: 4) {
        Button { confirmingPhrase = nil } label: { Image(systemName: "xmark") }
            .buttonStyle(.plain).foregroundStyle(.secondary)
        Button {
            try? SkillFileEditor.removeTriggerPhrase(phrase, from: path)
            onTriggersSaved()
            confirmingPhrase = nil
        } label: { Image(systemName: "checkmark.circle.fill") }
            .buttonStyle(.plain).foregroundStyle(.red)
    }
} else {
    Button { confirmingPhrase = phrase } label: {
        Image(systemName: "xmark.circle.fill")...
    }
}
```
Альтернатива — toast «Removed "X" · Undo» на несколько секунд вместо confirm-до-действия; дороже в
реализации (нужно хранить снятую фразу для отката), но меньше трения для реактивного сценария персоны
«Точечный доработчик триггеров». Confirm — минимальный вариант, переиспользует уже написанный паттерн.

Оба фикса — только предложены, не применены: `DescriptionPopover.swift` активно не редактировался в этой
сессии, но `SkillDensityMenu.swift` и соседние файлы — да, правки идут параллельно с этим разговором,
поэтому код не трогаю без явной просьбы приложить именно эти патчи.

**Регресс «Updated: HH:mm:ss» — решение владельца: вернуть.** Восстановить видимый текстовый лейбл рядом с
иконкой рефреша (было убрано в пользу tooltip-only). Предлагаемый снип для футера `SkillDensityMenu.swift`
(на месте текущего `Image(systemName: "arrow.clockwise")` в ветке `else`):
```swift
} else {
    HStack(spacing: 7) {
        Text("Updated: \(model.lastUpdated.formatted(date: .omitted, time: .standard))")
            .font(.caption)
            .foregroundStyle(.white)
        Image(systemName: "arrow.clockwise")
            .font(.system(size: 11))
    }
}
```
Не применено — тот же принцип: файл сейчас в активной ручной правке.

**Находки №2 и №5 — статус без изменений, всё ещё открыты.** `DescriptionPopover.swift` (где живёт логика
`AppendField` и удаления триггера) не тронут этой правкой ни строкой — обе Major-находки остаются в силе,
это по-прежнему топ приоритет для публичного релиза.

**Находка №4 (приглушённый счётчик роутера) — без изменений, всё ещё открыта.** Не задета этой правкой.

**Находка №3 (chevron) — частично смягчена.** У `chevron.right` появилось hover-состояние (светлеет при
наведении: `isHovering ? .primary : .tertiary`) — небольшой сигнал интерактивности, которого не было. Сама
семантическая неоднозначность (выглядит как «раскрыть», на деле открывает инфо-попап) не изменилась —
находка остаётся, но с чуть меньшей остротой.

**Новая мелкая находка, не в исходном отчёте.** В футере убран текстовый лейбл «Updated: HH:mm:ss» —
теперь это доступно только через `.help()`-тултип на иконке рефреша при наведении. Раньше время последнего
обновления было видно всегда, теперь только по ховеру — небольшой шаг назад по «Видимости состояния
системы» (эвристика 1), симметричный по духу находкам №2/№5 (успех/статус спрятан за лишним действием).
Severity: Minor — не блокер, но стоит держать в поле зрения.

**Позитивная находка вне скоупа исходного аудита.** `SkillDensityModel.swift` теперь строит список из
*всех* известных на диске скиллов (`allKnownNames`), а не только из тех, что хоть раз вызывались
(`invokedNames`) — раньше скилл с нулём вызовов за всё время не появлялся в списке вообще. Это прямое
попадание в основной JTBD Персоны 1 («понять, какие скиллы реально живые») — раньше приложение физически не
могло показать «никогда не использовавшийся» скилл, теперь может. Плюс: правильно фильтрует отключённые
плагины (`enabledPlugins` из `settings.json`), чтобы они не флудили список нулями. Стоит внести в
`docs/features.md` после деплоя — сейчас это неразвёрнутый код, не факт продукта.

**Также появилось ручное изменение размера окна** (`ResizeHandle.swift`, drag за нижний край, только по
высоте) — новая возможность, не описанная нигде в `docs/features.md`, `mvp_scope.md` или PRD. Не фича из
исследования — родилась из собственной итерации владельца поверх находки эвристики 7 аудита («нет
шорткатов для... операций», хоть и про другое). Стоит задокументировать после деплоя и решить, входит ли
она в v1.0 или это уже v1.1-уровень polish.
