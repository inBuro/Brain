# _archive

Retired agent-persona folders and superseded router/registry versions. Move a folder here instead of deleting it — don't remove `agent-memory/` symlinks before moving, they still point at live memory in `~/Brain/.claude/agent-memory/<id>/` in case the persona is revived.

Nothing archived yet.
