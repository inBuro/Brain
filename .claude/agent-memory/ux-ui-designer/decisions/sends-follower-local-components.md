---
name: sends-follower-local-components
description: Локальные мастера на артборде SendsFollower — осознанный долг, не переносить в DS
metadata:
  type: project
---

# Sends Follower — локальные компоненты на артборде

**Решение (2026-06-24):** мастера `Frame 5` (`2505:21696`) и `Hero` (`2505:21695`) остаются локально на артборде `SendsFollower` — НЕ переносить в DS-файл (страница Atoms/Molecules/Organisms).

**Почему:** одноразовые page-layout компоненты, специфичные для этого экрана. Перенос в DS потребовал бы абстракции и поддержки, которая не оправдана для единственного use-case. Принято осознанно, не упущение.

**Технический долг:** если SendsFollower-экран будет реплицироваться или появится аналогичный pattern на другом экране — пересмотреть и оформить как DS-organism.

**Как применять:** при работе с `Frame 5` (`2505:21696`) — редактировать мастер напрямую (symbol на странице `11` или на артборде `2505:17290`), не через per-instance overrides. Инстанс `2505:21697` (RETURN) связан с мастером и получает изменения автоматически.
