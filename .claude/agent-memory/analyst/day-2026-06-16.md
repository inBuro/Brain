---
name: day-2026-06-16
description: The 2026-06-16 traffic day — first-ever buy_click in site history (US iPad, control variant), 4th-biggest day (22 sessions), reddit abletonlive_post #3 was the engine; also confirms the experiment exposure bug is FIXED
metadata:
  type: project
---

# 2026-06-16 — first buy_click ever + exposure bug fixed

Measured 2026-06-17 00:20 ICT (right after the day closed). Owner-excluded.

## TWO LANDMARK FINDINGS
1. **FIRST `buy_click` IN SITE HISTORY.** Until now buy_click had NEVER fired once
   (lifetime 0). On 06-16 17:56 ICT it fired exactly once. Session
   `019ed013-7f5b-…` (distinct_id `019ed013-7f4d-7732-af18-2875aae6c4b2`):
   - US, **Tablet (iPad), Chrome iOS 149, iOS**, referrer `$direct`, variant **`control`**
     (the feature-led hero).
   - Event flow: 17:56:39 `$pageview` on `/` → 17:56:41 **`buy_click`** label
     "Buy on Gumroad • $39" (confirmed by `$autocapture` `$el_text`="Buy on Gumroad • $39")
     → `$web_vitals`. Clicked Buy on `/` ~2s after landing, no scroll, no `$pageleave`
     (the click navigated away to Gumroad).
   - **NO `purchase` followed** (purchase=0 on 06-16 and 06-17). So: buy-intent click, not a sale.
   - SIGNIFICANCE: buy_click on `/` at 2s with no scroll means a buy CTA is now reachable on
     the homepage WITHOUT scrolling to #subscribe — i.e. the "put a paid CTA above the fold/in
     nav" hypothesis from [[buy-cta-visibility-2026-06-15]] appears to have shipped (or it was a
     returning visitor who knew where to click). Either way the buy surface is no longer
     bottom-only. Verify the current landing has a hero/nav Buy before relying on this.
   - n=1 — a signal, not a rate. Don't extrapolate a conversion %.

2. **EXPERIMENT EXPOSURE BUG IS FIXED (confirms c0dde357 worked).** `$feature_flag_called`
   now FIRES — lifetime 13 events / 13 persons, earliest 2026-06-15 15:40 ICT (right after the
   ~14:50 deploy), latest 06-16 22:09. By variant: **control 6 / test 7** (~50/50). This
   overturns the old "exposure STILL = 0 / UNVERIFIABLE" note in [[posthog-access]] — the dev's
   poll-until-posthog-exists fix landed and real visitors now emit exposure. Experiment 376381
   has finally started accumulating. (The 06-16 buy_click landed on `control`.)

## VOLUME — 4th-biggest day
06-16 = **27 pv / 22 sessions / 21 unique** (owner-excluded, bots NOT removed). Rank vs history:
06-10=30, 06-11=21(bot-inflated), 06-15=16, **06-16=22**. After discounting ~4 likely bots
(Microsoft Teams CDN referrers `teams.public.onecdn`/`statics.teams.cdn.office.net`, browser=None,
or `vh==sh` 1080==1080) → ~17-18 real human sessions. Evenly spread 12:00-23:00 ICT, peak 21:00
(4 sessions); NO same-minute scraper cluster (contrast 06-11). The closed-day total 06-16 (22) just
edged out 06-15 (16) — two solid days back to back after the dead 06-13/14.

## SOURCES (session-level, 06-16)
**Reddit `abletonlive_post` was the engine** — the r/abletonlive post #3 drove ~10 sessions, almost
all Mobile, almost all entering `/free-custom-modes` directly (the post linked the free-modes page),
mostly single-pageview bounces. Plus: telegram `organic` 1 (BY Desktop, video_play), maxforlive
`control_xl_listing` 1 (FI Mobile, `/` bounce), reddit `introduction_post` tail 1 (IN), direct/None
several (incl. the US iPad buy-click + the Teams-CDN bots). So the live reddit marker is now
`abletonlive_post` (NEW campaign value — add to the channel table; this is post #3, distinct from the
06-11 `ableton_post`).

## CONVERSIONS (06-16)
- **buy_click ×1** (the landmark above), **mode_download ×1** (DE Mobile Firefox iOS, reddit
  abletonlive_post, `/free-custom-modes`), **video_play ×2** (CA Mobile reddit + BY Desktop telegram),
  social_click 0, **real purchase 0**.
- Same shape as always EXCEPT the buy_click: reddit pulls free-mode/video engagement, and for the
  first time one visitor expressed paid intent.

## SCROLL (06-16, from $pageleave) — engaged sessions DID reach deep this time
Several `/` sessions hit 100% scroll/content (e.g. telegram BY 1735s 100%, a 168s 100%, reddit-CA
75% on `/`). So on 06-16, unlike 06-15, multiple visitors scrolled the whole page — yet still no
buy_click came from a deep-scroll session (the one buy_click was a 2s no-scroll click on `/`). The
deep scrollers were the free-modes/video crowd.

## NOTE ON "today" (06-17)
Measured at 00:20 ICT 06-17 = the day had just begun, ZERO events on 06-17 yet. All the day's
substance (YouTube comment thread, reddit post #3, telegram, Isotonik outreach context) lives in the
06-16 data, which had just closed. When asked about "today" near ICT midnight, report the just-closed
day and say so.
