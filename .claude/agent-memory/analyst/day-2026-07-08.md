---
name: day-2026-07-08
description: Biggest traffic day on record — 80 pv/60 DAU, driven by a CONFIRMED maxforlive.com listing for Sends Follower (20+ sessions, no UTM, broad EU/US geo) layered on top of the tail of the 07-07 Telegram wave; owner confirmed maxforlive is an ongoing channel; "Max for Live" custom channel rule fixed to catch referrer-only maxforlive traffic
metadata:
  type: project
---

Full-day read, Asia/Bangkok 2026-07-08 00:00–24:00, `filterTestAccounts:true` + a per-session
SQL audit (owner excluded by `person.properties.email`).

## Headline numbers
- **80 `$pageview` / 60 unique visitors (DAU) / 62 external sessions** — the biggest day on
  record, beating the 07-07 Telegram-launch day (32 pv / 21 DAU) and blowing past the trailing
  7-day average before it (07-01→07-07: 14.0 pv/day, 8.0 DAU/day) — **5.7x the pv average, 7.5x
  the DAU average**, +150% pv / +186% DAU vs the day before. 4 owner sessions (Brave
  desktop+iOS QA) correctly excluded.
- Two layered traffic events, not one: (a) the tail of the [[sends-follower-telegram-launch-2026-07-07]]
  wave trickling into the morning (the HU Opera visitor who fired the first buy_click landed
  00:28 ICT — already documented in that memory), and (b) a **brand-new maxforlive.com listing
  spike** 19:05–22:46 ICT (evening), unrelated to Telegram.

## Sources (session-level, n=62 external)
| Channel | Sessions | Note |
|---|---|---|
| **maxforlive.com** (referrer only, **no UTM**) | **20** | Biggest single day yet for this channel; ALL 20 landed directly on `/sends-follower`. Spread over ~3.7h, 1-10 min apart, every session a different browser/OS/country → reads as real listing traffic, not a bot burst. **Owner-confirmed 2026-07-09: this is a real, intentional, ongoing listing** — not a one-off. |
| $direct (no referrer, no UTM) | 24 | Mixed bag — some are Control XL page visits with no discernible source, some HU-visitor returns. |
| Reddit (`com.reddit.frontpage` 5 + `www.reddit.com` 2 + 1 utm-tagged) | 8 | Business as usual. |
| fadercraft.com self-referrer (internal nav / same-session return) | 6 | Not a new-visit source. |
| YouTube | 2 | `utm_campaign=sends_follower_yt` — a known, owner-set-up tracked redirect for a Sends Follower video on the Fadercraft YouTube channel (confirmed by owner 2026-07-09, NOT a new/unexplained marker — no investigation needed). |
| Google organic | 2 | `www.google.com` referrer, no UTM, both entered `/control-xl`. |

## Top pages (external, pageviews/sessions)
`/sends-follower` **40/38** (all-time high for SF, almost entirely the maxforlive burst) >
`/control-xl` 30/29 > `/` (hub) 7/6 > `/legal` 2/2 > `/free-custom-modes` 1/1. No hits on
`/guide/launch-control-xl-mk3-across-live-sets` or `/updates` yesterday.

## Geo / device
Broad Western spread, no single-country dominance: US 10, DE 6, GB 6, ES 4, IT 4, BE 3, FR 3,
then AU/CA/CH/HU/SE/TH/UA 2 each + several 1s (AT/FI/LV/NL/PL/PT/RO/SG/TN). Device split
Desktop 31 / Mobile 29 — balanced. This geo shape (wide EU/US spread, no RU cluster) is itself
evidence the maxforlive burst is a different, more "organic Western" audience than the 07-07
Telegram wave.

## Conversion
- **buy_click = 3, purchase = 0** (same pattern as every day since launch — nobody has ever
  completed checkout same-day). The three: (1) HU/Opera returning visitor, `/sends-follower`→
  `/control-xl`, no purchase — carryover from the 07-07 wave, already known; (2) FR/Firefox via
  `www.reddit.com` on `/control-xl`, genuinely engaged (video_play×2, demo_interact×1) before
  clicking buy; (3) Android/Chrome, geo unresolved (`None`), `$direct`, single pageview on
  `/sends-follower` then an immediate buy_click with zero other engagement — thin/ambiguous
  signal, possibly a fast bounce-click or bot; low priority given tiny n, but worth a replay
  check if the pattern recurs.
- `video_play` 23, `demo_interact` 10 — both well above typical daily counts, consistent with
  real people actually browsing the SF page during the maxforlive burst.
- `newsletter_signup` still has never fired (not in taxonomy).

## VPN-suspect cohort (399599)
**5 of 60 unique visitors (~8%)** — much lower than the 07-07 Telegram wave's ~42% hit rate.
Confirms yesterday's dominant new traffic (maxforlive) is NOT the RU/VPN audience — a genuinely
different, geographically clean Western channel.

## Anomalies / gaps found
- **No bot-burst signature in the maxforlive traffic** — spaced by minutes, diverse
  fingerprints, real referring domain. Read as genuine.
- **Channel-rule gap — FIXED 2026-07-09.** The existing "Max for Live" custom channel type
  ([[custom-channel-maxforlive]]) only matched `utm_source=maxforlive` exact, so the referrer-only
  maxforlive.com sessions were falling through to "Referral" in Web Analytics. Owner confirmed
  maxforlive.com is a real, intentional, ongoing Sends Follower channel (see below) — appended
  `referring_domain icontains maxforlive` (OR) to the same rule. Verified retroactively against
  07-08 session data: `$channel_type` for that day now reads **Direct 31, Max for Live 22,
  Reddit 8, Referral 4, Organic Search 2, Organic Video 2** — the maxforlive sessions moved out
  of Referral into "Max for Live". Full rule + verify query in [[custom-channel-maxforlive]].

## Confirmed channel status (owner, 2026-07-09)
**maxforlive.com is now a confirmed, ongoing acquisition channel for Sends Follower** — owner
placed the device on the maxforlive.com listing deliberately and expects continued (not
one-time) traffic from it. Treat future maxforlive-referrer sessions on `/sends-follower` as
expected, recurring channel volume, not a one-off spike to re-investigate each time.
