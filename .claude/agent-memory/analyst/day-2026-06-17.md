---
name: day-2026-06-17
description: The 2026-06-17 day — owner reports FIRST SALE, but PostHog had only setup/test pings (is_test=True) for it — NOT a webhook bug; 2nd-ever buy_click came from a RETURNING NL visitor; new events cta_view + demo_interact now live
metadata:
  type: project
---

# 2026-06-17 — "first sale" reported, but purchase webhook didn't capture it

Measured 2026-06-17 ~18:30 ICT (day still in progress). Owner-excluded.

## THE SALE vs PostHog data — RESOLVED 2026-06-18, root-cause corrected 2026-06-19
- Owner reported the FIRST SALE. On 06-18 a live query showed pings DID reach PostHog for
  this sale: `purchase` rows at **06-17 13:50:33 ICT, sale_id `aKrKs--u0JFZjW2haKM73w==`,
  order 1010996217, $39** — all landing **`is_test=True`** on fallback distinct_id
  `gumroad:5290360120821` (not stitched to the buyer). So analytics dropped them (is_test
  filter) and they weren't attached to the person.
- ROOT CAUSE (corrected 2026-06-19): NOT a webhook bug. `gumroad-ping.js` honestly mirrors
  Gumroad's `test` form-field (`is_test: f('test') === 'true'`) — no mis-set, no inversion.
  Those is_test=True rows are **setup/test pings** fired while the Gumroad Ping endpoint was
  being stood up (~same time as sale #1). There was no real non-test ping because the
  pipeline wasn't live yet when the sale happened. Verified vs deployed code, owner sign-off.
- FIX APPLIED: hand-backfilled one clean `purchase` (is_test=false, source=manual-backfill)
  attached to the buyer's distinct_id `019eca70-…` / person bcf21ee7 / the buy session
  `019ed447-…`, country NL, on 06-17 13:50. Full record + dedupe rule in
  [[sale-1-attribution]]. Real-purchase count is now 1, no dup; attribution = maxforlive,
  control variant.
- DONE (eng, 2026-06-19): `gumroad-ping.js` now derives a deterministic per-sale `uuid`
  from `sale_id`, so PostHog collapses Gumroad webhook RETRIES into one `purchase` event
  (no more row-per-retry). The Buy href ph_did/variant/cta stamp is live too (that
  attribution read in [[sale-1-attribution]] CODE FINDING holds). is_test needs no fix —
  a genuine non-test sale will simply land as purchase(is_test=False) on its own.

## SECOND-EVER buy_click — a RETURNING visitor (likely the buyer)
- 06-17 13:47 ICT: `buy_click` "Buy on Gumroad • $39", **NL, Desktop, Firefox**,
  variant `control`, referrer `fadercraft.com` (internal nav, not a fresh entry).
- distinct_id `019eca70-97b0-…` is a **RETURNER**: first seen **06-15 15:40** entering
  `/free-custom-modes` → `/`, lots of autocapture clicks (engaged), left. Came back
  **2 days later straight to `/`** and clicked Buy. The session had pv=0 in the day
  window because SPA didn't emit a new `$pageview` on the bare-`/` re-entry — the
  buy_click + autocapture + pageleave (3m57s on page) are the whole session.
- This is the textbook returning-buyer path and the best candidate for "the buyer"
  (NL, $39, today). n=1, can't prove it = the Gumroad sale, but timing+geo+intent fit.
- Only buy_click before this was 06-16 17:56 US iPad (see [[day-2026-06-16]]). So
  **lifetime buy_click = 2, both `control` variant, both this week.**

## NEW EVENTS NOW LIVE (correct old "GAP" notes)
- **`cta_view`** and **`demo_interact`** are NOW in the event schema (first fired 06-16).
  This RESOLVES the long-standing gaps: the demo-interaction gap ([[demo-engagement-baseline]],
  "NO demo-interaction event yet") is CLOSED — `demo_interact` exists and fires.
- Daily CTA-event counts (owner-excl): cta_view 06-16=7, 06-17=4. demo_interact 06-16=5,
  06-17=0. So a CTA-visibility/impression tracker + demo-interaction tracker shipped
  ~06-16 (fits the buy-cta-visibility fix in [[buy-cta-visibility-2026-06-15]]).
- `cta_view` = a Buy CTA became visible in viewport (impression). Pair it with buy_click
  for a true CTA-impression→click rate now (wasn't possible before).

## VOLUME
- Lifetime (owner-excl): **104 sessions / 94 people / 139 pv**; manual bot tag → ~20 bot
  / **~85 human sessions**. Last-7d (since 06-11): 69 sessions.
- Daily pv/sessions: 06-07=2, 06-09=3, 06-10=30, 06-11=21, 06-12=4, 06-14=1, 06-15=16,
  06-16=22, 06-17=6 (still running at measurement). Shape = repeated Reddit-post pushes,
  no organic tail; 06-15/16/17 = a sustained reddit run (abletonlive_post #3).

## SOURCES (session-level, lifetime)
direct 55, reddit 35, internal_nav 7, maxforlive 3, telegram 2, MS-Teams-CDN 2.
CAVEAT: reddit is UNDERCOUNTED — reddit app WebView strips referrer → lands in `direct`.
Much of the 06-10 "direct 27" was the bot burst + WebView reddit. Reddit is the real
top human source by engagement; raw-direct is inflated by bots/WebView.

## CONVERSION FUNNEL (lifetime, session-level, owner-excl)
pageview sessions 104 → cta_view 9 → demo_interact 5 / video_play 8 / mode_download 6
→ buy_click 2 → real purchase 0 (in PostHog). buy_click rate = 2/104 ≈ 1.9% of all
sessions (2/85 ≈ 2.4% of humans). mode_download ≈ 6/85 ≈ 7% of humans (steady free pull).
