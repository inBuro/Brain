---
name: day-2026-07-10
description: Full-day traffic audit 2026-07-10 — post-spike decay (34 pv/22 DAU/24 sessions) with Max for Live (maxforlive.com) now the clear majority channel (63%), 2 buy_click both via that channel on /sends-follower, 0 purchase, new unconfirmed Facebook utm_campaign=fb_page marker, Reddit absent for the day
metadata:
  type: project
---

Full-day read, Asia/Bangkok 2026-07-10 00:00-24:00, owner excluded by `person.properties.email`
(equivalent to `filterTestAccounts:true`), cross-checked events-table per-session audit against the
`sessions` table (6 owner session_ids identified and stripped from sessions-table aggregates —
`sessions` has no email column so it isn't owner-filtered by default).

## Headline numbers
34 `$pageview` / 22 unique visitors (DAU) / 24 external sessions.
- vs 07-09 (61 pv / 41 DAU / 43 sessions): **-44% pv, -46% DAU, -44% sessions** — continued decay
  off the 07-08 record spike (80 pv/60 DAU), now two days into normalization.
- vs trailing 7-day average (07-03→07-09, days: 12,5,18,8,32,80,61 pv): avg 30.9 pv/day, 21.1
  DAU/day, 22.0 sessions/day → 07-10 is **+10% pv, +4% DAU, +9% sessions vs that average** —
  roughly back to baseline, not a crash; the week average itself is inflated by the two spike days.

## Sources (24 external sessions, hand-classified from per-session SQL)
| Channel | Sessions | Share | Note |
|---|---|---|---|
| **Max for Live** (`utm_source=maxforlive` or referrer icontains maxforlive) | **15** | 63% | Dominant, all landing on `/sends-follower` except one on `/control-xl`. Confirms [[day-2026-07-08]] read — this is now the steady baseline channel, not a one-off. |
| Direct ($direct, no UTM, no maxforlive/facebook ref) | 6 | 25% | 4 unique people (2 are repeat visits same session-pair: GB Mobile Safari ×2, GB Desktop Safari ×2). |
| Ambiguous internal-referrer (ref=fadercraft.com, self-hop, origin unclear) | 2 | 8% | Same shape as the known [[sale-1-attribution]] gotcha (UTM lost after an on-site hop) — can't confirm true origin. |
| Facebook (**NEW marker**, see below) | 1 | 4% | `utm_source=facebook&utm_campaign=fb_page`, referrer `l.facebook.com`. |
| Reddit | 0 | 0% | Notable absence — first channel-null day for Reddit in a while. |

**NEW, unconfirmed Facebook marker: `utm_campaign=fb_page`** (PL/Desktop/Safari, ref `l.facebook.com`,
entry `/control-xl` then `/sends-follower`, footer_cta_view fired). Distinct from the known
`novation_group` campaign in [[posthog-access]]'s channel table — this looks like a Facebook Page
post/ad link-shortener redirect (`l.facebook.com` is FB's own link-tracking domain), NOT the
Novation-group post pattern. Single session so far — watch for a repeat before adding a permanent
channel-UTM table row.

## Top pages (external)
`/sends-follower` 24 pv / 22 sessions (89% of the day) > `/control-xl` 8 pv / 4 sessions >
`/` (hub) 1 pv / 1 > `/free-custom-modes` 1 pv / 1. Nothing on `/guide/...` or `/updates`.

## Geography
14 distinct countries in one day — wide spread, no single-country dominance: US 8 pv/6 sess,
**Thailand 8 pv/2 sess (flagged, see anomalies)**, GB 4 pv/4 sess, then CA/FR/PL/UA/EC/IL/IT/TR/AT/DE/NL
at 1-2 pv each. Device roughly Desktop-heavy today (most maxforlive sessions were Desktop).

## VPN/Proxy-suspect cohort (399599)
Only **1 of 22 unique visitors (~5%)** — the EC/Mobile Safari session (`es-419` locale vs EC geo).
Read as a likely **false positive**: `es-419` is a generic Latin-American Spanish locale, not a
specific-country subtag mismatch in the spirit the cohort targets (contrast the RU-locale-via-EU-VPN
pattern in [[vpn-proxy-suspects-cohort]]). No real VPN/geo-mismatch signal today.

## Behavior (bounce / duration, sessions-table, owner-6-session-ids stripped)
Of 22 sessions with a resolved bounce flag: **4 bounces ≈ 18% bounce rate**. Avg session duration
≈123s, but heavily skewed by two long engaged sessions (915s TR/maxforlive, 731s GB/direct repeat
visitor) — median session is much shorter (most land 0-110s). Typical shape for a listing-driven
audience: quick look, some engagement, leave.
(Note: raw `sessions` table returned 37 rows for the day before owner-stripping and included 8
zero-duration/`None`-bounce stub rows not present in the events-table pageview audit — likely
partial/technical session records, excluded from the bounce/duration read above for cleanliness.)

## Conversion
- **buy_click = 2**, both on `/sends-follower`, both via the **Max for Live** channel: UA/Desktop/Chrome
  (18:22 ICT, replay https://us.posthog.com/project/458316/replay/019f4bc3-a03e-71f1-8a2e-79dd8eb5963e,
  17 active seconds, 1 click) and DE/Desktop/Brave (22:08 ICT, footer_cta_view also fired same session,
  no separate recording found). First day both buy_clicks concentrate this cleanly in one channel+page
  combo.
- **purchase = 0** — no completed checkout same-day, consistent with the pattern since launch (checkout
  has never completed same-day as a buy_click).
- `footer_cta_view`/`cta_view` (Buy-CTA impression): fired in **9 of 24 sessions (~37%)** — CA, GB×2
  (same returning person), PL/facebook, NL, TR, DE, US, US-mobile.
- `video_play` = **15 total** across 8 sessions (IL, GB, GB, FR, IT, NL, FR, TR) — well above a typical
  day, real browsing engagement during the maxforlive-driven traffic.
- `demo_interact` = 1 (US/Safari, maxforlive UTM session on `/control-xl`).
- `faq_open` = 0, `social_click` = 0. `newsletter_signup` still has never fired (not in event schema).

## Anomalies / notable
1. **New unconfirmed Facebook campaign `fb_page`** (see Sources) — single session, needs a repeat to
   confirm as a real recurring channel vs one-off click.
2. **Reddit = 0 sessions today** — first zero-Reddit day in recent memory; worth watching if this
   persists (maxforlive may simply be crowding it out share-wise, not necessarily reddit itself dying).
3. **Two TH/Desktop/Brave sessions (1 pv + 7 pv) flagged ambiguous** — same browser/OS/city pattern as
   the owner's own tagged devices (see the [[day-2026-07-06]] precedent of a same-city untagged Brave
   bounce). Neither carries `email=hellokbbureau@gmail.com` so they pass the official filter, but
   plausibly an untagged owner device rather than a genuine external Thai visitor. Not re-flagged as
   owner (no hard evidence either way) — noting for pattern-matching if it recurs.
4. VPN-cohort hit is thin and looks like noise (see above) — no real geo-mismatch anomaly today.
5. Both conversions (buy_click ×2) landing in the Max for Live channel on the same page reinforces
   that channel's status as the primary demand driver right now, well ahead of Direct/Facebook/Reddit
   combined.

## New events discovered in schema (not previously documented)
`vpn_detected` and `purchase_refunded` now exist in the project's event taxonomy (confirmed via
`read-data-schema`) — not investigated in depth this session (out of scope for the day's traffic
read), but `vpn_detected` fired 4 times in the trailing week, all on 2026-07-08 (FI Desktop + SG
Mobile ×3), none on 07-10. Client-side VPN-detection logic apparently exists in the codebase already
— worth a closer look next time cohort/geo work comes up, since it may duplicate or complement the
manual [[vpn-proxy-suspects-cohort]] (399599) signal.
