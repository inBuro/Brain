---
name: day-2026-07-06
description: 2026-07-06 traffic audit answering "is it all bots or real people" — per-session bot/human/owner split, new guide page live, reddit visitor buy_click
metadata:
  type: project
---

Full-day per-session SQL audit (Asia/Bangkok, 2026-07-06 00:00–24:00), cross-checked against
`query-session-recordings-list` and official `filterTestAccounts:true` trends.

**Official filtered numbers (owner excluded, `filterTestAccounts:true`):** 8 `$pageview` across 3
paths — `/` = 3, `/guide/launch-control-xl-mk3-across-live-sets` = 3, `/free-custom-modes` = 2. DAU
per page == total per page (no repeat visitors on any page). This matched EXACTLY what per-session
SQL found once owner-email sessions were pulled out — confirms `filterTestAccounts` is doing its job
correctly today, no leakage.

**NEW PAGE LIVE: `/guide/launch-control-xl-mk3-across-live-sets`** — this is the JTBD guide asset
tracked as "PLANNED" in project-manager's [[../project-manager/content-assets.md]] — it went live
today (owner browsed it at 12:20–13:43 ICT). Worth flagging to PM to update its status.

**Bucket 1 — Owner/self (7 hits, correctly filtered out):** 5 full sessions + 2 orphan zero-pv
pings, all tagged `email=hellokbbureau@gmail.com`/`distinct_id=fadercraft-owner`. Two devices: Brave
Desktop/Mac (Hua Hin) doing heavy multi-page QA (8 pv across `/`, `/legal`, `/free-custom-modes` in
one session; another session 2 pv on the new guide page — this reads as the owner personally
checking the fresh guide page, not a "headless Brave" artifact despite real click/mouse-move replay
data — headless automation can still emit synthetic mouse events, so replay signal alone doesn't
disprove headless, but it doesn't matter here: owner-tagged sessions are excluded from official
numbers either way) + Chrome-iOS (= Brave iOS per longstanding UA note) mobile, also heavy clicking.
No session in the 12:00–19:00 window that's DEFINITELY the flagged "headless Brave test" stood out
as anomalous beyond the normal owner-QA pattern — if it happened, it's owner-tagged and excluded.

**Bucket 2 — Bot cluster (3 sessions, NOT real people):** 12:40:04–12:40:11 ICT, all three hit the
exact same brand-new URL (`/guide/launch-control-xl-mk3-across-live-sets`) within 7 seconds, 1
pageview each, zero clicks, no recording captured for any of the three:
- `019f35f0-da55-7dba-adf1-44df94556b57` — US Desktop, **`$browser`/`$os` = None, `$viewport_height`
  == `$screen_height` == 1400** — textbook headless/automation signature (no browser chrome, no UA
  parsed).
- `019f35f0-e9f6-7bbf-bf68-4b2caad84d57` + `019f35f0-efdc-70e6-8af5-664e3820048f` — TH Desktop
  Chrome/Windows, normal viewport-vs-screen delta (768/1080), but same URL seconds apart from the
  US headless hit — matches the recurring "TH Chrome/Windows burst right after a fresh URL exists"
  scanner/link-preview pattern documented since 2026-06-11/06-22. Read as a crawler/monitoring burst
  reacting to the new guide page, not a person. This pattern reappears every time a fresh URL goes
  live — treat any near-simultaneous multi-hit on a brand-new path as bot until proven otherwise.

**Bucket 3 — Real external humans, confirmed:**
- **Facebook (novation_group) → TH, 08:38 ICT** — Mobile Safari/iOS, entry `/free-custom-modes`,
  single-second bounce, no recording captured. Real device fingerprint, thin signal.
- **Reddit (`introduction_post` tail) → US/Tampa, 14:31 & 15:07 ICT — THE standout.** Same person
  (`distinct_id 019f3657-384e-…`), landed 14:31 on `/free-custom-modes`, tab went background for
  ~36 min (session timeout split it in two), came back 15:07 and in ~2 min: 50 clicks, 132 mouse
  events, `demo_interact`×1, **`buy_click`×1**, `footer_cta_view`×1. Replay:
  `https://us.posthog.com/project/458316/replay/019f3677-80b9-7eb7-834e-776725406158`. No `purchase`
  event fired today (checked) — so engaged but no completed sale, same pattern as every prior
  buy_click day.

**Bucket 4 — Ambiguous, low-confidence either way:**
- Two US/New York Mobile-Chrome-iOS sessions on `/`, 25s apart (15:12 ICT), zero clicks, no
  referrer/UTM — could be two real curious bounces or a scraper duo (same shape as historical
  same-second-pair bots, just not quite same-second). Recordings exist but show 0 mouse activity
  either way (`019f367c-22ee-74e5-926d-3bcae73f80a6`, `019f367c-8682-7a72-a1c9-74b8d1f39648`).
- TH Mobile Brave/Android, Hua Hin, 10:50 ICT, single 7s bounce on `/`, no email/no
  owner-flag — same city as the owner's own tagged devices today, so plausibly the owner's own
  untagged Android device rather than an external visitor. Not counted as a confirmed human.
- DE Mobile Safari, 00:55 ICT — real device fingerprint but 0 pageviews registered in-window
  (probably a trailing event from a visit that started just before midnight ICT); too thin to call
  a "session."

**Verdict for "is it all bots":** No — there's at least one clearly real, engaged human today (the
Reddit/Tampa buy-clicker), plus 1-2 plausible real bounces (Facebook/TH). But a meaningful slice of
today's "external-looking" pageviews (3 of 8 filtered pageviews, on the brand-new guide URL) is a bot
crawl burst, and another chunk of raw traffic is the owner's own QA. Total genuinely-external,
plausibly-human sessions today: ~2 confirmed (Facebook bounce + Reddit buy-clicker) + 2-3 ambiguous.
Traffic remains too thin for anything but Session Replay — no funnel/experiment reads today.
