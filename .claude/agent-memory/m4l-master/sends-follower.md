---
name: sends-follower
description: SendsFollower.amxd standalone device — paths, container layout, embedded JS, version-check feature, rack round-trip, LFO status. Read before any SendsFollower edit.
metadata:
  type: project
---

# Sends Follower — device facts (m4l-master)

Standalone project, separate from / unrelated to Control XL & Instrument Follower. Project wiki =
`~/Brain/Sends Follower/wiki/` (entities/concepts/index/log; docs in English, run Vale after edits).
Wiki has the functional model (LiveAPI send-gather → max → live.remote~ + buses); don't duplicate.

## Paths
- Device (edit in place): `~/Music/Ableton/User Library/Max Devices/SendsFollower.amxd`
- Archive (dated pre-edit backup, never overwrite): `~/Brain/Sends Follower/raw/archive/SendsFollower.YYYY-MM-DD[-HHMMSS].amxd`
- raw mirror (immutable, read-only): `~/Brain/Sends Follower/raw/SendsFollower.amxd`
- Rack wrapper: `~/Music/Ableton/User Library/Presets/Audio Effects/Audio Effect Rack/SendsFollowerRack.adg`
  (chain = Sends Follower → stock LFO). Note older wiki text says `MaxSendsFollower.adg` — actual file is `SendsFollowerRack.adg`.
- Embedded JS on-disk canon copies: `Max Devices/sends_follower.js` (5808 B, since Follow Mode 2026-06-17), `Max Devices/sf_version_check.js` (3106 B).

## Container layout (frozen, mx@c, like Control XL)
- `ampf` header → JSON @48 (BASE 0x20, of32=16) → embedded files → `dlst` dir + `mdat`.
- Resources in dlst (3 after 2026-06-17): patcher (JSON), `sends_follower.js` (TEXT), `sf_version_check.js` (TEXT).
- Invariants: `ptch`(LE@0x1C)=filesize−0x20; `0x30 + mx@c`(BE@0x2C) − dlst_pos = **16**; `dlst total` field = **N_dire×108 + 8** (2→224, 3→332).

## ⚠️ dlst dire record structure (CONFIRMED — reusable craft, see also amxd-format.md Path B)
Each field = `tag(4) + len(4 BE) + value(len−8)`. dire record = `dire + 108(BE) + 100B payload`.
Payload (fixed 100 B): `type`(ln12,4B 'JSON'/'TEXT') + `fnam`(ln28, **20B value null-padded → filename ≤19 chars + null**) + `sz32`(ln12,4B BE) + `of32`(ln12,4B BE, rel to BASE 0x20) + `vers`(ln12) + `flag`(ln12; JSON=17, TEXT=0/8) + `mdat`(ln12, checksum — Live ignores; new file can use 0).
**dires are FLAT (top-level), not nested.** Parse by walking TLV from `dlst+8` to EOF, not by trusting declared dire-len. `dlst total` field is NOT the byte length of payload (it's N×108+8); ignore it when parsing, recompute when writing.
**Filename ≤19 chars** (fnam value 20B incl. null) — `sends_version_check.js` (22) didn't fit → renamed `sf_version_check.js` (19). Check name length when adding a resource.
To add an embedded file (Path B): append body after last existing embedded file; new of32 = prev_end−BASE; rebuild whole dlst with all 3 dire (patch JSON sz32=L1+1, shift trailing-file of32 by ΔL_json, add new dire); patch ptch + mx@c; keep `0x30+mxc−dlst=16`.

## Version-check feature (added 2026-06-17) ✅
Mirror of Control XL update-check, **minus** `hdr_show`/`hdr_hide` (no header object here).
- 11 boxes / 10 lines added: `version_node`(`node.script sf_version_check.js @autostart 1`), `vlink_route`(`route dot url`), `vdot_sel`(`sel 0 1`), `version_link`(textbutton **"New Version"**, mint `[0.235,0.847,0.659,1.0]`, hidden:1, presentation_rect `[46,2,70,16]`), `vlink_show/hide`(`script show/hide version_link`), `vlink_thispatcher`, `vlink_fallback`(`loadmess https://library.gumroad.com`), `vlink_prepend`(`prepend set`), `vlink_store`(message, silent-store), `vlink_open`(`;\rmax launchbrowser $1`).
- Click path = Control XL silent-store v2: url → prepend set → store(message left inlet) → bang on click → vlink_open `$1` from left inlet. Don't use bare `v` (re-emits on receipt → opens browser unprompted).
- JS `sf_version_check.js` = Control XL `version_check.js` byte-for-byte EXCEPT header comment + 2 consts: `DEVICE_VERSION='1.0'`, `URL='https://fadercraft.com/api/sends-follower.json'`. Emits `dot 1/0` + `url <link>` on outlet 0, single `maxApi.post` diag, re-check 30 min.
- Release coupling: bump `DEVICE_VERSION` + manifest `latest` together; button lights when `latest > DEVICE_VERSION`.

## Follow Mode (Peak / Total) feature (added 2026-06-17; relabelled 2026-06-18) ✅
- Mode switch for the follow algorithm: **Peak** (per-return maximum of all sends, original behavior, default = index 0) ↔ **Total** (sum of all sends to the return, **clamped to 1.0** so downstream `scale 0. 1.` stays in range and percent monitor ≤ 100%, index 1). **Labels were "Max"/"Sum" until 2026-06-18 → renamed Peak/Total (cosmetic only; index↔behavior mapping UNCHANGED, Peak=0=old Max, Total=1=old Sum). JS handler `mode 0/1` untouched.**
- **Parameter name = "Mode"** (longname+shortname; was "Follow Mode" until 2026-06-18). **varname stays `follow_mode`** (cords are by object-id, not varname — never break them; no saved preset existed so longname change is safe).
- **Presentation label "Mode"** (plain `comment`, Arial Bold 9, grey `[0.74,0.74,0.74,1]`, presentation_rect `[7,134,60,14]`, varname `mode_label`) sits directly above the switch. Added 2026-06-18 (+1 box). Device had NO existing comment/live.comment idiom — used a Live-theme grey comment. To make room the switch moved **134→150** (`[7,150,116,17]`); dial bottom=133, label 134..148, switch 150..167, openrect height 169 — no overlaps with dial/switch/"New Version" button.
- 4 boxes / 4 lines: `follow_mode` (**live.tab**, presentation_rect now `[7,150,116,17]`, parameter_enable=1, enum `["Peak","Total"]`, `parameter_type` 2 / longname "Mode", `parameter_initial` [0] → saved with set + preset, default Peak); `mode_prepend` (`prepend mode`); `mode_loadbang` (`loadbang`); `mode_delay` (`delay 300`).
- Wiring: `follow_mode` outlet0 (index) → `prepend mode` → `js` (obj-46) inlet0. Initial value reaches JS on load via `loadbang → delay 300 → follow_mode` (bang makes live.tab re-emit its current/saved index). JS `followMode` also inits to 0 (Max), so even if the bang were late the default is correct.
- JS (`sends_follower.js`): added `var followMode=0`, new `function mode(m)` (parses "mode 0/1"), `bang()` now accumulates sum when followMode==1 with `if(result>1.0)result=1.0`, else max as before. **Output label `"max"` PRESERVED** (`outlet(0,"max",result)`) — downstream `route max`/`---max_send`/percent monitor depend on it; only the computed value changes.
- Percent at Sum ≤ 100% confirmed: clamp to 1.0 → `scale 0. 1. 0. 100.` → ≤ 100.

## Self-healing return-index fix — FROZEN 2026-06-18 ✅
- **DONE (re-freeze):** observer-based `sends_follower.js` (`3bc5ab8e`, 12303 B) embedded into device via Path B, replacing old `29ff530b` (5808 B, no observers). ΔL=+6495. Patcher untouched (65/65). `sf_version_check.js` (`a5d905fc`, 3106 B) kept. Container: JSON of32=16/sz32=32784 (unchanged); `sends_follower.js` of32=32800/sz32 5808→12303; `sf_version_check.js` of32 38608→45103/sz32 3106. ptch+6495, mx@c+6495, dlst total=332, `0x30+mxc−dlst=16`, ptch=filesize−0x20. Result device md5 **`bd21b848…`, 48585 B**, self-contained. Frozen archive: `raw/archive/SendsFollower.2026-06-18-110402-frozen.amxd`.
- **What it fixes:** return-index was resolved ONCE and stuck → device kept following first return's sends after move/add/del/reorder; reload revived once then froze again. Now JS owns detection (`detectReturnIndex` via `this_device canonical_parent` → `return_tracks (\d+)`, -1 on non-return) + 3 LiveAPI property-observers (device path `name`, `live_set` `return_tracks`, `live_set` `tracks`) + `resync(force)` rebuild-on-change with `rebuilding` reentrancy guard + anti-race `bang()` re-sync every `RESYNC_MS=500`. Patch `build N` now a hint only. Contract preserved: 1 in/out, `outlet(0,"max",result)` label intact, Follow Mode (`followMode`, Sum clamp `result>1.0`→1.0).
- Wiki: `concepts/self-healing-return-index.md` rewritten (script-side observer detection = source of truth, needs-verification resolved); `wiki/log.md` entry 2026-06-18.

## (historical) PENDING RE-FREEZE: "live target" fix in external canon JS (2026-06-18) — DONE, see above
- **BUG (founder-observed):** device only ever listens to sends of the FIRST return, regardless of which return it sits on; dragging it to a neighbour return revives it once, then it freezes again and ignores later-added tracks/returns.
- **ROOT (confirmed in patch + JS):** return-index was determined ONCE and stuck in TWO places:
  1. Patch chain (`obj-21 live.thisdevice`/`obj-22 loadbang → deferlow → delay 50/150/300 → obj-27 "path this_device" → obj-30 live.path → obj-31 live.object → obj-36 route path → obj-37 zl.slice 1 → obj-38 route return_tracks → obj-39 unpack i s i → obj-40 int → obj-41 change -1 → obj-42 prepend build → obj-43 "t l b b" → js "build N"`). Runs ONLY on load; after first success `obj-43` out2 sends `0 → obj-33 qmetro 500`, STOPPING the path re-poll. `change -1` (obj-41) suppresses repeats. So patch index never recomputes after first hit. Polling banger = `obj-34 "1" → obj-35 qmetro 33 → js` (33 ms).
  2. JS old `bang()` ran autoDetect ONLY when `sendRefs.length === 0`; after first build it never re-detected. `buildRefs()` was a static snapshot of `live_set tracks` → later add/remove tracks invisible.
  Drag revives because reload re-fires loadbang AND zeroes sendRefs — only on reload, not on move-without-reload nor on adding tracks.
- **FIX (external canon `sends_follower.js` ONLY, NOT frozen yet):** chose **LiveAPI property-observers** over throttled bang() probing. Observers fire only on real change (cheap, idiomatic) vs creating LiveAPI objects every ~300 ms forever. Three observers via `new LiveAPI(onLiveChange, ...)`: device path (`this_device canonical_parent`, prop `name`) catches device MOVE; `live_set` prop `return_tracks` catches return add/del/reorder (index shift); `live_set` prop `tracks` catches ordinary-track add/del (send set changes). `resync(force)` recomputes index via `detectReturnIndex()` + track count, rebuilds only on index-change OR count-change, guarded by `rebuilding` reentrancy flag. Anti-race: `bang()` cheaply re-syncs every `RESYNC_MS=500` (one detect + one getcount, no track walk) in case observers/path not yet resolved on cold load. `build N` from patch is now a hint only; JS's own detect is source of truth. Device on non-return/master → `detectReturnIndex` returns -1 → empty sendRefs, no console spam.
- **Contract PRESERVED:** inlets/outlets=1; inputs `build`/`bang`/`mode`; output `outlet(0,"max",result)` label UNCHANGED; Follow Mode (`followMode`, Sum clamp `result>1.0`→1.0) intact.
- **State:** external canon `~/Music/Ableton/User Library/Max Devices/sends_follower.js` REWRITTEN, **md5 `3bc5ab8e895e0c2f94ca1e6382e2e4df`, 12303 bytes on disk** (9472 unicode chars; size up from 5808 B due to observer logic + comments; cyrillic ×2 bytes), `node --check` clean. Frozen device still runs OLD embedded JS (`29ff530b`) — fix applies only after RE-FREEZE with Live CLOSED. Old embedded canon was `29ff530b`/5808 B.
- **RE-FREEZE PLAN (Live must be closed):** embed THIS `sends_follower.js` (`3bc5ab8e`, 12303 B, TEXT) in place of `29ff530b` via Path B; sz32 = 12303; of32 unchanged (still right after JSON); `sf_version_check.js` of32 shifts by Δ(12303−5808)=+6495; patch JSON sz32, ptch, mx@c; keep `dlst total=332`, `0x30+mxc−dlst=16`, ptch=filesize−0x20. Patcher boxes/lines UNCHANGED (65/65) — only the embedded JS body grows. Archive frozen result IMMEDIATELY (lesson from 2026-06-17 incident).

## Map button (SF-direct map to any Live param, BYPASS LFO) — FROZEN 2026-06-18 ✅
- **What:** front-panel **Map** button maps SF's OWN bipolar signal (−100..100, the SAME value driving the LFO leg via obj-16) straight onto a user-clicked Live parameter, via a SECOND `live.remote~` — **additive**, LFO leg (obj-11) untouched. Arm → click any Live param → captured → connected; Unmap (X icon) frees it; target path persists across reload via `pattr`.
- **UI = real stock LFO Map look.** Map button = `live.text` toggle (`appearance=2 mode=1`), exact stock colors from `liveui.map.maxpat` obj-48: idle bg `[0.1569×3,1]` + grey text `[0.549×3,1]`, **armed = orange text `[1.0,0.7098,0.1961,1]`** + orange border. Unmap "X" = `live.text` `usepicture=1 pictures=['multimap-unmap.svg','multimap-unmap.svg']` `usesvgviewbox=1 remapsvgcolors=1` — **REAL stock asset** `…/C74/packages/Max for Live/media/multimap-unmap.svg` (Cycling'74 built-in package, on Max search path on EVERY Live install → resolves by filename, **NOT embedded**, no `can't find file`). Target name label = `comment` (varname `map_label`, accepts `set <name>`). Presentation row at y=172; **openrect height 169→196** (+27). Stock Map button reference patcher: `/Applications/Ableton Live 12 Suite.app/…/C74/packages/Max for Live/patchers/liveui.map.maxpat` (obj-48 Map, obj-47 unmap-X).
- **Patcher (+16 box / +20 line; 66/65 → 82/85):** `map_button`(live.text Map toggle), `map_unmap`(live.text X svg), `map_label`(comment "—"), `map_route`(`route armstate target_path targetname store release`), `map_arm_set`(`prepend set`→button silent), `map_pathprep`(`prepend path`), `map_path`(`live.path`), `map_lblset`(`prepend set`→label), `map_id0`(msg `id 0`), `map_remote`(2nd `live.remote~`), `map_sig`(`sig~` tap from obj-16), `map_pattr`(`pattr map_target @parameter_enable 1 @autorestore 1`), `map_restoreprep`(`prepend restorepath`), `map_arm_send`(`prepend arm`), `map_unmap_tb`(`t b`), `map_unmap_msg`(msg `unmap`). **obj-46 (js) box bumped numoutlets 1→2** (outlet 1 = map control) — REQUIRED or Max drops the wire.
- **Wiring:** obj-16[0]→map_sig→map_remote[0] (signal tap, LFO leg intact); obj-46[1]→map_route; route out0 armstate→`prepend set`→map_button (silent, no feedback loop); out1 target_path→`prepend path`→map_path→[id]→map_remote[1]; out2 targetname→`prepend set`→map_label; out3 store→map_pattr; out4 release→`id 0`→map_remote[1]. Button click→`arm $1`→js inlet0. UnmapX click→`t b`→msg unmap→js. pattr out0 (autorestore)→`prepend restorepath`→js.
- **JS (`sends_follower.js`):** `outlets=2` (0=`max` value UNCHANGED; 1=map control). New: `arm(0/1)`, `unmap()`, `restorepath(...)` (varargs — path stored as TOKEN LIST not one symbol, no space-quoting dependency; sentinel `none`=clear), `selParamObs` on `live_set view selected_parameter` (captures only while `mapArmed`), `captureTarget`/`resolveAndConnect` (emits `target_path` list), `retryResolve` (defer via `Task.schedule(150)`, MAX 20 tries ≈3s — same anti-race as return-index), `emitTargetName`/`persistPath`. `selected_parameter` path CONFIRMED via Song.View. `live.remote~` right inlet = `id n` from live.path out0; **unmap = `id 0`** to right inlet (docs-confirmed).
- **Persist:** path tokens in `pattr` (`@parameter_enable 1 @autorestore 1`) save with device/preset; on load pattr emits list → restorepath → JS re-resolves with defer/retry.
- **Result device md5 `6f156eab973ecec0d9793f794c75cfce`, 68498 B, 82 box / 85 line.** Container Path B: JSON of32=16 sz32=42632; `sends_follower.js` **22368 B** of32=42648 md5 `c63475322970c9f8ea02d01620806551` (node --check clean); `sf_version_check.js` 3106 B of32=65016 md5 `a5d905fc` UNCHANGED. dlst total=332 (still 3 resources, svg NOT embedded), `0x30+mxc−dlst=16`, ptch=68466=fs−0x20. ⚠️ **Path-B gotcha re-learned:** prefix MUST be `d[:48]` (incl. mx@c sub-header at 0x20..0x30), NOT `d[:0x20]` — of32 is relative to 0x20 and JSON sits at 0x20+16=48; cutting at 0x20 shifts JSON 16 B early → first-`{` parse fail. Frozen archive `raw/archive/SendsFollower.2026-06-18-121509-frozen.amxd`. Pre-edit on-disk (Live-unfrozen `55726d24`) archived `…120211-predmap-ondisk.amxd`.
- **Preserved (verified):** obj-11→LFO Offset leg, obj-9 LFO path msg, obj-16 bipolar, obj-47 `route max`, follow_mode live.tab, mode_label "Mode", version_link, observer return-index fix, `outlet(0,"max",…)` label. Base = frozen `5a8ba853` patcher + canon JS `3bc5ab8e` extended.

## 8-slot mapper (replaces single Map button) — FROZEN 2026-06-18 ✅
- **What:** front-panel **8-slot mapping table** (Parameter | X | Map | Min | Max + Mode col), mirrors stock LFO map list. Each slot maps SF's RAW follow value 0..1 onto any clicked Live param, BYPASS LFO, scaled into [Min/100..Max/100]. **8× independent live.remote~** parallel to LFO leg (obj-11 untouched). Single-arm (arming one slot disarms previous). Replaces the single-Map experiment (folded in per founder's explicit request).
- **Base = on-disk user-handedited `c5b736c5` (67606 B, 82/84).** ⚠️ That on-disk file had been re-frozen by Live with a STALE embedded `sends_follower.js` (`29ff530b`, 5808 B, NO observers/NO map) even though the canon `.js` next to it was current — new freeze re-syncs them. Pre-edit archived `…134954-preedit-8slot-ondisk.amxd`.
- **Patcher: 82/84 → 256 box / 274 line.** Removed 16 single-Map boxes; added per slot ×8 (23 boxes): `map_btn_N`(live.text Map toggle `mode 1` `appearance 2`), `map_unmap_N`(live.text X svg), `map_lbl_N`(comment target name), `map_mode_N`(static "Remote"), `slot_min_N`/`slot_max_N`(live.numbox 0..100 `parameter_enable 1` → self-persist), `sr_N`(`route path name arm store release`), `sp_path_N`(prepend path)+`sl_path_N`(live.path), `sp_name_N`/`sp_arm_N`(prepend set), `sp_store_N`(prepend store)+`pattr_slot_N`(`pattr slot_N_path @parameter_enable 1 @autorestore 1`), `sp_restore_N`(`prepend restorepath N`), `sp_rel_N`(msg `id 0`), `remote_N`(live.remote~), `scale_N`(`scale 0. 1. 0. 1.`)+`sig_N`(sig~), `div_min_N`/`div_max_N`(`/ 100.`), `arm_msg_N`(`prepend arm N`), `unmap_tb_N`(t b)+`unmap_msg_N`(msg `unmap N`). Shared: `slot_route`(`route 0 1 2 3 4 5 6 7`), header comments hdr_param/hdr_mode/hdr_range + sub_min/sub_max. `obj-46` js kept numoutlets=2.
- **Wiring:** obj-46[1]→slot_route(demux by slot idx)→sr_N; sr_N out0 path→prepend path→live.path→`id n`→remote_N[1]; out1 name→prepend set→lbl; out2 arm→prepend set→btn(silent); out3 store→prepend store→pattr; out4 release→`id 0`→remote_N[1]. Signal: `obj-7 receive ---max_send`(RAW 0..1)→scale_N[0]→sig_N→remote_N[0]. Min→div_min→scale_N[3](outlow); Max→div_max→scale_N[4](outhigh). btn click→`prepend arm N`→js; X→t b→`unmap N`→js; pattr autorestore→`prepend restorepath N`→js.
- ⚠️ **Signal-source change vs single-Map:** slots tap RAW `obj-7` (0..1), NOT bipolar obj-16 (−100..100) — so Min/Max behave like stock LFO. (Single-Map tapped obj-16.)
- **JS (`sends_follower.js`):** slot-indexed map subsystem, `NSLOTS=8`. New/changed: `arm(slot,a)` (single-arm), `unmap(slot)`, `restorepath(slot, ...tokens)`, `captureTarget(slot,path)`, `resolveAndConnect(slot)`, `retryResolve(slot)` (Task.schedule 150ms ×20, anti-race), `emitTargetName(slot)`, `persistPath(slot)`, arrays `slotPath[]`/`slotRetry[]`, `armedSlot`. **Outlet-1 msgs now LEAD with slot idx** (`<slot> path|name|arm|store|release …`). `outlet(0,"max",result)` + Peak/Total + observer return-index UNCHANGED. node --check clean, md5 `53bdbfbd26cdfef548e6238b0cf425f6`, 24242 B.
- **Layout:** openrect `[…,169]` → `[0,0,340,385]` (wider+taller, like LFO map list — agreed for full list). Top controls (dial/Mode/New Version) unchanged; table below (header y=200, rows y=227 step 18 ×8). Visually close, NOT 1:1 (Live renderer proprietary).
- **Container Path B (4 resources, order JSON→svg→version→sends_follower):** JSON 57668→147778 (sz32 147779, of32 16); `multimap-unmap.svg` 535 B (`1a31f546`, EMBEDDED, of32 147795); `sf_version_check.js` 3106 B (`a5d905fc`, of32 148330); `sends_follower.js` 24242 B (`53bdbfbd`, of32 151436). `dlst total`=440 (4×108+8). **Result device md5 `194ff283e786d24f481020461fdba4cc`, 176150 B, 256/274**, self-contained. Frozen archive `raw/archive/SendsFollower.2026-06-18-135002-8slot-mapper-frozen.amxd`.
- ⚠️ **REUSABLE CRAFT (multi-file Path B):** when BOTH JSON and an embedded JS change size, `mx@c += ΔJSON` is WRONG — mx@c covers the WHOLE data region (JSON+all embedded files). Derive structurally: `mx@c = (dlst_pos + 16) − 0x30` (since invariant `0x30+mxc−dlst=16`). Compute of32 per resource by walking the new layout from of_json=16, NOT by delta. prefix = `d[:48]` (incl mx@c sub-header). Convert bytearray dire tags to `bytes()` for dict keys.
- ⚠️ **All 8 slots are FULLY functional** (arm→selected_parameter→capture→live.path→remote, Min/Max scale via scale+/100, persist via 8× pattr, unmap via id 0). No stubs. Mode column = intentional static "Remote" label (SF has one mode; stock LFO shows source mode there).

## Map FIX (selected_parameter → live.map) + UI cleanup — FROZEN 2026-06-18 ✅
- **ROOT CAUSE of dead Map buttons:** the 8-slot mapper (and the earlier single-Map) captured the user-clicked param by **observing `live_set view selected_parameter`** in JS. That mechanism is unreliable: clicking arbitrary params (esp. mixer faders/sends) does NOT consistently update `selected_parameter`, so the observer never fired → arm worked, capture never happened. The wiring + JS logic were BOTH structurally correct (verified by graph-walk + node mock: `arm(0,1)`→`onSelParamChange`→emits `[0,"path",…]`) — the bug was the **capture hook**, never validated on hardware (table was cut off at 169px until WIDE layout, so nobody had clicked Map before).
- **FIX = use stock `live.map`** (the object the stock LFO Map button actually uses — found in `…/Max for Live/patchers/liveui.map.maxpat` obj-3 `live.map @strict 1`). `live.map` implements Live's NATIVE map-grab: Map click → `prepend mapping` → live.map arms → user clicks ANY param → live.map captures it natively → **outlet 1 = `id n`** (→ live.remote~ right inlet directly, NO live.path needed), **outlet 2 = name**, **outlet 3 = mapping state (int)**, out0=path, out4=range. `@strict 1` = ignore clicks inside own device. `saved_object_attributes:{_persistence:1}` = **target persists across reload NATIVELY** (killed all our pattr/restorepath/retryResolve machinery).
- **X-visibility (show only when mapped):** stock chain = live.map out2 (name) → `zl compare <none>` (1=unmapped/0=mapped) → `hidden $1` → X box inlet 0. Simplified to 2 objs: `zl compare <none>` → `hidden $1`. X box ships `hidden:1`; mapped slot un-hides via `hidden 0`. Map button visual = live.map out3 (state) → `map_btn_N` inlet 0 (lights toggle).
- **Patcher 256/274 → 190 box / 193 line.** DELETED per slot ×8 (13 boxes): `arm_msg_N, unmap_tb_N, unmap_msg_N, sl_path_N, sp_path_N, sp_name_N, sp_arm_N, sp_store_N, sp_rel_N, pattr_slot_N, sp_restore_N, sr_N, map_lbl_N`(Parameter col); + shared `slot_route`, `hdr_param`. ADDED per slot ×8 (5 boxes): `livemap_N`(live.map @strict 1, _persistence 1), `mapprep_N`(prepend mapping), `xcmp_N`(zl compare <none>), `xhide_N`(message hidden $1), `unmapmsg_N`(message unmap). KEPT per slot: map_btn_N, map_unmap_N, map_mode_N(static "Remote"), slot_min_N/slot_max_N, div_min_N/div_max_N, scale_N, sig_N, remote_N. Net −106+40 box, −153+72 line.
- **New wiring/slot:** map_btn_N→mapprep_N→livemap_N[0]; map_unmap_N→unmapmsg_N→livemap_N[0]; livemap_N out1(id)→remote_N[1]; out3(state)→map_btn_N[0]; out2(name)→xcmp_N→xhide_N→map_unmap_N[0]. Signal chain UNCHANGED (obj-7→scale_N→sig_N→remote_N[0]; slot_min/max→div→scale outlow/outhigh). **JS NOT TOUCHED** (`53bdbfbd`, 24242 B, byte-identical embedded) — its slot arm/capture funcs are now dead code (nothing calls them); obj-46 outlet1 now unconnected. Follow/Peak-Total/observer-return-index/LFO leg all intact.
- **UI per founder:** removed "%" (Min%/Max%→Min/Max), removed entire Parameter column (8 map_lbl + hdr_param), X conditional (hidden until mapped), column order **Map(x130)|X(174,hidden)|Mode(192)|Min(240)|Max(282)**. hdr_range hidden. openrect `[0,0,444,169]`→`[0,0,328,169]` (narrower — Parameter col gone). **MAX(y+h)=166 ≤167 ✓, MAX(x+w)=320, width 328 (margin 8).**
- **Container Path B (4 res, order JSON→svg→version→sends_follower):** JSON 147757→**116572** (sz32 116573, of32 16); svg 535 of32 116589; sf_version_check.js 3106 of32 117124; sends_follower.js 24242 of32 120230. dlst total=440, `0x30+mxc−dlst=16` (mxc=144472, derived `dlst_pos+16−0x30`), ptch=144912=fs−0x20. NO trailing mdat chunk (file ends at dlst). **Result device md5 `fb0d2975fa05f72c00b422caa5a2ce08`, 144944 B, 190 box / 193 line**, self-contained (svg+both JS embedded byte-for-byte). Pre-edit `4eb57a06` archived `…143224-preedit-mapfix.amxd`; frozen result archived `raw/archive/SendsFollower.2026-06-18-144453-mapfix-livemap-frozen.amxd`.
- ⚠️ **REUSABLE CRAFT:** for any "map any Live param from an M4L device face" → use **`live.map`** (native grab), NOT `selected_parameter` observer. live.map out1=id→remote right inlet, out2=name, out3=state. `_persistence 1` = free reload-persistence. X-show = `zl compare <none>`+`hidden $1`. Reference patcher = stock LFO `liveui.map.maxpat`.

## WIDE layout (horizontal, fits in Live's 169px shelf) — FROZEN 2026-06-18 ✅
- **What:** founder chose WIDE layout. Reflowed presentation so EVERYTHING sits inside Live's fixed 169px device-view height. Left column = dial + "Mode" label + Peak/Total live.tab + (hidden) version button; right area = the full 8-slot table (header Parameter/Range[Min%/Max%]/Mode + 8 rows). Previously the table was stacked BELOW the dial (y 200..369) → invisible in Live (cut at 169). Device scrolls horizontally in chain.
- **ONLY presentation_rect (57 boxes) + openrect changed.** Proven by full diff vs prior `194ff283`: `lines` byte-identical (274), id-set identical (256), the ONLY box key that differs across all boxes = `presentation_rect`, the ONLY patcher-level change = `openrect`. Logic/wiring/varname/JS/8 slots all untouched. Embedded svg/version/sends JS byte-for-byte unchanged (md5 `1a31f546`/`a5d905fc`/`53bdbfbd`, both JS `node --check` clean).
- **openrect `[0,0,340,385]` → `[0,0,444,169]`.** Layout geometry: left col x5..125 (dial `[5,16,116,116]` bottom 132, mode_label `[5,134,60,14]`, follow_mode live.tab `[5,150,116,16]` bottom 166, version_link hidden `[5,2,70,14]`). Right table origin TX=134; cols: label x134 w116, unmap-X x252 w14, Map x268 w40, Min x312 w38, Max x352 w38, Remote x392 w44 (x+w max=436). Header y=4 h12 (hdr_param/hdr_range over Min..Max/hdr_mode), sub Min%/Max% y=16 h11, rows y=29 step17 h15 ×8 (row7 y148 bottom163). **MAX(y+h)=166 ≤167 ✓, MAX(x+w)=436 → openrect width 444 (margin 8).**
- **Container Path B (only JSON-patcher resized; JSON 147778→147757, ΔL=−21):** JSON sz32 147779→147758 of32=16; svg 535 of32 147795→147774; sf_version_check.js 3106 of32 148330→148309; sends_follower.js 24242 of32 151436→151415. dlst total=440 (4 res), `0x30+mxc−dlst=16` (mxc=175657, derived `dlst_pos+16−0x30`), ptch=176097=fs−0x20. ⚠️ **CRAFT NOTE:** this device has NO trailing `mdat` CHUNK — the `mdat` substring is only a subfield inside each dire payload. File ends exactly at the dlst chunk end (dlst_pos + 8 + N×108). Don't append `old[mdat:]` as a tail (caused a bogus +315 B first pass). dlst chunk spans to EOF.
- **Result device md5 `4eb57a065bf9499f4e3bb29617bac00d`, 176129 B, 256 box / 274 line**, self-contained. Frozen archive `raw/archive/SendsFollower.2026-06-18-142514-wide-layout-frozen.amxd`. Pre-edit on-disk (`194ff283`) archived `…142152-pre-wide-layout.amxd`.

## Map UI reconstruction under LFO reference (name-feedback + LCD panel + Mod/Remote) — FROZEN 2026-06-18 ✅
- **Base = on-disk UNFROZEN user-edit `ac1dc6f4` (157935 B, 190/193).** ⚠️ User had opened device in Max → Live re-saved it UNFROZEN: header `@0x20` is `{` (JSON starts at 0x20, NO mx@c subheader, NO dlst, NO embedded files; whole body 0x20..trailing-null = JSON). Parse unfrozen with `JS=0x20` not 48. Re-freeze re-embeds resources. User's coord edits (vs prev frozen `fb0d2975`): version_link `[5,2]`→`[54,4]`, mode_label `[5,134]`→`[5,118]`, follow_mode `[5,150]`→`[5,134]`, header heights bumped, slot_min/max h 15→12. **PRESERVED ALL** (only changed what 3 tasks required). map_unmap_1 runtime-visible reset to hidden:1. Pre-edit archived `…162815-preedit-mapui-recon-ondisk.amxd`.
- **TASK 1 — name in Map button (stock-true):** per slot, `livemap_N[2]` (name, fanout next to existing →xcmp_N) → `namesub_N`(`substitute <none> Map`) → `nameset_N`(`prepend set`) → `map_btn_N[0]`. Mapped→button shows target name; unmapped→"Map". This IS the stock LFO mechanism (liveui.map.maxpat: live.map name out2 → `p SetName`=`substitute <none> Map`→TruncateText→`text $1,texton $1`→live.text). Map button also got stock colors (idle grey `[0.549×3,1]`, armed orange `[1,0.7098,0.196,1]`, bg `[0.157×3,1]`, orange border); removed empty `saved_attribute_attributes` placeholders that nullified colors.
- **TASK 2 — black LCD panel:** `lcd_panel` (`maxclass panel`, `mode 0`, `rounded 7`, `bgcolor` dark + `saved_attribute_attributes.bgfillcolor.expression="themecolor.live_lcd_bg"` = stock LCD bg), presentation_rect `[128,2,196,164]` (y+h=166), **inserted as FIRST box** (paint order = behind table). Pattern lifted from stock `liveui.multimap.maxpat` panel obj-131.
- **TASK 3 — Mode=Remote↔Mod + conditional Range + true Mod modulation:** `map_mode_N` static comment "Remote" REPLACED by enum `live.text` toggle (faithful copy of stock LFO mode toggle `Abl.MapUi` obj-2: `appearance 2`, `outputmode 1`, `text`/`texton`="Remote"/"Mod", `automation`/`automationon`, teal accent `activebgoncolor [0,0.933,1,1]`, `parameter_enable 1` enum `["Remote","Mod"]` `parameter_initial [0]`=Remote default → native persist). outlet0=0 Remote/1 Mod.
  - **Conditional Range (show/hide via thispatcher):** mode out0 → `modesel_N`(`sel 0 1`): state0(Remote)→`msgrem_N`(`script show slot_min/max, script hide bipol/depth`); state1(Mod)→`msgmod_N`(`script hide slot_min/max, script show bipol/depth`) → `modetp_N`(thispatcher). New Mod controls overlay Min/Max columns: `bipol_N`(± enum live.text, "+"/"−" teal, hidden:1, over Min x240) + `depth_N`(live.numbox 0..100 default 50, hidden:1, over Max x282). Ship state = Min/Max visible, ±/depth hidden (matches Remote default). Mode toggles are params (`parameter_initial_enable 1`) → Live re-emits saved value on load → show/hide + id-route re-sync automatically (no loadbang; banging live.text would TOGGLE not re-emit — don't).
  - **TRUE Mod modulation via `live.modulate~`** (`…/C74/externals/m4l/live.modulate~.mxo`, on every install, portable; this is the REAL additive bipolar modulator the stock LFO uses for Mod, vs `live.remote~` which OVERWRITES). Per slot `mod_N`(live.modulate~) parallel to existing `remote_N`(live.remote~, Remote leg UNCHANGED). Mod signal: `obj-7`(follow 0..1)→`modscale_N`(`scale 0. 1. -1. 1.` bipolar)→`modmul_N`(`* 0.` ×k)→`modsig_N`(sig~)→`mod_N[0]`. k=(depth/100)×sign: `depth_N→/100.→kmul_N[0]`; `bipol_N→signif_N`(`if $i1==1 then -1. else 1.`)→`kmul_N[1]`; `kmul_N→modmul_N[1]`. So Mod = signed bipolar ±depth% follow offset around base value (TRUE mod, not overwrite).
  - **id routing by mode (single captured target → active leg only):** REMOVED old direct `livemap_N[1]→remote_N[1]`. Now `livemap_N[1]`(id)→`idsetreg_N`(prepend set)→`idreg_N`(message register)→`idgate_N`(`gate 2 1`); gate out0→`remote_N[1]`, out1→`mod_N[1]`. Gate control = `map_mode_N→modeplus_N`(`+ 1`)→idgate[0] (1=remote,2=mod; gate default open=1=remote). On mode change `modesel_N` sends `id 0` to the INACTIVE leg (id0a→remote / id0b→mod) AND bangs `idreg_N` to re-emit stored id to active leg. ⚠️ Fan-out ORDER: `map_mode→modeplus` line `order=1`, `map_mode→modesel` line `order=2` so gate control set BEFORE id re-emit.
- ⚠️ **Mod semantics HONESTY:** uses real `live.modulate~` (additive signed offset around base, correct). The `* 0.` modmul stores k in right inlet; follow streams into left inlet continuously so k re-applies — when ONLY depth/± change (not follow) the new k applies on next follow tick (fast, fine). Acceptable; not perfectly instantaneous on a paused follow. Remote leg byte-identical to prior (plain live.remote~, scale [Min/100,Max/100], founder-confirmed character).
- **JS NOT TOUCHED** (`sends_follower.js` `53bdbfbd`, 24242 B byte-identical; Mod done in patcher via live.modulate~, not JS). svg `1a31f546`, `sf_version_check.js` `a5d905fc` byte-identical.
- **Patcher 190/193 → 359 box / 425 line** (+169 box/+232 line: 1 panel + per-slot ×8 ×21 new objs). openrect `[0,0,328,169]`→`[0,0,332,169]` (content x+w max 324, panel 324, margin 8). **MAX(y+h)=166 ≤167 ✓, MAX(x+w)=324.** Column order UNCHANGED Map(130)|X(174,cond)|Mode(192,now toggle)|Min(240)|Max(282); ±/depth overlay Min/Max in Mod mode.
- **Container Path B (4 res, order JSON→svg→version→sends_follower):** JSON 116572→**218289** (sz32 218290, of32 16); svg 535 of32 218306; sf_version_check.js 3106 of32 218841; sends_follower.js 24242 of32 221947. dlst total=440, `0x30+mxc−dlst=16` (mxc=246189 = dlst_pos+16−0x30), ptch=246629=fs−0x20, NO trailing mdat (file ends at dlst). **Result device md5 `8a9203dd6cb46bf576b1fb344905e81d`, 246661 B, 359 box / 425 line**, self-contained. Frozen archive `raw/archive/SendsFollower.2026-06-18-164207-mapui-recon-frozen.amxd`.
- **Preserved (verified post-freeze):** obj-11 LFO leg, obj-16 bipolar, obj-47 route max, obj-46 js, obj-7 receive ---max_send, follow_mode Peak/Total, mode_label/version_link at USER coords, 8 slots full map-capture+scale chains, X conditional (zl compare <none>+hidden $1), observer return-index (in untouched JS). Both JS `node --check` clean.

## 4 visual mapper edits under reference (name feedback fix + teal Mode/± + transparent % numboxes) — FROZEN 2026-06-18 ✅
- **Base = on-disk frozen user-edit `2b20a1b0` (319178 B, 355/425).** User (in Max) had: (a) DELETED 4 column-header comments (`hdr_mode`/`hdr_range`/`sub_min`/`sub_max`); (b) reflowed all 8 table rows UP — y 29/46/…/148 → ~5.9/22.9/…/125 (step ~17, no header row), x nudged ~0.07; (c) depth_N pres h 12→15. **PRESERVED ALL** (only touched what 4 tasks needed; rows stay at user y). ⚠️ **Live re-save had re-embedded STALE `sends_follower.js` `29ff530b` (5808 B, old 5-slot, NO observers/NO map subsystem)** even though canon next to device was current — the running device's map buttons were DEAD + return-index frozen. **This freeze RESTORES correct canon `53bdbfbd` (24242 B).** (sf_version_check.js was NOT on-disk next to device — only embedded; kept from prior embed.) Pre-edit archived `…165808-preedit-4visual-ondisk.amxd`.
- **TASK 1 — name in Map button via `text`/`texton` (THE FIX) + fill state.** Root cause of broken name feedback: old chain `livemap_N[2]→namesub_N(substitute <none> Map)→nameset_N(prepend set)→map_btn_N` used `prepend set` — **live.text toggle IGNORES `set` for its label** (label = `text` off-state + `texton` on-state attributes). Stock LFO (`liveui.map.maxpat` `p SetName`) uses message `text $1, texton $1`. FIX: DELETE `nameset_N`; ADD per slot `namet_N`(`prepend text`) + `namet2_N`(`prepend texton`), both fed by `namesub_N[0]`, both → `map_btn_N[0]`. `prepend text` on list "Fade In" → `text Fade In` (sets off-label); `prepend texton` → `texton Fade In` (on-label). Robust for multi-word names (whole list prepended, not `$1` first-token). Fill state: reused `xcmp_N`(`zl compare <none>` = 1 unmapped/0 mapped) fanned to NEW `colsel_N`(`sel 0 1`): out0(mapped)→`colmapped_N`(msg: bg orange α1 + text dark) , out1(unmapped)→`colunmap_N`(msg: bg orange **α0** + text orange) → `map_btn_N[0]`. map_btn_N default attrs = unmapped look (transparent bg, orange border kept, orange text, "Map"). NET +5 box/slot (namet, namet2, colsel, colmapped, colunmap), −1 (nameset); +9 line/slot, −2.
- **TASK 2 — Mode toggle color #03C3D5.** `map_mode_N` `activebgoncolor` `[0,0.933,1,1]`→`[0.011765,0.764706,0.835294,1.0]` (= #03C3D5). attr-only.
- **TASK 3 — bipol_N square ± teal.** `bipol_N` (live.text +/- enum) → `text="±"`/`texton="±"` (single glyph both states; state via fill vs outline), pres+patch w 38→**15** (square 15×15), `activebgoncolor`+`bordercolor`+`textoffcolor`+`textcolor`=teal #03C3D5. Underlying enum stays "+"/"-" (logic via `signif_N if $i1==1 then -1. else 1.` UNCHANGED). attr+size only.
- **TASK 4 — slot_min/slot_max/depth → transparent + "%".** `parameter_unitstyle: 5` (=Percent, native "%" suffix) on each; `bgcolor`/`activebgcolor`/`bordercolor`/`tricolor`/`tribordercolor`/`activetricolor` α0 (transparent field/frame, only digits show); `textcolor` teal #03C3D5. Registered in `saved_attribute_attributes`. attr-only.
- **Patcher 355/425 → 387 box / 481 line** (+32 box = +5×8−8; +56 line = +9×8−2×8). openrect UNCHANGED `[0,0,332,169]` (no geometry growth — bipol shrank, rows already user-placed). **MAX(y+h)=166 ≤167 ✓ (lcd_panel `[128,2,196,164]`), MAX(x+w)=324.**
- **Container Path B (4 res JSON→svg→version→sends_follower):** JSON 309240→**272929** (shrank: user's Live re-save had verbose floats; `json.dumps(indent=1)` is leaner) sz32 272929 of32 16; svg 535 of32 272945 (`1a31f546`); sf_version_check.js 3106 of32 273480 (`a5d905fc`); **sends_follower.js 24242 of32 276586 (`53bdbfbd` — RESTORED canon, was stale 5808 `29ff530b`)**. dlst total=440, mxc=300828 (=dlst_pos+16−0x30), ptch=301268=fs−0x20, NO trailing mdat (EOF==dlst_end). **Result device md5 `c35eb20bcfb70957880604fdf7767dc6`, 301300 B, 387 box / 481 line**, self-contained (both JS `node --check` clean, svg embedded). Frozen archive `raw/archive/SendsFollower.2026-06-18-170149-4visual-frozen.amxd`.
- **Preserved (verified post-freeze):** obj-11 LFO leg, obj-16 bipolar, obj-47 route max, obj-7 receive ---max_send, 8× live.map / 9× live.remote~ / 8× live.modulate~ (Mod legs), follow_mode Peak/Total live.tab, lcd_panel, conditional Range (msgrem/msgmod show/hide bipol/depth — bipol still named in those msgs, square form doesn't break show/hide), js obj-46=`js sends_follower.js`, observer return-index (now in RESTORED JS, was missing in stale embed). 0 orphan lines.
- ⚠️ **REUSABLE CRAFT:** live.text label feedback at runtime = `prepend text` + `prepend texton` (NOT `prepend set` — set is ignored by live.text). Numbox "%" = `parameter_unitstyle:5`. Transparent numbox = α0 on bgcolor/activebgcolor/bordercolor/tricolor/tribordercolor/activetricolor. ⚠️ **Always check embedded `sends_follower.js` md5 after a Live re-save** — Live re-embeds whatever it thinks is current and can revert to a stale 5808-B copy; restore canon `53bdbfbd` on freeze.

## 2 real bugs (name feedback + Mod engage) + bigger font — FROZEN 2026-06-18 ✅
- **Base = on-disk `c35eb20b` (301300 B, 387/481), founder confirmed correct version + Live restarted → bugs real, not cache.** Pre-edit archived `…171903-preedit-2bugs-font.amxd`. JS NOT touched (both bugs pure patcher; embedded `sends_follower.js` `53bdbfbd` byte-identical, installObservers+arm present).
- **BUG #1 — name in Map button FINALLY works (text/texton WASN'T enough).** Prior "4 visual" freeze claimed `prepend text`+`prepend texton` was THE FIX — it WASN'T on hardware. **Read stock `liveui.map.maxpat` (obj-48 Map live.text, obj-81 `p SetName`):** name out of `live.map` outlet 2 → `substitute <none> Map` → `p TruncateText` (`atoi → zl slice 12 → zl join → itoa` = **guarantees ONE symbol**) → message **`text $1, texton $1`** → button. The MISSING piece in my chain was the **single-symbol guarantee**: `prepend text` on a multi-atom name ("Fade In") makes `text Fade In` (malformed/partial), and the `$1`-substitution message is the redraw-triggering form. **FIX:** per slot DELETE `prepend text`/`prepend texton` (`obj-51/52,56/57,61/62,66/67,71/72,76/77,81/82,86/87`); ADD `tosymbol_N` (concats any atoms→1 symbol, keeps spaces) + `namemsg_N` (message `text $1, texton $1`, stock-exact); rewire `livemap_N[2]→namesub_N→tosymbol_N→namemsg_N→map_btn_N[0]`. Net per slot −2 box (del 2 prepend, add tosymbol+namemsg=+2 → 0) … actually +0 box/slot for name (−2+2), lines −4+3=−1/slot. Map button kept `parameter_enable 0` (so text/texton honored; stock's is 1 but its label is enum-driven — non-param is fine for free-text label). Fill-state (xcmp→sel→color msgs) UNTOUCHED.
- **BUG #2 — Mod/Remote leg engages on map-capture WITHOUT toggling Mode.** Symptom: send didn't drive knob until you wiggled Mode. **ROOT (confirmed by graph-walk):** `livemap_N[1](id)→idsetreg_N(prepend set)→idreg_N(empty message)→idgate_N[1]`. `set <id>` into a message box **STORES silently, no output**; `idreg_N` only emitted when BANGED, and the sole banger was `modesel_N` (Mode-change). So captured id sat in register, leg never got id until a Mode toggle banged it. **FIX:** insert `idtrig_N` (`t b l`) between `livemap_N[1]` and the register. trigger fires R→L: outlet1=`l`(id, FIRST)→`idsetreg_N`→sets register; outlet0=`b`(SECOND)→bangs `idreg_N`→gate→active leg engages NOW. On load `_persistence 1` re-emits id on outlet 1 → same path → engages immediately. Gate control = `map_mode_N(+1)→gate[0]` (1 remote/2 mod, default open=remote); on load `map_mode_N` re-emits saved value → `modesel_N` re-bangs register → correct leg (self-corrects if id arrives before mode settles). +1 box/slot, +2 line/slot.
- **FONT bump:** live.text (Map/X/Mode/±) 9→**11**, live.numbox (Min/Max/depth) 8→**10**, mode_label comment 9→**11**, follow_mode live.tab default→**11**. NO box-height growth (live.text rows 16px fit 11pt; numbox 12px fits 10pt — Live auto-centers digits). max(y+h) unchanged 166.
- **Patcher 387/481 → 395 box / 489 line** (+8 box = 8×idtrig; +8 line = BUG1 −8 + BUG2 +16). openrect UNCHANGED `[0,0,332,169]`. **MAX(y+h)=166 ≤167 ✓ (lcd_panel `[128,2,196,164]`).**
- **Container Path B (4 res JSON→svg→version→sends_follower):** JSON 272928→**277911** (+4983) sz32 277912 (incl `\0`) of32 16; svg 535 of32 277928 (`1a31f546`); sf_version_check.js 3106 of32 278463 (`a5d905fc`); sends_follower.js 24242 of32 281569 (`53bdbfbd` byte-identical). dlst total=440, mxc=305811 (=dlst_pos+16−0x30), ptch=306251=fs−0x20, NO trailing mdat (EOF==dlst_end). **Result device md5 `f236f42d9f7597c71636840beb1a9806`, 306283 B, 395 box / 489 line**, self-contained. Frozen archive `raw/archive/SendsFollower.2026-06-18-172059-2bugs-font-frozen.amxd`.
- **Preserved (verified):** obj-11 LFO leg, obj-16 bipolar, obj-47 route max, obj-7 receive ---max_send, 8× live.map / 9× live.remote~ / 8× live.modulate~, follow_mode Peak/Total, lcd_panel, conditional Range (msgrem/msgmod show/hide bipol/depth), `_persistence`, observer return-index (in untouched `53bdbfbd` JS). 0 orphan lines, both JS node --check clean.
- ⚠️ **REUSABLE CRAFT (live.text runtime label — DEFINITIVE):** to set a live.text button label at runtime to an arbitrary (possibly multi-word) param name, you need BOTH (a) a **single symbol** (`tosymbol` after the `substitute`/source) AND (b) the **`text $1, texton $1` message form** (NOT bare `prepend text`/`prepend texton` on a raw list — that's what failed on HW). This is the exact stock `p SetName` mechanism (`substitute <none> Map → join-to-one-symbol → "text $1, texton $1"`). `set`/`setsymbol` also documented but stock uses text/texton. ⚠️ **Mapping captured-id to a gate/leg via a `set <id>` message register: the register stores but DOESN'T output — you MUST bang it (`t b l` after the id source) or the leg never engages until something else bangs it.**

## Name-in-Map via COMMENT overlay (live.text label gave up, 4th attempt) + Mode font — FROZEN 2026-06-18 ✅
- **Base = on-disk `f236f42d` (306283 B, 395/481→489), file FREE (lsof empty, founder's last build).** Pre-edit archived `…191851-preedit-name-comment-font.amxd`. JS NOT touched (`53bdbfbd` byte-identical, installObservers present, node-check clean). Pure patcher edit.
- **THE FIX — name now drawn by a `comment` OVERLAY, NOT live.text.** After 3 failed live.text approaches (`prepend set`, `prepend text/texton`, `tosymbol`+`text $1,texton $1`=stock-exact) NONE showed the name on hardware → **live.text does NOT update its displayed label by message in our context. Abandoned live.text for the label entirely.** Per slot: DELETE `tosymbol_N`+`namemsg_N` (dead label chain); KEEP `namesub_N`(`substitute <none> Map`); ADD `nameset_N`(`prepend set`)→`mapcom_N`(**Max `comment`**, presentation_rect == map_btn_N rect, `ignoreclick 1`, `textjustification 1`=center, fontsize 9, Arial Bold, default "Map"). Route `livemap_N[2](name)→namesub_N→nameset_N→mapcom_N[0]`. **comment DOES update displayed text via `set <text>`** (verified: comment methods = append/prepend/**set**/setwithtruncation/string); multi-word names ("Fade In") OK (comment `set` takes the whole list as text).
- **PROOF name arrives:** `livemap_N out2` fans to BOTH `xcmp_N` (X-visibility, founder-confirmed WORKING) AND `namesub_N` (→comment) — same output, so the comment is fed the exact name that already drives X. Verified all 8 slots.
- **Click-through:** `mapcom_N` has `ignoreclick 1` → clicks pass through the overlay to `map_btn_N` (live.text toggle) underneath, which still catches the arm-click. `map_btn_N` kept as clickable colored FRAME: text/texton emptied (`text=""`,`texton=""`) so it draws no label (comment draws the text now); fill-state chain UNTOUCHED (orange fill α1 when mapped via obj-53/58.../88 `sel 0 1`→color msgs; orange outline α0 when unmapped).
- **Comment color BY STATE (dynamic, honest):** reused the SAME per-slot fill-state `sel 0 1` (obj-53/58/63/68/73/78/83/88): out0(mapped)→`comdark_N`(msg `textcolor 0.117 0.117 0.117 1`=dark, readable on orange fill); out1(unmapped)→`comorng_N`(msg `textcolor 1 0.7098 0.196 1`=orange, on outline)→`mapcom_N[0]`. Default `textcolor`=orange (unmapped look). Text+color fire from same `livemap out2` event → stay in sync. NO single-color compromise needed — dynamic recolor works (comment honors `textcolor` attr-message).
- **Mode font 11→9** (`map_mode_N` Remote/Mod live.text toggle, all 8). Per task: only Map-comment(9) + Mode(9) shrank; numbox/labels (Min/Max/depth/mode_label) NOT touched (founder wants those bigger).
- **Patcher 395/489 → 411 box / 513 line.** Per slot: −2 box (del tosymbol+namemsg), +4 box (nameset+mapcom+comdark+comorng) = net +2/slot ×8 = +16. Lines: −3/slot (del namesub→tosymbol, tosymbol→namemsg, namemsg→map_btn) +6/slot (namesub→nameset, nameset→mapcom, sel0→comdark, sel1→comorng, comdark→mapcom, comorng→mapcom) = net +3/slot ×8 = +24. openrect UNCHANGED `[0,0,332,169]`. **MAX(y+h)=166 ≤167 ✓ (lcd_panel).**
- **Container Path B (4 res JSON→svg→version→sends_follower):** JSON 277911→**288878** (+10967) sz32 288879 (incl `\0`) of32 16; svg 535 of32 288895 (`1a31f546`); sf_version_check.js 3106 of32 289430 (`a5d905fc`); sends_follower.js 24242 of32 292536 (`53bdbfbd` byte-identical). dlst total=440, mxc=316778 (=dlst_pos+16−0x30), ptch=317218=fs−0x20, NO trailing mdat (EOF==dlst_end). **Result device md5 `d3b83238ee1e86e4f27ea614aec9e845`, 317250 B, 411 box / 513 line**, self-contained. Frozen archive `raw/archive/SendsFollower.2026-06-18-192423-name-comment-font-frozen.amxd`.
- **Preserved (verified post-build):** obj-11 LFO leg, obj-16 bipolar, obj-47 route max, obj-7 receive ---max_send, 8× live.map / 9× live.remote~ / 8× live.modulate~, follow_mode Peak/Total live.tab, mode_label "Mode", lcd_panel, conditional Range (msgrem/msgmod ×8 show/hide bipol/depth), idtrig_N `t b l` engage-fix ×8, `_persistence`, version_link, observer return-index (in untouched JS). 0 orphan lines, both JS node-check clean, X-visibility chain (xcmp→hidden $1) intact.
- ⚠️ **REUSABLE CRAFT (DEFINITIVE — live.text label is a DEAD END for runtime text):** to show an arbitrary runtime string over a live.text button, do NOT fight live.text's label (`set`/`text`/`texton`/`tosymbol`+`text $1` ALL failed on HW). Instead overlay a **Max `comment`** at the same presentation_rect with **`ignoreclick 1`** (click passes to the live.text frame under it) and feed it `prepend set` → comment; empty the live.text's own text/texton so it's just a colored clickable frame. comment `set` updates displayed text reliably (incl multi-word). Recolor comment by state via `textcolor r g b a` message. This is the pattern that finally worked.

## 3 color edits (Mode two-color, Map name readable, Min/Max orange) — FROZEN 2026-06-18 ✅
- **Base = `d3b83238` (317250 B, 411/513, frozen, FREE).** Pure COLOR edit — NO box/line change (411/513 unchanged), only attrs + 16 message-box texts. Pre-edit archived `…193239-preedit-3color.amxd`. JS untouched (`53bdbfbd`/`a5d905fc` byte-identical, node-check clean).
- **EDIT 1 — Mode toggle (`map_mode_0..7`) ALWAYS FILLED, two-color.** off=Remote(out0)→`bgcolor`/`activebgcolor`=ORANGE `[1,0.7098,0.196,1]`; on=Mod(out1)→`bgoncolor`(ADDED)/`activebgoncolor`=CYAN #03C3D5 `[0.0118,0.7647,0.8353,1]`; text DARK `[0.117,0.117,0.117,1]` both states (`textcolor`/`textoffcolor`/`textoncolor`/`activetextcolor`/`activetextoncolor`). enum stays `["Remote","Mod"]` default 0=Remote. New attrs registered in `saved_attribute_attributes`. (Was: dark off-fill + teal on-fill, grey text.)
- **EDIT 2 — Map name READABLE (compromise: orange text both states, NO orange fill).** ⚠️ **ROOT CAUSE of "black/invisible name":** Max draws ALL live.* objects in a TOP layer ABOVE plain objects. `map_btn_N` (live.text) mapped-state orange FILL (α1) painted OVER the `mapcom_N` `comment` overlay → dark comment text sank UNDER the fill → invisible. (`lcd_panel` paint-index 346 is also above the live.text boxes, but live.* render in their own top layer regardless of box-index, which is why numboxes/toggles show through.) **live.text label is dead for runtime text (4 prior fails) AND comment can't sit above a live.text fill** → reference "dark text on orange fill" UNREACHABLE with this mechanism. **FIX = remove the mapped fill** (per-slot mapped color-msg `obj-54/59/64/69/74/79/84/89`: `bgcolor`/`bgoncolor`/`activebgcolor`/`activebgoncolor` α 1→0 = frame-only, no fill) so comment is NEVER covered; **make comment ORANGE in BOTH states** (`comdark_N` repointed dark→orange; `comorng_N` already orange; `mapcom_N` default textcolor orange). Result: name (and "Map") always orange on dark LCD = clearly readable, both mapped+unmapped. Map button = orange OUTLINE-only indicator (no fill) when mapped. Fill-state chain (`xcmp_N zl compare <none>`→`obj-58 sel 0 1`→msgs) wiring UNTOUCHED; only msg TEXT alphas changed.
- **EDIT 3 — Min/Max digits ORANGE in Remote.** `slot_min_N`/`slot_max_N` `textcolor` teal→ORANGE `[1,0.7098,0.196,1]` (transparent field/% kept). `depth_N` (Mod) LEFT teal #03C3D5. So Remote%=orange, Mod depth%=teal.
- **Patcher 411/513 UNCHANGED** (color-only). openrect `[0,0,332,169]` unchanged. **MAX(y+h)=166 ≤167 ✓ (lcd_panel).**
- **Container Path B (4 res JSON→svg→version→sends_follower):** JSON 288878→**294030** (+5152, dL) sz32 294031 of32 16; svg 535 of32 294047 (`1a31f546`); sf_version_check.js 3106 of32 294582 (`a5d905fc`); sends_follower.js 24242 of32 297688 (`53bdbfbd` byte-identical). dlst total=440, mxc=321738 (=dlst_pos 321962 +16−0x30), ptch=322370=fs−0x20, NO trailing mdat (EOF==dlst_end 322402). **Result device md5 `308ada9c492ddc04d829e0f7c6522843`, 322402 B, 411 box / 513 line**, self-contained. Frozen archive `raw/archive/SendsFollower.2026-06-18-193524-3color-frozen.amxd`. Pre-edit `d3b83238` archived `…193239-preedit-3color.amxd`.
- **Preserved (verified post-build):** obj-11 LFO leg, obj-16 bipolar, obj-47 route max, obj-7 receive ---max_send, 8× live.map / 9× live.remote~ / 8× live.modulate~, follow_mode Peak/Total, mode_label, lcd_panel, conditional Range (show/hide bipol/depth), idtrig `t b l` ×8, mapcom name-feed (`prepend set`) ×8, `_persistence`, version_link, observer return-index (untouched JS). 0 orphan lines, both JS node-check clean.
- ⚠️ **REUSABLE CRAFT (Max paint layering — DEFINITIVE):** live.* objects (live.text/live.numbox/live.tab) render in a TOP layer ABOVE plain objects (comment/panel) regardless of box-array index. So a `comment` overlay CANNOT sit above a live.text FILL — if you need text over a live.text button, the button must be FILL-FREE (frame/outline only) for the comment to show. Combined with "live.text label is dead for runtime text", the only readable runtime name over a Map button = comment overlay + frame-only (no fill) live.text underneath + comment colored to contrast the LCD (orange on dark). Reference fill colors are unreachable here.

## ± per-state glyph + instant range recompute — FROZEN 2026-06-18 ✅
- **Base = `308ada9c` (322402 B, 411/513, frozen, FREE).** Pre-edit archived `…194608-preedit-bipol-instant.amxd`. JS untouched (`53bdbfbd`/`a5d905fc`/svg `1a31f546` byte-identical, both node-check clean — both edits are PURE PATCHER).
- **EDIT 1 — `bipol_N` shows ONE active polarity by state (not "±").** live.text toggle (`outputmode 1`, `appearance 2`): off-label=`text`, on-label=`texton`. enum `["+","-"]` idx0→`signif if 0==1 then -1 else 1`=+1 (positive depth), idx1→−1 (negative). Set `text="+"` (off/state0 = positive depth), `texton="−"` (on/state1 = negative depth, U+2212). Teal #03C3D5 / 15×15 square form / borders kept (only text/texton changed). `automation`/`automationon` ("+"/"-") = param-automation labels, untouched. All 8. attr-only, no box/line change.
- **EDIT 2 — range edit applies INSTANTLY (no follow movement needed).** ⚠️ ROOT: slot scale chains are `obj-7(follow 0..1)→scale_N[0]→sig_N→remote_N` (Remote) and `obj-7→modscale_N[0]→modmul_N→modsig_N→mod_N` (Mod). Min/Max set scale_N RIGHT inlets (outlow/outhigh via `div_min/max_N /100`); depth/± set `modmul_N` RIGHT inlet k (via `depthdiv_N`+`signif_N`→`kmul_N`→modmul[1]). Max `scale`/`* ` DON'T re-output on right-inlet-only change → new bound waits for next follow tick; if follow static (transport stopped/sends frozen) it NEVER applies. **FIX:** added ONE shared `followf` (`f` float) — `obj-7[0]`→`followf` RIGHT inlet (silent store of latest follow); `followf[0]`→ALL 16 `scale_N[0]`+`modscale_N[0]` (left inlets, parallel to obj-7). Per slot ×4 controls (min/max/depth/±) inserted `t b l` trigger (replacing direct control→math line): trigger fires R→L so outlet1=`l`→math (sets bound/k FIRST, full chain to modmul right inlet completes synchronously), outlet0=`b`→`followf` bang (recompute SECOND with new bound). On any range edit → followf re-emits held follow → scale/modscale recompute → sig/modsig → remote/modulate update NOW. **Works at static follow** (followf holds last value, default 0 = correct floor output). Works in BOTH legs (Remote via scale→remote~, Mod via modscale→modmul→modulate~). Normal follow flow intact (obj-7 still drives scale/modscale left inlets directly; followf is just a passive store there — no double-fire). idtrig engage-fix / `_persistence` / LFO leg / Peak-Total / observers untouched.
- **Patcher 411/513 → 444 box / 594 line** (+33 box = 1 followf + 32 `t b l` triggers [4×8]; +81 line = 32 rewired controls ×[−1 old +3 new = +2 → 64] + 1 obj-7→followf + 16 followf→scale/modscale). openrect `[0,0,332,169]` unchanged. **MAX(y+h)=166 ≤167 ✓** (new objects are patching-canvas only, x≥1500, no presentation). New objects have NO presentation (`presentation` absent) so device face unchanged.
- **Container Path B (4 res JSON→svg→version→sends_follower):** JSON 294030→**314387** (+20357 dL) sz32 314388 (incl `\0`) of32 16; svg 535 of32 314404 (`1a31f546`); sf_version_check.js 3106 of32 314939 (`a5d905fc`); sends_follower.js 24242 of32 318045 (`53bdbfbd` byte-identical). dlst total=440, mxc=342287 (=dlst_pos 342319 +16−0x30), ptch=342727=fs−0x20, NO trailing mdat (EOF==dlst_end 342759). **Result device md5 `7ebe7d15ffcd8fc49aad46aa93baafbd`, 342759 B, 444 box / 594 line**, self-contained (svg+both JS embedded byte-for-byte, both node-check clean). Frozen archive `raw/archive/SendsFollower.2026-06-18-194608-bipol-instant-frozen.amxd`. Pre-edit `308ada9c` archived `…194608-preedit-bipol-instant.amxd`.
- **Preserved (verified post-build):** obj-11 LFO leg, obj-16 bipolar, obj-47 route max, obj-7 receive ---max_send, 8× live.map / 9× live.remote~ / 8× live.modulate~, follow_mode Peak/Total, mode_label, lcd_panel, conditional Range (msgrem/msgmod show/hide bipol/depth), idtrig `t b l` engage-fix ×8, mapcom name-feed ×8, X-visibility, `_persistence`, version_link, observer return-index (untouched JS). 0 orphan lines.
- ⚠️ **REUSABLE CRAFT (instant recompute on a scale's right-inlet change):** `scale`/`*`/math objects DON'T re-output when only the right inlet (range/coeff) changes — output is stale until the LEFT inlet is re-banged. To make a bound/depth edit apply NOW (static signal source), store the source value in an `f` (fed to its RIGHT inlet) and re-bang the `f` (left inlet) after the bound is set; gate order with `t b l` (value first via outlet1, bang second via outlet0). For multi-hop right-inlet chains (signif→kmul→modmul), the value-outlet's full downstream completes synchronously before trigger's bang-outlet fires, so the coeff is in place before recompute.

## Current state (one slice)
- **md5 `7ebe7d15ffcd8fc49aad46aa93baafbd`, 342759 B, 444 box / 594 line** (2026-06-18, CURRENT, frozen; ± per-state glyph [text="+" off/positive, texton="−" on/negative] + instant range recompute [shared `followf` `f` + 32× `t b l` triggers → bound/depth/± edits apply NOW even at static follow, both Remote+Mod legs]). JS `53bdbfbd`/`a5d905fc`/svg `1a31f546` byte-identical. Frozen archive `raw/archive/SendsFollower.2026-06-18-194608-bipol-instant-frozen.amxd`. Pre-edit `308ada9c` archived `…194608-preedit-bipol-instant.amxd`.
- Prior frozen (3 color edits: Mode toggle two-color [Remote=orange/Mod=cyan, dark text], Map name orange both states + mapped-fill removed, Min/Max digits orange in Remote [depth teal]): **md5 `308ada9c492ddc04d829e0f7c6522843`, 322402 B, 411 box / 513 line** (2026-06-18). JS `53bdbfbd` byte-identical. Frozen archive `raw/archive/SendsFollower.2026-06-18-193524-3color-frozen.amxd`.
- Prior frozen (name-in-Map via `comment` overlay [live.text label abandoned after 3 fails — comment `set` updates text, `ignoreclick 1` passes click to live.text frame under it, dynamic textcolor dark/orange by mapped state]; Mode toggle font 11→9; numbox/labels untouched). JS `53bdbfbd` byte-identical. Frozen archive `raw/archive/SendsFollower.2026-06-18-192423-name-comment-font-frozen.amxd`. Pre-edit `f236f42d` archived `…191851-preedit-name-comment-font.amxd`.
- Prior frozen (2 real-bug fixes + font, name-via-tosymbol+text/texton=STILL BROKEN on HW): **md5 `f236f42d9f7597c71636840beb1a9806`, 306283 B, 395 box / 489 line** (2026-06-18). Frozen archive `raw/archive/SendsFollower.2026-06-18-172059-2bugs-font-frozen.amxd`.
- Prior frozen (4 visual mapper edits, name-via-text/texton-only=BROKEN-on-HW, teal Mode/±, transparent % numboxes): **md5 `c35eb20bcfb70957880604fdf7767dc6`, 301300 B, 387 box / 481 line** (2026-06-18). Frozen archive `raw/archive/SendsFollower.2026-06-18-170149-4visual-frozen.amxd`.
- Prior frozen (Map UI reconstruction: name-feedback-via-prepend-set[BROKEN] + LCD panel + Mode Remote/Mod toggle + `live.modulate~` Mod leg): **md5 `8a9203dd6cb46bf576b1fb344905e81d`, 246661 B, 359 box / 425 line** (2026-06-18). sends_follower.js `53bdbfbd` (24242 B). Frozen archive `raw/archive/SendsFollower.2026-06-18-164207-mapui-recon-frozen.amxd`. Pre-edit on-disk UNFROZEN `ac1dc6f4` archived `…162815-preedit-mapui-recon-ondisk.amxd`.
- Prior frozen (MAP FIX live.map, static Remote, no LCD/Mod): **md5 `fb0d2975fa05f72c00b422caa5a2ce08`, 144944 B, 190 box / 193 line** (2026-06-18, frozen; **MAP FIX = live.map native grab** replaces unreliable selected_parameter observer; UI: no "%", no Parameter column, X conditional, cols Map|Mode|Min|Max; openrect `[0,0,328,169]`, max(y+h)=166). sends_follower.js `53bdbfbd26cdfef548e6238b0cf425f6` (24242 B, UNCHANGED — its slot funcs now dead code). Frozen archive `raw/archive/SendsFollower.2026-06-18-144453-mapfix-livemap-frozen.amxd`.
- Prior frozen (WIDE layout, BROKEN map via selected_parameter): **md5 `4eb57a065bf9499f4e3bb29617bac00d`, 176129 B, 256 box / 274 line** (2026-06-18; full 8-slot table fits 169px shelf but Map buttons didn't fire). sends_follower.js `53bdbfbd` (24242 B). Frozen archive `raw/archive/SendsFollower.2026-06-18-142514-wide-layout-frozen.amxd`.
- Prior frozen (8-slot mapper, TALL layout openrect `[0,0,340,385]`): **md5 `194ff283e786d24f481020461fdba4cc`, 176150 B, 256 box / 274 line** (2026-06-18; table stacked below dial, y200..369 → invisible in Live). sends_follower.js `53bdbfbd` (24242 B). Frozen archive `raw/archive/SendsFollower.2026-06-18-135002-8slot-mapper-frozen.amxd`.
- Prior frozen (single Map button): **md5 `6f156eab973ecec0d9793f794c75cfce`, 68498 B, 82 box / 85 line** (2026-06-18; Map button + 2nd live.remote~ on top of Peak/Total + observer return-index). sends_follower.js `c63475322970c9f8ea02d01620806551` (22368 B). Frozen archive `raw/archive/SendsFollower.2026-06-18-121509-frozen.amxd`. ⚠️ A later Live re-save reverted on-disk to `c5b736c5` (82/84) with stale embedded JS `29ff530b` — superseded by 8-slot freeze.
- Prior frozen (Peak/Total relabel, no Map): **md5 `5a8ba853a3c31d80cb84870e977a13f4`, 49134 B, 66 box / 65 line** (2026-06-18, frozen; cosmetic relabel Peak/Total + "Mode" name + "Mode" label, observer return-index fix + Follow Mode). Path-B (ΔL=+549 vs `bd21b848`): JSON of32=16 sz32=**33333** flag=17; embedded `sends_follower.js` **12303 B** of32=**33349** flag=0 (md5 `3bc5ab8e`, observers, UNCHANGED); `sf_version_check.js` 3106 B of32=**45652** flag=8 (md5 `a5d905fc`, UNCHANGED) (order: JSON → sends_follower → version_check). dlst total=332, `0x30+mxc−dlst=16`, ptch=filesize−0x20 (ptch=49102, mxc=48758). Both embedded JS = canon byte-for-byte, `node --check` clean, `outlet(0,"max"…)` + `route max` intact. Frozen archive: `raw/archive/SendsFollower.2026-06-18-113530-frozen.amxd`. Pre-edit archived: `raw/archive/SendsFollower.2026-06-18-110402-frozen.amxd` (`bd21b848`).
- Prior frozen (Follow Mode labels "Max/Sum", param "Follow Mode", no "Mode" label): md5 `bd21b8483df868a1d02cab71a3b9b603`, 48585 B, 65 box / 65 line. JSON sz32=32784; `sends_follower.js` of32=32800; `sf_version_check.js` of32=45103. Archived `…110402-frozen.amxd`.
- Prior frozen (no observers, old JS `29ff530b`): md5 `7ca595b44db993c8bf91269fbeb7d97a`, 42090 B, 65/65 (2026-06-18, Follow Mode, frozen, REBUILT). Layout: JSON of32=16 sz32=32784; `sends_follower.js` 5808 B of32=32800; `sf_version_check.js` 3106 B of32=38608. Archived `…002423-frozen.amxd`; source of that rebuild was unfrozen `3f31990a` (52813 B, `…002025.amxd`).
- ⚠️ **INCIDENT 2026-06-17→18:** prior frozen `725a06ea` (41504 B) was overwritten/re-frozen by Live on save BEFORE it hit the archive → lost. The on-disk file Live left (`3f31990a`, 52813 B) was, despite "unfrozen" label, ALREADY a valid self-contained freeze (3 dire, both JS embedded = canon, all invariants passed) — just bloated (verbose float serialization, JSON 43518) and unarchived. Rebuilt lean equivalent via Path B from compact `json.dumps(indent=1)` → `7ca595b4`, 42090 B (≈41504 target; ~586 B over = indent-1 vs Max exact whitespace + ordering). LESSON ENFORCED: dated archive of frozen result IMMEDIATELY after freeze, before any Live open.
- Self-contained: patcher + `sends_follower.js` + `sf_version_check.js` all embedded; no external `.js` needed.
- Lost frozen build (Follow Mode, pre-incident): md5 `725a06ea762716e96f13a9b63cb3e3f9`, 41504 B, 65/65 — overwritten by Live, never archived (see incident above). Reproduced by `7ca595b4`.
- Prior frozen build (version-check, no Follow Mode): md5 `b5286b33d9adc12e023981ab1a117859`, 37444 B, 61/61.
- Earlier frozen build (JS-embedded, no version-check): md5 `6b85d12dcff2e412f15ae75897505f20`, 36211 B, 50/51 → backup `raw/archive/SendsFollower.2026-06-17.amxd`.

## Rack round-trip + LFO status (re-verified 2026-06-17)
- `SendsFollowerRack.adg` (file md5 `7c875964319ee6352698ed2c093b1e81`, gzip 6406 B → **88296 B** XML, Live 12.4.2). Internal `UserName` of group dev = `SendsFollowerRack`; `MaxSendsFollower` survives only inside rack's own `LastPresetRef`/`PresetRef` metadata, NOT the file name.
- **Structure:** single-chain `AudioEffectGroupDevice` → 1 `BranchPresets` → 1 `AudioEffectBranchPreset` → 1 `DevicePresets` → exactly **2** `MxDeviceAudioEffect`: (1) Sends Follower (`UserName=send_follower`, FileRef `Max Devices/SendsFollower.amxd`, path type 6), (2) stock LFO (FileRef `Devices/Audio Effects/LFO/Ableton Folder Info/LFO.amxd`, path type 7). No 3rd device, no reorder.
- **FileRef pulls frozen device** (md5 `b5286b33…`): rel + abs path → current User Library `.amxd`, name/path unchanged → loads fine.
- **LFO = STOCK Ableton device**, ships with Live Suite → NOT custom, do NOT bundle.
- ⚠️ **Stock LFO patcher is ENCRYPTED — cannot be decoded or forked (verified 2026-06-18, read-only).** Container = `ampf → meta → ciph → ptch` (542384 B), **NO `mx@c` chunk** (vs our cleartext devices which have `mx@c` + `boxes` ~off 512). Zero cleartext anywhere: no `boxes`/`maxclass`/`patchline`/`live.tab`/`loadbang`/`presentation`/even `patcher`; body entropy ~7.93/8.0. `ciph` = Ableton's protection wrapper. Only one `LFO.amxd` on system (Core Library `LFO/` holds only `.adv` presets, not the patcher). Our `.amxd` Path-A/B recipe does NOT apply to protected devices.
  - Consequence for the «always open LFO on the mapping view (Map 1/Map 2 ranges)» ask: **IMPOSSIBLE without a from-scratch custom LFO clone.** The view toggle (params↔mapping icon) is a PURE UI object, NOT a parameter: LFO's full `.adv` ParameterList (Depth, Freq Rate, Hold, Jitter, **Offset**, Phase, Re-Trigger, Shape, Sync Rate, Time Mode, WaveShape + 3 map-target refs `obj-7::obj-5` / `obj-7::obj-16::obj-5` / `obj-7::obj-16::obj-130`) has NO view/page/mode entry (checked ViewMode/PageIndex/ParameterView/SelectedPage/DisplayMode/MappingView — all absent). So: not addressable via LiveAPI (no `parameters N` for view → Sends Follower can't set it via `this_device canonical_parent devices 1 …`); saved view in `.adg` doesn't stick (patch forces a default view on load — can't confirm which object due to encryption); macros don't switch view; AND can't fork the device (encrypted). Only escape = build our own open LFO device (then must bundle/maintain it).
- ⚠️ Pre-existing: `send_follower.adv` referenced by rack is MISSING, but only in `<LastPresetRef>` metadata, not load path → device still loads from `.amxd` FileRef. `LFO.adv` exists on disk (also only LastPresetRef metadata).
- **Macros/mapping clean after re-save:** 16 default unnamed macros, NO `MacroMappings`/`ControllerTargets`/`MapModeMin/Max`. 26 `ModulationTarget` blocks = empty automation stubs (`LockEnvelope=0` only), not routings. No envelopes. Rack adds zero wiring.
- **`devices 1 parameters 5`** (in the `.amxd` patcher, `path this_device canonical_parent devices 1 parameters 5` → live.path → live.remote~): `devices 1`=LFO; `parameters 5` = the LFO's **Offset** parameter — **founder-confirmed 2026-06-17** (he built it; SF drives the LFO's Offset and nothing else). **CLOSED** in wiki — no more open needs-verification on the modulation write path. NOTE: `.adg` alone can't prove the name (it stores ParameterList ALPHABETICALLY — Depth, Freq Rate, Hold, Jitter, Map1… — NOT LiveAPI index order); don't re-open from `.adg`. Positional addressing is now a **usage caveat** (not a verify item): swap/reorder the 2nd device → target moves off Offset.
- **Bipolar modulation = CONFIRMED design-intent (founder, 2026-06-17).** `scale 0. 1. -100. 100.` (`obj-16`) → bipolar -100..+100; `live.remote~` (`obj-11`) applies a **signed offset** to the target (offsets around base value, doesn't overwrite). Intentional: modulates amount AND direction (follow value pushes target up/down + into negative). Prior needs-verification flag CLOSED in wiki. This is the *character* of the modulation (signed offset, semantics of `live.remote~` + the −100..100 scale). Param identity ALSO now closed: `parameters 5` == LFO's **Offset** (founder-confirmed 2026-06-17, see line above). No open needs-verification left on the modulation path.
- **Size growth 52194→88296 EXPLAINED:** re-save in Live 12.4.2; LFO device block = 62989 B (~71% of file) now serializes LFO's full ~91-entry ParameterList. SF block only ~4699 B. NO embedded samples/audio/blobs (`OriginalFileSize` 21673/542384 = stale FileRef metadata, not embedded data). Delta = pure LFO param-state bloat from newer Live.

## Rack self-containment — `.adg` CANNOT embed the `.amxd` (verified 2026-06-17)
- An `.adg` rack references its M4L device by `FileRef` (`PatchSlot`→`MxPatchRef`→`FileRef RelativePath="Max Devices/SendsFollower.amxd"`, `RelativePathType=6`=RelativeToUserLibrary), **never embeds it**. Proven: 88286 B decompressed XML has **0** `ampf`/`mx@c`/`ptch` bytes; both `MxDBlob` slots empty (`<Blob />`). So a lone `.adg` is NOT device-self-contained — recipient still needs `SendsFollower.amxd` in their User Library `Max Devices/`. There is NO XML field to fold `.amxd` bytes into `.adg`; don't try to fabricate one (would make a broken file). Freeze is an `.amxd`-level op only (our device already frozen, b5286b33). The `<Blob>`/`HasData` slot = Max parameter-state blob, not the device container.
- Single-deliverable routes: **Pack `.alp`** (File>Manage Files>Manage Project>Create Pack — installs `.amxd` into User Library, then rack finds it; `.alp` proprietary gzip+manifest, must export from inside Live, NOT buildable by us) OR ship `.amxd`+`.adg` with a "drop `.amxd` into Max Devices/" step (Control XL model).
- **`.adg` round-trip recipe:** `.adg` = gzip(-9) of XML. `gunzip -c rack.adg > x.xml`; edit; `gzip -c -9 x.xml > new.adg`; validate `xmllint --noout`, gzip magic `1f8b`, round-trip `gunzip(new.adg)==edited xml`. ⚠️ The on-disk `.adg` can change between sessions (re-saved in Live) — always re-`gunzip` the CURRENT disk file right before editing, don't trust an XML extracted earlier in the session.
- **Canonical rack name = `SendsFollowerRack`** (file + group `UserName`). Stale `MaxSendsFollower` is GONE from the current on-disk file (was only in old LastPresetRef metadata). Current `.adg`: md5 `66896a8b2fce2ff050feae0d09d24468`, gzip 6371 B → 88286 B XML. Broken `send_follower.adv` ref persists only in PresetRef/LastPresetRef metadata — harmless, not on load path; leave untouched.

## "Map button on SF face" feasibility (READ-ONLY study 2026-06-18)
- **Arm stock-LFO map remotely = NO.** LFO encrypted (no `mx@c`, ciph wrapper); its map-arm is a pure UI object, NOT a parameter, NOT in the LFO's `.adv` ParameterList → not addressable via `this_device canonical_parent devices 1 …`, not in LiveAPI. Can't fork (encrypted). Confirmed.
- **SF-hosted Map = YES, feasible from a frozen device, no external deps.** Standard stock-pattern (LFO/Envelope Follower/3rd-party "Button Parameter Mapper" all do it): Map button arms map-mode → user clicks ANY param in Live → device reads `live_set view selected_parameter` (CONFIRMED correct path) → grabs its id → feeds `live.remote~`. Observe via `live.observer`/JS `LiveAPI` on `property selected_parameter`. Already have `live.remote~` (obj-11), `live.path`/`live.object` idioms in patch.
- **Signal caveat (product fork):** SF can't tap LFO's audio out / re-route it. An SF-hosted map drives SF's OWN signal (the existing `scale … -100. 100.` bipolar follow value, or raw 0..1, or a new depth-scaled bus) straight to the clicked target, BYPASSING the LFO. So this map is an *alternate output path*, parallel to (or replacing) the LFO leg — not a remote control of the LFO's mapping. Founder must choose: keep LFO leg + add SF-direct leg (two targets) vs SF-direct replaces LFO (LFO becomes redundant for that path).
- **Persistence:** clicked-param id is session-specific → on map, store the param's CANONICAL PATH (LiveAPI `unquoted_path`/`path`), re-resolve to id on load via `live.path` after `live.thisdevice` (same cold-load race pattern already used for return-index; defer + retry). Unmap = clear stored path + free the live.remote~ target (send empty/clear to its right inlet, null the LiveAPI ref).
- **UI space:** current presentation (on-disk SendsFollower.amxd 44122 B, 66/66) openrect height **169**, width ~123 (content x=7..123). Items by y: "New Version" textbutton `[46,2,70,16]`; dial `[7,17,116,116]` (bottom 133); "Mode" comment `[7,118,109,17]`; Mode live.tab `[7,134,116,17]` (bottom 151). Free: ~18px below tab (151→169) — too tight for a real map row. A map button needs a layout change: either grow openrect height (~+22) to add a Map row at y~153, or reclaim the empty top-left `[0..46, 2..18]` next to "New Version" for a compact map toggle. Object type for map button = `live.text` in toggle/map style (mappable look matching Live theme) or `textbutton`+JS; stock devices use a `live.text`-style toggle.
- ⚠️ on-disk size/md5 differs from memory's "current frozen 49134 B" — Live re-saved (44122 B). Re-read actual file before any future edit; memory current-state slice may lag.

## `Abl.*` map-engine feasibility (READ-ONLY study 2026-06-18) — NO-GO, stay on live.map
- **`Abl.MapUi`/`Abl.MultiMap`/`Abl.Map.maxpat` are NOT stock Live resources.** They exist on disk ONLY in the user's UNPACKED LFO project `/Users/Kirill/Documents/Max 9/Max for Live Devices/LFO Project/` (plain JSON, readable, recent Save-As). On a clean Live install they're **encrypted inside `LFO.amxd`** (`ampf→meta→ciph→ptch`, 542384 B, no `mx@c`/`dlst`/cleartext `Abl.Map`) → **NOT on a buyer's search-path**. bpatcher resolves by name (all 3 have empty `dependency_cache`) but the names aren't there for buyers → `can't find file`.
- **Engine = audio-rate.** `poly~ Abl.Map` (1 voice/slot) takes `in~ 1` (SIGNAL, not message): `in~→clip~ 0. 1.→scale~ (Min/Max)→live.modulate~ (obj-10, real Mod offset) OR live.remote~ @normalized 1 (obj-5, via p RemoteOrModulate)` + thispoly~/mute for CPU. `Abl.MultiMap` inlets = `(signal) signal to map`,`(float) depth`,`(bool) bipolar`; holds 7× poly~ Abl.Map + 7× bpatcher Abl.MapUi + `js getObjectRect` (dynamic layout via thispatcher script). `Abl.MapUi` uses `#1` instance args + shared-receive `---modMode`/`---mappedState` (private parent-orchestrated namespace).
- **Buyer-bundle deps if forced:** 3 patchers + `getObjectRect.js` + `Polarity_*.svg` — all MISSING on stock path (only in user's unpacked LFO project). Embeddable (plain JSON) via Path B but = shipping unpacked proprietary Ableton assets (legal + version-break risk). Private/undocumented (internal comment "don't forget to update Abl.MapWithScaledOuput.maxpat") → high risk Ableton renames them across Live versions. **NO-GO.**
- **We're already on the native path.** Stock, ON search-path, portable to every buyer: `…/C74/packages/Max for Live/patchers/liveui.map.maxpat` (= `live.map @strict 1` + unmap/`hidden $1`/`zl compare <none>`/`prepend mapping`) and `liveui.multimap.maxpat` (**exactly 8 slots** of bpatcher `LiveUI.Map`=liveui.map). This is EXACTLY our current MAP FIX mechanism (`live.map @strict 1` + `_persistence 1` + prepend mapping + zl compare <none> + hidden $1). Our manual 8-slot mapper IS the native Ableton map-grab from the same primitives — only the UI render (frames/colors/icons) is approximate, not pixel-1:1 (that exact look needs the proprietary `LiveUI.Map` bpatcher we don't bundle). **Verdict: keep current live.map reconstruction.**
- Cheap stock-true polish WITHOUT bundling proprietary (optional, on request): `multimap-unmap.svg` already used; could copy self-contained `p ButtonColor`/`p SetName`/`p Scale` subpatchers FROM `liveui.map.maxpat` into our OWN patcher (no external deps). For a REAL Mod/Remote engine (vs bare signed-offset live.remote~) the only stock+portable object is **`live.modulate~.mxo`** (`…/C74/externals/m4l/live.modulate~.mxo`, on every install) — instantiable directly as `live.modulate~`, no Abl.Map wrapper. Separate feature, not needed for current look.

## Stock `liveui.multimap` distributability + interface (READ-ONLY study + TEST BUILD 2026-06-18) — DISTRIBUTABLE, but UI+scaling only
- **Re-checked the STOCK `liveui.multimap.maxpat`** (≠ `Abl.MultiMap` decompile): `/Applications/Ableton Live 12 Suite.app/Contents/App-Resources/Max/Max.app/Contents/Resources/C74/packages/Max for Live/patchers/liveui.multimap.maxpat` (1533220 B). Distinct file from LFO-decompile `Abl.MultiMap.maxpat` (86187 B, md5 differs).
- **ALL deps are STOCK / on every install's search path — DISTRIBUTABLE:** (1) 8× bpatcher `LiveUI.Map.maxpat` — each `embed:1` with FULL inline `patcher` (self-contained inside the file; the named `liveui.map.maxpat` also exists in stock patchers/ as fallback); (2) 3 SVGs `multimap-unmap.svg`/`multimap-closed-off.svg`/`multimap-open-off.svg` — all in stock `…/Max for Live/media/` (resolve by name, NOT embedded as bytes, same model as our existing unmap.svg); (3) externals `live.map`/`live.colors`/`live.modulate~`/`live.remote~` all in stock `…/C74/externals/m4l/*.mxo`. **NO `js`, NO `node.script`, NO `poly~`, NO `gen~`, NO `Abl.*` anywhere** (recursive walk: only `t/sel/zl/route/scale/clip~/+~/*~/pak/live.map/live.colors/live.thisdevice`). So a device embedding stock `liveui.multimap` raises NO `can't find file` for a buyer.
- **Interface (external contract is TINY):** **1 inlet = `(signal) signal IN`** (fanned to all 8 slot bpatchers' inlet 0). **2 outlets: out0 = `(list) parameter id`, out1 = `(signal)` passthrough** (input echoed, for chaining/resize msgs). ALL map-arm / Min / Max / ±/depth / mode UI is SELF-CONTAINED inside the embedded `LiveUI.Map` slots (driven by clicks via native `live.map @strict 1`). To feed it our follow signal: `obj-7 (---max_send 0..1) → sig~ → multimap inlet 0`. No poly~/gen~ needed to drive it.
- ⚠️ **THE CATCH (why it's not a drop-in replacement): stock multimap has NO `live.remote~`/`live.modulate~` inside.** Each slot = `live.map` (native grab) + `p Scale` (Min/Max/±/depth scaling) → emits a **(signal) scaled signal** + **(lom id) parameter id** to the PARENT. In the stock LFO the parent's WRITE layer = `Abl.Map` poly~ voices (the ones holding `live.remote~`/`live.modulate~`) — and `Abl.Map` is **LFO-decompile-only / non-distributable** (NO-GO, see prior study). So using stock multimap means WE supply our own stock `live.remote~`/`live.modulate~` write layer fed by the slots. BUT: at the multimap's TOP-LEVEL boundary the **per-slot scaled signals are NOT exposed** (they go into internal `pak`→`zl sum` housekeeping for the resize UI); only ONE combined `parameter id` list + signal passthrough come out. So you cannot pick off slot-N's scaled signal + slot-N's id at the multimap boundary to wire your own per-slot remote~. To get per-slot signal+id you'd have to EDIT the inlined `LiveUI.Map` slot patchers (expose `p Scale` out + `live.map` id to new slot outlets) — i.e. fork the stock UI internally. Doable (it's cleartext, embeddable via Path B) but then it's a maintained fork, not "use stock as-is".
- ⚠️ **EMBED OF STOCK MULTIMAP BREAKS IN LIVE (2026-06-18).** First MM-Test build embedded the full 1.5 MB `liveui.multimap` inline (`embed:1`, container valid: `ampf`, `ptch==fs−0x20`, JSON re-parsed py+jq, 1142795 B `523bffbe`). Live REJECTED it: **«The Max function CreateDevice returned with error 6: Device file broken»** + Console «…MM Test.amxd: error -1 making directory» ×5. Container recipe was fine; the giant inline bpatcher payload is what Live chokes on (likely tries to extract/materialize a project dir for the embedded sub-patcher). → **Do NOT inline-embed a multi-MB stock bpatcher into a hand-built `.amxd`.** Archived broken `raw/archive/SendsFollower MM Test.2026-06-18-204320-broken-embed.amxd`.
- ✅ **REFERENCE-BY-NAME RESOLVES — native path is mechanically possible (2026-06-18).** Rebuilt MM-Test referencing the multimap **by name, NO inline patcher, NO embed**: bpatcher box `{"maxclass":"bpatcher","name":"liveui.multimap.maxpat","numinlets":1,"numoutlets":2}` (no `patcher`/`embed` keys). Two independent stock mechanisms make `liveui.multimap` resolve on EVERY install's search path: (1) **package `patchers/` dir is on the global Max search path** — PROVEN by our shipped device already loading `media/multimap-unmap.svg` from this same package by bare filename (no embed, no `can't find file`); `liveui.multimap.maxpat` sits in the sibling `patchers/` subdir → resolves identically by filename. (2) **`…/Max for Live/init/live-objectmappings.txt`** registers `max objectfile liveui.multimap liveui.multimap.maxpat;` (+ `liveui.map`) — an init objectfile mapping loaded for every M4L patch, so `liveui.multimap` is even instantiable as a bare `newobj` short-name abstraction. ⚠️ Note Cycling'74's OWN reference clipping (`clippings/Live UI Abstractions/Multi-Mapping.maxpat`) uses `embed:1` + full inline (934 KB box) — embed is their default but NOT required; the `name` field alone resolves via search-path.
- **CURRENT TEST BUILD (reference, tiny, valid):** `~/Music/Ableton/User Library/Max Devices/SendsFollower MM Test.amxd` — minimal UNFROZEN device **3471 B** (`694a6f85`, was 1.14 MB embed): `live.thisdevice` + `live.dial` (Source, 0..1, presentation) → `sig~` → **bpatcher `name="liveui.multimap.maxpat"` BY REFERENCE (no inline, no embed)** → `print MMPARAM` (tapped off multimap outlet 1 = `(list) parameter id`). Container = header(48, reused from prior valid file) + JSON(@48) + single `\x00`; NO dlst/embedded files/mdat (unfrozen). Set `ptch=mxc+16` (LE@0x1C), `mxc=len(JSON)+1` (BE@0x2C). Validated: `ptch==fs−0x20` ✓, `mxc==JSONlen+1` ✓, JSON re-parses py+jq ✓, brace-balance 0 ✓, 5 box/3 line, bpatcher has `name` + NO inline `patcher`/`embed` ✓, zero `dlst`/`dire`/`mdat`/`TEXT` leftovers ✓ → no embed-extraction trigger, so the «error -1 making directory» cause is removed. Did NOT touch working `SendsFollower.amxd` (`7ebe7d15`). **Verify in Live:** drop on Audio/MIDI track → expect native 8-row multimap UI to render with NO «Device file broken»; click a slot Map → click any Live param → captures + shows name natively. Wiggle Source dial — param won't MOVE yet (no remote~/modulate~ write layer; multimap only scales/exposes id). If it loads = reference path is REAL → next step is the per-slot write-layer integration (still needs forking inlined slots to surface per-slot scaled-signal+id, per the CATCH above). If Live still says broken even by-reference → packages `patchers/` not on a custom device's path after all → native path impractical (embed breaks, reference fails) → stay on hand-built `7ebe7d15`.
- **VERDICT:** stock `liveui.multimap` = **DISTRIBUTABLE** (zero proprietary deps). It gives the EXACT native map-UI (names/Mode·Remote/±/Range) the founder wants, for free. The work it does NOT do = writing to the mapped params; we add stock `live.remote~`/`live.modulate~` for that, but feeding them per-slot requires forking the inlined slots (expose scaled-signal+id per slot) since the stock boundary only exposes a combined id list. RECOMMENDATION: viable path forward = embed stock multimap + lightly fork the 8 inlined `LiveUI.Map` slots to surface per-slot (scaled signal, id) → our own 8× live.remote~/live.modulate~ (the legs we already build by hand). That trades our hand-built UI battles for the native render, at the cost of maintaining an internal slot-fork. Vs current hand-reconstruction `7ebe7d15` which is fully working & 100% ours. Founder's call: native pixel-perfect UI + slot-fork maintenance, vs current approximate-but-fully-owned UI.

## MM Test — diagnosis refinement (DIAGNOSIS-ONLY pass 2026-06-18) — crash cause CONFIRMED, fork point CORRECTED
- **ON-DISK STATE NOW:** `SendsFollower MM Test.amxd` in User Library = the **BROKEN embed variant** `523bffbe` (1142795 B, `embed:1` + full 1.5 MB inline multimap), IDENTICAL to archive `…204320-broken-embed.amxd`. NOT the 3471 B stub. The stub (`694a6f85`) lives only in archive `…205643-stub-userlib-preedit.amxd`. (Founder said "valid snapshot restored" — but the file on disk is the rejected embed build. Both MM Test files are MY prior test builds, not a founder session snapshot.) Working device `SendsFollower.amxd` = `7ebe7d15` (342759 B), untouched.
- **CRASH MECHANISM CONFIRMED:** `error -1 making directory` is triggered by INLINE-EMBEDDING the multi-MB multimap, NOT by referencing/using a stock internal-UI component per se. Embed payload = 43 nested `patcher` + 9 `bpatcher`/`embed` + full Max project metadata (`dependency_cache`, `subpatcher_template`, `autosave`, `fileversion`×43). On load Live tries to materialize a working/project dir for that embedded sub-patcher tree and fails (`-1 making directory`) → `CreateDevice error 6: Device file broken`. Container itself was valid (`ptch==fs−0x20`, JSON parsed py+jq). So: crash = embed-of-giant-bpatcher, NOT "any stock internal-UI instance in a custom device".
- **REFERENCE (no-embed) variant = structurally valid, crash cause removed.** Stub 3471 B: `ptch==fs−0x20` ✓, `mxc==JSONlen+1` ✓, JSON re-parses, ZERO trailing (no dlst/embed/mdat), bpatcher has `name` + NO inline `patcher`/`embed`. Searchpath proof STRONGENED: shipped `SendsFollower.amxd` already resolves `multimap-unmap.svg` (17 refs) by BARE name from this same package's `media/` → package IS on Max search path; `liveui.multimap.maxpat` is in sibling `patchers/` + registered in `init/live-objectmappings.txt` (`max objectfile liveui.multimap …`). ⚠️ STILL UNCONFIRMED on hardware: whether `patchers/` (vs `media/`) resolves for a CUSTOM device's bpatcher — needs a Live drop-test (can't drive loader from agent env). This is the ONE open empirical link.
- **CORRECTED fork point (was wrong in prior note):** per-slot scaled-signal + id ARE exposed AT THE SLOT LEVEL — inline `LiveUI.Map` slot = numout 2: **outlet1 = `(signal) scaled signal`, outlet2 = `(lom) parameter id`**. They get summed at the TOP-LEVEL multimap into `pak`/`zl sum` (resize housekeeping) and only a COMBINED id list + signal passthrough leave the multimap boundary. So to surface per-slot (signal,id) you fork the **TOP-LEVEL `liveui.multimap.maxpat`** (re-route each of the 8 slot bpatchers' outlet1/outlet2 to NEW top-level outlets) — you do NOT need to edit the 8 inline slots themselves. Less invasive than prior memory claimed. Then feed 8× our own stock `live.remote~`/`live.modulate~`.
- **Real LFO write-layer (confirmed via user's unpacked LFO Project, read-only):** `Abl.MultiMap` (7 slots) = `poly~ Abl.Map` voices + `Abl.MapUi` bpatchers. The WRITE happens in **`Abl.Map`** voice: `in~ 1 → clip~ 0. 1. → scale~ → p RemoteOrModulate → live.remote~ @normalized 1` OR `live.modulate~` (+ `p ModulationScale`, `p Mapping`, `p StoreId`, thispoly~). `Abl.*` = decompile-only, NON-distributable (encrypted in buyer's `LFO.amxd`: `ampf→meta→ciph→ptch`, entropy 7.92/8, no `mx@c`/cleartext — re-confirmed). So buyer-safe = stock `liveui.multimap` (UI+scale only) + OUR stock `live.remote~`/`live.modulate~`.
- **RECOMMENDATION (diagnosis pass):** the fully-owned hand-built mapper in `7ebe7d15` (8× live.map + live.remote~ + live.modulate~, native grab, Min/Max/±/depth, persistence, instant recompute) ALREADY DELIVERS the working "LFO-style modulation + SendsFollower" the experiment was chasing — it IS the same native `live.map` primitives the stock multimap uses, just with our own render. The MM-Test/stock-multimap route's only upside = pixel-exact native UI, at cost of (a) unconfirmed `patchers/` searchpath for custom devices, (b) a maintained top-level multimap fork, (c) Ableton renaming risk across Live versions. NOT worth abandoning `7ebe7d15`. If founder wants the exact native look: first do the cheap Live drop-test of the reference stub; only if it loads, pursue the top-level-fork route.

## MM Test — STUB REJECTION DIAGNOSED + clean reference test built (2026-06-18, drop-test pass 2)
- **Founder dropped the no-embed stub `694a6f85` (3471 B) → SAME `CreateDevice error 6: Device file broken`.** Re-read as "container/device parse failure, NOT a bpatcher-frame-resolve failure" (a frame miss would load the device with empty/red bpatcher, not reject the whole device).
- **ROOT CAUSE = the stub was never a valid M4L device, independent of the bpatcher.** Diffed its patcher vs known-good `7ebe7d15`. The stub MISSES every device-level marker M4L writes: **no `parameters`** dict, **no `project` block** (so **no `amxdtype`** — the audio/midi/instrument type tag; `7ebe7d15` has `project.amxdtype = 1633771873 = 0x61616161 = 'aaaa' = audio effect`, the same `aaaa` seen at header offset 8), **no `plugin~`/`plugout~`** audio passthrough, no `platform_compatibility`/`minimum_live_version`/`minimum_max_version`/`latency`/`is_mpe`/`openrect`/`openrectmode`/`oscreceiveudpport`/`autosave`/`saved_attribute_attributes`. PLUS it carries `subpatcher_template` (a plain-editor maxpat marker a device must NOT have) and `appversion 8.6.0` (built on a stale/foreign skeleton; real device = 9.1.4). → It's a bare maxpat wrapped in an `ampf` container, not a device. This is WHY static invariants passed (they check the CONTAINER: ptch/mx@c/JSON-parse) while Live rejected it (Live validates DEVICE semantics: parameters + project.amxdtype + audio I/O). Lesson: container-valid ≠ device-valid; a hand-built `.amxd` MUST copy the device-env keys (`project`+amxdtype, `parameters`, `plugin~`/`plugout~`, appversion 9.x) from a known-good device.
- **CLEAN DECISIVE TEST BUILT:** `~/Music/Ableton/User Library/Max Devices/MM Ref Test.amxd` (`5a3b7de1`, 5120 B, NEW name to dodge Live's path cache). Constructed from scratch with the FULL device env copied byte-faithful from `7ebe7d15`: `project` (incl. `amxdtype` aaaa), `parameters {}`, `appversion 9.1.4`, `saved_attribute_attributes`, `plugin~→plugout~` stereo passthrough, `live.thisdevice`, `live.dial` (Source param) → `sig~` → **bpatcher `name:"liveui.multimap.maxpat"` BY REFERENCE (no inline patcher, no embed)** → `print MMPARAM` off outlet 1. openrect `[0,0,332,169]`. Header skeleton from SF with recomputed ptch(LE@1C)/mx@c(BE@2C); fixed regions 0x00..0x1C & 0x20..0x2C byte-identical to SF; first byte `{`, last `\x00`. Validated: `ptch==fs−0x20` ✓, `mx@c==len(JSON)+1` (unfrozen, no dlst tail) ✓, JSON re-parses py `json.loads` + `jq -e .` ✓, has parameters+project+amxdtype+plugin~/plugout~, no `subpatcher_template`, bpatcher has `name` + NO `patcher`/`embed`. 7 box / 5 line. Did NOT touch `SendsFollower.amxd` (`7ebe7d15`) nor the old `SendsFollower MM Test.amxd`.
- ⚠️ **PREDICTABILITY CAVEAT (statics burned us once):** this test has a fully-valid DEVICE env now (copied from a loading device), so it isolates the ONE remaining variable = does a custom device's bpatcher resolve `liveui.multimap.maxpat` from the package `patchers/` dir. Two NEW risk factors found inside the multimap itself that statics can't predict: (1) it nests **8× bpatcher `LiveUI.Map.maxpat`** (CamelCase) while the on-disk sibling is `liveui.map.maxpat` (lowercase) — resolves on case-insensitive macOS FS but is fragile; (2) it carries its OWN **44 parameters** (`obj-16::obj-34` …) that must merge into the device's namespace on load. So even with a valid skeleton, by-reference load can still fail at the nested-resolve/param-merge stage. Needs the hardware drop-test to answer.
- **STILL: this is a UI-strategy test ONLY.** Even if it loads, stock multimap does NOT write to mapped params (no remote~/modulate~ inside) — the founder's actual feature (existing sender-fed encoder gets a black multi-map frame, map to any param of any device, per-slot Min/Max) is ALREADY fully built & owned in `7ebe7d15` via the same native `live.map` primitives. The reference route's only payoff = pixel-exact native render, at the cost of a maintained top-level-multimap fork (to surface per-slot signal+id) + Ableton-rename risk + the (still-unproven) custom-device `patchers/` searchpath.

## Distribution note
Manifest `https://fadercraft.com/api/sends-follower.json` + Gumroad NOT touched by me (out of scope) — deploy is a separate explicit step. Device works offline (fallback URL = library.gumroad.com) until manifest is live.
