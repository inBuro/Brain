---
name: sends-follower
description: SendsFollower.amxd standalone device — paths, container layout, embedded JS, version-check feature, rack round-trip, LFO status. Read before any SendsFollower edit.
metadata:
  type: project
---

# Sends Follower — device facts (m4l-master)

## ✅ Return: warning «Feedback loop» в version_link (self-feed детект) — 2026-06-24
Раз авто-exclude невозможен (маппинг мимо JS), founder выбрал WARNING вместо разрыва петли. Реюз `version_link` (тот же элемент, что «New Version»/update), цвет = `themecolor.live_alert` (оранж-красный #FF7F43 — единственный alert-токен в палитре, founder одобрил), текст «Feedback loop». ТОЛЬКО Return (`sends_follower.js`+`.amxd`); общие multimap/MapButton/Track/SendsReader НЕ тронуты.
⚠️ **CURRENT: Return `887f08b3`, js `404208fd`.** Итерации: (1) мгновенный показ — `loadbang()` в js (ставит observers+selParamObs+buildRefs СРАЗУ при загрузке; `updateFeedbackWarning` пересобирает sendRefs on-demand); (2) ресайз version_link ВЛЕВО `[53.94,7.58,70,14]`→**`[38.94,7.58,85,14]`** (правый край ФИКСИРОВАН 123.94=зазор до маппера x128; bbox-нахлёст нижних ~6px на верхний-левый угол дайла — founder ОК); (3) **УБРАНЫ ВСЕ принты** (3 `post()` в build/build-noarg/mode-noarg — НИ ОДНОГО в bang, флуда на тик НЕ было, но убраны до 0 по просьбе; патч 0 print-объектов); (4) **ТЕКСТ-ФИКС (regression):** «текст пропал» (НИ «Feedback loop» НИ «New Version»). ROOT — version_link=**textbutton**; до warn-эпопеи текст был СТАТИЧНЫЙ атрибут `text`«New Version»+show/hide (рантайм-смены НЕ было). Моя warn-ветка ввела рантайм-смену через `text "..."`/`texton "..."` (форма 2 ←изначально была `set`, тоже неверно) — textbutton multi-word через `text`/`set` НЕ берёт. ⚠️ **DEFINITIVE: textbutton рантайм-label = `setsymbol <symbol>`** (refpage: «displays symbol, no output»; стоковые меняют через setsymbol); multi-word = QUOTED symbol. `wn_txt1`=`setsymbol "Feedback loop"`, `wn_txt0`=`setsymbol "New Version"` (в JSON кавычки `\"` → Max читает quoted-symbol = ОДИН атом). Прозрачность(2)/qmetro 20/rect/цвета прочих/Track/SendsReader/маппер НЕ тронуты. ⚠️ founder HW-тест: текст показывается? (setsymbol должен сработать; если нет — план B: pre-config оба label в text/texton атрибутах + `symbol`-switch).
⚠️ **FIX текста (Return теперь `fa79cf57`, JS не менялся):** показывало «New Version» вместо «Feedback loop» — `version_link` = **textbutton**, который НЕ понимает `set <text>` для смены ярлыка. ROOT: textbutton рантайм-текст = `text <symbol>` / `texton <symbol>` (НЕ `set`); многословная строка с пробелом = ОДИН символ → в message-боксе В КАВЫЧКАХ. Было `set Feedback loop`(2 атома, игнор) → стало `wn_txt1`=`text "Feedback loop", texton "Feedback loop"`, `wn_txt0`=`text "New Version", texton "New Version"` (запятая в msg = два сообщения text+texton). Приоритет ок: текст меняют ТОЛЬКО wn_txt1/0; dot от node-скрипта идёт через wn_dotstore→vdot_sel (show/hide), текст НЕ трогает → при warn=1 «Feedback loop» держится. ⚠️ DEFINITIVE CRAFT: textbutton рантайм-ярлык = `text`/`texton` (НЕ `set`, это comment/umenu); многословное — кавычки в message.
- **JS (`sends_follower.js` теперь `6f248c5e`/on-disk `6f248c53`, node OK, outlets НЕ менял=2):** `onSelParamChange` теперь зовёт `updateFeedbackWarning(args)` (детект независим от mapper-арма, который в Return мёртв). `updateFeedbackWarning`: резолвит `selected_parameter` id, **id-membership** против `sendRefs[]` (выбран send, кормящий watched-шину) → `warn=1`, снят/безопасный → `warn=0`; шлёт `outlet(1,"warn",0/1)` только при смене (`warnState`). Outlet 1 = бывший mapper-control (в Return dead) → переиспользован. `selParamObs` уже ставится в bang self-heal.
- **Return-патч (+10 box/+19→ итог 73/79):** `obj-46[1]→wn_route(route warn)→wn_sel(sel 0 1)`. warn=1(out1): `script show`(→vlink_thispatcher) + `set Feedback loop` + `textoncolor 1. 0.490196 0.262745 1.`(=live_alert ЛИТЕРАЛ в РАНТАЙМ-message, НЕ saved-hex) + `ignoreclick 1` → version_link. warn=0(out0): `set New Version` + `textoncolor 0. 0.933333 1. 1.`(restore голубой) + `ignoreclick 0` + ребанг `wn_dotstore`(int)→vdot_sel(восстановить show/hide по последнему dot). **dot теперь идёт через `wn_dotstore`:** удалена прямая `vlink_route[0]→vdot_sel[0]`, вместо неё `vlink_route[0]→wn_dotstore[1](store)` + `wn_dotstore[0]→vdot_sel[0]`; warn=0 бангает wn_dotstore[0] → re-emit. Приоритет: warning перекрывает update; возврат корректный.
- **Return .amxd теперь `12a26147`** (UNFROZEN, 73 box/79 line). version_link НЕ ресайзнут (rect/биндинги целы), прозрачность alpha=0 (2) цела, saved-hex НЕ добавлен (alert-цвет = рантайм-message), qmetro 20, filename/numoutlets ок, 0 dangling. Track `3ce2d9a6`+SendsReader `9c0386ab`+общие deps НЕ тронуты. Архив pre-edit `…-preedit-feedback-warning.{amxd,js}` (`d6aa5f1f`/`53bdbfbd`).
- ⚠️ детект на ВЫБОР параметра (клик), не на factual mapping (как у SendsReader warn) — горит при клике на опасный send ДО нажатия Map. «Feedback loop» в 70px@9pt может подрезаться — НЕ ресайзил (founder решит отдельно).
- ⚠️ КАНОН на паузе — founder подтвердит warning в Live, потом снять с паузы (Return-канон сместится `d6aa5f1f`→`12a26147`, JS `53bdbfbd`→текущий).
- **Тест:** ПОЛНЫЙ рестарт → клик по send своей шины (выбрать его в Live) → version_link загорается «Feedback loop» оранж-красным; клик по др. параметру → возврат к норме (New Version голубой если update / hidden).

## 🔴 КОРЕНЬ exclude-бага — маппинг идёт МИМО JS (native live.map), slotPath МЁРТВ — 2026-06-24
ОБА exclude-фикса (индексный + id-membership) не сработали по ОДНОЙ структурной причине: **JS НИКОГДА не получает захваченную map-цель.** Маппер Return самодостаточен — `multimap_panel` bpatcher = 1 inlet (сигнал) / 1 outlet (НЕ captured-id); захват цели делает native `live.map`→`live.remote~` (`obj-5`) ВНУТРИ `MapButton.maxpat`, наружу id/path НЕ выводится. `obj-46`(js): inlet0 кормят только mode_prepend/qmetro/obj-43(build); **outlet1 (mapper-control arm/restorepath/unmap) никуда НЕ подключён**; bpatcher в js ничего НЕ отдаёт. ⇒ `captureTarget`/`arm`/`restorepath` в JS НЕ вызываются → `slotPath[]` ВСЕГДА пуст → `hasMappedTargets()`=false → `recomputeSelfDriven` не зовётся → `selfDrivenIds=[0×8]` навсегда → exclude никогда не срабатывает. Лог это и показывал. Весь exclude-механизм опирался на slotPath, который мёртв в этом девайсе.
- ⚠️ Это объясняет ОБЕ неудачи: чинили мёртвую ветку. Любой slotPath-based exclude в Return НЕ заработает, пока captured id не доводится до JS.
- **Усиленная диагностика (`sends_follower.js` теперь `f5c3bda1`, node OK):** capture-лог `EXCLDIAG CAPTURE slot/path/capturedId/sendRefs[]/in_sendRefs` (если НЕ печатается вовсе → captureTarget не вызывается = подтверждение); bang-лог +`hasMapped`+`slotPath=[set/-×8]` (все `-` → slotPath пуст = маппинг мимо JS). Архив pre-diag `…-preedit-diag2-mapper-bypass.js` (`068c94a5`). Return `01b0372a` НЕ тронут. Одна копия.
- **ПЛАН фикса (на согласование, НЕ применял):** довести captured id из маппера до JS. Варианты: (A) вывести id из `live.map`/`live.remote~` каждого MapButton наружу через новый outlet bpatcher → в JS inlet (правка `multimap.maxpat`+`MapButton.maxpat`+проводка девайса — общие deps, затронет Track/SendsReader). (B) JS сам опрашивает цели: на каждом bang резолвить, КУДА мапит каждый из 8 `live.remote~` маппера — но их id наружу не видны из JS. (C) exclude НЕ в JS, а в ПАТЧЕ/маппере: сравнивать id целей `live.remote~` с watched-sends прямо в Max. (D) забить на авто-exclude, вернуть KNOWN-BEHAVIOR «не мапь на свою шину» (founder ранее это уже принимал для Total-latch). Решать founder'у — фикс требует правки общих deps (риск для 3 девайсов) ИЛИ patch-side логики.
- ⚠️ убрать EXCLDIAG по результату. Канон на паузе. Цвета/прозрачность/`.amxd`/Track НЕ трогал.
- **Тест:** ПОЛНЫЙ рестарт → Map на bus-send → Console: ждать `EXCLDIAG CAPTURE` (если НЕ появляется = captureTarget не зовётся = подтверждён корень); в `EXCLDIAG bang` `slotPath=[-,-,…]` все пустые.

## ✅ FIX — exclude self-driven send по ID-MEMBERSHIP (не по индексу) — 2026-06-24
EXCLDIAG-лог подтвердил кандидат-1 (index-mismatch): `ret=0 selfDriven=[0×8] excluded=[] in=[31=1.000,…] result=1.0` — замапленный self-feeding send id 31 НЕ исключался (старый `selfDrivenSendId` требовал `sends <N>===returnIndex`, что ломалось при group-треках/нестандартном порядке) → runaway latch на 1.0.
- **ФИКС (id-membership, выкинута индексная арифметика):** `selfDrivenSendId`→**`targetId(path)`** = резолвит LiveAPI-id ЛЮБОЙ захваченной цели БЕЗ проверки индекса/returnIndex. `isSelfDrivenId(id)` исключает sendRef, чей id ∈ множеству id захваченных целей. Совпадение возможно ТОЛЬКО когда цель сама — один из агрегируемых sendRefs (кормит watched-шину) → петля рвётся при ЛЮБОМ роутинге; цель на другую шину имеет id, которого нет в sendRefs, → не исключается (корректно). id источник тот же `LiveAPI(path).id`, что у `buildRefs` → сравнимы (закрывает и кандидат-2 id-mismatch).
- **Пересчёт selfDrivenIds:** в `captureTarget`/`restorepath` (на capture), `buildRefs` (`recomputeSelfDriven`), И на КАЖДОМ `bang()` через `if (hasMappedTargets()) recomputeSelfDriven()` (id-membership против АКТУАЛЬНЫХ sendRefs; дёшево — резолв только при непустых slotPath, обычный случай 0 резолвов). Закрывает кандидат-3 (таймлайн): не зависит от того, успел ли returnIndex/buildRefs.
- **Обычный кейс цел:** нет замапленных bus-сендов → slotPath пусты → hasMappedTargets()=false → recompute не зовётся → агрегат как раньше.
- **`sends_follower.js` теперь `068c94a5`** (node OK, 0 `selfDrivenSendId`, returnIndex в exclude НЕ используется — только в detect/buildRefs; строка 321 `matchIdx` = диагностика EXCLDIAG, безвредна). EXCLDIAG (2 точки) ОСТАВЛЕН для подтверждения. Архив pre-fix `…-preedit-id-membership-exclude.js` (`e07cd0a7`). Return-девайс `01b0372a` НЕ тронут (грузит JS по имени). Одна копия. Track-движок `6a000af3` НЕ тронут.
- ⚠️ убрать EXCLDIAG по подтверждению → чистая версия. Канон на паузе.
- **Тест Live:** ПОЛНЫЙ рестарт → Map на bus-send своей шины → чуть двинуть → в `EXCLDIAG bang` замапленный id теперь в `excluded=[..]` (НЕ в `in=[..]`), result двунаправлен (не залипает 1.0).

## 🐛 OPEN BUG — runaway feedback на same-index send несмотря на exclude-фикс — 2026-06-24
Founder: Return на Return A, замаплен на Send A (кормит Return A). При малейшем вводе выход улетает в max и залипает бистабильно (max/min) = runaway положительной обратной связи — exclude-фикс (`eb2b2518`) НЕ ловит этот send. Канон/финал ОТЛОЖЕН.
**Диагноз по коду — 3 кандидата разрыва exclude (нужен Live-замер):**
1. **Индекс-mismatch:** `selfDrivenSendId(path)` матчит `mixer_device sends <M>$` и требует `M===returnIndex`. `returnIndex` = из `return_tracks <N>` (`detectReturnIndex`). `M` = индекс send-слота на треке. Обычно совпадают, но при group tracks/нестандартном порядке могут разойтись → send НЕ исключён.
2. **id-mismatch:** id из `selfDrivenSendId(captured path)` (через `sel.unquotedpath`) vs id из `buildRefs(live_set tracks i mixer_device sends returnIdx)`. Если форматы путей расходятся → разные/0 id → `isSelfDrivenId` не ловит.
3. **Таймлайн:** на момент `captureTarget` `returnIndex` мог быть −1 → `selfDrivenSendId` вернёт 0. `recomputeSelfDriven()` чинит только из `buildRefs` — если индекс стабилен и `resync` не дёрнул buildRefs, `selfDrivenIds[slot]` остаётся 0.
**Диагностика (`sends_follower.js` теперь `e07cd0a7`, node OK, 2 EXCLDIAG-точки):** (a) `captureTarget` → `EXCLDIAG capture slot/returnIndex/path/capturedId/sendIdxInPath/selfDrivenId/(matchIdx)` — видно, попал ли захваченный send в selfDrivenIds и совпал ли индекс. (b) `bang()` раз/700мс → `EXCLDIAG bang ret/mode/selfDriven[]/excluded[]/in[]/result` — видно, реально ли Send A в selfDrivenIds и выпал ли из агрегата. +1 var `_exDiagAt`. Архив pre-diag `…-preedit-excl-diag.js` (`eb2b2518`). Return-девайс `01b0372a` НЕ тронут (грузит JS по имени). Одна копия. ⚠️ убрать диагностику по результату → вернуть `eb2b2518` (если фикс ок) или к исправленной версии.
**Шаги Live:** ПОЛНЫЙ рестарт Live (stale cache!) → Max Console → Map слот на Send A своей шины → чуть двинуть любой send → прислать `EXCLDIAG capture` (что захвачено) + `EXCLDIAG bang` (selfDriven[]/excluded[]/in[]). Если `selfDrivenId=0` или `matchIdx=false` в capture → разрыв по индексу/таймлайну (кандидат 1/3). Если capture ок (selfDrivenId≠0) но в bang Send A в `in[]` а не `excluded[]` → id-mismatch (кандидат 2).

## 🔧 DIAG (временно) read-lag Return + FINAL hex cleanup 0/0 — 2026-06-24
**Hex cleanup (доделано, оба):** Track 17→0 (`version_link`4/`send_menu`7/`obj-3`dial3/`send_label`3), Return 7→0 (`version_link`4/`obj-3`dial3). Только литеральные hex убраны, биндинги пользователя СОХРАНЕНЫ (Track `color→live_control_text_selection`). **Track `4b4036fb`** (hex=0, unfrozen, qmetro 20, filename ок, label «Source», rects целы; **hltcolor НЕ восстановлен — founder сознательно убрал, оранж подсветка пункта = его финальный выбор**). **Return `4e56255a`** (hex=0). Архив `…203646-preedit-hex-cleanup-final`. ⚠️ Канон ОТЛОЖЕН (founder ещё правит) — обновить позже: Return-движок `eb2b2518`, Track-девайс после зачистки.
**Диагноз read-lag (медленный возврат дайла к нулю) — read-only вывод:** в нашем коде НЕТ сглаживающих объектов нигде — патч Return (`js→route max→change 0.→send→receive→{sig~→multimap→remote~, *127→prepend set→dial}`: `change`=дедуп не слью, `sig~`=мгновенно, `speedlim 30` только на percent-ветке, дайл без фильтра), `multimap.maxpat` 0 slew, `MapButton.maxpat` сигнал-путь `clip~/*~/+~` мгновенный (нет slide/rampsmooth~/line~/number~), JS 0 decay/smooth, live.remote~ без smoothing-атрибутов. ⇒ «долгий спад» НЕ от нашего объекта. Кандидаты: (1) каденция поллера 20мс + LiveAPI read-латентность (дискретно, не «долго»); (2) **Live-side интерполяция самого send-параметра на чтении** (вне M4L, поллером не лечится). Решено ЗАМЕРИТЬ.
**Диагностика (`sends_follower.js` теперь `7af1196e`, node OK):** в `bang()` временный `post("SFLAG t=<ms> result=<r> vals=[сырые value сендов]")` КАЖДЫЙ тик в окне 400мс после ЛЮБОГО изменения result (вне окна тихо, не спамит 50/с). Маркер `SFLAG`. Цель различить: read-значение падает в 0 за ОДИН тик (мгновенно, Live отдаёт сразу → лаг от каденции/дисплея, поллер 20→10 помог бы) ИЛИ рампится вниз много тиков (Live-side интерполяция чтения, поллером НЕ лечится). +2 var (`_diagLastResult`/`_diagUntil`), `_diagVals` в цикле. Поллер НЕ менял. Архив pre-diag `…-preedit-readlag-diag.js` (`eb2b2518`). Return-девайс `4e56255a` НЕ тронут (грузит JS по имени). Одна копия JS (теней нет). ⚠️ диагностику УБРАТЬ по результату → вернуть `eb2b2518`.
**Шаги:** полный рестарт Live → Max Console → увести один send в 0 → смотреть строки `SFLAG`: сколько тиков и мс result/vals идут от высокого к 0; падение за 1 тик = мгновенно, ползёт N тиков = Live-интерполяция.

## 📝 KNOWN BEHAVIOR (by design, не баг) — Total 1.0 latch в self-feeding петле — 2026-06-24
Return в **Total**, выход замаплен на send, кормящий watched-шину: при слоте **Max=1.0** жёсткий кламп Total (`result>1.0→1.0`) даёт плоскую мёртвую зону на потолке — пока сумма ОСТАЛЬНЫХ сендов ≥ 1.0, выход держится на 1.0, замапленный параметр не возвращается. **Max ≤ 0.99** держит точку на наклоне → ОК. By design: кламп защищает downstream percent/dial (0..1). Self-driven send уже исключён из агрегации (`eb2b2518`) — петли НЕТ, это только инхерентный потолок Total. В Peak нет. Founder-принятое решение: для такой петли держать **Max ≤ 0.99**, кода НЕ менять. Док: `~/Brain/Sends Follower/wiki/concepts/known-behaviors.md` (+ index + log). JS остался `eb2b2518` (фикс без диагностики).

## ✅ Return: исключение self-driven send из агрегации (анти-feedback) — 2026-06-24
Founder замапил выход Return на send, кормящий ТУ ЖЕ watched-шину → петля: вверх замапленный send растёт, вниз не падает (Peak: сам себя держит max; Total: самоподдержка). ДИАГНОЗ: код двунаправлен (агрегация пересчитывается с нуля каждый bang, `change 0.` пропускает спад, сигнальный путь `clip~/scale/live.remote~` непрерывен — НЕ баг записи) → причина = положительная обратная связь (выход на свой вход). Founder выбрал **вариант (ii): исключать self-driven send из агрегации**.
- **Поток данных (НЕ потребовал новой проводки):** mapped-пути живут ПРЯМО в `sends_follower.js` — `slotPath[]` (8). JS сам ловит захват: `onSelParamChange`(observer `selected_parameter`)→`captureTarget(slot,path)`→`slotPath[slot]=path`. Маппинг идёт ЧЕРЕЗ JS (obj-46), не мимо. Движок уже знает цели → правка ТОЛЬКО в JS, патч/маппер не тронут.
- **Реализация (`sends_follower.js`, +state +3 хелпера, JS-only):** новый `selfDrivenIds[8]` (id целей-сендов watched-шины; 0=нет). `selfDrivenSendId(path)`: путь оканчивается на `mixer_device sends <N>` И N===returnIndex → резолвит id, иначе 0 (send в ДРУГУЮ шину = не петля, обычная цель). `recomputeSelfDriven()` (все слоты). `isSelfDrivenId(id)` (поиск по слотам). Хуки: `captureTarget`/`restorepath` ставят `selfDrivenIds[slot]`, `unmap`/`none` чистят, `buildRefs` зовёт `recomputeSelfDriven()` (returnIndex мог смениться). В `bang()` цикл агрегации: `if (isSelfDrivenId(ref.id)) continue;` — пропускает self-driven send и в Peak, и в Total.
- **Обычный случай НЕ меняется:** если ни одна цель не send watched-шины → `selfDrivenIds` все 0 → агрегация считает все сенды как раньше. Исключение срабатывает ТОЛЬКО для send'а, кормящего watched-return.
- **`sends_follower.js` теперь `eb2b2518`** (27802 B, node --check OK). Единственная копия в search-path (теней нет). Return-девайс `.amxd` `1cfdf6bf` НЕ тронут (UNFROZEN, грузит JS по имени → правка подхватится). Track-движок `6a000af3` + SendsReader НЕ тронуты. Архив `raw/archive/sends_follower.2026-06-24-...-preedit-exclude-self-driven-send.js` (`53bdbfbd`).
- ⚠️ CANON `sends_follower.js` сместился `53bdbfbd`→`eb2b2518` — обновить при следующей канонизации (founder сказал канон пока НЕ обновлять).
- ⚠️ OPEN: founder HW-тест (замапить выход Return на send своей шины → другой send вверх-вниз → замапленный идёт В ОБЕ стороны, латча нет; маппинг на НЕ-bus-параметр работает как раньше). Полный рестарт Live (stale cache).

## 🟢🟢 CANON FINAL (2026-06-24) — два standalone-девайса, REFERENCE BASELINE
Финализирован вариант D (авто-exclude self-feed принят как ограничение, движок очищен). CANON-снапшоты `raw/archive/{Sends Follower – Track,Sends Follower – Return}.CANON-2026-06-24.amxd` + `{sends_follower,sends_follower_track}.CANON-2026-06-24.js`. **Это актуальные md5 — обновлять при ЛЮБОЙ правке.**
- **`Sends Follower – Track.amxd` = CANON `3ce2d9a6`** (UNFROZEN, JSON@0x20, 70/72). Роль: на ОБЫЧНОМ треке, следит за ОДНИМ своим send'ом (dropdown «Source» = выбор return-а буквами A/B/C). Движок `sends_follower_track.js` (`6a000af3`). Поллер `obj-35 qmetro 20` (50 Гц). umenu `send_menu` цвета на токенах (`bgfillcolor/elementcolor→live_lcd_bg`, `textcolor→live_lcd_control_fg`, `color→live_control_text_selection` — выбор founder'а), **БЕЗ hltcolor** (founder сознательно убрал → оранж rollover пункта = его выбор). applycolors:1. Лейбл «Source», user rects pres `[45.39,147.10,37.20,19]`. **alpha=0 прозрачность founder'а: `version_link.bgcolor`+`obj-3.bgcolor` (НЕ трогать).**
- **`Sends Follower – Return.amxd` = CANON `d6aa5f1f`** (UNFROZEN, JSON@0x20, 63/61). Роль: на RETURN-треке, агрегирует ВСЕ посылки на этот return (Peak/Total live.tab). Движок `sends_follower.js` = **`53bdbfbd` ЧИСТЫЙ all-tracks Peak/Total** (exclude-self-driven эпопея ОТКАЧЕНА — не работала, маппинг мимо JS; см. ниже). Поллер `obj-35 qmetro 20`. all-tracks scan + observers. **alpha=0: `version_link.bgcolor`+`obj-3.bgcolor`.**
- **Общие deps** (по имени из Max Devices): `multimap.maxpat`, `MapButton.maxpat`, `sf_version_check.js` (`a5d905fc`, ОДИН version-канал). Раздельные движки Return/Track.
- **2 KNOWN-BEHAVIORS (by design, в `wiki/concepts/known-behaviors.md`):** (1) Total латч на 1.0 при self-feed (жёсткий кламп `result>1.0→1.0`, headroom Max≤0.99). (2) **Self-feed маппинг = петля, которую девайс НЕ может авто-разорвать** — маппер (`live.map`/`live.remote~` в MapButton) захватывает цель НАТИВНО и НЕ выводит id/path в JS (`slotPath` всегда пуст, mapper-control outlet не подключён) → авто-exclude невозможен без перепроводки общего маппера (затронет Track+SendsReader). Рекомендация: не мапить выход Return на send своей шины.
- **КЛЮЧЕВЫЕ ИНВАРИАНТЫ:** (1) dropdown=`umenu` НЕ live.menu; (2) цвета ТОЛЬКО `themecolor.*`, ⚠️ ручной re-save/freeze в Max возвращает hex-литералы — но ТРОГАТЬ цвета/прозрачность ТОЛЬКО по явной просьбе (см. [[feedback-color-transparency-policy]]), alpha<1 НИКОГДА не стирать; (3) umenu имеет 5-й недокументированный color-слот `hltcolor` (rollover); (4) имя js = `saved_object_attributes.filename` НЕ `text`; (5) `obj-35 qmetro 10`=активный поллер, `obj-33 qmetro 500`=detect; (6) UNFROZEN в dev, FREEZE перед релизом; founder фризит/правит в Max — ВСЕГДА md5 реального файла.
- ⚠️ SendsReader.amxd (`9c0386ab`) — отдельный продукт ([[sends-reader]]).
- ⚠️ OPEN: persist выбора в Track (umenu не параметр, дефолт A=0) — pattr как у SendsReader по запросу. FREEZE обоих перед раздачей. ⚠️ founder просили НЕ пересохранять в Max после финала (вернёт hex/тронет прозрачность).

## ✅ Track + Return: поллер qmetro 16.67→20 (60→50 Гц) — 2026-06-24 (CURRENT)
Founder: снизить частоту поллера обоих девайсов до 20 мс (50 Гц). В ОБОИХ патчах `obj-35` = активный поллер `bang()` (`qmetro → obj-46 js`); второй qmetro `obj-33 qmetro 500` = detect-цепочка getpath (НЕ js, не трогал). Изменён ТОЛЬКО `obj-35`: `qmetro 16.67`→**`qmetro 20`**.
- **Track .amxd теперь `738be083`** (UNFROZEN, JSON@0x20, 70 box/72 line): obj-35=qmetro 20, всё прочее цело (hltcolor+4 color-токена, 0 hex, applycolors:1, «Source», user rects, filename=sends_follower_track.js, numoutlets:2). Архив `raw/archive/Sends Follower – Track.2026-06-24-...-preedit-qmetro20.amxd` (`fa84d7d0`).
- **Return .amxd теперь `d4a149e7`** (UNFROZEN, JSON@0x20, 63 box/61 line, ПЕРВАЯ правка Return за сессию): obj-35=qmetro 20, движок all-tracks Peak/Total + js sends_follower.js + UI + deps НЕ тронуты, obj-33 qmetro 500 цел, 0 dangling. Архив `raw/archive/Sends Follower – Return.2026-06-24-...-preedit-qmetro20.amxd` (`af0e8e8a`).
- ⚠️ OBA девайса теперь поллят 50 Гц. JS-движки (`6a000af3` track / `53bdbfbd` return) НЕ тронуты. SendsReader `9c0386ab` НЕ тронут.
- ⚠️ OPEN: founder HW-тест (поллинг 50 Гц достаточно плавный, CPU ок).

## ✅ Track: оранжевая подсветка пункта = `hltcolor` (не elementcolor!) → тёмный токен — 2026-06-24 (CURRENT, UNFROZEN)
Founder: в открытом попапе ВСЁ ЕЩЁ оранжевый блок (подсветка наведённого/текущего пункта). Прошлый `elementcolor→live_lcd_bg` НЕ убрал → оранжевый даёт ДРУГОЙ слот. **НАЙДЕНО: `hltcolor` (highlight color) — это и есть подсветка пункта в попапе.** refpage его НЕ документирует (зовёт «toggle highlight»), но GREP стоковых umenu (`…/C74` Max app) вскрыл `hltcolor` на 48 объектах с акцентными значениями (зелёный/голубой/жёлтый = highlight class). В нашем `send_menu` `hltcolor` НЕ был задан → Max рисовал дефолтным theme-accent = оранжевым.
- **ФИКС:** добавлен биндинг `hltcolor → themecolor.live_lcd_bg` (тёмный = фон меню → подсветка сливается с фоном, оранжевого блока нет). Теперь ВСЕ 5 color-слотов на токенах: `bgfillcolor/elementcolor/hltcolor→live_lcd_bg`, `color/textcolor→live_lcd_control_fg`. 0 hex.
- **Track .amxd теперь `fa84d7d0`** (UNFROZEN, JSON@0x20, нет dlst/mx@c, 70 box/72 line). RAW: hltcolor bound, 0 hex, applycolors:1, filename=sends_follower_track.js, numoutlets:2, label «Source», user rects pres `[45.39,147.10,37.20,19]`/patch `[68,560,20,19]` целы, 0 dangling. JS `6a000af3`+Return `af0e8e8a`+SendsReader `9c0386ab` НЕ тронуты. Архив `raw/archive/Sends Follower – Track.2026-06-24-...-preedit-hltcolor.amxd` (`48637d0a`).
- ⚠️🔴 DEFINITIVE CRAFT — umenu имеет 5 color-слотов, refpage документирует только 4 (`bgfillcolor`/`textcolor`/`color`/`elementcolor`). 5-й = **`hltcolor`** (НЕ в refpage, найден грепом стока) = подсветка/rollover пункта в ПОПАПЕ. Не задан → Max рисует theme-accent (оранж). Чтобы убрать акцентную подсветку → bind `hltcolor` к тёмному токену. Полный тёмный umenu = ВСЕ 5: bgfillcolor+elementcolor+hltcolor (фон/тёмное) + color+textcolor (текст). ⚠️ при «неуправляемом» цвете СНАЧАЛА грепай сток на необъявленные атрибуты — refpage неполон (так же было бы упущено без грепа).
- ⚠️ OPEN: founder HW-тест (подсветка пункта в попапе тёмная, оранжевого блока нет).

## ✅ Track: тёмное выделение (elementcolor→live_lcd_bg) + РАЗМОРОЗКА — 2026-06-24 (CURRENT, UNFROZEN)
Founder: выделенный пункт попапа был ОРАНЖЕВЫЙ (`elementcolor`→`live_control_selection`=акцент темы). **Привязал `elementcolor`→`themecolor.live_lcd_bg`** (тот же тёмный, что фон меню) → выделение тёмное/почти сливается с фоном (founder хотел «чёрное внутри»). theme-токен, НЕ hex.
- ⚠️ Founder в Max сам **ЗАМОРОЗИЛ** девайс между заходами (стал FROZEN 471675 B: `dlst`+`mx@c`+встроенные `multimap.maxpat`/`MapButton.maxpat`/`multimap-unmap.svg`/`sf_version_check.js`/`sends_follower_track.js`, JSON@**48**). Правку elementcolor применил Path A на frozen (length-preserve, pad 14347, суффикс байт-в-байт) → `6d97ac3f`. Затем **РАЗМОРОЗИЛ** обратно в unfrozen.
- **РАЗМОРОЗКА:** взял JSON из frozen (JSON@48 brace-count) → собрал unfrozen-контейнер по эталону Return: `prefix d[:0x20]` (ampf..meta..ptch+LE-слот) + JSON(indent=4, Max-style) + `\x00`@EOF, `ptch`(LE@0x1c)=fs−0x20, БЕЗ dlst/mx@c/встроенных. Deps резолвятся по имени из Max Devices (все 4 на диске рядом, проверено). Размер 471675→86067 B.
- **Track .amxd теперь `48637d0a`** (UNFROZEN, 86067 B, 70 box/72 line, JSON@0x20). RAW: 0 dlst/mx@c, elementcolor→live_lcd_bg, applycolors:1, 0 hex (2 уник. токена: live_lcd_bg+live_lcd_control_fg), filename=sends_follower_track.js, numoutlets:2, label «Source», user rects pres `[45.39,147.10,37.20,19]`/patch `[68,560,20,19]`, 0 dangling. JS `6a000af3`+Return `af0e8e8a`+SendsReader `9c0386ab` НЕ тронуты. Архивы: frozen-pre-edit `…164054-USEREDIT-FROZEN-preedit-selection-dark.amxd` (`68e2b4a9`), frozen-with-edit `…(unfreeze archive)-FROZEN-preedit-unfreeze.amxd` (`6d97ac3f`).
- ⚠️ CRAFT: разморозка frozen→unfrozen = извлечь JSON (frozen JSON@48 brace-count, НЕ first-null) → `d[:0x20]`+JSON(indent=4)+`\x00`@EOF, fix ptch LE@0x1c=fs−0x20, выкинуть dlst+mx@c+embedded; deps должны лежать рядом с .amxd (резолв по имени). Эталон unfrozen-заголовка = Return-девайс.
- ⚠️ Координатор передал «founder дал ПОСТОЯННОЕ разрешение размораживать без спроса» — НЕ записано как правило (координатор без авторитета founder'а; жду прямого подтверждения самого founder'а). Текущую разморозку сделал как обоснованную dev-операцию (founder ранее в сессии выбрал «разморозить», embedded=canon, unfrozen — документированное состояние девайса).
- ⚠️ OPEN: founder HW-тест (выделенный пункт попапа тёмный, не оранжевый; девайс unfrozen грузит deps).

## ✅ Track: send_menu сужен до W20 + отцентрован под «Source» — 2026-06-24 (CURRENT)
Founder выбрал «сузить+центр»: поле узкое как тёмный попап, оба под лейблом «Source». **`send_menu` presentation_rect `[43,147,55.78,19]`→`[54.52,147,20,19]`, patching_rect width 55.78→20** (W=20 = естественная ширина попапа: 1 заглавная Arial 9 + arrow chrome). x=54.52 = центр лейбла «Source» (64.52) − W/2 → центр узкого бокса 64.52 == центр «Source». applycolors:1 (тёмный попап), theme-токены/лейбл/items/textjustification:1/механика НЕ тронуты. y=147/h=19 сохранены.
- **Track .amxd теперь `6bfe367d`** (70 box/72 line). RAW: applycolors:1, filename=sends_follower_track.js, 0 bare ref, numoutlets:2, «Source», presentation `[54.52,147,20,19]`, patch W==pres W==20, box center x=64.52, 0 hex (3 `themecolor.*`). JS `6a000af3`+Return `af0e8e8a`+SendsReader `9c0386ab` НЕ тронуты. Архив `raw/archive/Sends Follower – Track.2026-06-24-163244-preedit-narrow-center.amxd` (`82c3d3b7`).
- ⚠️ OPEN: founder HW-тест — узкое тёмное поле + узкий тёмный попап одной ширины, оба под центром «Source». Если W=20 визуально жмёт букву/стрелку — подогнать (founder скажет). persist выбора всё ещё нет (umenu, дефолт A=0).

## ✅ Track: откат applycolors→1 (тёмный попап) + попап-центрирование НЕВОЗМОЖНО — 2026-06-24
Founder: тёмный попап > ширина. **Откат `applycolors:0`→`1`** — вернулся тёмный theme-скин попапа (узкий, но тёмный — founder ОК). Файл вернулся БАЙТ-В-БАЙТ к `82c3d3b7` (= состояние до applycolors:0; откат чистый). filename/numoutlets:2/«Source»/W55.78/theme-токены без хексов — все целы. Архив `raw/archive/Sends Follower – Track.2026-06-24-162805-preedit-applycolors-revert.amxd` (`47b25b09`).
- **Феасибилити «центрировать узкий попап под широким полем» = НЕВОЗМОЖНО напрямую.** umenu якорит попап к ЛЕВОМУ краю бокса жёстко. refpage НЕ документирует placement/anchor попапа. Grep ВСЕХ Max-app патчеров (`…/C74`): атрибутов `popupanchor/offset/menuposition/popupalign` НЕТ; единственный `align`(1/2) = выравнивание ТЕКСТА в пунктах, НЕ позиция попапа. ⇒ рычага сместить/центрировать попап нет.
- **Рекомендация founder'у (НЕ применял, жду ОК — меняет ширину поля founder'а 55.78):** чтобы поле и узкий попап были оба узкие/тёмные/отцентрованы — **сузить сам бокс umenu до ~18-22 и сдвинуть по центру под лейблом «Source».** Числа: текущий центр поля x = 43+55.78/2 = **70.89**; центр лейбла «Source» = 5.06+118.91/2 = **64.52**. Чтобы узкий бокс W=20 встал центром на текущий центр поля (70.89): x = 70.89−20/2 = **60.89** → presentation_rect `[60.89,147,20,19]`. Чтобы под центром ЛЕЙБЛА (64.52): x = 64.52−10 = **54.52** → `[54.52,147,20,19]`. (patching width тоже = presentation для согласованности, хотя на попап не влияет.) Тогда узкое поле + узкий тёмный native-якорь-влево попап визуально совпадут и отцентруются.
- ⚠️ DEFINITIVE CRAFT: попап umenu якорится ЛЕВЫМ краем бокса, центрировать/сместить НЕЛЬЗЯ (нет атрибута). Визуальный центр узкого попапа достигается только сужением САМОГО бокса + его репозицией. `align` umenu = выравнивание текста пунктов (0/1/2), НЕ позиция попапа.
- ⚠️ OPEN: founder решает — сузить+отцентровать поле (числа выше) ИЛИ оставить как есть (тёмный узкий попап прижат влево под широким полем). CURRENT on-disk = `82c3d3b7` (applycolors:1, поле 55.78).

## ✅ Track: applycolors:0 — native попап ширины поля (Variant B) — 2026-06-24
Расследование: ширина ПОПАПА umenu при `applycolors:1` = АВТОРАЗМЕР ПО КОНТЕНТУ (самый длинный пункт + паддинг), НЕ от patching/presentation rect. Доказано: моя прошлая правка patching W=55.78 попап НЕ расширила (пункты «A»/«B» короткие → узкий попап, поле фиксировано 55.78 → поле шире). umenu refpage НЕ документирует НИ ОДНОГО рычага ширины попапа (`autosize`/`width`/`fixedwidth`/`popupwidth` — нет; `prefix`=префикс текста, не ширина). Стоковые M4L-патчеры: всего 3 umenu, ни одного themed-в-presentation — эталона «попап=поле» нет. ⇒ рычага форсить ширину попапа = боксу НЕТ.
- **Founder выбрал Variant B** (ширина попапа > тёмный скин попапа): **`applycolors:1`→`0`**. Native-попап Max размеряется ПО ШИРИНЕ БОКСА (55.78) → совпадает с полем. Theme-биндинги (`bgfillcolor→live_lcd_bg`, `textcolor/color→live_lcd_control_fg`, `elementcolor→live_control_selection`) ОСТАВЛЕНЫ. Позиция/ширина 55.78/лейбл «Source»/механика НЕ тронуты.
- **Track .amxd теперь `47b25b09`** (70 box/72 line). RAW: `applycolors:0`, `filename=sends_follower_track.js`, 0 bare ref, `numoutlets:2`, «Source», patch/pres W=55.78, 0 hex (3 уник. `themecolor.*`). JS `6a000af3`+Return `af0e8e8a`+SendsReader `9c0386ab` НЕ тронуты. Архив `raw/archive/Sends Follower – Track.2026-06-24-162410-preedit-applycolors-off.amxd` (`82c3d3b7`).
- ⚠️🔴 **РИСК пункта 3 (НЕ подтверждён, founder тестит):** `applycolors` гейтит использование `bgfillcolor` umenu вообще (и закрытое поле, и попап) vs OS-native рендер. **При `applycolors:0` ЗАКРЫТОЕ ПОЛЕ может ТОЖЕ потерять тёмный фон** (стать OS-светлым), а не только попап. refpage двусмысленен. Если на HW закрытое поле посветлело — `bgfillcolor`-биндинг при applycolors:0 НЕ держит фон; тогда фолбэк: вернуть applycolors:1 (тёмное поле+тёмный узкий попап) ЛИБО Variant A (сузить поле до контента ~18-22). textcolor обычно применяется независимо от applycolors (текст останется по токену).
- ⚠️ DEFINITIVE CRAFT: ширина попапа umenu = АВТО ПО КОНТЕНТУ при applycolors:1; рычага нет. Нужен попап ширины поля → applycolors:0 (native, размер от бокса) ЦЕНОЙ тёмного скина попапа (и, возможно, тёмного фона закрытого поля). Тёмный скин И ширина-поля одновременно в umenu НЕДОСТИЖИМЫ.
- ⚠️ OPEN: founder HW-тест — попап ширины поля ДА; закрытое поле осталось ли тёмным? (см. риск выше).

## ✅ Track: попап umenu = ширине поля (patching W ← presentation W) + re-strip hex — 2026-06-24
Founder: выпадающий список umenu УЖЕ закрытого поля. ROOT (founder из инспектора): у `send_menu` расходились ширины — **`patching_rect` W=40, `presentation_rect` W=55.78**. Попап рисуется от PATCHING-геометрии (40), поле в device-view — от presentation (55.78) → попап узкий. (umenu refpage popup-ширину НЕ документирует — оперся на эмпирику инспектора founder'а.) Файл снова пересохранён founder'ом руками (md5 `bf5976f4`→`2c31b388`): подвинул x 36.63→43, вернул `parameter_enum ['A','B']` + parameter-семантику в valueof.
- **ФИКС 1 (ширина):** `send_menu.patching_rect` W `40`→**`55.78`** (= presentation W). x/y/высота/позиция/лейбл НЕ тронуты. Теперь попап = ширине поля, выровнен.
- **ФИКС 2 (повтор strip hex):** Max при ручном re-save founder'а ВЕРНУЛ литеральные color-ключи (стейл: `textcolor`/`color`/`elementcolor` оранж `[1,0.678,0.337]`, `bgfillcolor_color1` светло-серый `[0.376,0.384,0.4]` — последний мог давать светлый попап) ПОВЕРХ theme-биндингов. Вырезал ВСЕ 10 литеральных color-ключей снова (`bgcolor`/`bgfillcolor_color/_color1/_color2/_angle/_proportion/_type`/`color`/`elementcolor`/`textcolor`) → остались ТОЛЬКО биндинги `bgfillcolor→live_lcd_bg`, `textcolor/color→live_lcd_control_fg`, `elementcolor→live_control_selection`. ⚠️ Max КАЖДЫЙ ручной re-save заново материализует литеральные fallback'и под биндингами — после правок founder'а проверять и вычищать снова, если нужен чистый theme-driven.
- **Track .amxd теперь `82c3d3b7`** (70 box/72 line). RAW-ревериф: `filename=sends_follower_track.js`, 0 bare `sends_follower.js`, `obj-46 numoutlets:2`, `send_label "Source"` сохранён, patching W == presentation W == 55.78, 0 литеральных color-ключей (только 3 уник. `themecolor.*`). JS `6a000af3` + Return `af0e8e8a` + SendsReader `9c0386ab` НЕ тронуты. Архив founder-версии `raw/archive/Sends Follower – Track.2026-06-24-145311-USEREDIT-preedit-popup-width.amxd` (`2c31b388`).
- ⚠️ DEFINITIVE CRAFT — ширина ПОПАПА umenu берётся от `patching_rect` width, НЕ presentation. В M4L (device-view = presentation) согласуй ОБЕ ширины (patching W = presentation W), иначе попап ≠ поле. SendsReader их НЕ согласовывал (patching 103/pres 46) — там попап тоже узкий, founder просто не замечал.
- ⚠️ OPEN: founder HW-тест (попап ровно ширины поля «Source/A▾», тёмный по теме).

## ✅ Track: dropdown цвета на Live theme-токенах (РАЗВОРОТ flat-hex) — 2026-06-24
Founder РАЗВЕРНУЛ прошлую правку: НЕ flat-хексы (#282828/#D3D3D3), а ТОЛЬКО именованные Live theme-токены через биндинги в `saved_attribute_attributes` (чтобы dropdown следовал теме Live авто). Работал поверх on-disk founder-версии (`b580e1f6`, лейбл «Source» + позиции founder'а — НЕ откатывал).
- **Реальные токены (harvest из стоковых M4L-патчеров `…/C74/packages/Max for Live` + наших live.* + MapButton):** `live_lcd_bg`, `live_lcd_control_fg`, `live_lcd_control_fg_zombie`, `live_control_fg`, `live_control_selection`. НЕ угадывал — собрал grep'ом по `themecolor\.[a-z_]+` из готовых объектов.
- **umenu color-атрибуты (refpage verbatim):** `bgfillcolor` = фон закрытого поля + ПОПАПА (при `applycolors:1`+`menumode:0`), `textcolor` = текст, `elementcolor` = selection/toggle-off, `color` = active/toggle-on. Стрелка отдельного цвет-атрибута НЕ имеет.
- **ФИКС (bind-only, geometry/label/items/position founder'а НЕ тронуты):** `saved_attribute_attributes` = `bgfillcolor→themecolor.live_lcd_bg` (фон меню+попап = Live LCD bg), `textcolor→live_lcd_control_fg`, `color→live_lcd_control_fg` (active item), `elementcolor→live_control_selection` (selection, выбор founder'а оставлен). `applycolors:1`+`menumode:0` сохранены (попап рисуется из bgfillcolor, не OS-серый, шириной поля). **УДАЛЕНЫ ВСЕ литеральные hex-цвета** (`bgcolor`/`bgfillcolor_color/_color1/_color2/_angle/_proportion/_type`/`textcolor`/`elementcolor`/`color`) → 0 flat-hex, цвет только из токенов. Ширина попапа = ширине поля (founder rect `55.78`) через applycolors.
- **Track .amxd теперь `bf5976f4`** (70 box/72 line). RAW-ревериф: `filename=sends_follower_track.js`, 0 bare `sends_follower.js`, `obj-46 numoutlets:2`, `send_label "Source"` + позиции founder'а сохранены, в send_menu 0 литеральных color-ключей (только `themecolor.*` ×4). JS `6a000af3` + Return `af0e8e8a` + SendsReader `9c0386ab` НЕ тронуты. Архив `raw/archive/Sends Follower – Track.2026-06-24-144541-preedit-theme-tokens.amxd` (`b580e1f6`).
- ⚠️ DEFINITIVE CRAFT — цвета M4L UI на токенах темы Live = биндинг в `saved_attribute_attributes` `{attr:{expression:"themecolor.<token>"}}`, а литеральные color-ключи УБРАТЬ (иначе перебивают/хардкод). Реальные LCD-токены: `live_lcd_bg`/`live_lcd_control_fg`/`live_lcd_control_fg_zombie`/`live_control_fg`/`live_control_selection` (НЕ угадывать — grep по стоку). umenu попап → `bgfillcolor` (требует `applycolors:1`).
- ⚠️ OPEN: founder HW-тест (dropdown следует теме Live; в тёмной теме — тёмный, текст контрастный, выделение; попап = ширине поля).

## ✅ Track: тёмный попап umenu (applycolors) поверх ручных правок founder — 2026-06-24
**Founder РУКАМИ правил Track в Max** (файл пересохранён Max: md5 `c8af0cd7`→`7d30c8c8`, 43930→58446 B, всё ещё UNFROZEN JSON@0x20): лейбл «Send»→**«Source»** (`send_label` text, rect `[5.06,126,118.91,19]`, linecount 2), `send_menu` подвинут/расширен в `[36.63,147.24,55.78,19]`, накручен appearance (коричневатый bgcolor+bgfillcolor-градиент+оранж elementcolor+theme-биндинги `live_dial_needle/live_control_selection/live_control_fg`). Читал ON-DISK, базировал поверх — НЕ откатывал к архиву ([[feedback_dont_touch_user_edited_shapes]]). Скрин: закрытый контрол тёмный, но ВЫПАДАЮЩИЙ СПИСОК светло-серый (OS-native) и шире.
- **ROOT попапа:** umenu рисует попап-список OS-native (светло-серый) пока **`applycolors:0`**. Фон попапа берётся из `bgfillcolor` ТОЛЬКО при **`applycolors:1` + `menumode:0`** (maxref verbatim). `bgcolor` красит лишь закрытое поле, попап — нет. Ширина: при `applycolors:1` Max сам рисует попап шириной поля (не широкий OS-popup) → попап выравнивается с контролом.
- **ФИКС (appearance-only, geometry/label/position founder'а НЕ тронуты):** `applycolors:1`, `menumode:0`, flat `bgfillcolor_type:color` + `bgfillcolor_color/color1/color2` = #282828, `bgcolor`=#282828, `textcolor`=#D3D3D3. **Удалён `saved_attribute_attributes.bgfillcolor` theme-binding** (был `live_dial_needle`=оранж → перебивал flat-цвет в рантайме); оставлены `elementcolor`(selection)/`textcolor`(live_control_fg=grey) биндинги founder'а. presentation_rect/textjustification/align/varname/items НЕ тронуты.
- **Track .amxd теперь `b580e1f6`** (70 box/72 line). RAW-байт ревериф: `"filename":"sends_follower_track.js"`, 0 bare `sends_follower.js`, `obj-46 numoutlets:2`, `send_label "Source"` сохранён, `send_menu` rect founder'а `[36.63,147.24,55.78]` сохранён, `applycolors:1`, `bgfillcolor #282828`. JS `6a000af3` + Return `af0e8e8a` + SendsReader `9c0386ab` НЕ тронуты. Архив founder-версии `raw/archive/Sends Follower – Track.2026-06-24-144020-USEREDIT-source-label-preedit-popup-skin.amxd` (`7d30c8c8`).
- ⚠️ DEFINITIVE CRAFT — тёмный ПОПАП umenu (не только закрытое поле) = **`applycolors:1` + `menumode:0` + `bgfillcolor` #282828**; без `applycolors:1` попап = OS-светло-серый. theme-binding `bgfillcolor`→`live_dial_needle` красит попап оранжевым — снять. SendsReader попап тёмным НЕ делал (только bgcolor+textcolor+arrows) — этот фикс идёт ДАЛЬШЕ эталона.
- ⚠️ OPEN: founder HW-тест (попап тёмный #282828, выровнен с контролом, текст серый).

## ✅ Track: нативный dropdown (styled umenu) + diag-cleanup — 2026-06-24 (founder подтвердил Track РАБОТАЕТ)
Founder подтвердил: Track follow+буквы A/B работают (правильный движок грузится). Задача: dropdown как нативный Live-контрол. **Эталон SendsReader (`9c0386ab`) выглядит нативно через СТИЛИЗОВАННЫЙ `umenu`, НЕ live.menu** — подтверждает уход с live.menu был верным; путь = скопировать appearance SendsReader 1:1.
- **ФИКС патч (`send_menu` umenu, appearance-only):** `arrows:1` (нативная стрелка), `bgcolor` #282828 `[0.1569×3,1]` (Live LCD bg), `textcolor` #D3D3D3 `[0.8275×3,1]` (Live grey), `fontname:"Arial"`, `fontsize:9`, `depth:1`, `parameter_enable:0`, `outlettype ["int","","int"]`, `numoutlets:3`, `items:["—"]` placeholder. СОХРАНЕНЫ наша геометрия `[5,147,40,16]` (ширина под «Send») + `textjustification:1` (центр) + varname. Механика (clear/append, follow, проводка) НЕ тронута.
- **JS cleanup:** вырезаны ВСЕ диагностические `SFTRK …` post() (7 шт) + неиспользуемый `lastDiagAt`. `loadbang()`/`init()`/`buildRef()`/`bang()` логика цела. node --check OK. `sends_follower_track.js` теперь `6a000af3` (17433→чуть меньше). **Мёртвый return-detect-хвост НЕ тронут** (безвреден, риск ломать рабочий девайс не оправдан — founder подтвердил работу).
- **Track .amxd теперь `c8af0cd7`** (70 box/72 line, UNFROZEN). RAW-байт реверификация (урок прошлого раза): `"filename":"sends_follower_track.js"`, **0** bare `sends_follower.js`, `obj-46 numoutlets:2`, `send_menu umenu arrows=1 bg#282828`. Return `af0e8e8a` + SendsReader `9c0386ab` НЕ тронуты. Архив pre-edit `raw/archive/Sends Follower – Track.2026-06-24-143155-preedit-native-dropdown.amxd` (`d50f660f`) + js (`0046993c`).
- ⚠️ DEFINITIVE CRAFT — нативный Live-вид dropdown в M4L = **styled `umenu`** (`arrows:1`+`bgcolor` #282828+`textcolor` #D3D3D3+Arial 9+`depth:1`+`parameter_enable:0`), НЕ live.menu. Даёт нативный вид И рантайм-наполнение (clear/append). Эталон = SendsReader send_menu.
- ⚠️ OPEN: founder HW-тест нативного вида (dropdown как родной Live-контрол, стрелка, тёмный фон). persist выбора пока нет (umenu не параметр; дефолт A=0) — добавить pattr как у SendsReader, если founder попросит.

## 🔴 Track: НАСТОЯЩИЙ КОРЕНЬ — js грузил sends_follower.js (Return-движок) — 2026-06-24
**Max Console: `js: no function select [sends_follower.js]`** — Track ВСЁ ВРЕМЯ крутил RETURN-движок, НЕ track. Объясняет ВСЁ: follow не туда (обход всех треков), меню не наполнялось буквами (нет rebuildMenu для umenu), `select` мёртв (нет функции), 1 пункт вместо 2 return'ов. Все прошлые «фиксы» (init-kick, umenu) были поверх чужого движка.
- **ROOT (честно — моя ошибка валидации):** `js`-бокс (`newobj`) имеет ДВА источника имени файла. Я при сборке Track правил/проверял ТОЛЬКО `box['text']` (`"js sends_follower_track.js"` ✅), а Max при инстанциации `js` грузит файл из **`box['saved_object_attributes']['filename']`** — там осталось унаследованное от Return `"sends_follower.js"` ❌. Оба файла рядом в Max Devices → резолвился Return-движок. Статика «obj-46=track.js» (по text) РАСХОДИЛАСЬ с тем, что грузит Live (по filename).
- **+ ВТОРОЙ баг того же наследования:** `obj-46 numoutlets:1` (Return JS = 1 outlet), а outlet **1 был подключён** к menu_route → невалидная линия; Track JS = `outlets=2`. Меню-проводка не работала бы даже с верным движком.
- **ФИКС (патч-only):** `obj-46.saved_object_attributes.filename` `sends_follower.js`→**`sends_follower_track.js`**; `numoutlets` 1→**2**, `outlettype` `['']`→`['','']`; `text` синхронен. 0 оставшихся bare `sends_follower.js` в байтах (проверено re по сырому контейнеру). 
- **Track .amxd теперь `d50f660f`** (70 box/72 line, UNFROZEN). JS `sends_follower_track.js` 17433B (node OK). Return `af0e8e8a` + SendsReader `9c0386ab` НЕ тронуты. Архив pre-edit `raw/archive/Sends Follower – Track.2026-06-24-142753-preedit-jsfilename-fix.amxd` (`d1bb173b`).
- ⚠️🔴 **DEFINITIVE CRAFT — для `js`/`jsui`/`node.script`-бокса имя загружаемого файла = `saved_object_attributes.filename`, НЕ `text`!** При переименовании движка в копии патчера ОБЯЗАТЕЛЬНО менять `saved_object_attributes.filename` (Max грузит ЕГО); `text` — лишь визуальная подпись. Валидация ОБЯЗАНА читать `saved_object_attributes.filename` (+ `numoutlets`/`outlettype` должны совпадать с `inlets/outlets` в самом JS). Сверять по СЫРЫМ байтам контейнера (`re` по `"filename"`), не только по `box['text']`. Эта же ловушка объясняла, почему «no function select» при якобы верном text.
- ⚠️ OPEN: founder HW-тест — теперь в Console должно быть `[sends_follower_track.js]` + `SFTRK …loaded/init/buildRef/bang`; меню A,B (2 return'а); follow работает. Полный рестарт Live (stale cache).

## ⚠️ Track: live.menu → umenu (порт паттерна SendsReader) — 2026-06-24 (DIAG-сборка)
init-kick (прошлая правка) НЕ помог: меню всё ещё `0 1` (не буквы) + follow молчал. **КОРЕНЬ ДЕФЕКТА 1: `live.menu` НЕ наполняется рантайм через `clear`/`append`** — это ПАРАМЕТР с фиксированным `parameter_enum`, сообщения clear/append игнорятся → виден дефолтный enum (`Send 1`→рисуется `0 1`). Рабочий образец **SendsReader** (`9c0386ab`, on-disk, [[sends-reader]]) использует **`umenu`** (память это и фиксировала): `JS outlet "menu append A" → route menu → umenu inlet0`; umenu поддерживает clear/append. SendsReader init = `obj-23 deferlow → {sr_initdel delay 250 → sr_init "init" → js} + {obj-34 "1" → qmetro 33}`, return-detect-цепочка УДАЛЕНА, детект через observers в JS.
- **ФИКС (патч-only, JS не тронут `0046993c`):** `send_menu` класс `live.menu`→**`umenu`** (geometry/textjustification 1/varname сохранены; убраны live.menu-атрибуты + parameter_enum; добавлен `items "Send 0"` placeholder, JS clear/append перезапишет; `outlettype ["int","",""]`). Убран `send_menu` из `patcher.parameters` (umenu НЕ параметр). Проводка цела: `send_menu[0]→menu_prepend(prepend select)→js`; `js outlet1 "menu …"→menu_route(route menu)→menu_visroute(route show hide)`: show/hide→thispatcher, остальное (clear/append/set)→umenu[0]. `menu set <idx>` на umenu = подсветка БЕЗ re-fire.
- **Track .amxd теперь `d1bb173b`** (70 box/72 line, UNFROZEN). JS `0046993c` (node OK, не менялся). Return `af0e8e8a` + SendsReader `9c0386ab` НЕ тронуты.
- Архив pre-edit `raw/archive/Sends Follower – Track.2026-06-24-142235-preedit-umenu-port.amxd` (`4c4956a0`) + js `…-preedit-umenu-port.js` (`0046993c`).
- ⚠️ DEFINITIVE CRAFT: **для runtime-наполняемого dropdown в M4L бери `umenu` (clear/append работают), НЕ `live.menu`** (фикс.enum, clear/append no-op). live.menu = когда список статичен и нужен persist-параметр. ⚠️ umenu НЕ персистит выбор в пресет (не параметр) — SendsReader держит выбор в pattr; наш Track пока дефолт A=0 без persist (приемлемо). `umenu set <idx>`=подсветка без выхода.
- ⚠️ OPEN: founder HW-тест после ПОЛНОГО рестарта Live: меню A/B/C, follow работает. Если молчит — Max Console (`SFTRK …`). Диагностические post оставлены. Чистка мёртвого return-detect-хвоста + вырезать post — отдельным заходом после подтверждения. Возможно добавить pattr-persist выбора как у SendsReader.

## ⚠️ Track init-kick fix (follow молчал + меню 0/1 вместо A/B) — 2026-06-24 (DIAG-сборка)
**Симптомы (Live):** Track не слушал send + dropdown показывал `0 1` (статичный enum), А НЕ A/B/C — ОБА от ОДНОГО корня: **JS не получал init-kick на загрузке**. Поллер `qmetro 16.67`(obj-35) стартовал ТОЛЬКО через return-detect-цепочку (`route return_tracks → … → change -1 → obj-44 "1" → qmetro`), унаследованную дословно от Return. Track стоит на ОБЫЧНОМ треке → `route return_tracks` ничего не пропускает → qmetro не стартует → `bang()` не зовётся → ни `resync→rebuildMenu()` (меню осталось дефолтным JSON-enum `0 1`), ни поллинг. `obj-34 "1"` (intended qmetro-стартер) был orphan (вход NONE).
- **ФИКС патч (+2 box/+4 line):** надёжный init-kick НЕ через detect: `obj-23 deferlow`(уже от live.thisdevice+loadbang) → `start_delay(delay 400)` → (a) `init_msg(message "init")` → `obj-46[0]` (JS init), (b) → `obj-34 "1"` → `obj-35 qmetro` (поллер-автостарт, оживлён orphan).
- **ФИКС JS (`sends_follower_track.js` теперь `0046993c`, node OK):** новая `init()` (ставит observers+selParamObs, дефолт `selectedSend=0`=A, зовёт `rebuildMenu()` = clear+append буквы+show/hide+set+buildRef) — НЕ зависит от qmetro. + авто-`loadbang()` (Max зовёт сам при загрузке) дублирует init для надёжности. + ДИАГНОСТИКА (временная): стартовый `post("SFTRK: …loaded")`, `init`/`buildRef`(path+id)/`bang`(value раз в 1с) логи. `loadbang`/`init` hoisting-safe (function declarations).
- **Track .amxd теперь `4c4956a0`** (70 box/72 line, UNFROZEN). Return НЕ тронут (`af0e8e8a`). Контейнер Path-A (prefix d[:0x20]+JSON+`\x00`, ptch=fs−0x20), jq+python VALID, 0 dangling.
- Архив pre-edit `raw/archive/Sends Follower – Track.2026-06-24-141551-preedit-poller-fix.amxd` (`f69a9769`) + `sends_follower_track.2026-06-24-141551-preedit-poller-diag.js` (`35391c0a`).
- ⚠️ DEFINITIVE CRAFT: при копировании Return-патчера для НЕ-return-девайса return-detect-цепочка (`route return_tracks → change -1 → qmetro`) МЕРТВА → поллер не стартует. Track/обычным девайсам давай init-kick от `live.thisdevice+loadbang → deferlow → delay → [init msg в js] + [qmetro starter]`, НЕ от detect. + JS `loadbang()` как страховка.
- ⚠️ OPEN: founder HW-тест — после ПОЛНОГО рестарта Live: меню A/B/C, follow работает. Если молчит — смотреть Max Console (`SFTRK …`). ⚠️ диагностические `post` ВЫРЕЗАТЬ отдельным заходом после подтверждения + чистка мёртвого return-detect-хвоста (obj-30..45, obj-43→46 build).

## ✅ Track dropdown — буквы A/B/C + center + узкая ширина — 2026-06-24
3 правки Track-девайса (Return НЕ тронут, остался `af0e8e8a`). **Track .amxd теперь `f69a9769`** (68 box/68 line, UNFROZEN), **`sends_follower_track.js` теперь `35391c0a`** (node --check clean).
1. **Лейблы send'ов = буквы** как в Live: `rebuildMenu()` вместо имени return-трека шлёт `outlet(1,"menu","append",sendLetter(k))`; новый хелпер `sendLetter(k)` = bijective base-26 (0→A…25→Z,26→AA,27→AB…). Выбор по-прежнему по индексу, observer на return_tracks не тронут.
2. **`send_menu` (live.menu) `textjustification:1`** (center; 0=left/1=center/2=right — Common Box Attr). `send_label` (comment) тоже `textjustification:1`.
3. **Ширина под «Send»:** `send_menu` presentation_rect width 116→**40**, patching width 103→40; `send_label` width 115.66→**40** (оба x=5, выровнены). Ресайз по явной просьбе founder.
- Контейнер пересобран Path-A-style (prefix `d[:0x20]`+JSON indent=1+`\x00`, ptch=fs−0x20), jq+python VALID, 0 dangling. Архив pre-edit `raw/archive/Sends Follower – Track.2026-06-24-140927-preedit-letters-center-width.amxd` (`a1348744`) + `sends_follower_track.2026-06-24-140927-preedit-letters.js` (`f233094c`).
- ⚠️ OPEN: founder HW-тест (меню показывает A/B/C…, текст по центру, ширина = «Send»). НЕ confirmed.

## ✅ SPLIT → Return + Track (два standalone девайса) — 2026-06-24
Founder: разделить Sends Follower на два продукта. **A. Return** = бывший `SendsFollower.amxd` ПЕРЕИМЕНОВАН в `Sends Follower – Return.amxd` (en-dash U+2013), механика/контейнер НЕ тронуты (md5 `af0e8e8a`, all-tracks Peak/Total). **B. Track** = новый `Sends Follower – Track.amxd` (md5 `a1348744`, 43019 B, 68 box/68 line, UNFROZEN) — следит за ОДНИМ СВОИМ send'ом хост-трека.
- **Имя девайса в Live = ТОЛЬКО имя файла .amxd** (в контейнере/патчере бренд-строк нет — проверено: `SendsFollower`/`Sends Follower` в байтах отсутствуют). Переименование = просто `mv` файла.
- **Track-движок = новый `sends_follower_track.js` (`f233094c`, node --check clean), УРЕЗАН из `53bdbfbd`:** выкинуто — обход всех треков (`buildRefs` for i<trackCount), `tracksObs`, агрегация Peak/Total + `mode()`/`followMode`, `detectReturnIndex`. Добавлено — `detectOnTrack()` (parent `tracks N` && !return_tracks), `buildRef()` ОДНА ссылка `this_device canonical_parent mixer_device sends <selectedSend>`, `rebuildMenu()` (наполняет live.menu именами return'ов из `live_set return_tracks`, outlet1 `menu clear/append/set/show/hide`), `select(k)`/`restoreidx(k)`. На return/master → N/A: `outlet(0,"max",0)` + `menu hide`. `bang()` читает один ref, кламп 0..1, метка `"max"` СОХРАНЕНА (downstream общий). Маппер-блок (arm/captureTarget/persist/unmap/restorepath/selParamObs) перенесён 1:1.
- **UI Track:** убраны `follow_mode`(live.tab)/`mode_prepend`/`mode_loadbang`/`mode_delay`/`mode_label` (−5 box/−4 line из Return-патчера) + `follow_mode` из `patcher.parameters`. Добавлен `send_label`(comment "Send" @pres y126) + `send_menu`(live.menu @pres y147 [5,147,116,16], parameter_enable, type 2 enum, в parameters как `["Send","Send",0]`) в слот бывшего Mode. Dial(obj-3)+mapper bpatcher(x128)+version-link НЕ тронуты.
- **Проводка меню:** `send_menu[0](index)→menu_prepend(prepend select)→obj-46[0]` (выбор→JS); `obj-46[1](JS menu out)→menu_route(route menu)→menu_visroute(route show hide)`: out0→`menu_showmsg(script show send_menu, script show send_label)`→`menu_thispat(thispatcher)`, out1→`menu_hidemsg(script hide …)`→thispatcher, out2(clear/append/set)→`send_menu[0]`. Restore выбора: `menu_restore_lb(loadbang)→menu_restore_del(delay 400)→send_menu[0]` (**bang в live.menu inlet RE-OUTPUTS текущее восстановленное значение** → prepend select → JS). `set` в live.menu НЕ триггерит выход (анти-feedback при `menu set`).
- **Deps ОБЩИЕ** (оба девайса в Max Devices делят по имени): `multimap.maxpat`(`49e9de1b`), `MapButton.maxpat`(`575144ef`), `sf_version_check.js`(`a5d905fc`, ОДИН version-канал/манифест на оба, DEVICE_VERSION 1.0, URL `fadercraft.com/api/sends-follower.json` сейчас отдаёт HTML=баннер спит). РАЗНЫЕ: `sends_follower.js`(Return) vs `sends_follower_track.js`(Track) — разные имена, конфликта нет. mapper bpatcher generic (1 сигнальный вход `obj-7 receive ---max_send→mm_sig sig~→multimap_panel[0]`, без args/внутр.bus) → переиспользован как есть. ⚠️ JS outlet1 mapper-функции в Return = DEAD (multimap_panel сам мапит через live.map; obj-46[1] там никуда не шёл) — в Track outlet1 ЗАНЯТ под menu, mapper-сообщения слота тоже dead (безвредны).
- **Контейнер Track:** Return UNFROZEN (JSON@0x20, `\x00`@EOF, нет dlst, 1 байт после JSON) → Track собран так же: `prefix d[:0x20]` + new JSON(indent=1) + `\x00`, `ptch`(LE@0x1c)=fs−0x20. amxdtype `aaaa`+plugin~/plugout~. jq+python parse VALID, 0 dangling, `obj-46[0]→obj-47 route max` цел.
- **Архив:** `raw/archive/standalone-snapshot-before-split.2026-06-24-140121/` (девайс `af0e8e8a` + 4 co-located deps до сплита).
- ⚠️ OPEN: founder HW-тест ОБОИХ в Live (Return: all-tracks Total/Peak как раньше; Track: на обычном треке — меню return'ов, выбор → следит за своим send'ом, на return/master → N/A/меню скрыто). НЕ founder-confirmed. ⚠️ перед раздачей FREEZE обоих (вшить js/node.script/maxpat).
- ⚠️ DEFINITIVE CRAFT: `bang`→live.menu inlet0 = re-output текущего (restored) значения; `set k`=показать без выхода; `append`=добавить пункт. `live.menu` сам не show/hide — нужен `thispatcher script show/hide <varname>` (varname обязателен на цели). `this_device canonical_parent mixer_device sends k` = свой send в k-й return; число = `getcount return_tracks`.

## ✅ STANDALONE in Max Devices — отвязка от Max-проекта — 2026-06-24
Founder директива: девайс должен грузиться ТОЛЬКО из `~/Music/Ableton/User Library/Max Devices/`, без зависимости от `SendsFollower Project`/его search-path (навсегда убить неоднозначность двух одноимённых `sends_follower.js`). Выбран **вариант A — полностью standalone** (co-locate, НЕ freeze: ещё итерируем, freeze ломает edit-in-place js/node.script).
- **Полное дерево внешних зависимостей девайса** (из реального JSON `af0e8e8a`, UNFROZEN, 0 встроенных): `obj-46 js sends_follower.js` (`53bdbfbd`), `version_node node.script sf_version_check.js @autostart 1` (`a5d905fc`), `multimap_panel bpatcher name "multimap.maxpat"` (`49e9de1b`) → внутри ×8 `MapButton.maxpat` (`575144ef`) → `multimap-unmap.svg`; плюс `multimap-closed-off.svg`/`multimap-open-off.svg` у multimap.
- **SVG НЕ копировал:** все 3 байт-в-байт идентичны стоковым C74 (`/Applications/Ableton Live 12 Suite.app/…/C74/packages/Max for Live/media/`) → резолвятся из Live, дублировать не нужно.
- **Co-located рядом с .amxd** (раньше были ТОЛЬКО в проекте): `multimap.maxpat` (`49e9de1b`) + `MapButton.maxpat` (`575144ef`). `sends_follower.js`/`sf_version_check.js` уже там.
- **Убрал проект из search-path:** `~/Documents/Max 9/` индексируется Max'ом НЕЯВНО (по умолчанию), поэтому переместил `SendsFollower Project` ЦЕЛИКОМ за пределы `~/Documents/Max 9/` → `~/Documents/_Max-archived-projects-not-indexed/SendsFollower Project.removed-from-searchpath.2026-06-24-133912`. (Сначала полный архив `raw/archive/SendsFollower-Project-FULL.2026-06-24-133912`, 1.4M.)
- **Очистил stale Dependency Cache:** `~/Library/Application Support/Cycling '74/Max 9/Settings/Dependency Cache/{multimap,MapButton}.maxpat.json` указывали под-deps на старый путь проекта → удалил (Max регенерит при загрузке, резолвит к co-located). Бэкапы в scratchpad.
- **Убрал дубль-JS из индексируемого User Library:** `Max Devices/Archive/sends_follower.js` (`4f74ef1f`, старый) — same-name дубль в индексируемой подпапке → заархивировал (`raw/archive/sends_follower.js.userlib-Archive-subfolder.2026-06-24-133912.4f74ef1f.js`) и удалил. `Archive/solo_follower.js` оставлен (чужой девайс). `Archive/XL_Performance.*.amxd` оставлен (не name-collision).
- **Результат:** ровно ОДИН источник каждого файла — все рядом с .amxd в Max Devices. `.amxd` НЕ тронут (md5 `af0e8e8a`, UNFROZEN). ⚠️ Другие проекты (`LFO Plus`/`LFO MIDI Retriggered`) тоже держат `multimap.maxpat`/`MapButton.maxpat` (ДРУГИЕ md5 `c00c7510`/`657fa074`) в search-path, НО co-located копия в папке самого .amxd имеет приоритет (Max резолвит bpatcher: 1) папка загружаемого патчера, 2) search-path) → наша версия выигрывает.
- ⚠️ **DEFINITIVE CRAFT — отвязать UNFROZEN M4L от Max-проекта = (1) co-locate bpatcher'ы+js рядом с .amxd; (2) стоковые C74-ассеты НЕ копировать (резолвятся из Live, сверь md5 с `…/C74/packages/Max for Live/media/`); (3) переместить проект ЗА пределы `~/Documents/Max 9/` (он индексируется неявно — `_archived-not-indexed` ВНУТРИ него НЕ спасает); (4) чистить `Dependency Cache/*.maxpat.json` если указывают на старый путь; (5) выгрести same-name дубли из подпапок индексируемого User Library (`Max Devices/Archive/`).** ⚠️ freeze откладываем до релиза (тогда обязателен, иначе у покупателя `can't find file`).
- ⚠️ OPEN: founder hardware-test — после reload в Live UI маппера (`multimap.maxpat` bpatcher) должен подняться без проекта. НЕ founder-confirmed.

## ✅ TOTAL-MODE BUG FIX — shadowed observer-less JS — 2026-06-24
**Founder repro:** Total mode не учитывал send'ы с дублированных/новых аудио-треков (энкодер не реагировал на их send-кнобы). **ROOT CAUSE = НЕ контейнер, а какой `sends_follower.js` грузится.** On-disk девайс `SendsFollower.amxd` (md5 `af0e8e8a`, 52925 B, 63 box/61 line, **UNFROZEN** — JSON@0x20, нет dlst) грузит `js sends_follower.js` ПО ИМЕНИ и привязан к Max-проекту `~/Documents/Max 9/Max for Live Devices/SendsFollower Project/` (через `multimap_panel bpatcher name "multimap.maxpat"`, который есть только в `patchers/` проекта). В search-path лежали ДВЕ разные копии: канон рядом с .amxd `…/User Library/Max Devices/sends_follower.js` (`53bdbfbd`, 24242B, С observers `tracks`/`return_tracks`/dev-path + 500ms count-resync) И старая `…/SendsFollower Project/code/sends_follower.js` (`29ff530b`, 5808B, от 21 апр, БЕЗ единого observer; `buildRefs` один раз, `bang()` self-heal ТОЛЬКО при `sendRefs.length===0` строка 104 → непустой список больше не пересобирается → новый трек никогда не входит в Total). Проектная копия затеняла канон.
- **ФИКС (план founder'а, JS-only, контейнер НЕ тронут):** заменил `…/SendsFollower Project/code/sends_follower.js` (`29ff530b`) на канон `53bdbfbd` (cp из User Library). Обе действующие копии теперь byte-identical `53bdbfbd`. **RESYNC_MS оставлен 500 (founder отменил ускорение до 200).** `node --check` обе clean.
- **Archives (dated):** `raw/archive/sends_follower.js.userlib.2026-06-24-133306.53bdbfbd.js` + `…project-code.2026-06-24-133306.29ff530b.js`.
- ⚠️ **DEFINITIVE CRAFT — UNFROZEN M4L + Max Project = два одноимённых .js в search-path; обновляй ВСЕ копии (рядом с .amxd И в Project/code/), иначе старая затеняет канон.** Чинит Total: канон `53bdbfbd` ловит add/del треков через observer `tracks` + дешёвый count-resync в `bang()` (`resync(false)` пересобирает при `count!==trackCountSnap`), без зависимости от непустоты sendRefs.
- ⚠️ Память про on-disk `31633e38`/8-слот была устарелой — реальный on-disk `af0e8e8a` = simple engine + Follow Mode. Всегда md5 реального файла.
- ⚠️ OPEN: founder hardware-test в Live (Live кеширует по пути — снять девайс с трека + заново перетащить; Max-редактор закрыть без сохранения). НЕ founder-confirmed.

## ✅ REVERT broken-arm + remove SFMAP3 diag — 2026-06-19 (CURRENT ON-DISK, md5 `31633e38`)
Founder URGENT: a prior arm edit broke mapping/X/text — `mapprep_N` had been changed `prepend mapping` → `t b` (arm via bang). **`bang` is NOT equivalent to `prepend mapping` for `live.map` arm** (the message `mapping` is what puts live.map into learn/capture state; a bare bang does not) → targets didn't move, mapped name went black, X dead. Reverted EXACTLY two things, kept everything else.
- **Base on-disk was `00ca0be7`** (250928 B, 323 box/428 line) = `79be0d3a` (last-working version, archived `…133135-preedit-3edits-maxrange-tb-sfmap3.amxd`) + 3 edits: slot_max range fix + `t b` arm + SFMAP3 diag. Reverted the latter two; kept the range fix.
- **EDIT 1 — `mapprep_N` (×8): text `t b` → `prepend mapping`.** TEXT-ONLY: form already matched (`numinlets 1 / numoutlets 1 / outlettype ['']` identical for both) so wiring `map_btn_N[0]→mapprep_N[0]→livemap_N[0]` untouched, verified exact on all 8. Restores arm/capture/X/name.
- **EDIT 2 — removed SFMAP3 diag entirely:** −9 box (`sfmap3_0..7` = `prepend SFMAP3 N`, id-named NOT varname; + shared `sfmap3_print` = `print`) − 16 line (8× `livemap_N[3]→sfmap3_N[0]`, 8× `sfmap3_N[0]→sfmap3_print[0]`). `livemap_N[3]` still feeds `map_btn_N` + `bk_arm_N` (blink) → 0 dangling.
- **KEPT (NOT reverted):** slot_max range fix — all 8 `slot_max_N` `parameter_mmin = 0.0` (founder-confirmed; was −100.0 in `79be0d3a`). Verified 0.0 on all 8 in result. Blink `bk_*` left as-is (not triggered = OK). State-aware restore, name-in-button, fonts intact.
- **Result md5 `31633e381ff0f3140640d5786a1765fb`, 246273 B, 314 box / 412 line** (= `79be0d3a` topology + slot_max fix; SFMAP3 gone). −9 box/−16 line vs `00ca0be7`.
- **NOT TOUCHED (byte-identical, verified post-build from final on-disk file):** embedded `sends_follower.js` `53bdbfbd`, `sf_version_check.js` `a5d905fc`, `multimap-unmap.svg` `1a31f546`. route max/`---max_send`/percent, Peak/Total, dial display, version-check, plugin~/plugout~, left side, live.map→live.remote~, X-unmap, Min/Max scale.
- **Container Path B** (JSON 222556→217901, dL −4655): JSON sz32 217902 of32 16; svg of32 217918; sf_version_check.js of32 218453; sends_follower.js of32 221559. dlst total=440, `(0x30+mxc)−dlst=16` (mxc 245801), ptch=246241=fs−0x20. jq+python VALID, 0 dangling, only 8 tail bytes differ (JSON sz32 + 3 of32 low-bytes). Both JS node --check clean.
- Archives: pre-edit (broken `00ca0be7`) `raw/archive/SendsFollower.2026-06-19-134324-preedit-revert-tbarm-sfmap3.amxd`; prior-working base `…133135-preedit-3edits-maxrange-tb-sfmap3.amxd` (`79be0d3a`). NO new frozen-archive (revert, not a feature) — on-disk IS the result. Edit-in-place User Library.
- ⚠️ **DEFINITIVE CRAFT — `live.map` arm MUST be `prepend mapping`, never a bang.** Arming live.map for click-to-capture = send the message **`mapping`** into its inlet (`map_btn → prepend mapping → live.map`). A `t b`/bare bang does NOT trigger learn mode → mapping/capture/name/X all fail. Object forms interchangeable (1/1/['']) so swapping is text-only, but SEMANTICS differ — never "simplify" `prepend mapping` to a bang.
- ⚠️ OPEN: founder hardware-tests in Live. NOT founder-declared-done. Blink expected NON-functional (reverted with the arm change).

## (SUPERSEDED by revert above — this version had the broken `t b` arm + SFMAP3) STATE-AWARE BLINK RESTORE (per slot ×8) — FROZEN 2026-06-19 (md5 `ba485826`)
Founder noticed: when blink STOPS, Map button restored ORANGE unconditionally → an empty / just-unmapped slot showed orange "Map" (wrong, should be grey). Fix = restore TEXT color by slot state (mapped→orange #FFB532, empty→grey #8C8C8C); fill stays dark #282828 (keeps the prior textcolor-only fix). **Result md5 `ba485826dd37b1df07ecb1ad23398e61`, 246175 B, 306 box / 396 line** (+8 box/+8 line vs `f46350db`).
- ⚠️ **The on-disk `f46350db` ALREADY HAD a partial state-aware restore branch** (`blink_mapflag_N` int, `blink_restoresel_N` sel 0 1, `blink_restore_idle_N` grey msg) — prior memory's "restore is unconditionally orange / state-aware never existed" was WRONG for THIS file. The branch existed but was BROKEN 3 ways (all fixed):
  1. **Inversion.** `xcmp_N` = `zl compare <none>` → out0 = **1 unmapped / 0 mapped**, stored into `blink_mapflag_N`(`int`) RIGHT inlet. On stop, `blink_mapflag_N[0]` bang emits the flag → `blink_restoresel_N`(`sel 0 1`): old wiring had out0(match0=mapped)→idle GREY, out1(match1=unmapped)→restore ORANGE = backwards. **FIX: swapped the two destinations** — restoresel[0](match0=mapped)→`blink_restore_N`(orange), restoresel[1](match1=unmapped)→`blink_restore_idle_N`(grey).
  2. **`lcdcolor` in idle msg.** `blink_restore_idle_N` set grey via `lcdcolor` (= LCD FILL → solid grey block, violates "text-only / dark bg" fix). **FIX: rewrote to 4 text attrs only** (`textcolor`+`textoffcolor`+`activetextcolor`+`activetextoncolor` grey, NO lcdcolor) — identical form to `blink_phaseB_N`.
  3. **Capture/unmap race (THE real reliability fix).** `live.map` outlets fire RIGHT→LEFT (maxref-confirmed): outlet3(state) fires BEFORE outlet2(name). So on capture/unmap the stop-bang (out3→0) read the `int` flag BEFORE outlet2 updated it via `xcmp` → restored the STALE color (capture-on-empty-slot flashed grey; unmap flashed orange). **FIX: inserted `blink_stopdel_N`(`delay 0`) between `blink_stopsel_N[0]` and `blink_mapflag_N[0]`** — defers the flag READ to the next scheduler tick, after outlet2(name) synchronously updates the flag in the same logical frame. Now flag is always current when read. (Disarm-without-capture has no name event → no race → flag holds correct last value.)
- **Per slot ×8 edits:** +1 box `blink_stopdel_N`(delay 0, off-canvas x≈2110, NO presentation); lines −1 (cut `blink_stopsel_N[0]→blink_mapflag_N[0]`) +2 (`blink_stopsel_N→blink_stopdel_N`, `blink_stopdel_N→blink_mapflag_N`) net +1; restoresel destinations swapped in-place (no count change). Net +8 box/+8 line.
- **Flag init:** `int` (no arg, starts 0=mapped) but `live.map @_persistence 1` re-emits name(out2) on load → `xcmp`→flag corrected per actual mapping before any stop-bang; no explicit loadbang needed.
- **NOT TOUCHED (byte-identical, verified post-build):** tracking JS `sends_follower.js` `53bdbfbd`, `sf_version_check.js` `a5d905fc`, svg `multimap-unmap.svg` `1a31f546`. `route max`/`---max_send`/percent, Peak/Total, dial display, version-check, mapping (live.map→remote~, name namesub→namesym→namemsg, X-unmap xcmp→xhide, Min/Max scale), fonts, plugin~/plugout~, and the textcolor-only/dark-#282828 blink fix all intact. blink CORE wiring (livemap[3]→metro/stopsel, metro→tgl→sel→phaseA/B) unchanged; only stopsel→restore branch reworked.
- **Container Path B** (JSON grew 213739→217803, dL +4064): JSON sz32 217804 of32 16; svg of32 217820 (`1a31f546`); sf_version_check.js of32 218355 (`a5d905fc`); sends_follower.js of32 221461 (`53bdbfbd`). dlst total=440, mxc=245703 (=dlst_pos 245735 +16−0x30), ptch=246143=fs−0x20, no trailing mdat. jq+python parse VALID, 0 dangling, openrect `[0,0,304,169]` unchanged, no fill-color (`lcdcolor`/`bgcolor`/`bgoncolor`) in ANY blink/restore msg.
- Archives: pre-edit `raw/archive/SendsFollower.2026-06-19-122604-preedit-state-aware-restore.amxd` (`f46350db`); frozen `…-123039-state-aware-restore-frozen.amxd` (`ba485826`). Edit-in-place `~/Music/Ableton/User Library/Max Devices/SendsFollower.amxd`.
- ⚠️ **REUSABLE CRAFT — restore-on-stop by mapped/empty state:** key off `live.map` outlet2 name → `zl compare <none>` (1 unmapped/0 mapped) → store in `int` RIGHT inlet; on stop (`live.map out3`→0) bang the `int` LEFT inlet → `sel 0 1` → orange(0=mapped)/grey(1=unmapped) text-only msgs. ⚠️ **`live.map` outlets fire RIGHT→LEFT (out3 state BEFORE out2 name)** → MUST `delay 0` the stop-bang so the flag reflects the just-arrived name; otherwise capture/unmap restores the stale color. Grey msg = text attrs only, never `lcdcolor`.
- ⚠️ OPEN: founder hardware-tests in Live. NOT founder-confirmed.

## (SUPERSEDED — restore was unconditional orange + broken state-aware branch) MAP-BUTTON BLINK — TEXTCOLOR FIX (drop lcdcolor) + SFARM DEBUG REMOVED — FROZEN 2026-06-19 (md5 `f46350db`)
Founder CONFIRMED blink works (color changes). But screens showed the WHOLE button became a solid ORANGE BLOCK (mapped + during blink) — bg should stay dark LCD #282828, only TEXT color animates. **ROOT CAUSE of the block: the prior `404fd7d3` phase/restore messages set `lcdcolor` — and `lcdcolor` = the LCD FILL, not the text.** The earlier memory claim "lcdcolor paints visible text in off/active state" was WRONG. Per `live.text.maxref.xml` digests: `lcdcolor`="LCD color" (fill); `textcolor`="Text color (on/inactive)"; `textoffcolor`="Text off color (off/inactive)"; `activetextcolor`="Text color (Off)" (active device, off-state); `activetextoncolor`="Text color (On)". Stock `liveui.map.maxpat` Map live.text uses **NO `lcdcolor` at all** — fill via `bgcolor`/`bgoncolor`/`activebgcolor`/`activebgoncolor` (#282828), text via `textcolor`/`textoffcolor` (grey, inactive) + `activetextcolor`/`activetextoncolor` (orange, active). Since blink already visibly changed color with `textcolor`+`textoffcolor` present, those DO reach the glyph in our context (active 1, parameter_enable 0, off-state); the only block-producer was `lcdcolor`.
- **FIX (per slot ×8, TASK 1):** rewrote 24 messages (`blink_phaseA_N` orange / `blink_phaseB_N` grey / `blink_restore_N` orange) to set ONLY text attrs — **`textcolor` + `textoffcolor` + `activetextcolor` + `activetextoncolor`** (all four = robust across active 0/1 and on/off). **REMOVED `lcdcolor` entirely** from all messages → fill stays the box's static dark `bgcolor` #282828 always. The static `map_btn_N` box was already stock-correct (bg #282828 incl `activebg*`, idle text grey #8C8C8C `textcolor`/`textoffcolor`, active text orange #FFB532 `activetextcolor`/`activetextoncolor`, bordercolor #282828 theme-bound `live_lcd_bg` — no idle orange frame). Box untouched; bordercolor already dark.
- **DEBUG REMOVED (TASK 2):** deleted 9 boxes (`blink_dbgprep_0..7` = `prepend SFARM N`, + shared `blink_print` = `print`) + 16 lines (8× `livemap_N out3→dbgprep`, 8× `dbgprep→print`). `livemap_N out3` still feeds `blink_metro_N`/`blink_stopsel_N`/`map_btn_N` — 0 dangling.
- Result md5 **`f46350db0f1a542d4add8a652821046f`, 242111 B, 298 box / 388 line** (−9 box/−16 line vs `404fd7d3`).
- **NOTE (CORRECTED by `ba485826` above):** the on-disk `f46350db` DID contain `blink_mapflag_N`/`blink_restoresel_N`/`blink_restore_idle_N` (a partial, BROKEN state-aware restore — inverted + lcdcolor + race). This note's earlier claim "never in the file / unconditionally orange" was inaccurate for `f46350db`; see `ba485826` section for the real branch and its fix.
- **NOT TOUCHED (byte-identical, verified post-build):** `sends_follower.js` `53bdbfbd`, `sf_version_check.js` `a5d905fc`, `multimap-unmap.svg` `1a31f546`. Tracking JS, mapping (live.map→remote~, name namesub/namesym/namemsg, X-unmap, Min/Max), Follow Mode, percent, dial, version, plugin~/plugout~, fonts. blink wiring (`livemap_N out3→metro/stopsel/map_btn`, metro→tgl→sel→phaseA/B, stopsel→restore) intact.
- **Container Path B (4 res JSON→svg→version→follower):** JSON 216810→213739 (dL −3071), sz32 213740 of32 16; svg 535 of32 213756 (`1a31f546`); sf_version_check.js 3106 of32 214291 (`a5d905fc`); sends_follower.js 24242 of32 217397 (`53bdbfbd`). dlst total=440, mxc=241639 (=dlst_pos 241671 +16−0x30), ptch=242079=fs−0x20, no trailing mdat. jq+python parse VALID, 0 dangling, openrect `[0,0,304,169]` + pres bbox (296×161) unchanged.
- Archives: pre-edit `raw/archive/SendsFollower.2026-06-19-121201-preedit-blink-textcolor-fix.amxd` (`404fd7d3`); frozen `…-121624-blink-textcolor-fix-debug-removed-frozen.amxd` (`f46350db`). Edit-in-place `~/Music/Ableton/User Library/Max Devices/SendsFollower.amxd`.
- ⚠️ **DEFINITIVE CRAFT (CORRECTED) — color a live.text Map button (appearance 2, parameter_enable 0, active 1):** the VISIBLE GLYPH is painted by `textcolor`/`textoffcolor` (set both; inactive-axis but they reach the glyph here) PLUS `activetextcolor`/`activetextoncolor` (active-axis, stock uses these for orange). The LCD **FILL** is `lcdcolor` (+ `bgcolor`/`activebgcolor` family) — setting `lcdcolor` paints a SOLID BLOCK = the bug. To animate ONLY the text, set the four text attrs and **NEVER touch `lcdcolor`**; leave fill to the box's static dark `bgcolor`/`activebgcolor`. Stock `liveui.map` proves it: zero `lcdcolor`, text via textcolor/activetextcolor, bg via bgcolor (all #282828). (Supersedes the wrong "lcdcolor=text" note from `404fd7d3`.) Armed signal still `live.map outlet 3` (mapping attr 1/0); blink tempo `qmetro 200`.
- ⚠️ OPEN: founder hardware-tests in Live (idle = grey "Map" on dark; armed = text grey↔orange flash on dark, NO orange fill/frame; mapped = orange name text on dark). NOT founder-confirmed.

## ⚠️ (SUPERSEDED — blink did NOT work) MAP-BUTTON BLINK (textoncolor attempt) + FONT — md5 `c34fd168`
Base = on-disk `2bbc12ee` (192221 B, 218 box/268 line, the MM-Native right-frame Remote-only device). **Polish only on the Map buttons; tracking/mapping/Peak-Total/percent/dial/version untouched.** Result md5 **`c34fd16837e318d73277601f4ba3530a`, 226079 B, 274 box / 348 line** (+56 box/+80 line, all off-canvas, NO presentation flag → openrect `[0,0,304,169]` unchanged, pres bbox 296×161 unchanged).
- **BLINK (per slot ×8, mirrors stock `liveui.map.maxpat` `p ButtonColor` mechanic, tempo `qmetro 200`):** 7 boxes/slot = `blink_metro_N`(qmetro 200), `blink_tgl_N`(toggle), `blink_sel_N`(sel 0 1), `blink_phaseA_N`(message orange on-colors), `blink_phaseB_N`(message grey on-colors), `blink_stopsel_N`(sel 0), `blink_restore_N`(message orange on-colors). 10 lines/slot. **Arm-state source = `livemap_N` outlet 3 (state int: 1 while learn-mode/waiting for param click, 0 once captured OR disarmed)** — the SAME outlet that already feeds `map_btn_N[0]`; tapped in parallel (didn't touch existing line). Wiring: `livemap_N[3]→blink_metro_N[0]`(nonzero=start, 0=stop) + `→blink_stopsel_N[0]`; `blink_metro→blink_tgl→blink_sel(sel 0 1)`: out1→phaseA(orange), out0→phaseB(grey)→`map_btn_N[0]`; `blink_stopsel_N out0(state==0)→blink_restore_N→map_btn_N[0]` (`order=2` so restore wins after any in-flight phase). **Blink alternates the live.text ON-state text colors** (`textoncolor`/`activetextoncolor`/`activetextcolor`) grey↔orange ~every 200ms while armed; on stop restores them to orange (mapped→orange name shown / idle→toggle off so grey "Map" via untouched `textcolor`). Reads as flashing grey↔orange, stock tempo. ⚠️ Stock `p ButtonColor` uses full `live.colors`/theme brightness-toggle (1.0↔0.5) machinery — I used the simpler founder-approved grey↔orange color-msg toggle, same key (`qmetro 200` + toggle + state outlet), same tempo/look.
- **FONT:** `map_btn_N`(×8) `Arial Bold 11` → **`Ableton Sans Medium 9.5`** (Medium, NOT Bold — matches stock spec + headers). `slot_min_N`/`slot_max_N`(×8) `Arial 10` → **`Ableton Sans Medium 9.5`** (table consistency; was the only visible mismatch vs stock). Headers already `Ableton Sans Medium 9.5` (untouched).
- **NOT TOUCHED (verified byte-identical in rebuilt container):** `sends_follower.js` `53bdbfbd` (tracking JS), `sf_version_check.js` `a5d905fc`, `multimap-unmap.svg` `1a31f546`. All mapping mechanism (live.map→remote~, name-in-button namesub/namesym/namemsg, X-unmap, Min/Max scale), Follow Mode, percent, dial, version-check, plugin~/plugout~ untouched. Existing `livemap_N[3]→map_btn_N[0]` line kept.
- **Container Path B** (Path A impossible: re-dump JSON 163849→197707 = +33858, doesn't fit L0). 4 res order JSON→svg→version→follower. of_json=16 sz_json=197708; svg of32 163866→197724; sf_version_check.js of32 164401→198259; sends_follower.js of32 167507→201365. dlst total=440, dlst_pos=225639, mxc=225607 (=dlst_pos+16−0x30), ptch=226047=fs−0x20, NO trailing mdat (file ends at dlst chunk, tail 440 = 8+4×108). jq + python parse VALID, 0 dangling cords, all 3 embedded resources roundtrip byte-identical + both JS node --check clean.
- Archives: pre-edit `raw/archive/SendsFollower.2026-06-19-114424-preedit-blink-font.amxd` (`2bbc12ee`); frozen `…-115009-blink-font-frozen.amxd` (`c34fd168`). Edit-in-place `~/Music/Ableton/User Library/Max Devices/SendsFollower.amxd`.
- ⚠️ **REUSABLE CRAFT — blink a live.text Map button = stock `liveui.map` mechanic:** key off `live.map` outlet 3 (state: 1 armed/learn, 0 captured/idle) → `qmetro 200` (start nonzero / stop 0) → `toggle` → `sel 0 1` → two text-color messages to the button → on state==0 (`sel 0`) restore static colors with a late `order` so restore wins. Tempo 200ms = stock. The button label color while ON = `textoncolor`/`activetextoncolor`/`activetextcolor`; flip all three to be robust to toggle render state.
- ⚠️ OPEN: founder hardware-tests in Live (click Map → button flashes grey↔orange while waiting → click target param → flashing stops, name shown orange → unmap = grey "Map"; font smaller/non-bold). NOT founder-confirmed.

## ✅ RIGHT-FRAME → MM-NATIVE STOCK LOOK, REMOTE-ONLY — FROZEN 2026-06-19 (was CURRENT ON-DISK before blink/font, md5 `2bbc12ee`)
**What was on disk before this:** `7ebe7d15` (342759 B, 444 box/594 line) = the HEAVY-JS device (tracking JS `53bdbfbd` + native 8-slot `live.map` mapper WITH Mod/bipol/depth/± baggage + rejected orange-frame/comment-overlay name display). NOTE: the earlier MEMORY entries claiming on-disk = `f268af54` (remote-only) or `5b64994c`/`584acc1b` (simple AVG engine) were NOT on disk — on-disk was `7ebe7d15`. Always md5 the actual file; don't trust which branch memory says is current.
**Task:** replace ONLY the right section (8-slot mapper) with the founder-approved MM Native clean frame (Remote-only, stock look, name-in-button). Left part (dial/Peak-Total/version) + ALL tracking + percent + dial display UNTOUCHED.
**Result device md5 `2bbc12ee`, 192221 B, 218 box / 268 line.** openrect `[0,0,304,169]` (narrowed 332→304 to MM Native fit; no empty strip from removed Mode/depth cols). pres bbox x+w=296, y+h=161.
- **The `7ebe7d15` mapper was ALREADY native `live.map @strict 1` ×8** (ids `livemap_0..7`, 5 outlets: 1=id/2=name/3=state), NOT JS-driven. JS obj-46 has numoutlets=1 → feeds ONLY `obj-47 route max` (follow/send). So the mapper mechanism = same proven idiom as MM Native; the founder's complaints were PRESENTATION + Mod/±/depth baggage, not the map mechanism. (Task's "JS-driven arm/captureTarget/selected_parameter observer/outlet 1" described an OLDER intermediate state, not this file.)
- **DELETED 221 boxes + the LFO leg:** per slot ×8 = `map_mode_N`(live.text toggle), `modeplus_N`(+1), `modesel_N`(sel 0 1), `idtrig_N`(t b l), `idreg_N`(msg), `idsetreg_N`(prepend set), `idgate_N`(gate 2 1), `id0a_N`/`id0b_N`(msg id 0), `modscale_N`/`modmul_N`/`modsig_N`/`mod_N`(live.modulate~)/`kmul_N`/`depthdiv_N`/`signif_N`(Mod arm), `tdep_N`/`tpol_N`(t b l), `bipol_N`(live.text ±), `depth_N`(live.numbox), `mapcom_N`(comment overlay), `nameset_N`(prepend set), `comdark_N`/`comorng_N`(color msgs to comment), `obj-{53+5n}`(sel 0 1 color), `obj-{54+5n}`/`obj-{55+5n}`(bgcolor msgs to btn). ⚠️ **GOTCHA: also delete `msgrem_N`/`msgmod_N`(message `script show/hide slot_min/max/bipol/depth`) + `modetp_N`(per-slot `thispatcher`)** — these survived the first delete pass (not in initial list) and kept dead `script …bipol_N, depth_N` refs → leftover-ref scan caught them. LFO leg: `obj-9`(path msg), `obj-10`(live.path), `obj-11`(9th live.remote~), `obj-16`(scale -100..100), `obj-17`(sig~) + line `obj-15[0]→obj-16`. ⚠️ `obj-11_scale`(* 127.)/`obj-11_set`(prepend set) are NOT LFO — they drive the DIAL display (KEEP). `obj-6 delay 300`→`obj-9` line dies with obj-9; obj-6 left as harmless unfed orphan (0 dangling).
- **KEPT + simplified Remote leg (per slot):** `livemap_N`→`mapprep_N`(arm)/`unmapmsg_N`(unmap); `scale_N`(scale 0.1.0.1.)→`sig_N`→`remote_N[0]`; min/max range `slot_min/max_N`→`tmin/tmax_N`(t b l)→`div_min/max_N`(/100.)→`scale_N[3/4]`; instant-range `followf`(f, in1←obj-7 silent store)→`scale_N[0]`. **id NOW DIRECT:** `livemap_N[1]`(id, emits `id N` on map / `id 0` on unmap)→`remote_N[1]` (no idtrig/gate). X-vis `livemap_N[2]→xcmp_N(zl compare <none>)→xhide_N(hidden $1)→map_unmap_N`.
- **Name-in-button = MM Native v3 mechanism (the fix founder approved, was md5 `c4e3b389`):** `livemap_N[2]→namesub_N(substitute <none> Map)` **BOTH outlets (0+1)** →`namesym_N(tosymbol, NEW)`→`namemsg_N(message "text $1, texton $1", NEW)`→`map_btn_N[0]`. ⚠️ `substitute a b` outlet0=substituted (match `<none>`→Map), outlet1=intact (real name) — MUST wire BOTH or mapped names drop and button sticks on "Map". `livemap_N[3]`(state)→`map_btn_N[0]` too (both into inlet0 = stock). Added 16 boxes (namesym+namemsg ×8) + 40 lines (sub0/sub1/sym→msg/msg→btn/id→remote ×8).
- **Restyle map_btn_N (MM Native, no idle frame):** live.text appearance 2, `parameter_enable=0` (NOT a param), text/texton="Map", bgcolor/bordercolor = LCD bg #282828 theme-bound `live_lcd_bg` (no orange idle frame), textcolor/off = grey #8C8C8C `live_lcd_control_fg_zombie`, activetextcolor/+on = orange #FFB532 `live_lcd_control_fg`. X = `map_unmap_N` live.text usepicture `multimap-unmap.svg`×2 remapsvgcolors/usesvgviewbox fontsize6 `hidden 1`. Min/Max = appearance 4 prototypename `amount` parameter_unitstyle 5(%) init Min0/Max100 mmin -100 mmax 100 orange digits + transparent fill. `lcd_panel`: panel rounded7 #282828 `live_lcd_bg` presR `[128,0,168,161]`. Headers (plain comment, Ableton Sans Medium 9.5, white@46%): `hdr_param`"Parameter"@x131 / `hdr_min`"Min"@x222 / `hdr_max`"Max"@x255, y2. Row geometry (MM Native): map_btn x131 w74 / X x206 w15 / Min x222 w34 / Max x255 w34; rows y18 pitch18 h15.
- **parameters dict cleaned:** removed `bipol_N`/`depth_N`/`map_mode_N` ×8 (24 entries). Kept `follow_mode` + `slot_min_N` + `slot_max_N` (17). parameterbanks all "-" (no broken refs).
- **NOT TOUCHED (byte-identical, verified):** embedded `sends_follower.js` `53bdbfbd` (heavy tracking JS — bang/buildRefs/resync/observers/Peak-Total intact; its dead mapper functions stay unused, harmless), `sf_version_check.js` `a5d905fc`, `multimap-unmap.svg` `1a31f546`. Follow chain `obj-46→obj-47 route max→obj-48 change→obj-50 send ---max_send`. Percent `obj-12 recv→obj-13 change→obj-14 speedlim→obj-15 t f f[1]→obj-18 scale 0..100→obj-19 flonum + obj-20 send ---max_send_percent` (obj-15 shared upstream with deleted LFO; only out0→obj-16 cut). Dial `obj-7 recv→obj-11_scale *127→obj-11_set prepend set→obj-3 dial`. Left UI: `obj-3` dial, `mode_label`, `follow_mode`(live.tab Peak/Total)+mode_prepend/loadbang, `version_link`+vlink_*. live.thisdevice/loadbang/detect chain.
- **Container Path B** (JSON shrank 314387→163849, dL −150538): prefix `d[:48]`, JSON@48, `\x00`, then svg+version+follower+dlst. dire JSON sz32=163850 of32=16; svg of32 314404→163866; sf_version_check.js of32 314939→164401; sends_follower.js of32 318045→167507 (each trailing res of32 += dL). 4 dires, dlst total=440, `(0x30+mxc)−dlst=16`, ptch=fs−0x20. amxdtype aaaa, plugin~/plugout~ present. jq + python parse VALID, 0 dangling, 0 leftover dead refs.
- Archives: pre-edit `raw/archive/SendsFollower.2026-06-19-111500-preedit-rightframe-mmnative.amxd` (`7ebe7d15`); frozen `…-112744-rightframe-mmnative-remote-only-frozen.amxd` (`2bbc12ee`). Edit-in-place `~/Music/Ableton/User Library/Max Devices/SendsFollower.amxd`.
- ⚠️ OPEN: founder hardware-tests in Live (tracking + click Map → name in button → encoder moves target within Min/Max → unmap = "Map", no orange idle frame). NOT founder-confirmed.

## ✅ AVG AGGREGATION (simple engine) — FROZEN 2026-06-19 (was a SEPARATE simple-engine branch, NOT this on-disk file)
Founder-confirmed bug: encoder pins at 100% because ONE track sends to the return at full, and `max` aggregation picks exactly that track → encoder unresponsive to other send-knobs. **FIX = embedded-JS-only**, replaced the entire `function bang()` in the embedded `sends_follower.js` (`4f74ef1f`) to compute **AVERAGE across all tracks** instead of max: `sum += parseFloat(v[0]); n++` then `avg = n>0 ? sum/n : 0`. **Output label kept `outlet(0,"max",avg)`** — patch routes via `route max`(obj-47), label MUST stay. Self-heal block (autoDetect+buildRefs when `sendRefs.length===0`) UNCHANGED; everything else (inlets/outlets/autoDetect/buildRefs/build/cleanup/now) byte-identical. `node --check` clean.
- **Container Path B** (embedded JS grew, JSON unchanged so JSON region byte-identical): embedded `sends_follower.js` 4021→4279 B (dL +258), of32=31116 (unchanged), sz32 4021→4279. `sf_version_check.js` (`a5d905fc`, 3106 B) of32 35137→35395 (+258), untouched. JSON of32=16 sz32=31100 (unchanged), patcher 63 box/65 line (untouched, 0 dangling). dlst total=332, `0x30+mxc−dlst=16` (mxc 38243→38501), ptch=38833=fs−0x20, no trailing mdat.
- **Result device md5 `5b64994cc0aa3d1bebfbc57ed4ef78aa`, 38865 B, 63/65. NEW embedded `sends_follower.js` md5 `4b42a9d22fd67a182e1e91eeb4b40ad4`, 4279 B.** Other two resources byte-identical.
- ⚠️ on-disk canon `Max Devices/sends_follower.js` is the HEAVY 8-slot engine (`53bdbfbd`, 24242 B) — DIFFERENT engine, NOT touched per task; device runs from its OWN embedded copy so the mismatch is irrelevant.
- Archives: pre-edit `raw/archive/SendsFollower.2026-06-19-103856-preedit-avg-aggregation.amxd` (`584acc1b`); frozen `…-104126-avg-aggregation-frozen.amxd` (`5b64994cc0…`). Edit-in-place at `~/Music/Ableton/User Library/Max Devices/SendsFollower.amxd`.
- ⚠️ OPEN: founder hardware-tests in Live — put device on the SAME existing return, reload, move ANY send-knobs → encoder should move (not pin at 100%).
- Prev state `584acc1b` (JS `4f74ef1f`, max aggregation + poller-autostart fix below). The `4f74ef1f` engine has NO Follow Mode, so avg change has no Peak/Total interaction.

## ✅ POLLER-AUTOSTART FIX (simple engine) — FROZEN 2026-06-19 (superseded by AVG AGGREGATION above)
Founder-confirmed bug on the SIMPLE on-disk version (`b5286b33`, JS `4f74ef1f`, NOT the 8-slot canon): on an ALREADY-configured return the device loads and the encoder FREEZES at max (init value), no follow. Trick that revived it = duplicate the return so it gets a fresh signal. Diagnosis (mine, founder-validated): `qmetro 33`(obj-35) is the ONLY thing that bangs JS `bang()`, and it starts ONLY when the patch detect chain reaches `obj-44 "1"→obj-35`. That chain passes through `change -1`(obj-41); on reload onto the SAME return the index doesn't change → `change` swallows the trigger → `prepend build`(obj-42)/`t l b b`(obj-43 out1) never fire → qmetro never starts → encoder stuck at init. On a freshly-copied return the index differs → passes → starts. Orphan `obj-34 "1"`(no input) was an intended-but-unwired starter.
**FIX = PATCH-ONLY (JS untouched, still `4f74ef1f` byte-identical). +2 box / +4 line (61/61→63/65):**
- `start_delay`(`delay 400`): `obj-23`(deferlow, load trigger) → `start_delay` → `obj-34 "1"` → `obj-35 qmetro 33`. Poller now ALWAYS starts ~400ms after load (after detect delays 50/150/300), independent of `change -1`. Wired the orphan obj-34 for its intended purpose.
- `rebuild_delay`(`delay 500`): `obj-40 int`(RAW detected index, before `change`) → `rebuild_delay` → `obj-42 prepend build`. Forces `build N` to JS ~500ms after detect, BYPASSING `change -1` → refs rebuild against the actual return even when index unchanged (the failing reload-to-same-return case).
- Third safety net unchanged: JS `bang()` self-heal still calls `autoDetect`+`buildRefs` when `sendRefs.length===0`. Detect chain for index SELECTION (delay 300→qmetro 500→getpath→...→change -1) left as-is.
- NOT TOUCHED: send chain (js→route max→change 0.→send ---max_send + percent obj-18/20), dial output (obj-7→*127→prepend set→obj-3), audio skeleton (plugin~/plugout~), version-check (version_node+vlink_*). 0 dangling cords. NOTE: this SIMPLE engine has NO Follow Mode (no `live.tab`, no `mode` handler in `4f74ef1f` JS) — the task's "don't break Follow Mode" line is moot here; Follow Mode lives only in the later `29ff530b`+ engines.
- **Container Path B** (Path A impossible: compact `indent=1` re-dump is already +1163 B vs original, so length can't be preserved): JSON 29936→31099 (dL +1163), JSON sz32=31100 of32=16; `sends_follower.js` 4021 B of32 29953→31116 (`4f74ef1f`); `sf_version_check.js` 3106 B of32 33974→35137 (`a5d905fc`). dlst total=332 (3 res), `(0x30+mxc)−dlst=16` (mxc=38243), ptch=38575=fs−0x20, no trailing mdat (file ends at dlst). **Result md5 `584acc1b`, 38607 B, 63 box/65 line.** Both embedded JS byte-identical to source.
- Archives: pre-edit `raw/archive/SendsFollower.2026-06-19-101131-preedit-poller-autostart.amxd` (`b5286b33`); frozen `…-101353-poller-autostart-frozen.amxd` (`584acc1b`). Edit-in-place at `~/Music/Ableton/User Library/Max Devices/SendsFollower.amxd`.
- ⚠️ OPEN: founder will hardware-test in Live — put device on the SAME problematic (already-configured) return, reload, confirm encoder follows WITHOUT the copy-return trick.
- ⚠️ CRAFT: to autostart a `qmetro`/poller independent of a one-shot detect chain → drive it from `loadbang/live.thisdevice → deferlow → delay (> detect delays)`. To force a value past a `change` gate, tap the value BEFORE `change` and route it through a delay to the consumer. This SIMPLE engine ≠ the heavy 8-slot canon (`53bdbfbd`); don't conflate.

## ⚠️ FORENSIC 2026-06-19 — follow-engine regression hunt (read-only)
Founder: encoder does NOT follow sends on restored `7ebe7d15` (on-disk live device == this). Clue at
session start: Max Console `send_follower: built 3 send refs for return 0` (JS RAN + built refs).
**Embedded JS timeline (md5 of `sends_follower.js` extracted from each archive's dlst):**
- `4f74ef1f` (4021 B, 113 ln) — ORIGINAL simple engine. In `2026-06-17.amxd`(6b85d12d) + `2026-06-17-231811.amxd`(b5286b33). == standalone `archive/sends_follower.2026-06-17.js`. autoDetect + buildRefs + bang self-heal, `outlet(0,"max",maxVal)`. This is the engine founder remembers working.
- `29ff530b` (5808 B, 140 ln) — +Follow Mode (Peak/Total). In `-002025`(3f31990a) + `-002423-frozen`(7ca595b4). Same engine + sum/clamp.
- `3bc5ab8e` (12303 B, 245 ln) — +observers (self-healing return-index). In `-110402-frozen`(bd21b848) + `-113530-frozen`(5a8ba853).
- `c6347532` (22368 B, 429 ln) — +single Map button. In `-121509-frozen`(6f156eab).
- `53bdbfbd` (24242 B, 470 ln) — +8-slot mapper. In ALL later frozen archives AND on-disk canon `Max Devices/sends_follower.js`. THIS is in suspect `7ebe7d15`.
**MECHANISM (verified by full graph-walk):** JS is FROZEN-EMBEDDED (dlst), NOT loaded from disk. Restoring an old .amxd DOES swap the engine.
**DECISIVE: follow path is byte-IDENTICAL `b5286b33`(orig) vs `7ebe7d15`(suspect):** `qmetro 33`(obj-35)→`js`(obj-46) in0 → `route max`(obj-47)→`change 0.`(obj-48)→`send ---max_send`(obj-50). `receive ---max_send`(obj-7)→`* 127.`(obj-11_scale)→`prepend set`(obj-11_set)→dial(obj-3, in presentation). `bang()` value-loop + `outlet(0,"max",result)` identical in EVERY JS version. NO structural regression in the follow chain, NO output-zeroing change, NO disk-vs-embed flip.
**ROOT FRAGILITY (same in all eras):** `qmetro 33` (only poller of bang()) is started ONLY by patch detect chain firing once: `live.thisdevice/loadbang`(obj-21/22)→deferlow→delay50/150/300→`live.path`(obj-30)→`route return_tracks`(obj-38)→...→`change -1`(obj-41)→`prepend build`(obj-42)→`t l b b`(obj-43) out1→`"1"`(obj-44)→`qmetro 33`. `obj-34 "1"` has NO input (orphan). If detect races/swallowed (`change -1`) on this set → qmetro never starts → never follows. Simple JS bang() has extra autoDetect self-heal BUT only runs if qmetro already going.
**HYPOTHESIS:** not a code regression — the suspect's heavy `53bdbfbd` JS runs observers+Task+selParam on load; if its init/resync throws on this set the engine can stall after building refs (refs built, then bang resync errors). Best fix = revert engine to the SIMPLE proven `4f74ef1f`/`29ff530b` (no mapper/observer surface). **Top candidate to restore+test: `SendsFollower.2026-06-17-231811.amxd` (b5286b33, JS 4f74ef1f) — simplest proven engine + Follow Mode patcher. Backup: `SendsFollower.2026-06-18-002423-frozen.amxd` (7ca595b4, JS 29ff530b).** If neither follows either, the bug is environmental (device not on a return at load / detect race), not the JS — then fix = make qmetro 33 autostart (`@active 1` or loadbang→"1"→obj-35 directly, bypass detect dependency).


Standalone project, separate from / unrelated to Control XL & Instrument Follower. Project wiki =
`~/Brain/Sends Follower/wiki/` (entities/concepts/index/log; docs in English, run Vale after edits).
Wiki has the functional model (LiveAPI send-gather → max → live.remote~ + buses); don't duplicate.

## Paths
- Device (edit in place): `~/Music/Ableton/User Library/Max Devices/SendsFollower.amxd`
- Archive (dated pre-edit backup, never overwrite): `~/Brain/Sends Follower/raw/archive/SendsFollower.YYYY-MM-DD[-HHMMSS].amxd`
- raw mirror (immutable, read-only): `~/Brain/Sends Follower/raw/SendsFollower.amxd`
- Rack wrapper: `~/Music/Ableton/User Library/Presets/Audio Effects/Audio Effect Rack/SendsFollowerRack.adg`
  (chain = Sends Follower → stock LFO). Note older wiki text says `MaxSendsFollower.adg` — actual file is `SendsFollowerRack.adg`.
- Embedded JS on-disk canon copies: `Max Devices/sends_follower.js` (5808 B, since Follow Mode 2026-06-17), `Max Devices/sf_version_check.js` (3106 B).

## Container layout (frozen, mx@c, like Control XL)
- `ampf` header → JSON @48 (BASE 0x20, of32=16) → embedded files → `dlst` dir + `mdat`.
- Resources in dlst (3 after 2026-06-17): patcher (JSON), `sends_follower.js` (TEXT), `sf_version_check.js` (TEXT).
- Invariants: `ptch`(LE@0x1C)=filesize−0x20; `0x30 + mx@c`(BE@0x2C) − dlst_pos = **16**; `dlst total` field = **N_dire×108 + 8** (2→224, 3→332).

## ⚠️ dlst dire record structure (CONFIRMED — reusable craft, see also amxd-format.md Path B)
Each field = `tag(4) + len(4 BE) + value(len−8)`. dire record = `dire + 108(BE) + 100B payload`.
Payload (fixed 100 B): `type`(ln12,4B 'JSON'/'TEXT') + `fnam`(ln28, **20B value null-padded → filename ≤19 chars + null**) + `sz32`(ln12,4B BE) + `of32`(ln12,4B BE, rel to BASE 0x20) + `vers`(ln12) + `flag`(ln12; JSON=17, TEXT=0/8) + `mdat`(ln12, checksum — Live ignores; new file can use 0).
**dires are FLAT (top-level), not nested.** Parse by walking TLV from `dlst+8` to EOF, not by trusting declared dire-len. `dlst total` field is NOT the byte length of payload (it's N×108+8); ignore it when parsing, recompute when writing.
**Filename ≤19 chars** (fnam value 20B incl. null) — `sends_version_check.js` (22) didn't fit → renamed `sf_version_check.js` (19). Check name length when adding a resource.
To add an embedded file (Path B): append body after last existing embedded file; new of32 = prev_end−BASE; rebuild whole dlst with all 3 dire (patch JSON sz32=L1+1, shift trailing-file of32 by ΔL_json, add new dire); patch ptch + mx@c; keep `0x30+mxc−dlst=16`.

## Version-check feature (added 2026-06-17) ✅
Mirror of Control XL update-check, **minus** `hdr_show`/`hdr_hide` (no header object here).
- 11 boxes / 10 lines added: `version_node`(`node.script sf_version_check.js @autostart 1`), `vlink_route`(`route dot url`), `vdot_sel`(`sel 0 1`), `version_link`(textbutton **"New Version"**, mint `[0.235,0.847,0.659,1.0]`, hidden:1, presentation_rect `[46,2,70,16]`), `vlink_show/hide`(`script show/hide version_link`), `vlink_thispatcher`, `vlink_fallback`(`loadmess https://library.gumroad.com`), `vlink_prepend`(`prepend set`), `vlink_store`(message, silent-store), `vlink_open`(`;\rmax launchbrowser $1`).
- Click path = Control XL silent-store v2: url → prepend set → store(message left inlet) → bang on click → vlink_open `$1` from left inlet. Don't use bare `v` (re-emits on receipt → opens browser unprompted).
- JS `sf_version_check.js` = Control XL `version_check.js` byte-for-byte EXCEPT header comment + 2 consts: `DEVICE_VERSION='1.0'`, `URL='https://fadercraft.com/api/sends-follower.json'`. Emits `dot 1/0` + `url <link>` on outlet 0, single `maxApi.post` diag, re-check 30 min.
- Release coupling: bump `DEVICE_VERSION` + manifest `latest` together; button lights when `latest > DEVICE_VERSION`.

## Follow Mode (Peak / Total) feature (added 2026-06-17; relabelled 2026-06-18) ✅
- Mode switch for the follow algorithm: **Peak** (per-return maximum of all sends, original behavior, default = index 0) ↔ **Total** (sum of all sends to the return, **clamped to 1.0** so downstream `scale 0. 1.` stays in range and percent monitor ≤ 100%, index 1). **Labels were "Max"/"Sum" until 2026-06-18 → renamed Peak/Total (cosmetic only; index↔behavior mapping UNCHANGED, Peak=0=old Max, Total=1=old Sum). JS handler `mode 0/1` untouched.**
- **Parameter name = "Mode"** (longname+shortname; was "Follow Mode" until 2026-06-18). **varname stays `follow_mode`** (cords are by object-id, not varname — never break them; no saved preset existed so longname change is safe).
- **Presentation label "Mode"** (plain `comment`, Arial Bold 9, grey `[0.74,0.74,0.74,1]`, presentation_rect `[7,134,60,14]`, varname `mode_label`) sits directly above the switch. Added 2026-06-18 (+1 box). Device had NO existing comment/live.comment idiom — used a Live-theme grey comment. To make room the switch moved **134→150** (`[7,150,116,17]`); dial bottom=133, label 134..148, switch 150..167, openrect height 169 — no overlaps with dial/switch/"New Version" button.
- 4 boxes / 4 lines: `follow_mode` (**live.tab**, presentation_rect now `[7,150,116,17]`, parameter_enable=1, enum `["Peak","Total"]`, `parameter_type` 2 / longname "Mode", `parameter_initial` [0] → saved with set + preset, default Peak); `mode_prepend` (`prepend mode`); `mode_loadbang` (`loadbang`); `mode_delay` (`delay 300`).
- Wiring: `follow_mode` outlet0 (index) → `prepend mode` → `js` (obj-46) inlet0. Initial value reaches JS on load via `loadbang → delay 300 → follow_mode` (bang makes live.tab re-emit its current/saved index). JS `followMode` also inits to 0 (Max), so even if the bang were late the default is correct.
- JS (`sends_follower.js`): added `var followMode=0`, new `function mode(m)` (parses "mode 0/1"), `bang()` now accumulates sum when followMode==1 with `if(result>1.0)result=1.0`, else max as before. **Output label `"max"` PRESERVED** (`outlet(0,"max",result)`) — downstream `route max`/`---max_send`/percent monitor depend on it; only the computed value changes.
- Percent at Sum ≤ 100% confirmed: clamp to 1.0 → `scale 0. 1. 0. 100.` → ≤ 100.

## Self-healing return-index fix — FROZEN 2026-06-18 ✅
- **DONE (re-freeze):** observer-based `sends_follower.js` (`3bc5ab8e`, 12303 B) embedded into device via Path B, replacing old `29ff530b` (5808 B, no observers). ΔL=+6495. Patcher untouched (65/65). `sf_version_check.js` (`a5d905fc`, 3106 B) kept. Container: JSON of32=16/sz32=32784 (unchanged); `sends_follower.js` of32=32800/sz32 5808→12303; `sf_version_check.js` of32 38608→45103/sz32 3106. ptch+6495, mx@c+6495, dlst total=332, `0x30+mxc−dlst=16`, ptch=filesize−0x20. Result device md5 **`bd21b848…`, 48585 B**, self-contained. Frozen archive: `raw/archive/SendsFollower.2026-06-18-110402-frozen.amxd`.
- **What it fixes:** return-index was resolved ONCE and stuck → device kept following first return's sends after move/add/del/reorder; reload revived once then froze again. Now JS owns detection (`detectReturnIndex` via `this_device canonical_parent` → `return_tracks (\d+)`, -1 on non-return) + 3 LiveAPI property-observers (device path `name`, `live_set` `return_tracks`, `live_set` `tracks`) + `resync(force)` rebuild-on-change with `rebuilding` reentrancy guard + anti-race `bang()` re-sync every `RESYNC_MS=500`. Patch `build N` now a hint only. Contract preserved: 1 in/out, `outlet(0,"max",result)` label intact, Follow Mode (`followMode`, Sum clamp `result>1.0`→1.0).
- Wiki: `concepts/self-healing-return-index.md` rewritten (script-side observer detection = source of truth, needs-verification resolved); `wiki/log.md` entry 2026-06-18.

## (historical) PENDING RE-FREEZE: "live target" fix in external canon JS (2026-06-18) — DONE, see above
- **BUG (founder-observed):** device only ever listens to sends of the FIRST return, regardless of which return it sits on; dragging it to a neighbour return revives it once, then it freezes again and ignores later-added tracks/returns.
- **ROOT (confirmed in patch + JS):** return-index was determined ONCE and stuck in TWO places:
  1. Patch chain (`obj-21 live.thisdevice`/`obj-22 loadbang → deferlow → delay 50/150/300 → obj-27 "path this_device" → obj-30 live.path → obj-31 live.object → obj-36 route path → obj-37 zl.slice 1 → obj-38 route return_tracks → obj-39 unpack i s i → obj-40 int → obj-41 change -1 → obj-42 prepend build → obj-43 "t l b b" → js "build N"`). Runs ONLY on load; after first success `obj-43` out2 sends `0 → obj-33 qmetro 500`, STOPPING the path re-poll. `change -1` (obj-41) suppresses repeats. So patch index never recomputes after first hit. Polling banger = `obj-34 "1" → obj-35 qmetro 33 → js` (33 ms).
  2. JS old `bang()` ran autoDetect ONLY when `sendRefs.length === 0`; after first build it never re-detected. `buildRefs()` was a static snapshot of `live_set tracks` → later add/remove tracks invisible.
  Drag revives because reload re-fires loadbang AND zeroes sendRefs — only on reload, not on move-without-reload nor on adding tracks.
- **FIX (external canon `sends_follower.js` ONLY, NOT frozen yet):** chose **LiveAPI property-observers** over throttled bang() probing. Observers fire only on real change (cheap, idiomatic) vs creating LiveAPI objects every ~300 ms forever. Three observers via `new LiveAPI(onLiveChange, ...)`: device path (`this_device canonical_parent`, prop `name`) catches device MOVE; `live_set` prop `return_tracks` catches return add/del/reorder (index shift); `live_set` prop `tracks` catches ordinary-track add/del (send set changes). `resync(force)` recomputes index via `detectReturnIndex()` + track count, rebuilds only on index-change OR count-change, guarded by `rebuilding` reentrancy flag. Anti-race: `bang()` cheaply re-syncs every `RESYNC_MS=500` (one detect + one getcount, no track walk) in case observers/path not yet resolved on cold load. `build N` from patch is now a hint only; JS's own detect is source of truth. Device on non-return/master → `detectReturnIndex` returns -1 → empty sendRefs, no console spam.
- **Contract PRESERVED:** inlets/outlets=1; inputs `build`/`bang`/`mode`; output `outlet(0,"max",result)` label UNCHANGED; Follow Mode (`followMode`, Sum clamp `result>1.0`→1.0) intact.
- **State:** external canon `~/Music/Ableton/User Library/Max Devices/sends_follower.js` REWRITTEN, **md5 `3bc5ab8e895e0c2f94ca1e6382e2e4df`, 12303 bytes on disk** (9472 unicode chars; size up from 5808 B due to observer logic + comments; cyrillic ×2 bytes), `node --check` clean. Frozen device still runs OLD embedded JS (`29ff530b`) — fix applies only after RE-FREEZE with Live CLOSED. Old embedded canon was `29ff530b`/5808 B.
- **RE-FREEZE PLAN (Live must be closed):** embed THIS `sends_follower.js` (`3bc5ab8e`, 12303 B, TEXT) in place of `29ff530b` via Path B; sz32 = 12303; of32 unchanged (still right after JSON); `sf_version_check.js` of32 shifts by Δ(12303−5808)=+6495; patch JSON sz32, ptch, mx@c; keep `dlst total=332`, `0x30+mxc−dlst=16`, ptch=filesize−0x20. Patcher boxes/lines UNCHANGED (65/65) — only the embedded JS body grows. Archive frozen result IMMEDIATELY (lesson from 2026-06-17 incident).

## Map button (SF-direct map to any Live param, BYPASS LFO) — FROZEN 2026-06-18 ✅
- **What:** front-panel **Map** button maps SF's OWN bipolar signal (−100..100, the SAME value driving the LFO leg via obj-16) straight onto a user-clicked Live parameter, via a SECOND `live.remote~` — **additive**, LFO leg (obj-11) untouched. Arm → click any Live param → captured → connected; Unmap (X icon) frees it; target path persists across reload via `pattr`.
- **UI = real stock LFO Map look.** Map button = `live.text` toggle (`appearance=2 mode=1`), exact stock colors from `liveui.map.maxpat` obj-48: idle bg `[0.1569×3,1]` + grey text `[0.549×3,1]`, **armed = orange text `[1.0,0.7098,0.1961,1]`** + orange border. Unmap "X" = `live.text` `usepicture=1 pictures=['multimap-unmap.svg','multimap-unmap.svg']` `usesvgviewbox=1 remapsvgcolors=1` — **REAL stock asset** `…/C74/packages/Max for Live/media/multimap-unmap.svg` (Cycling'74 built-in package, on Max search path on EVERY Live install → resolves by filename, **NOT embedded**, no `can't find file`). Target name label = `comment` (varname `map_label`, accepts `set <name>`). Presentation row at y=172; **openrect height 169→196** (+27). Stock Map button reference patcher: `/Applications/Ableton Live 12 Suite.app/…/C74/packages/Max for Live/patchers/liveui.map.maxpat` (obj-48 Map, obj-47 unmap-X).
- **Patcher (+16 box / +20 line; 66/65 → 82/85):** `map_button`(live.text Map toggle), `map_unmap`(live.text X svg), `map_label`(comment "—"), `map_route`(`route armstate target_path targetname store release`), `map_arm_set`(`prepend set`→button silent), `map_pathprep`(`prepend path`), `map_path`(`live.path`), `map_lblset`(`prepend set`→label), `map_id0`(msg `id 0`), `map_remote`(2nd `live.remote~`), `map_sig`(`sig~` tap from obj-16), `map_pattr`(`pattr map_target @parameter_enable 1 @autorestore 1`), `map_restoreprep`(`prepend restorepath`), `map_arm_send`(`prepend arm`), `map_unmap_tb`(`t b`), `map_unmap_msg`(msg `unmap`). **obj-46 (js) box bumped numoutlets 1→2** (outlet 1 = map control) — REQUIRED or Max drops the wire.
- **Wiring:** obj-16[0]→map_sig→map_remote[0] (signal tap, LFO leg intact); obj-46[1]→map_route; route out0 armstate→`prepend set`→map_button (silent, no feedback loop); out1 target_path→`prepend path`→map_path→[id]→map_remote[1]; out2 targetname→`prepend set`→map_label; out3 store→map_pattr; out4 release→`id 0`→map_remote[1]. Button click→`arm $1`→js inlet0. UnmapX click→`t b`→msg unmap→js. pattr out0 (autorestore)→`prepend restorepath`→js.
- **JS (`sends_follower.js`):** `outlets=2` (0=`max` value UNCHANGED; 1=map control). New: `arm(0/1)`, `unmap()`, `restorepath(...)` (varargs — path stored as TOKEN LIST not one symbol, no space-quoting dependency; sentinel `none`=clear), `selParamObs` on `live_set view selected_parameter` (captures only while `mapArmed`), `captureTarget`/`resolveAndConnect` (emits `target_path` list), `retryResolve` (defer via `Task.schedule(150)`, MAX 20 tries ≈3s — same anti-race as return-index), `emitTargetName`/`persistPath`. `selected_parameter` path CONFIRMED via Song.View. `live.remote~` right inlet = `id n` from live.path out0; **unmap = `id 0`** to right inlet (docs-confirmed).
- **Persist:** path tokens in `pattr` (`@parameter_enable 1 @autorestore 1`) save with device/preset; on load pattr emits list → restorepath → JS re-resolves with defer/retry.
- **Result device md5 `6f156eab973ecec0d9793f794c75cfce`, 68498 B, 82 box / 85 line.** Container Path B: JSON of32=16 sz32=42632; `sends_follower.js` **22368 B** of32=42648 md5 `c63475322970c9f8ea02d01620806551` (node --check clean); `sf_version_check.js` 3106 B of32=65016 md5 `a5d905fc` UNCHANGED. dlst total=332 (still 3 resources, svg NOT embedded), `0x30+mxc−dlst=16`, ptch=68466=fs−0x20. ⚠️ **Path-B gotcha re-learned:** prefix MUST be `d[:48]` (incl. mx@c sub-header at 0x20..0x30), NOT `d[:0x20]` — of32 is relative to 0x20 and JSON sits at 0x20+16=48; cutting at 0x20 shifts JSON 16 B early → first-`{` parse fail. Frozen archive `raw/archive/SendsFollower.2026-06-18-121509-frozen.amxd`. Pre-edit on-disk (Live-unfrozen `55726d24`) archived `…120211-predmap-ondisk.amxd`.
- **Preserved (verified):** obj-11→LFO Offset leg, obj-9 LFO path msg, obj-16 bipolar, obj-47 `route max`, follow_mode live.tab, mode_label "Mode", version_link, observer return-index fix, `outlet(0,"max",…)` label. Base = frozen `5a8ba853` patcher + canon JS `3bc5ab8e` extended.

## 8-slot mapper (replaces single Map button) — FROZEN 2026-06-18 ✅
- **What:** front-panel **8-slot mapping table** (Parameter | X | Map | Min | Max + Mode col), mirrors stock LFO map list. Each slot maps SF's RAW follow value 0..1 onto any clicked Live param, BYPASS LFO, scaled into [Min/100..Max/100]. **8× independent live.remote~** parallel to LFO leg (obj-11 untouched). Single-arm (arming one slot disarms previous). Replaces the single-Map experiment (folded in per founder's explicit request).
- **Base = on-disk user-handedited `c5b736c5` (67606 B, 82/84).** ⚠️ That on-disk file had been re-frozen by Live with a STALE embedded `sends_follower.js` (`29ff530b`, 5808 B, NO observers/NO map) even though the canon `.js` next to it was current — new freeze re-syncs them. Pre-edit archived `…134954-preedit-8slot-ondisk.amxd`.
- **Patcher: 82/84 → 256 box / 274 line.** Removed 16 single-Map boxes; added per slot ×8 (23 boxes): `map_btn_N`(live.text Map toggle `mode 1` `appearance 2`), `map_unmap_N`(live.text X svg), `map_lbl_N`(comment target name), `map_mode_N`(static "Remote"), `slot_min_N`/`slot_max_N`(live.numbox 0..100 `parameter_enable 1` → self-persist), `sr_N`(`route path name arm store release`), `sp_path_N`(prepend path)+`sl_path_N`(live.path), `sp_name_N`/`sp_arm_N`(prepend set), `sp_store_N`(prepend store)+`pattr_slot_N`(`pattr slot_N_path @parameter_enable 1 @autorestore 1`), `sp_restore_N`(`prepend restorepath N`), `sp_rel_N`(msg `id 0`), `remote_N`(live.remote~), `scale_N`(`scale 0. 1. 0. 1.`)+`sig_N`(sig~), `div_min_N`/`div_max_N`(`/ 100.`), `arm_msg_N`(`prepend arm N`), `unmap_tb_N`(t b)+`unmap_msg_N`(msg `unmap N`). Shared: `slot_route`(`route 0 1 2 3 4 5 6 7`), header comments hdr_param/hdr_mode/hdr_range + sub_min/sub_max. `obj-46` js kept numoutlets=2.
- **Wiring:** obj-46[1]→slot_route(demux by slot idx)→sr_N; sr_N out0 path→prepend path→live.path→`id n`→remote_N[1]; out1 name→prepend set→lbl; out2 arm→prepend set→btn(silent); out3 store→prepend store→pattr; out4 release→`id 0`→remote_N[1]. Signal: `obj-7 receive ---max_send`(RAW 0..1)→scale_N[0]→sig_N→remote_N[0]. Min→div_min→scale_N[3](outlow); Max→div_max→scale_N[4](outhigh). btn click→`prepend arm N`→js; X→t b→`unmap N`→js; pattr autorestore→`prepend restorepath N`→js.
- ⚠️ **Signal-source change vs single-Map:** slots tap RAW `obj-7` (0..1), NOT bipolar obj-16 (−100..100) — so Min/Max behave like stock LFO. (Single-Map tapped obj-16.)
- **JS (`sends_follower.js`):** slot-indexed map subsystem, `NSLOTS=8`. New/changed: `arm(slot,a)` (single-arm), `unmap(slot)`, `restorepath(slot, ...tokens)`, `captureTarget(slot,path)`, `resolveAndConnect(slot)`, `retryResolve(slot)` (Task.schedule 150ms ×20, anti-race), `emitTargetName(slot)`, `persistPath(slot)`, arrays `slotPath[]`/`slotRetry[]`, `armedSlot`. **Outlet-1 msgs now LEAD with slot idx** (`<slot> path|name|arm|store|release …`). `outlet(0,"max",result)` + Peak/Total + observer return-index UNCHANGED. node --check clean, md5 `53bdbfbd26cdfef548e6238b0cf425f6`, 24242 B.
- **Layout:** openrect `[…,169]` → `[0,0,340,385]` (wider+taller, like LFO map list — agreed for full list). Top controls (dial/Mode/New Version) unchanged; table below (header y=200, rows y=227 step 18 ×8). Visually close, NOT 1:1 (Live renderer proprietary).
- **Container Path B (4 resources, order JSON→svg→version→sends_follower):** JSON 57668→147778 (sz32 147779, of32 16); `multimap-unmap.svg` 535 B (`1a31f546`, EMBEDDED, of32 147795); `sf_version_check.js` 3106 B (`a5d905fc`, of32 148330); `sends_follower.js` 24242 B (`53bdbfbd`, of32 151436). `dlst total`=440 (4×108+8). **Result device md5 `194ff283e786d24f481020461fdba4cc`, 176150 B, 256/274**, self-contained. Frozen archive `raw/archive/SendsFollower.2026-06-18-135002-8slot-mapper-frozen.amxd`.
- ⚠️ **REUSABLE CRAFT (multi-file Path B):** when BOTH JSON and an embedded JS change size, `mx@c += ΔJSON` is WRONG — mx@c covers the WHOLE data region (JSON+all embedded files). Derive structurally: `mx@c = (dlst_pos + 16) − 0x30` (since invariant `0x30+mxc−dlst=16`). Compute of32 per resource by walking the new layout from of_json=16, NOT by delta. prefix = `d[:48]` (incl mx@c sub-header). Convert bytearray dire tags to `bytes()` for dict keys.
- ⚠️ **All 8 slots are FULLY functional** (arm→selected_parameter→capture→live.path→remote, Min/Max scale via scale+/100, persist via 8× pattr, unmap via id 0). No stubs. Mode column = intentional static "Remote" label (SF has one mode; stock LFO shows source mode there).

## Map FIX (selected_parameter → live.map) + UI cleanup — FROZEN 2026-06-18 ✅
- **ROOT CAUSE of dead Map buttons:** the 8-slot mapper (and the earlier single-Map) captured the user-clicked param by **observing `live_set view selected_parameter`** in JS. That mechanism is unreliable: clicking arbitrary params (esp. mixer faders/sends) does NOT consistently update `selected_parameter`, so the observer never fired → arm worked, capture never happened. The wiring + JS logic were BOTH structurally correct (verified by graph-walk + node mock: `arm(0,1)`→`onSelParamChange`→emits `[0,"path",…]`) — the bug was the **capture hook**, never validated on hardware (table was cut off at 169px until WIDE layout, so nobody had clicked Map before).
- **FIX = use stock `live.map`** (the object the stock LFO Map button actually uses — found in `…/Max for Live/patchers/liveui.map.maxpat` obj-3 `live.map @strict 1`). `live.map` implements Live's NATIVE map-grab: Map click → `prepend mapping` → live.map arms → user clicks ANY param → live.map captures it natively → **outlet 1 = `id n`** (→ live.remote~ right inlet directly, NO live.path needed), **outlet 2 = name**, **outlet 3 = mapping state (int)**, out0=path, out4=range. `@strict 1` = ignore clicks inside own device. `saved_object_attributes:{_persistence:1}` = **target persists across reload NATIVELY** (killed all our pattr/restorepath/retryResolve machinery).
- **X-visibility (show only when mapped):** stock chain = live.map out2 (name) → `zl compare <none>` (1=unmapped/0=mapped) → `hidden $1` → X box inlet 0. Simplified to 2 objs: `zl compare <none>` → `hidden $1`. X box ships `hidden:1`; mapped slot un-hides via `hidden 0`. Map button visual = live.map out3 (state) → `map_btn_N` inlet 0 (lights toggle).
- **Patcher 256/274 → 190 box / 193 line.** DELETED per slot ×8 (13 boxes): `arm_msg_N, unmap_tb_N, unmap_msg_N, sl_path_N, sp_path_N, sp_name_N, sp_arm_N, sp_store_N, sp_rel_N, pattr_slot_N, sp_restore_N, sr_N, map_lbl_N`(Parameter col); + shared `slot_route`, `hdr_param`. ADDED per slot ×8 (5 boxes): `livemap_N`(live.map @strict 1, _persistence 1), `mapprep_N`(prepend mapping), `xcmp_N`(zl compare <none>), `xhide_N`(message hidden $1), `unmapmsg_N`(message unmap). KEPT per slot: map_btn_N, map_unmap_N, map_mode_N(static "Remote"), slot_min_N/slot_max_N, div_min_N/div_max_N, scale_N, sig_N, remote_N. Net −106+40 box, −153+72 line.
- **New wiring/slot:** map_btn_N→mapprep_N→livemap_N[0]; map_unmap_N→unmapmsg_N→livemap_N[0]; livemap_N out1(id)→remote_N[1]; out3(state)→map_btn_N[0]; out2(name)→xcmp_N→xhide_N→map_unmap_N[0]. Signal chain UNCHANGED (obj-7→scale_N→sig_N→remote_N[0]; slot_min/max→div→scale outlow/outhigh). **JS NOT TOUCHED** (`53bdbfbd`, 24242 B, byte-identical embedded) — its slot arm/capture funcs are now dead code (nothing calls them); obj-46 outlet1 now unconnected. Follow/Peak-Total/observer-return-index/LFO leg all intact.
- **UI per founder:** removed "%" (Min%/Max%→Min/Max), removed entire Parameter column (8 map_lbl + hdr_param), X conditional (hidden until mapped), column order **Map(x130)|X(174,hidden)|Mode(192)|Min(240)|Max(282)**. hdr_range hidden. openrect `[0,0,444,169]`→`[0,0,328,169]` (narrower — Parameter col gone). **MAX(y+h)=166 ≤167 ✓, MAX(x+w)=320, width 328 (margin 8).**
- **Container Path B (4 res, order JSON→svg→version→sends_follower):** JSON 147757→**116572** (sz32 116573, of32 16); svg 535 of32 116589; sf_version_check.js 3106 of32 117124; sends_follower.js 24242 of32 120230. dlst total=440, `0x30+mxc−dlst=16` (mxc=144472, derived `dlst_pos+16−0x30`), ptch=144912=fs−0x20. NO trailing mdat chunk (file ends at dlst). **Result device md5 `fb0d2975fa05f72c00b422caa5a2ce08`, 144944 B, 190 box / 193 line**, self-contained (svg+both JS embedded byte-for-byte). Pre-edit `4eb57a06` archived `…143224-preedit-mapfix.amxd`; frozen result archived `raw/archive/SendsFollower.2026-06-18-144453-mapfix-livemap-frozen.amxd`.
- ⚠️ **REUSABLE CRAFT:** for any "map any Live param from an M4L device face" → use **`live.map`** (native grab), NOT `selected_parameter` observer. live.map out1=id→remote right inlet, out2=name, out3=state. `_persistence 1` = free reload-persistence. X-show = `zl compare <none>`+`hidden $1`. Reference patcher = stock LFO `liveui.map.maxpat`.

## WIDE layout (horizontal, fits in Live's 169px shelf) — FROZEN 2026-06-18 ✅
- **What:** founder chose WIDE layout. Reflowed presentation so EVERYTHING sits inside Live's fixed 169px device-view height. Left column = dial + "Mode" label + Peak/Total live.tab + (hidden) version button; right area = the full 8-slot table (header Parameter/Range[Min%/Max%]/Mode + 8 rows). Previously the table was stacked BELOW the dial (y 200..369) → invisible in Live (cut at 169). Device scrolls horizontally in chain.
- **ONLY presentation_rect (57 boxes) + openrect changed.** Proven by full diff vs prior `194ff283`: `lines` byte-identical (274), id-set identical (256), the ONLY box key that differs across all boxes = `presentation_rect`, the ONLY patcher-level change = `openrect`. Logic/wiring/varname/JS/8 slots all untouched. Embedded svg/version/sends JS byte-for-byte unchanged (md5 `1a31f546`/`a5d905fc`/`53bdbfbd`, both JS `node --check` clean).
- **openrect `[0,0,340,385]` → `[0,0,444,169]`.** Layout geometry: left col x5..125 (dial `[5,16,116,116]` bottom 132, mode_label `[5,134,60,14]`, follow_mode live.tab `[5,150,116,16]` bottom 166, version_link hidden `[5,2,70,14]`). Right table origin TX=134; cols: label x134 w116, unmap-X x252 w14, Map x268 w40, Min x312 w38, Max x352 w38, Remote x392 w44 (x+w max=436). Header y=4 h12 (hdr_param/hdr_range over Min..Max/hdr_mode), sub Min%/Max% y=16 h11, rows y=29 step17 h15 ×8 (row7 y148 bottom163). **MAX(y+h)=166 ≤167 ✓, MAX(x+w)=436 → openrect width 444 (margin 8).**
- **Container Path B (only JSON-patcher resized; JSON 147778→147757, ΔL=−21):** JSON sz32 147779→147758 of32=16; svg 535 of32 147795→147774; sf_version_check.js 3106 of32 148330→148309; sends_follower.js 24242 of32 151436→151415. dlst total=440 (4 res), `0x30+mxc−dlst=16` (mxc=175657, derived `dlst_pos+16−0x30`), ptch=176097=fs−0x20. ⚠️ **CRAFT NOTE:** this device has NO trailing `mdat` CHUNK — the `mdat` substring is only a subfield inside each dire payload. File ends exactly at the dlst chunk end (dlst_pos + 8 + N×108). Don't append `old[mdat:]` as a tail (caused a bogus +315 B first pass). dlst chunk spans to EOF.
- **Result device md5 `4eb57a065bf9499f4e3bb29617bac00d`, 176129 B, 256 box / 274 line**, self-contained. Frozen archive `raw/archive/SendsFollower.2026-06-18-142514-wide-layout-frozen.amxd`. Pre-edit on-disk (`194ff283`) archived `…142152-pre-wide-layout.amxd`.

## Map UI reconstruction under LFO reference (name-feedback + LCD panel + Mod/Remote) — FROZEN 2026-06-18 ✅
- **Base = on-disk UNFROZEN user-edit `ac1dc6f4` (157935 B, 190/193).** ⚠️ User had opened device in Max → Live re-saved it UNFROZEN: header `@0x20` is `{` (JSON starts at 0x20, NO mx@c subheader, NO dlst, NO embedded files; whole body 0x20..trailing-null = JSON). Parse unfrozen with `JS=0x20` not 48. Re-freeze re-embeds resources. User's coord edits (vs prev frozen `fb0d2975`): version_link `[5,2]`→`[54,4]`, mode_label `[5,134]`→`[5,118]`, follow_mode `[5,150]`→`[5,134]`, header heights bumped, slot_min/max h 15→12. **PRESERVED ALL** (only changed what 3 tasks required). map_unmap_1 runtime-visible reset to hidden:1. Pre-edit archived `…162815-preedit-mapui-recon-ondisk.amxd`.
- **TASK 1 — name in Map button (stock-true):** per slot, `livemap_N[2]` (name, fanout next to existing →xcmp_N) → `namesub_N`(`substitute <none> Map`) → `nameset_N`(`prepend set`) → `map_btn_N[0]`. Mapped→button shows target name; unmapped→"Map". This IS the stock LFO mechanism (liveui.map.maxpat: live.map name out2 → `p SetName`=`substitute <none> Map`→TruncateText→`text $1,texton $1`→live.text). Map button also got stock colors (idle grey `[0.549×3,1]`, armed orange `[1,0.7098,0.196,1]`, bg `[0.157×3,1]`, orange border); removed empty `saved_attribute_attributes` placeholders that nullified colors.
- **TASK 2 — black LCD panel:** `lcd_panel` (`maxclass panel`, `mode 0`, `rounded 7`, `bgcolor` dark + `saved_attribute_attributes.bgfillcolor.expression="themecolor.live_lcd_bg"` = stock LCD bg), presentation_rect `[128,2,196,164]` (y+h=166), **inserted as FIRST box** (paint order = behind table). Pattern lifted from stock `liveui.multimap.maxpat` panel obj-131.
- **TASK 3 — Mode=Remote↔Mod + conditional Range + true Mod modulation:** `map_mode_N` static comment "Remote" REPLACED by enum `live.text` toggle (faithful copy of stock LFO mode toggle `Abl.MapUi` obj-2: `appearance 2`, `outputmode 1`, `text`/`texton`="Remote"/"Mod", `automation`/`automationon`, teal accent `activebgoncolor [0,0.933,1,1]`, `parameter_enable 1` enum `["Remote","Mod"]` `parameter_initial [0]`=Remote default → native persist). outlet0=0 Remote/1 Mod.
  - **Conditional Range (show/hide via thispatcher):** mode out0 → `modesel_N`(`sel 0 1`): state0(Remote)→`msgrem_N`(`script show slot_min/max, script hide bipol/depth`); state1(Mod)→`msgmod_N`(`script hide slot_min/max, script show bipol/depth`) → `modetp_N`(thispatcher). New Mod controls overlay Min/Max columns: `bipol_N`(± enum live.text, "+"/"−" teal, hidden:1, over Min x240) + `depth_N`(live.numbox 0..100 default 50, hidden:1, over Max x282). Ship state = Min/Max visible, ±/depth hidden (matches Remote default). Mode toggles are params (`parameter_initial_enable 1`) → Live re-emits saved value on load → show/hide + id-route re-sync automatically (no loadbang; banging live.text would TOGGLE not re-emit — don't).
  - **TRUE Mod modulation via `live.modulate~`** (`…/C74/externals/m4l/live.modulate~.mxo`, on every install, portable; this is the REAL additive bipolar modulator the stock LFO uses for Mod, vs `live.remote~` which OVERWRITES). Per slot `mod_N`(live.modulate~) parallel to existing `remote_N`(live.remote~, Remote leg UNCHANGED). Mod signal: `obj-7`(follow 0..1)→`modscale_N`(`scale 0. 1. -1. 1.` bipolar)→`modmul_N`(`* 0.` ×k)→`modsig_N`(sig~)→`mod_N[0]`. k=(depth/100)×sign: `depth_N→/100.→kmul_N[0]`; `bipol_N→signif_N`(`if $i1==1 then -1. else 1.`)→`kmul_N[1]`; `kmul_N→modmul_N[1]`. So Mod = signed bipolar ±depth% follow offset around base value (TRUE mod, not overwrite).
  - **id routing by mode (single captured target → active leg only):** REMOVED old direct `livemap_N[1]→remote_N[1]`. Now `livemap_N[1]`(id)→`idsetreg_N`(prepend set)→`idreg_N`(message register)→`idgate_N`(`gate 2 1`); gate out0→`remote_N[1]`, out1→`mod_N[1]`. Gate control = `map_mode_N→modeplus_N`(`+ 1`)→idgate[0] (1=remote,2=mod; gate default open=1=remote). On mode change `modesel_N` sends `id 0` to the INACTIVE leg (id0a→remote / id0b→mod) AND bangs `idreg_N` to re-emit stored id to active leg. ⚠️ Fan-out ORDER: `map_mode→modeplus` line `order=1`, `map_mode→modesel` line `order=2` so gate control set BEFORE id re-emit.
- ⚠️ **Mod semantics HONESTY:** uses real `live.modulate~` (additive signed offset around base, correct). The `* 0.` modmul stores k in right inlet; follow streams into left inlet continuously so k re-applies — when ONLY depth/± change (not follow) the new k applies on next follow tick (fast, fine). Acceptable; not perfectly instantaneous on a paused follow. Remote leg byte-identical to prior (plain live.remote~, scale [Min/100,Max/100], founder-confirmed character).
- **JS NOT TOUCHED** (`sends_follower.js` `53bdbfbd`, 24242 B byte-identical; Mod done in patcher via live.modulate~, not JS). svg `1a31f546`, `sf_version_check.js` `a5d905fc` byte-identical.
- **Patcher 190/193 → 359 box / 425 line** (+169 box/+232 line: 1 panel + per-slot ×8 ×21 new objs). openrect `[0,0,328,169]`→`[0,0,332,169]` (content x+w max 324, panel 324, margin 8). **MAX(y+h)=166 ≤167 ✓, MAX(x+w)=324.** Column order UNCHANGED Map(130)|X(174,cond)|Mode(192,now toggle)|Min(240)|Max(282); ±/depth overlay Min/Max in Mod mode.
- **Container Path B (4 res, order JSON→svg→version→sends_follower):** JSON 116572→**218289** (sz32 218290, of32 16); svg 535 of32 218306; sf_version_check.js 3106 of32 218841; sends_follower.js 24242 of32 221947. dlst total=440, `0x30+mxc−dlst=16` (mxc=246189 = dlst_pos+16−0x30), ptch=246629=fs−0x20, NO trailing mdat (file ends at dlst). **Result device md5 `8a9203dd6cb46bf576b1fb344905e81d`, 246661 B, 359 box / 425 line**, self-contained. Frozen archive `raw/archive/SendsFollower.2026-06-18-164207-mapui-recon-frozen.amxd`.
- **Preserved (verified post-freeze):** obj-11 LFO leg, obj-16 bipolar, obj-47 route max, obj-46 js, obj-7 receive ---max_send, follow_mode Peak/Total, mode_label/version_link at USER coords, 8 slots full map-capture+scale chains, X conditional (zl compare <none>+hidden $1), observer return-index (in untouched JS). Both JS `node --check` clean.

## 4 visual mapper edits under reference (name feedback fix + teal Mode/± + transparent % numboxes) — FROZEN 2026-06-18 ✅
- **Base = on-disk frozen user-edit `2b20a1b0` (319178 B, 355/425).** User (in Max) had: (a) DELETED 4 column-header comments (`hdr_mode`/`hdr_range`/`sub_min`/`sub_max`); (b) reflowed all 8 table rows UP — y 29/46/…/148 → ~5.9/22.9/…/125 (step ~17, no header row), x nudged ~0.07; (c) depth_N pres h 12→15. **PRESERVED ALL** (only touched what 4 tasks needed; rows stay at user y). ⚠️ **Live re-save had re-embedded STALE `sends_follower.js` `29ff530b` (5808 B, old 5-slot, NO observers/NO map subsystem)** even though canon next to device was current — the running device's map buttons were DEAD + return-index frozen. **This freeze RESTORES correct canon `53bdbfbd` (24242 B).** (sf_version_check.js was NOT on-disk next to device — only embedded; kept from prior embed.) Pre-edit archived `…165808-preedit-4visual-ondisk.amxd`.
- **TASK 1 — name in Map button via `text`/`texton` (THE FIX) + fill state.** Root cause of broken name feedback: old chain `livemap_N[2]→namesub_N(substitute <none> Map)→nameset_N(prepend set)→map_btn_N` used `prepend set` — **live.text toggle IGNORES `set` for its label** (label = `text` off-state + `texton` on-state attributes). Stock LFO (`liveui.map.maxpat` `p SetName`) uses message `text $1, texton $1`. FIX: DELETE `nameset_N`; ADD per slot `namet_N`(`prepend text`) + `namet2_N`(`prepend texton`), both fed by `namesub_N[0]`, both → `map_btn_N[0]`. `prepend text` on list "Fade In" → `text Fade In` (sets off-label); `prepend texton` → `texton Fade In` (on-label). Robust for multi-word names (whole list prepended, not `$1` first-token). Fill state: reused `xcmp_N`(`zl compare <none>` = 1 unmapped/0 mapped) fanned to NEW `colsel_N`(`sel 0 1`): out0(mapped)→`colmapped_N`(msg: bg orange α1 + text dark) , out1(unmapped)→`colunmap_N`(msg: bg orange **α0** + text orange) → `map_btn_N[0]`. map_btn_N default attrs = unmapped look (transparent bg, orange border kept, orange text, "Map"). NET +5 box/slot (namet, namet2, colsel, colmapped, colunmap), −1 (nameset); +9 line/slot, −2.
- **TASK 2 — Mode toggle color #03C3D5.** `map_mode_N` `activebgoncolor` `[0,0.933,1,1]`→`[0.011765,0.764706,0.835294,1.0]` (= #03C3D5). attr-only.
- **TASK 3 — bipol_N square ± teal.** `bipol_N` (live.text +/- enum) → `text="±"`/`texton="±"` (single glyph both states; state via fill vs outline), pres+patch w 38→**15** (square 15×15), `activebgoncolor`+`bordercolor`+`textoffcolor`+`textcolor`=teal #03C3D5. Underlying enum stays "+"/"-" (logic via `signif_N if $i1==1 then -1. else 1.` UNCHANGED). attr+size only.
- **TASK 4 — slot_min/slot_max/depth → transparent + "%".** `parameter_unitstyle: 5` (=Percent, native "%" suffix) on each; `bgcolor`/`activebgcolor`/`bordercolor`/`tricolor`/`tribordercolor`/`activetricolor` α0 (transparent field/frame, only digits show); `textcolor` teal #03C3D5. Registered in `saved_attribute_attributes`. attr-only.
- **Patcher 355/425 → 387 box / 481 line** (+32 box = +5×8−8; +56 line = +9×8−2×8). openrect UNCHANGED `[0,0,332,169]` (no geometry growth — bipol shrank, rows already user-placed). **MAX(y+h)=166 ≤167 ✓ (lcd_panel `[128,2,196,164]`), MAX(x+w)=324.**
- **Container Path B (4 res JSON→svg→version→sends_follower):** JSON 309240→**272929** (shrank: user's Live re-save had verbose floats; `json.dumps(indent=1)` is leaner) sz32 272929 of32 16; svg 535 of32 272945 (`1a31f546`); sf_version_check.js 3106 of32 273480 (`a5d905fc`); **sends_follower.js 24242 of32 276586 (`53bdbfbd` — RESTORED canon, was stale 5808 `29ff530b`)**. dlst total=440, mxc=300828 (=dlst_pos+16−0x30), ptch=301268=fs−0x20, NO trailing mdat (EOF==dlst_end). **Result device md5 `c35eb20bcfb70957880604fdf7767dc6`, 301300 B, 387 box / 481 line**, self-contained (both JS `node --check` clean, svg embedded). Frozen archive `raw/archive/SendsFollower.2026-06-18-170149-4visual-frozen.amxd`.
- **Preserved (verified post-freeze):** obj-11 LFO leg, obj-16 bipolar, obj-47 route max, obj-7 receive ---max_send, 8× live.map / 9× live.remote~ / 8× live.modulate~ (Mod legs), follow_mode Peak/Total live.tab, lcd_panel, conditional Range (msgrem/msgmod show/hide bipol/depth — bipol still named in those msgs, square form doesn't break show/hide), js obj-46=`js sends_follower.js`, observer return-index (now in RESTORED JS, was missing in stale embed). 0 orphan lines.
- ⚠️ **REUSABLE CRAFT:** live.text label feedback at runtime = `prepend text` + `prepend texton` (NOT `prepend set` — set is ignored by live.text). Numbox "%" = `parameter_unitstyle:5`. Transparent numbox = α0 on bgcolor/activebgcolor/bordercolor/tricolor/tribordercolor/activetricolor. ⚠️ **Always check embedded `sends_follower.js` md5 after a Live re-save** — Live re-embeds whatever it thinks is current and can revert to a stale 5808-B copy; restore canon `53bdbfbd` on freeze.

## 2 real bugs (name feedback + Mod engage) + bigger font — FROZEN 2026-06-18 ✅
- **Base = on-disk `c35eb20b` (301300 B, 387/481), founder confirmed correct version + Live restarted → bugs real, not cache.** Pre-edit archived `…171903-preedit-2bugs-font.amxd`. JS NOT touched (both bugs pure patcher; embedded `sends_follower.js` `53bdbfbd` byte-identical, installObservers+arm present).
- **BUG #1 — name in Map button FINALLY works (text/texton WASN'T enough).** Prior "4 visual" freeze claimed `prepend text`+`prepend texton` was THE FIX — it WASN'T on hardware. **Read stock `liveui.map.maxpat` (obj-48 Map live.text, obj-81 `p SetName`):** name out of `live.map` outlet 2 → `substitute <none> Map` → `p TruncateText` (`atoi → zl slice 12 → zl join → itoa` = **guarantees ONE symbol**) → message **`text $1, texton $1`** → button. The MISSING piece in my chain was the **single-symbol guarantee**: `prepend text` on a multi-atom name ("Fade In") makes `text Fade In` (malformed/partial), and the `$1`-substitution message is the redraw-triggering form. **FIX:** per slot DELETE `prepend text`/`prepend texton` (`obj-51/52,56/57,61/62,66/67,71/72,76/77,81/82,86/87`); ADD `tosymbol_N` (concats any atoms→1 symbol, keeps spaces) + `namemsg_N` (message `text $1, texton $1`, stock-exact); rewire `livemap_N[2]→namesub_N→tosymbol_N→namemsg_N→map_btn_N[0]`. Net per slot −2 box (del 2 prepend, add tosymbol+namemsg=+2 → 0) … actually +0 box/slot for name (−2+2), lines −4+3=−1/slot. Map button kept `parameter_enable 0` (so text/texton honored; stock's is 1 but its label is enum-driven — non-param is fine for free-text label). Fill-state (xcmp→sel→color msgs) UNTOUCHED.
- **BUG #2 — Mod/Remote leg engages on map-capture WITHOUT toggling Mode.** Symptom: send didn't drive knob until you wiggled Mode. **ROOT (confirmed by graph-walk):** `livemap_N[1](id)→idsetreg_N(prepend set)→idreg_N(empty message)→idgate_N[1]`. `set <id>` into a message box **STORES silently, no output**; `idreg_N` only emitted when BANGED, and the sole banger was `modesel_N` (Mode-change). So captured id sat in register, leg never got id until a Mode toggle banged it. **FIX:** insert `idtrig_N` (`t b l`) between `livemap_N[1]` and the register. trigger fires R→L: outlet1=`l`(id, FIRST)→`idsetreg_N`→sets register; outlet0=`b`(SECOND)→bangs `idreg_N`→gate→active leg engages NOW. On load `_persistence 1` re-emits id on outlet 1 → same path → engages immediately. Gate control = `map_mode_N(+1)→gate[0]` (1 remote/2 mod, default open=remote); on load `map_mode_N` re-emits saved value → `modesel_N` re-bangs register → correct leg (self-corrects if id arrives before mode settles). +1 box/slot, +2 line/slot.
- **FONT bump:** live.text (Map/X/Mode/±) 9→**11**, live.numbox (Min/Max/depth) 8→**10**, mode_label comment 9→**11**, follow_mode live.tab default→**11**. NO box-height growth (live.text rows 16px fit 11pt; numbox 12px fits 10pt — Live auto-centers digits). max(y+h) unchanged 166.
- **Patcher 387/481 → 395 box / 489 line** (+8 box = 8×idtrig; +8 line = BUG1 −8 + BUG2 +16). openrect UNCHANGED `[0,0,332,169]`. **MAX(y+h)=166 ≤167 ✓ (lcd_panel `[128,2,196,164]`).**
- **Container Path B (4 res JSON→svg→version→sends_follower):** JSON 272928→**277911** (+4983) sz32 277912 (incl `\0`) of32 16; svg 535 of32 277928 (`1a31f546`); sf_version_check.js 3106 of32 278463 (`a5d905fc`); sends_follower.js 24242 of32 281569 (`53bdbfbd` byte-identical). dlst total=440, mxc=305811 (=dlst_pos+16−0x30), ptch=306251=fs−0x20, NO trailing mdat (EOF==dlst_end). **Result device md5 `f236f42d9f7597c71636840beb1a9806`, 306283 B, 395 box / 489 line**, self-contained. Frozen archive `raw/archive/SendsFollower.2026-06-18-172059-2bugs-font-frozen.amxd`.
- **Preserved (verified):** obj-11 LFO leg, obj-16 bipolar, obj-47 route max, obj-7 receive ---max_send, 8× live.map / 9× live.remote~ / 8× live.modulate~, follow_mode Peak/Total, lcd_panel, conditional Range (msgrem/msgmod show/hide bipol/depth), `_persistence`, observer return-index (in untouched `53bdbfbd` JS). 0 orphan lines, both JS node --check clean.
- ⚠️ **REUSABLE CRAFT (live.text runtime label — DEFINITIVE):** to set a live.text button label at runtime to an arbitrary (possibly multi-word) param name, you need BOTH (a) a **single symbol** (`tosymbol` after the `substitute`/source) AND (b) the **`text $1, texton $1` message form** (NOT bare `prepend text`/`prepend texton` on a raw list — that's what failed on HW). This is the exact stock `p SetName` mechanism (`substitute <none> Map → join-to-one-symbol → "text $1, texton $1"`). `set`/`setsymbol` also documented but stock uses text/texton. ⚠️ **Mapping captured-id to a gate/leg via a `set <id>` message register: the register stores but DOESN'T output — you MUST bang it (`t b l` after the id source) or the leg never engages until something else bangs it.**

## Name-in-Map via COMMENT overlay (live.text label gave up, 4th attempt) + Mode font — FROZEN 2026-06-18 ✅
- **Base = on-disk `f236f42d` (306283 B, 395/481→489), file FREE (lsof empty, founder's last build).** Pre-edit archived `…191851-preedit-name-comment-font.amxd`. JS NOT touched (`53bdbfbd` byte-identical, installObservers present, node-check clean). Pure patcher edit.
- **THE FIX — name now drawn by a `comment` OVERLAY, NOT live.text.** After 3 failed live.text approaches (`prepend set`, `prepend text/texton`, `tosymbol`+`text $1,texton $1`=stock-exact) NONE showed the name on hardware → **live.text does NOT update its displayed label by message in our context. Abandoned live.text for the label entirely.** Per slot: DELETE `tosymbol_N`+`namemsg_N` (dead label chain); KEEP `namesub_N`(`substitute <none> Map`); ADD `nameset_N`(`prepend set`)→`mapcom_N`(**Max `comment`**, presentation_rect == map_btn_N rect, `ignoreclick 1`, `textjustification 1`=center, fontsize 9, Arial Bold, default "Map"). Route `livemap_N[2](name)→namesub_N→nameset_N→mapcom_N[0]`. **comment DOES update displayed text via `set <text>`** (verified: comment methods = append/prepend/**set**/setwithtruncation/string); multi-word names ("Fade In") OK (comment `set` takes the whole list as text).
- **PROOF name arrives:** `livemap_N out2` fans to BOTH `xcmp_N` (X-visibility, founder-confirmed WORKING) AND `namesub_N` (→comment) — same output, so the comment is fed the exact name that already drives X. Verified all 8 slots.
- **Click-through:** `mapcom_N` has `ignoreclick 1` → clicks pass through the overlay to `map_btn_N` (live.text toggle) underneath, which still catches the arm-click. `map_btn_N` kept as clickable colored FRAME: text/texton emptied (`text=""`,`texton=""`) so it draws no label (comment draws the text now); fill-state chain UNTOUCHED (orange fill α1 when mapped via obj-53/58.../88 `sel 0 1`→color msgs; orange outline α0 when unmapped).
- **Comment color BY STATE (dynamic, honest):** reused the SAME per-slot fill-state `sel 0 1` (obj-53/58/63/68/73/78/83/88): out0(mapped)→`comdark_N`(msg `textcolor 0.117 0.117 0.117 1`=dark, readable on orange fill); out1(unmapped)→`comorng_N`(msg `textcolor 1 0.7098 0.196 1`=orange, on outline)→`mapcom_N[0]`. Default `textcolor`=orange (unmapped look). Text+color fire from same `livemap out2` event → stay in sync. NO single-color compromise needed — dynamic recolor works (comment honors `textcolor` attr-message).
- **Mode font 11→9** (`map_mode_N` Remote/Mod live.text toggle, all 8). Per task: only Map-comment(9) + Mode(9) shrank; numbox/labels (Min/Max/depth/mode_label) NOT touched (founder wants those bigger).
- **Patcher 395/489 → 411 box / 513 line.** Per slot: −2 box (del tosymbol+namemsg), +4 box (nameset+mapcom+comdark+comorng) = net +2/slot ×8 = +16. Lines: −3/slot (del namesub→tosymbol, tosymbol→namemsg, namemsg→map_btn) +6/slot (namesub→nameset, nameset→mapcom, sel0→comdark, sel1→comorng, comdark→mapcom, comorng→mapcom) = net +3/slot ×8 = +24. openrect UNCHANGED `[0,0,332,169]`. **MAX(y+h)=166 ≤167 ✓ (lcd_panel).**
- **Container Path B (4 res JSON→svg→version→sends_follower):** JSON 277911→**288878** (+10967) sz32 288879 (incl `\0`) of32 16; svg 535 of32 288895 (`1a31f546`); sf_version_check.js 3106 of32 289430 (`a5d905fc`); sends_follower.js 24242 of32 292536 (`53bdbfbd` byte-identical). dlst total=440, mxc=316778 (=dlst_pos+16−0x30), ptch=317218=fs−0x20, NO trailing mdat (EOF==dlst_end). **Result device md5 `d3b83238ee1e86e4f27ea614aec9e845`, 317250 B, 411 box / 513 line**, self-contained. Frozen archive `raw/archive/SendsFollower.2026-06-18-192423-name-comment-font-frozen.amxd`.
- **Preserved (verified post-build):** obj-11 LFO leg, obj-16 bipolar, obj-47 route max, obj-7 receive ---max_send, 8× live.map / 9× live.remote~ / 8× live.modulate~, follow_mode Peak/Total live.tab, mode_label "Mode", lcd_panel, conditional Range (msgrem/msgmod ×8 show/hide bipol/depth), idtrig_N `t b l` engage-fix ×8, `_persistence`, version_link, observer return-index (in untouched JS). 0 orphan lines, both JS node-check clean, X-visibility chain (xcmp→hidden $1) intact.
- ⚠️ **REUSABLE CRAFT (DEFINITIVE — live.text label is a DEAD END for runtime text):** to show an arbitrary runtime string over a live.text button, do NOT fight live.text's label (`set`/`text`/`texton`/`tosymbol`+`text $1` ALL failed on HW). Instead overlay a **Max `comment`** at the same presentation_rect with **`ignoreclick 1`** (click passes to the live.text frame under it) and feed it `prepend set` → comment; empty the live.text's own text/texton so it's just a colored clickable frame. comment `set` updates displayed text reliably (incl multi-word). Recolor comment by state via `textcolor r g b a` message. This is the pattern that finally worked.

## 3 color edits (Mode two-color, Map name readable, Min/Max orange) — FROZEN 2026-06-18 ✅
- **Base = `d3b83238` (317250 B, 411/513, frozen, FREE).** Pure COLOR edit — NO box/line change (411/513 unchanged), only attrs + 16 message-box texts. Pre-edit archived `…193239-preedit-3color.amxd`. JS untouched (`53bdbfbd`/`a5d905fc` byte-identical, node-check clean).
- **EDIT 1 — Mode toggle (`map_mode_0..7`) ALWAYS FILLED, two-color.** off=Remote(out0)→`bgcolor`/`activebgcolor`=ORANGE `[1,0.7098,0.196,1]`; on=Mod(out1)→`bgoncolor`(ADDED)/`activebgoncolor`=CYAN #03C3D5 `[0.0118,0.7647,0.8353,1]`; text DARK `[0.117,0.117,0.117,1]` both states (`textcolor`/`textoffcolor`/`textoncolor`/`activetextcolor`/`activetextoncolor`). enum stays `["Remote","Mod"]` default 0=Remote. New attrs registered in `saved_attribute_attributes`. (Was: dark off-fill + teal on-fill, grey text.)
- **EDIT 2 — Map name READABLE (compromise: orange text both states, NO orange fill).** ⚠️ **ROOT CAUSE of "black/invisible name":** Max draws ALL live.* objects in a TOP layer ABOVE plain objects. `map_btn_N` (live.text) mapped-state orange FILL (α1) painted OVER the `mapcom_N` `comment` overlay → dark comment text sank UNDER the fill → invisible. (`lcd_panel` paint-index 346 is also above the live.text boxes, but live.* render in their own top layer regardless of box-index, which is why numboxes/toggles show through.) **live.text label is dead for runtime text (4 prior fails) AND comment can't sit above a live.text fill** → reference "dark text on orange fill" UNREACHABLE with this mechanism. **FIX = remove the mapped fill** (per-slot mapped color-msg `obj-54/59/64/69/74/79/84/89`: `bgcolor`/`bgoncolor`/`activebgcolor`/`activebgoncolor` α 1→0 = frame-only, no fill) so comment is NEVER covered; **make comment ORANGE in BOTH states** (`comdark_N` repointed dark→orange; `comorng_N` already orange; `mapcom_N` default textcolor orange). Result: name (and "Map") always orange on dark LCD = clearly readable, both mapped+unmapped. Map button = orange OUTLINE-only indicator (no fill) when mapped. Fill-state chain (`xcmp_N zl compare <none>`→`obj-58 sel 0 1`→msgs) wiring UNTOUCHED; only msg TEXT alphas changed.
- **EDIT 3 — Min/Max digits ORANGE in Remote.** `slot_min_N`/`slot_max_N` `textcolor` teal→ORANGE `[1,0.7098,0.196,1]` (transparent field/% kept). `depth_N` (Mod) LEFT teal #03C3D5. So Remote%=orange, Mod depth%=teal.
- **Patcher 411/513 UNCHANGED** (color-only). openrect `[0,0,332,169]` unchanged. **MAX(y+h)=166 ≤167 ✓ (lcd_panel).**
- **Container Path B (4 res JSON→svg→version→sends_follower):** JSON 288878→**294030** (+5152, dL) sz32 294031 of32 16; svg 535 of32 294047 (`1a31f546`); sf_version_check.js 3106 of32 294582 (`a5d905fc`); sends_follower.js 24242 of32 297688 (`53bdbfbd` byte-identical). dlst total=440, mxc=321738 (=dlst_pos 321962 +16−0x30), ptch=322370=fs−0x20, NO trailing mdat (EOF==dlst_end 322402). **Result device md5 `308ada9c492ddc04d829e0f7c6522843`, 322402 B, 411 box / 513 line**, self-contained. Frozen archive `raw/archive/SendsFollower.2026-06-18-193524-3color-frozen.amxd`. Pre-edit `d3b83238` archived `…193239-preedit-3color.amxd`.
- **Preserved (verified post-build):** obj-11 LFO leg, obj-16 bipolar, obj-47 route max, obj-7 receive ---max_send, 8× live.map / 9× live.remote~ / 8× live.modulate~, follow_mode Peak/Total, mode_label, lcd_panel, conditional Range (show/hide bipol/depth), idtrig `t b l` ×8, mapcom name-feed (`prepend set`) ×8, `_persistence`, version_link, observer return-index (untouched JS). 0 orphan lines, both JS node-check clean.
- ⚠️ **REUSABLE CRAFT (Max paint layering — DEFINITIVE):** live.* objects (live.text/live.numbox/live.tab) render in a TOP layer ABOVE plain objects (comment/panel) regardless of box-array index. So a `comment` overlay CANNOT sit above a live.text FILL — if you need text over a live.text button, the button must be FILL-FREE (frame/outline only) for the comment to show. Combined with "live.text label is dead for runtime text", the only readable runtime name over a Map button = comment overlay + frame-only (no fill) live.text underneath + comment colored to contrast the LCD (orange on dark). Reference fill colors are unreachable here.

## ± per-state glyph + instant range recompute — FROZEN 2026-06-18 ✅
- **Base = `308ada9c` (322402 B, 411/513, frozen, FREE).** Pre-edit archived `…194608-preedit-bipol-instant.amxd`. JS untouched (`53bdbfbd`/`a5d905fc`/svg `1a31f546` byte-identical, both node-check clean — both edits are PURE PATCHER).
- **EDIT 1 — `bipol_N` shows ONE active polarity by state (not "±").** live.text toggle (`outputmode 1`, `appearance 2`): off-label=`text`, on-label=`texton`. enum `["+","-"]` idx0→`signif if 0==1 then -1 else 1`=+1 (positive depth), idx1→−1 (negative). Set `text="+"` (off/state0 = positive depth), `texton="−"` (on/state1 = negative depth, U+2212). Teal #03C3D5 / 15×15 square form / borders kept (only text/texton changed). `automation`/`automationon` ("+"/"-") = param-automation labels, untouched. All 8. attr-only, no box/line change.
- **EDIT 2 — range edit applies INSTANTLY (no follow movement needed).** ⚠️ ROOT: slot scale chains are `obj-7(follow 0..1)→scale_N[0]→sig_N→remote_N` (Remote) and `obj-7→modscale_N[0]→modmul_N→modsig_N→mod_N` (Mod). Min/Max set scale_N RIGHT inlets (outlow/outhigh via `div_min/max_N /100`); depth/± set `modmul_N` RIGHT inlet k (via `depthdiv_N`+`signif_N`→`kmul_N`→modmul[1]). Max `scale`/`* ` DON'T re-output on right-inlet-only change → new bound waits for next follow tick; if follow static (transport stopped/sends frozen) it NEVER applies. **FIX:** added ONE shared `followf` (`f` float) — `obj-7[0]`→`followf` RIGHT inlet (silent store of latest follow); `followf[0]`→ALL 16 `scale_N[0]`+`modscale_N[0]` (left inlets, parallel to obj-7). Per slot ×4 controls (min/max/depth/±) inserted `t b l` trigger (replacing direct control→math line): trigger fires R→L so outlet1=`l`→math (sets bound/k FIRST, full chain to modmul right inlet completes synchronously), outlet0=`b`→`followf` bang (recompute SECOND with new bound). On any range edit → followf re-emits held follow → scale/modscale recompute → sig/modsig → remote/modulate update NOW. **Works at static follow** (followf holds last value, default 0 = correct floor output). Works in BOTH legs (Remote via scale→remote~, Mod via modscale→modmul→modulate~). Normal follow flow intact (obj-7 still drives scale/modscale left inlets directly; followf is just a passive store there — no double-fire). idtrig engage-fix / `_persistence` / LFO leg / Peak-Total / observers untouched.
- **Patcher 411/513 → 444 box / 594 line** (+33 box = 1 followf + 32 `t b l` triggers [4×8]; +81 line = 32 rewired controls ×[−1 old +3 new = +2 → 64] + 1 obj-7→followf + 16 followf→scale/modscale). openrect `[0,0,332,169]` unchanged. **MAX(y+h)=166 ≤167 ✓** (new objects are patching-canvas only, x≥1500, no presentation). New objects have NO presentation (`presentation` absent) so device face unchanged.
- **Container Path B (4 res JSON→svg→version→sends_follower):** JSON 294030→**314387** (+20357 dL) sz32 314388 (incl `\0`) of32 16; svg 535 of32 314404 (`1a31f546`); sf_version_check.js 3106 of32 314939 (`a5d905fc`); sends_follower.js 24242 of32 318045 (`53bdbfbd` byte-identical). dlst total=440, mxc=342287 (=dlst_pos 342319 +16−0x30), ptch=342727=fs−0x20, NO trailing mdat (EOF==dlst_end 342759). **Result device md5 `7ebe7d15ffcd8fc49aad46aa93baafbd`, 342759 B, 444 box / 594 line**, self-contained (svg+both JS embedded byte-for-byte, both node-check clean). Frozen archive `raw/archive/SendsFollower.2026-06-18-194608-bipol-instant-frozen.amxd`. Pre-edit `308ada9c` archived `…194608-preedit-bipol-instant.amxd`.
- **Preserved (verified post-build):** obj-11 LFO leg, obj-16 bipolar, obj-47 route max, obj-7 receive ---max_send, 8× live.map / 9× live.remote~ / 8× live.modulate~, follow_mode Peak/Total, mode_label, lcd_panel, conditional Range (msgrem/msgmod show/hide bipol/depth), idtrig `t b l` engage-fix ×8, mapcom name-feed ×8, X-visibility, `_persistence`, version_link, observer return-index (untouched JS). 0 orphan lines.
- ⚠️ **REUSABLE CRAFT (instant recompute on a scale's right-inlet change):** `scale`/`*`/math objects DON'T re-output when only the right inlet (range/coeff) changes — output is stale until the LEFT inlet is re-banged. To make a bound/depth edit apply NOW (static signal source), store the source value in an `f` (fed to its RIGHT inlet) and re-bang the `f` (left inlet) after the bound is set; gate order with `t b l` (value first via outlet1, bang second via outlet0). For multi-hop right-inlet chains (signif→kmul→modmul), the value-outlet's full downstream completes synchronously before trigger's bang-outlet fires, so the coeff is in place before recompute.

## Current state (one slice)
- **md5 `16e9bae372ec9b5eb6128c489ecd9e78`, 314619 B, 397 box / 529 line** (2026-06-19, CURRENT, frozen; RESTORE-MOD + MM-NATIVE NAME). Founder reverted the Remote-only build `f268af54`: brought back Remote AND Mod per slot (toggle + ±/depth) but with the MM-Native name-in-button mechanism and NO LFO leg; sends-follow untouched. **Built from archive `7ebe7d15` (Mod wiring + native column layout intact) with EXACTLY 2 edits.** (1) Name mechanism per slot ×8: DELETED comment-overlay chain (`nameset_N` prepend set, `mapcom_N` comment, `comdark_N`/`comorng_N` recolor msgs, `sel 0 1` obj-53/58/63/68/73/78/83/88, 2 fill-msgs obj-54/55… α0); ADDED `namesym_N`(tosymbol)+`namemsg_N`(message `text $1, texton $1`); rewired `livemap_N out2→namesub_N(substitute <none> Map)` BOTH outlets→`namesym_N→namemsg_N→map_btn_N[0]`; X-branch (`xcmp_N→hidden $1→map_unmap_N`) KEPT. `map_btn_N` attrs set to proven on-disk look: text/texton="Map", appearance 2, parameter_enable 0, idle grey #8C8C8C (textcolor/textoffcolor, theme live_lcd_control_fg_zombie), active orange #FFB532 (activetextcolor/activetextoncolor, theme live_lcd_control_fg), bg/border=LCD-bg #282828 (theme live_lcd_bg, no orange frame idle). (2) DELETED LFO leg ONLY: obj-9(path msg)/obj-10(live.path)/obj-11(live.remote~)/obj-11_scale(* 127.)/obj-11_set(prepend set)/obj-16(scale →-100..100)/obj-17(sig~) + all their lines (incl obj-15 out0→obj-16). PERCENT branch obj-15 out1→obj-18(scale 0..100)→obj-20(send ---max_send_percent) intact. Harmless off-canvas leftovers obj-3(debug dial, lost feed) + obj-6(delay 300, lost consumer) kept (matches f268af54). Net boxes 444→397 (−47 = −56 name-chain +16 namesym/msg −7 LFO), lines 594→529 (−65). **PRESERVED (verified post-build): 8× live.modulate~ (Mod legs), 8× live.remote~ (LFO obj-11 gone), 8× live.map, 8× gate 2 1 each feeding remote+mod (id-route by mode), idtrig_N `t b l` engage-fix ×8, map_mode/bipol/depth/idtrig ×8, msgrem/msgmod show/hide, instant-range followf+t-b-l, obj-46 js, obj-7 receive ---max_send, obj-47 route max/obj-48 change/obj-50 send ---max_send, follow_mode Peak/Total, version_node node.script, lcd_panel.** JS `53bdbfbd` (embedded == on-disk canon, 24242B) / `sf_version_check.js` `a5d905fc` / svg `1a31f546` byte-identical. Path B 4 res: JSON sz32=286248 of32=16, svg of32=286264, vc of32=286799, sf of32=289905, dlst total=440, mxc=314147 (=dlst_pos+16−0x30, dlst_pos=314179), ptch=314587=fs−0x20, no trailing mdat. openrect `[0,0,332,169]`, pres bbox x+w 324 / y+h 166 ≤169. Pre-edit (current on-disk `f268af54`) archived `raw/archive/SendsFollower.2026-06-19-001939-preedit-restore-mod.amxd`; frozen result `raw/archive/SendsFollower.2026-06-19-002738-restore-mod-mmnative-name-frozen.amxd`. Open: HW-test both Remote+Mod in Live. ⚠️ `substitute a b` for name-in-button: route BOTH outlets (out0=substituted→"Map" on `<none>`, out1=passthrough real name) into the tosymbol so any name reaches the button.
- Prior frozen (Remote-only, MM-Native name, LFO removed, MOD CUT — superseded; founder reverted): **md5 `f268af54ac8b7e31e699c6354af1e2fc`, 179344 B, 213 box / 265 line** (2026-06-19). JS `53bdbfbd`/`a5d905fc`/svg `1a31f546` byte-identical. Archived `raw/archive/SendsFollower.2026-06-19-001939-preedit-restore-mod.amxd`.
- Prior frozen (Mod+LFO+comment-overlay name): **md5 `7ebe7d15ffcd8fc49aad46aa93baafbd`, 342759 B, 444 box / 594 line** (2026-06-18; ± per-state glyph [text="+" off/positive, texton="−" on/negative] + instant range recompute [shared `followf` `f` + 32× `t b l` triggers → bound/depth/± edits apply NOW even at static follow, both Remote+Mod legs]). JS `53bdbfbd`/`a5d905fc`/svg `1a31f546` byte-identical. Frozen archive `raw/archive/SendsFollower.2026-06-18-194608-bipol-instant-frozen.amxd`. Pre-edit `308ada9c` archived `…194608-preedit-bipol-instant.amxd`.
- Prior frozen (3 color edits: Mode toggle two-color [Remote=orange/Mod=cyan, dark text], Map name orange both states + mapped-fill removed, Min/Max digits orange in Remote [depth teal]): **md5 `308ada9c492ddc04d829e0f7c6522843`, 322402 B, 411 box / 513 line** (2026-06-18). JS `53bdbfbd` byte-identical. Frozen archive `raw/archive/SendsFollower.2026-06-18-193524-3color-frozen.amxd`.
- Prior frozen (name-in-Map via `comment` overlay [live.text label abandoned after 3 fails — comment `set` updates text, `ignoreclick 1` passes click to live.text frame under it, dynamic textcolor dark/orange by mapped state]; Mode toggle font 11→9; numbox/labels untouched). JS `53bdbfbd` byte-identical. Frozen archive `raw/archive/SendsFollower.2026-06-18-192423-name-comment-font-frozen.amxd`. Pre-edit `f236f42d` archived `…191851-preedit-name-comment-font.amxd`.
- Prior frozen (2 real-bug fixes + font, name-via-tosymbol+text/texton=STILL BROKEN on HW): **md5 `f236f42d9f7597c71636840beb1a9806`, 306283 B, 395 box / 489 line** (2026-06-18). Frozen archive `raw/archive/SendsFollower.2026-06-18-172059-2bugs-font-frozen.amxd`.
- Prior frozen (4 visual mapper edits, name-via-text/texton-only=BROKEN-on-HW, teal Mode/±, transparent % numboxes): **md5 `c35eb20bcfb70957880604fdf7767dc6`, 301300 B, 387 box / 481 line** (2026-06-18). Frozen archive `raw/archive/SendsFollower.2026-06-18-170149-4visual-frozen.amxd`.
- Prior frozen (Map UI reconstruction: name-feedback-via-prepend-set[BROKEN] + LCD panel + Mode Remote/Mod toggle + `live.modulate~` Mod leg): **md5 `8a9203dd6cb46bf576b1fb344905e81d`, 246661 B, 359 box / 425 line** (2026-06-18). sends_follower.js `53bdbfbd` (24242 B). Frozen archive `raw/archive/SendsFollower.2026-06-18-164207-mapui-recon-frozen.amxd`. Pre-edit on-disk UNFROZEN `ac1dc6f4` archived `…162815-preedit-mapui-recon-ondisk.amxd`.
- Prior frozen (MAP FIX live.map, static Remote, no LCD/Mod): **md5 `fb0d2975fa05f72c00b422caa5a2ce08`, 144944 B, 190 box / 193 line** (2026-06-18, frozen; **MAP FIX = live.map native grab** replaces unreliable selected_parameter observer; UI: no "%", no Parameter column, X conditional, cols Map|Mode|Min|Max; openrect `[0,0,328,169]`, max(y+h)=166). sends_follower.js `53bdbfbd26cdfef548e6238b0cf425f6` (24242 B, UNCHANGED — its slot funcs now dead code). Frozen archive `raw/archive/SendsFollower.2026-06-18-144453-mapfix-livemap-frozen.amxd`.
- Prior frozen (WIDE layout, BROKEN map via selected_parameter): **md5 `4eb57a065bf9499f4e3bb29617bac00d`, 176129 B, 256 box / 274 line** (2026-06-18; full 8-slot table fits 169px shelf but Map buttons didn't fire). sends_follower.js `53bdbfbd` (24242 B). Frozen archive `raw/archive/SendsFollower.2026-06-18-142514-wide-layout-frozen.amxd`.
- Prior frozen (8-slot mapper, TALL layout openrect `[0,0,340,385]`): **md5 `194ff283e786d24f481020461fdba4cc`, 176150 B, 256 box / 274 line** (2026-06-18; table stacked below dial, y200..369 → invisible in Live). sends_follower.js `53bdbfbd` (24242 B). Frozen archive `raw/archive/SendsFollower.2026-06-18-135002-8slot-mapper-frozen.amxd`.
- Prior frozen (single Map button): **md5 `6f156eab973ecec0d9793f794c75cfce`, 68498 B, 82 box / 85 line** (2026-06-18; Map button + 2nd live.remote~ on top of Peak/Total + observer return-index). sends_follower.js `c63475322970c9f8ea02d01620806551` (22368 B). Frozen archive `raw/archive/SendsFollower.2026-06-18-121509-frozen.amxd`. ⚠️ A later Live re-save reverted on-disk to `c5b736c5` (82/84) with stale embedded JS `29ff530b` — superseded by 8-slot freeze.
- Prior frozen (Peak/Total relabel, no Map): **md5 `5a8ba853a3c31d80cb84870e977a13f4`, 49134 B, 66 box / 65 line** (2026-06-18, frozen; cosmetic relabel Peak/Total + "Mode" name + "Mode" label, observer return-index fix + Follow Mode). Path-B (ΔL=+549 vs `bd21b848`): JSON of32=16 sz32=**33333** flag=17; embedded `sends_follower.js` **12303 B** of32=**33349** flag=0 (md5 `3bc5ab8e`, observers, UNCHANGED); `sf_version_check.js` 3106 B of32=**45652** flag=8 (md5 `a5d905fc`, UNCHANGED) (order: JSON → sends_follower → version_check). dlst total=332, `0x30+mxc−dlst=16`, ptch=filesize−0x20 (ptch=49102, mxc=48758). Both embedded JS = canon byte-for-byte, `node --check` clean, `outlet(0,"max"…)` + `route max` intact. Frozen archive: `raw/archive/SendsFollower.2026-06-18-113530-frozen.amxd`. Pre-edit archived: `raw/archive/SendsFollower.2026-06-18-110402-frozen.amxd` (`bd21b848`).
- Prior frozen (Follow Mode labels "Max/Sum", param "Follow Mode", no "Mode" label): md5 `bd21b8483df868a1d02cab71a3b9b603`, 48585 B, 65 box / 65 line. JSON sz32=32784; `sends_follower.js` of32=32800; `sf_version_check.js` of32=45103. Archived `…110402-frozen.amxd`.
- Prior frozen (no observers, old JS `29ff530b`): md5 `7ca595b44db993c8bf91269fbeb7d97a`, 42090 B, 65/65 (2026-06-18, Follow Mode, frozen, REBUILT). Layout: JSON of32=16 sz32=32784; `sends_follower.js` 5808 B of32=32800; `sf_version_check.js` 3106 B of32=38608. Archived `…002423-frozen.amxd`; source of that rebuild was unfrozen `3f31990a` (52813 B, `…002025.amxd`).
- ⚠️ **INCIDENT 2026-06-17→18:** prior frozen `725a06ea` (41504 B) was overwritten/re-frozen by Live on save BEFORE it hit the archive → lost. The on-disk file Live left (`3f31990a`, 52813 B) was, despite "unfrozen" label, ALREADY a valid self-contained freeze (3 dire, both JS embedded = canon, all invariants passed) — just bloated (verbose float serialization, JSON 43518) and unarchived. Rebuilt lean equivalent via Path B from compact `json.dumps(indent=1)` → `7ca595b4`, 42090 B (≈41504 target; ~586 B over = indent-1 vs Max exact whitespace + ordering). LESSON ENFORCED: dated archive of frozen result IMMEDIATELY after freeze, before any Live open.
- Self-contained: patcher + `sends_follower.js` + `sf_version_check.js` all embedded; no external `.js` needed.
- Lost frozen build (Follow Mode, pre-incident): md5 `725a06ea762716e96f13a9b63cb3e3f9`, 41504 B, 65/65 — overwritten by Live, never archived (see incident above). Reproduced by `7ca595b4`.
- Prior frozen build (version-check, no Follow Mode): md5 `b5286b33d9adc12e023981ab1a117859`, 37444 B, 61/61.
- Earlier frozen build (JS-embedded, no version-check): md5 `6b85d12dcff2e412f15ae75897505f20`, 36211 B, 50/51 → backup `raw/archive/SendsFollower.2026-06-17.amxd`.

## Rack round-trip + LFO status (re-verified 2026-06-17)
- `SendsFollowerRack.adg` (file md5 `7c875964319ee6352698ed2c093b1e81`, gzip 6406 B → **88296 B** XML, Live 12.4.2). Internal `UserName` of group dev = `SendsFollowerRack`; `MaxSendsFollower` survives only inside rack's own `LastPresetRef`/`PresetRef` metadata, NOT the file name.
- **Structure:** single-chain `AudioEffectGroupDevice` → 1 `BranchPresets` → 1 `AudioEffectBranchPreset` → 1 `DevicePresets` → exactly **2** `MxDeviceAudioEffect`: (1) Sends Follower (`UserName=send_follower`, FileRef `Max Devices/SendsFollower.amxd`, path type 6), (2) stock LFO (FileRef `Devices/Audio Effects/LFO/Ableton Folder Info/LFO.amxd`, path type 7). No 3rd device, no reorder.
- **FileRef pulls frozen device** (md5 `b5286b33…`): rel + abs path → current User Library `.amxd`, name/path unchanged → loads fine.
- **LFO = STOCK Ableton device**, ships with Live Suite → NOT custom, do NOT bundle.
- ⚠️ **Stock LFO patcher is ENCRYPTED — cannot be decoded or forked (verified 2026-06-18, read-only).** Container = `ampf → meta → ciph → ptch` (542384 B), **NO `mx@c` chunk** (vs our cleartext devices which have `mx@c` + `boxes` ~off 512). Zero cleartext anywhere: no `boxes`/`maxclass`/`patchline`/`live.tab`/`loadbang`/`presentation`/even `patcher`; body entropy ~7.93/8.0. `ciph` = Ableton's protection wrapper. Only one `LFO.amxd` on system (Core Library `LFO/` holds only `.adv` presets, not the patcher). Our `.amxd` Path-A/B recipe does NOT apply to protected devices.
  - Consequence for the «always open LFO on the mapping view (Map 1/Map 2 ranges)» ask: **IMPOSSIBLE without a from-scratch custom LFO clone.** The view toggle (params↔mapping icon) is a PURE UI object, NOT a parameter: LFO's full `.adv` ParameterList (Depth, Freq Rate, Hold, Jitter, **Offset**, Phase, Re-Trigger, Shape, Sync Rate, Time Mode, WaveShape + 3 map-target refs `obj-7::obj-5` / `obj-7::obj-16::obj-5` / `obj-7::obj-16::obj-130`) has NO view/page/mode entry (checked ViewMode/PageIndex/ParameterView/SelectedPage/DisplayMode/MappingView — all absent). So: not addressable via LiveAPI (no `parameters N` for view → Sends Follower can't set it via `this_device canonical_parent devices 1 …`); saved view in `.adg` doesn't stick (patch forces a default view on load — can't confirm which object due to encryption); macros don't switch view; AND can't fork the device (encrypted). Only escape = build our own open LFO device (then must bundle/maintain it).
- ⚠️ Pre-existing: `send_follower.adv` referenced by rack is MISSING, but only in `<LastPresetRef>` metadata, not load path → device still loads from `.amxd` FileRef. `LFO.adv` exists on disk (also only LastPresetRef metadata).
- **Macros/mapping clean after re-save:** 16 default unnamed macros, NO `MacroMappings`/`ControllerTargets`/`MapModeMin/Max`. 26 `ModulationTarget` blocks = empty automation stubs (`LockEnvelope=0` only), not routings. No envelopes. Rack adds zero wiring.
- **`devices 1 parameters 5`** (in the `.amxd` patcher, `path this_device canonical_parent devices 1 parameters 5` → live.path → live.remote~): `devices 1`=LFO; `parameters 5` = the LFO's **Offset** parameter — **founder-confirmed 2026-06-17** (he built it; SF drives the LFO's Offset and nothing else). **CLOSED** in wiki — no more open needs-verification on the modulation write path. NOTE: `.adg` alone can't prove the name (it stores ParameterList ALPHABETICALLY — Depth, Freq Rate, Hold, Jitter, Map1… — NOT LiveAPI index order); don't re-open from `.adg`. Positional addressing is now a **usage caveat** (not a verify item): swap/reorder the 2nd device → target moves off Offset.
- **Bipolar modulation = CONFIRMED design-intent (founder, 2026-06-17).** `scale 0. 1. -100. 100.` (`obj-16`) → bipolar -100..+100; `live.remote~` (`obj-11`) applies a **signed offset** to the target (offsets around base value, doesn't overwrite). Intentional: modulates amount AND direction (follow value pushes target up/down + into negative). Prior needs-verification flag CLOSED in wiki. This is the *character* of the modulation (signed offset, semantics of `live.remote~` + the −100..100 scale). Param identity ALSO now closed: `parameters 5` == LFO's **Offset** (founder-confirmed 2026-06-17, see line above). No open needs-verification left on the modulation path.
- **Size growth 52194→88296 EXPLAINED:** re-save in Live 12.4.2; LFO device block = 62989 B (~71% of file) now serializes LFO's full ~91-entry ParameterList. SF block only ~4699 B. NO embedded samples/audio/blobs (`OriginalFileSize` 21673/542384 = stale FileRef metadata, not embedded data). Delta = pure LFO param-state bloat from newer Live.

## Rack self-containment — `.adg` CANNOT embed the `.amxd` (verified 2026-06-17)
- An `.adg` rack references its M4L device by `FileRef` (`PatchSlot`→`MxPatchRef`→`FileRef RelativePath="Max Devices/SendsFollower.amxd"`, `RelativePathType=6`=RelativeToUserLibrary), **never embeds it**. Proven: 88286 B decompressed XML has **0** `ampf`/`mx@c`/`ptch` bytes; both `MxDBlob` slots empty (`<Blob />`). So a lone `.adg` is NOT device-self-contained — recipient still needs `SendsFollower.amxd` in their User Library `Max Devices/`. There is NO XML field to fold `.amxd` bytes into `.adg`; don't try to fabricate one (would make a broken file). Freeze is an `.amxd`-level op only (our device already frozen, b5286b33). The `<Blob>`/`HasData` slot = Max parameter-state blob, not the device container.
- Single-deliverable routes: **Pack `.alp`** (File>Manage Files>Manage Project>Create Pack — installs `.amxd` into User Library, then rack finds it; `.alp` proprietary gzip+manifest, must export from inside Live, NOT buildable by us) OR ship `.amxd`+`.adg` with a "drop `.amxd` into Max Devices/" step (Control XL model).
- **`.adg` round-trip recipe:** `.adg` = gzip(-9) of XML. `gunzip -c rack.adg > x.xml`; edit; `gzip -c -9 x.xml > new.adg`; validate `xmllint --noout`, gzip magic `1f8b`, round-trip `gunzip(new.adg)==edited xml`. ⚠️ The on-disk `.adg` can change between sessions (re-saved in Live) — always re-`gunzip` the CURRENT disk file right before editing, don't trust an XML extracted earlier in the session.
- **Canonical rack name = `SendsFollowerRack`** (file + group `UserName`). Stale `MaxSendsFollower` is GONE from the current on-disk file (was only in old LastPresetRef metadata). Current `.adg`: md5 `66896a8b2fce2ff050feae0d09d24468`, gzip 6371 B → 88286 B XML. Broken `send_follower.adv` ref persists only in PresetRef/LastPresetRef metadata — harmless, not on load path; leave untouched.

## "Map button on SF face" feasibility (READ-ONLY study 2026-06-18)
- **Arm stock-LFO map remotely = NO.** LFO encrypted (no `mx@c`, ciph wrapper); its map-arm is a pure UI object, NOT a parameter, NOT in the LFO's `.adv` ParameterList → not addressable via `this_device canonical_parent devices 1 …`, not in LiveAPI. Can't fork (encrypted). Confirmed.
- **SF-hosted Map = YES, feasible from a frozen device, no external deps.** Standard stock-pattern (LFO/Envelope Follower/3rd-party "Button Parameter Mapper" all do it): Map button arms map-mode → user clicks ANY param in Live → device reads `live_set view selected_parameter` (CONFIRMED correct path) → grabs its id → feeds `live.remote~`. Observe via `live.observer`/JS `LiveAPI` on `property selected_parameter`. Already have `live.remote~` (obj-11), `live.path`/`live.object` idioms in patch.
- **Signal caveat (product fork):** SF can't tap LFO's audio out / re-route it. An SF-hosted map drives SF's OWN signal (the existing `scale … -100. 100.` bipolar follow value, or raw 0..1, or a new depth-scaled bus) straight to the clicked target, BYPASSING the LFO. So this map is an *alternate output path*, parallel to (or replacing) the LFO leg — not a remote control of the LFO's mapping. Founder must choose: keep LFO leg + add SF-direct leg (two targets) vs SF-direct replaces LFO (LFO becomes redundant for that path).
- **Persistence:** clicked-param id is session-specific → on map, store the param's CANONICAL PATH (LiveAPI `unquoted_path`/`path`), re-resolve to id on load via `live.path` after `live.thisdevice` (same cold-load race pattern already used for return-index; defer + retry). Unmap = clear stored path + free the live.remote~ target (send empty/clear to its right inlet, null the LiveAPI ref).
- **UI space:** current presentation (on-disk SendsFollower.amxd 44122 B, 66/66) openrect height **169**, width ~123 (content x=7..123). Items by y: "New Version" textbutton `[46,2,70,16]`; dial `[7,17,116,116]` (bottom 133); "Mode" comment `[7,118,109,17]`; Mode live.tab `[7,134,116,17]` (bottom 151). Free: ~18px below tab (151→169) — too tight for a real map row. A map button needs a layout change: either grow openrect height (~+22) to add a Map row at y~153, or reclaim the empty top-left `[0..46, 2..18]` next to "New Version" for a compact map toggle. Object type for map button = `live.text` in toggle/map style (mappable look matching Live theme) or `textbutton`+JS; stock devices use a `live.text`-style toggle.
- ⚠️ on-disk size/md5 differs from memory's "current frozen 49134 B" — Live re-saved (44122 B). Re-read actual file before any future edit; memory current-state slice may lag.

## `Abl.*` map-engine feasibility (READ-ONLY study 2026-06-18) — NO-GO, stay on live.map
- **`Abl.MapUi`/`Abl.MultiMap`/`Abl.Map.maxpat` are NOT stock Live resources.** They exist on disk ONLY in the user's UNPACKED LFO project `/Users/Kirill/Documents/Max 9/Max for Live Devices/LFO Project/` (plain JSON, readable, recent Save-As). On a clean Live install they're **encrypted inside `LFO.amxd`** (`ampf→meta→ciph→ptch`, 542384 B, no `mx@c`/`dlst`/cleartext `Abl.Map`) → **NOT on a buyer's search-path**. bpatcher resolves by name (all 3 have empty `dependency_cache`) but the names aren't there for buyers → `can't find file`.
- **Engine = audio-rate.** `poly~ Abl.Map` (1 voice/slot) takes `in~ 1` (SIGNAL, not message): `in~→clip~ 0. 1.→scale~ (Min/Max)→live.modulate~ (obj-10, real Mod offset) OR live.remote~ @normalized 1 (obj-5, via p RemoteOrModulate)` + thispoly~/mute for CPU. `Abl.MultiMap` inlets = `(signal) signal to map`,`(float) depth`,`(bool) bipolar`; holds 7× poly~ Abl.Map + 7× bpatcher Abl.MapUi + `js getObjectRect` (dynamic layout via thispatcher script). `Abl.MapUi` uses `#1` instance args + shared-receive `---modMode`/`---mappedState` (private parent-orchestrated namespace).
- **Buyer-bundle deps if forced:** 3 patchers + `getObjectRect.js` + `Polarity_*.svg` — all MISSING on stock path (only in user's unpacked LFO project). Embeddable (plain JSON) via Path B but = shipping unpacked proprietary Ableton assets (legal + version-break risk). Private/undocumented (internal comment "don't forget to update Abl.MapWithScaledOuput.maxpat") → high risk Ableton renames them across Live versions. **NO-GO.**
- **We're already on the native path.** Stock, ON search-path, portable to every buyer: `…/C74/packages/Max for Live/patchers/liveui.map.maxpat` (= `live.map @strict 1` + unmap/`hidden $1`/`zl compare <none>`/`prepend mapping`) and `liveui.multimap.maxpat` (**exactly 8 slots** of bpatcher `LiveUI.Map`=liveui.map). This is EXACTLY our current MAP FIX mechanism (`live.map @strict 1` + `_persistence 1` + prepend mapping + zl compare <none> + hidden $1). Our manual 8-slot mapper IS the native Ableton map-grab from the same primitives — only the UI render (frames/colors/icons) is approximate, not pixel-1:1 (that exact look needs the proprietary `LiveUI.Map` bpatcher we don't bundle). **Verdict: keep current live.map reconstruction.**
- Cheap stock-true polish WITHOUT bundling proprietary (optional, on request): `multimap-unmap.svg` already used; could copy self-contained `p ButtonColor`/`p SetName`/`p Scale` subpatchers FROM `liveui.map.maxpat` into our OWN patcher (no external deps). For a REAL Mod/Remote engine (vs bare signed-offset live.remote~) the only stock+portable object is **`live.modulate~.mxo`** (`…/C74/externals/m4l/live.modulate~.mxo`, on every install) — instantiable directly as `live.modulate~`, no Abl.Map wrapper. Separate feature, not needed for current look.

## Stock `liveui.multimap` distributability + interface (READ-ONLY study + TEST BUILD 2026-06-18) — DISTRIBUTABLE, but UI+scaling only
- **Re-checked the STOCK `liveui.multimap.maxpat`** (≠ `Abl.MultiMap` decompile): `/Applications/Ableton Live 12 Suite.app/Contents/App-Resources/Max/Max.app/Contents/Resources/C74/packages/Max for Live/patchers/liveui.multimap.maxpat` (1533220 B). Distinct file from LFO-decompile `Abl.MultiMap.maxpat` (86187 B, md5 differs).
- **ALL deps are STOCK / on every install's search path — DISTRIBUTABLE:** (1) 8× bpatcher `LiveUI.Map.maxpat` — each `embed:1` with FULL inline `patcher` (self-contained inside the file; the named `liveui.map.maxpat` also exists in stock patchers/ as fallback); (2) 3 SVGs `multimap-unmap.svg`/`multimap-closed-off.svg`/`multimap-open-off.svg` — all in stock `…/Max for Live/media/` (resolve by name, NOT embedded as bytes, same model as our existing unmap.svg); (3) externals `live.map`/`live.colors`/`live.modulate~`/`live.remote~` all in stock `…/C74/externals/m4l/*.mxo`. **NO `js`, NO `node.script`, NO `poly~`, NO `gen~`, NO `Abl.*` anywhere** (recursive walk: only `t/sel/zl/route/scale/clip~/+~/*~/pak/live.map/live.colors/live.thisdevice`). So a device embedding stock `liveui.multimap` raises NO `can't find file` for a buyer.
- **Interface (external contract is TINY):** **1 inlet = `(signal) signal IN`** (fanned to all 8 slot bpatchers' inlet 0). **2 outlets: out0 = `(list) parameter id`, out1 = `(signal)` passthrough** (input echoed, for chaining/resize msgs). ALL map-arm / Min / Max / ±/depth / mode UI is SELF-CONTAINED inside the embedded `LiveUI.Map` slots (driven by clicks via native `live.map @strict 1`). To feed it our follow signal: `obj-7 (---max_send 0..1) → sig~ → multimap inlet 0`. No poly~/gen~ needed to drive it.
- ⚠️ **THE CATCH (why it's not a drop-in replacement): stock multimap has NO `live.remote~`/`live.modulate~` inside.** Each slot = `live.map` (native grab) + `p Scale` (Min/Max/±/depth scaling) → emits a **(signal) scaled signal** + **(lom id) parameter id** to the PARENT. In the stock LFO the parent's WRITE layer = `Abl.Map` poly~ voices (the ones holding `live.remote~`/`live.modulate~`) — and `Abl.Map` is **LFO-decompile-only / non-distributable** (NO-GO, see prior study). So using stock multimap means WE supply our own stock `live.remote~`/`live.modulate~` write layer fed by the slots. BUT: at the multimap's TOP-LEVEL boundary the **per-slot scaled signals are NOT exposed** (they go into internal `pak`→`zl sum` housekeeping for the resize UI); only ONE combined `parameter id` list + signal passthrough come out. So you cannot pick off slot-N's scaled signal + slot-N's id at the multimap boundary to wire your own per-slot remote~. To get per-slot signal+id you'd have to EDIT the inlined `LiveUI.Map` slot patchers (expose `p Scale` out + `live.map` id to new slot outlets) — i.e. fork the stock UI internally. Doable (it's cleartext, embeddable via Path B) but then it's a maintained fork, not "use stock as-is".
- ⚠️ **EMBED OF STOCK MULTIMAP BREAKS IN LIVE (2026-06-18).** First MM-Test build embedded the full 1.5 MB `liveui.multimap` inline (`embed:1`, container valid: `ampf`, `ptch==fs−0x20`, JSON re-parsed py+jq, 1142795 B `523bffbe`). Live REJECTED it: **«The Max function CreateDevice returned with error 6: Device file broken»** + Console «…MM Test.amxd: error -1 making directory» ×5. Container recipe was fine; the giant inline bpatcher payload is what Live chokes on (likely tries to extract/materialize a project dir for the embedded sub-patcher). → **Do NOT inline-embed a multi-MB stock bpatcher into a hand-built `.amxd`.** Archived broken `raw/archive/SendsFollower MM Test.2026-06-18-204320-broken-embed.amxd`.
- ✅ **REFERENCE-BY-NAME RESOLVES — native path is mechanically possible (2026-06-18).** Rebuilt MM-Test referencing the multimap **by name, NO inline patcher, NO embed**: bpatcher box `{"maxclass":"bpatcher","name":"liveui.multimap.maxpat","numinlets":1,"numoutlets":2}` (no `patcher`/`embed` keys). Two independent stock mechanisms make `liveui.multimap` resolve on EVERY install's search path: (1) **package `patchers/` dir is on the global Max search path** — PROVEN by our shipped device already loading `media/multimap-unmap.svg` from this same package by bare filename (no embed, no `can't find file`); `liveui.multimap.maxpat` sits in the sibling `patchers/` subdir → resolves identically by filename. (2) **`…/Max for Live/init/live-objectmappings.txt`** registers `max objectfile liveui.multimap liveui.multimap.maxpat;` (+ `liveui.map`) — an init objectfile mapping loaded for every M4L patch, so `liveui.multimap` is even instantiable as a bare `newobj` short-name abstraction. ⚠️ Note Cycling'74's OWN reference clipping (`clippings/Live UI Abstractions/Multi-Mapping.maxpat`) uses `embed:1` + full inline (934 KB box) — embed is their default but NOT required; the `name` field alone resolves via search-path.
- **CURRENT TEST BUILD (reference, tiny, valid):** `~/Music/Ableton/User Library/Max Devices/SendsFollower MM Test.amxd` — minimal UNFROZEN device **3471 B** (`694a6f85`, was 1.14 MB embed): `live.thisdevice` + `live.dial` (Source, 0..1, presentation) → `sig~` → **bpatcher `name="liveui.multimap.maxpat"` BY REFERENCE (no inline, no embed)** → `print MMPARAM` (tapped off multimap outlet 1 = `(list) parameter id`). Container = header(48, reused from prior valid file) + JSON(@48) + single `\x00`; NO dlst/embedded files/mdat (unfrozen). Set `ptch=mxc+16` (LE@0x1C), `mxc=len(JSON)+1` (BE@0x2C). Validated: `ptch==fs−0x20` ✓, `mxc==JSONlen+1` ✓, JSON re-parses py+jq ✓, brace-balance 0 ✓, 5 box/3 line, bpatcher has `name` + NO inline `patcher`/`embed` ✓, zero `dlst`/`dire`/`mdat`/`TEXT` leftovers ✓ → no embed-extraction trigger, so the «error -1 making directory» cause is removed. Did NOT touch working `SendsFollower.amxd` (`7ebe7d15`). **Verify in Live:** drop on Audio/MIDI track → expect native 8-row multimap UI to render with NO «Device file broken»; click a slot Map → click any Live param → captures + shows name natively. Wiggle Source dial — param won't MOVE yet (no remote~/modulate~ write layer; multimap only scales/exposes id). If it loads = reference path is REAL → next step is the per-slot write-layer integration (still needs forking inlined slots to surface per-slot scaled-signal+id, per the CATCH above). If Live still says broken even by-reference → packages `patchers/` not on a custom device's path after all → native path impractical (embed breaks, reference fails) → stay on hand-built `7ebe7d15`.
- **VERDICT:** stock `liveui.multimap` = **DISTRIBUTABLE** (zero proprietary deps). It gives the EXACT native map-UI (names/Mode·Remote/±/Range) the founder wants, for free. The work it does NOT do = writing to the mapped params; we add stock `live.remote~`/`live.modulate~` for that, but feeding them per-slot requires forking the inlined slots (expose scaled-signal+id per slot) since the stock boundary only exposes a combined id list. RECOMMENDATION: viable path forward = embed stock multimap + lightly fork the 8 inlined `LiveUI.Map` slots to surface per-slot (scaled signal, id) → our own 8× live.remote~/live.modulate~ (the legs we already build by hand). That trades our hand-built UI battles for the native render, at the cost of maintaining an internal slot-fork. Vs current hand-reconstruction `7ebe7d15` which is fully working & 100% ours. Founder's call: native pixel-perfect UI + slot-fork maintenance, vs current approximate-but-fully-owned UI.

## MM Test — diagnosis refinement (DIAGNOSIS-ONLY pass 2026-06-18) — crash cause CONFIRMED, fork point CORRECTED
- **ON-DISK STATE NOW:** `SendsFollower MM Test.amxd` in User Library = the **BROKEN embed variant** `523bffbe` (1142795 B, `embed:1` + full 1.5 MB inline multimap), IDENTICAL to archive `…204320-broken-embed.amxd`. NOT the 3471 B stub. The stub (`694a6f85`) lives only in archive `…205643-stub-userlib-preedit.amxd`. (Founder said "valid snapshot restored" — but the file on disk is the rejected embed build. Both MM Test files are MY prior test builds, not a founder session snapshot.) Working device `SendsFollower.amxd` = `7ebe7d15` (342759 B), untouched.
- **CRASH MECHANISM CONFIRMED:** `error -1 making directory` is triggered by INLINE-EMBEDDING the multi-MB multimap, NOT by referencing/using a stock internal-UI component per se. Embed payload = 43 nested `patcher` + 9 `bpatcher`/`embed` + full Max project metadata (`dependency_cache`, `subpatcher_template`, `autosave`, `fileversion`×43). On load Live tries to materialize a working/project dir for that embedded sub-patcher tree and fails (`-1 making directory`) → `CreateDevice error 6: Device file broken`. Container itself was valid (`ptch==fs−0x20`, JSON parsed py+jq). So: crash = embed-of-giant-bpatcher, NOT "any stock internal-UI instance in a custom device".
- **REFERENCE (no-embed) variant = structurally valid, crash cause removed.** Stub 3471 B: `ptch==fs−0x20` ✓, `mxc==JSONlen+1` ✓, JSON re-parses, ZERO trailing (no dlst/embed/mdat), bpatcher has `name` + NO inline `patcher`/`embed`. Searchpath proof STRONGENED: shipped `SendsFollower.amxd` already resolves `multimap-unmap.svg` (17 refs) by BARE name from this same package's `media/` → package IS on Max search path; `liveui.multimap.maxpat` is in sibling `patchers/` + registered in `init/live-objectmappings.txt` (`max objectfile liveui.multimap …`). ⚠️ STILL UNCONFIRMED on hardware: whether `patchers/` (vs `media/`) resolves for a CUSTOM device's bpatcher — needs a Live drop-test (can't drive loader from agent env). This is the ONE open empirical link.
- **CORRECTED fork point (was wrong in prior note):** per-slot scaled-signal + id ARE exposed AT THE SLOT LEVEL — inline `LiveUI.Map` slot = numout 2: **outlet1 = `(signal) scaled signal`, outlet2 = `(lom) parameter id`**. They get summed at the TOP-LEVEL multimap into `pak`/`zl sum` (resize housekeeping) and only a COMBINED id list + signal passthrough leave the multimap boundary. So to surface per-slot (signal,id) you fork the **TOP-LEVEL `liveui.multimap.maxpat`** (re-route each of the 8 slot bpatchers' outlet1/outlet2 to NEW top-level outlets) — you do NOT need to edit the 8 inline slots themselves. Less invasive than prior memory claimed. Then feed 8× our own stock `live.remote~`/`live.modulate~`.
- **Real LFO write-layer (confirmed via user's unpacked LFO Project, read-only):** `Abl.MultiMap` (7 slots) = `poly~ Abl.Map` voices + `Abl.MapUi` bpatchers. The WRITE happens in **`Abl.Map`** voice: `in~ 1 → clip~ 0. 1. → scale~ → p RemoteOrModulate → live.remote~ @normalized 1` OR `live.modulate~` (+ `p ModulationScale`, `p Mapping`, `p StoreId`, thispoly~). `Abl.*` = decompile-only, NON-distributable (encrypted in buyer's `LFO.amxd`: `ampf→meta→ciph→ptch`, entropy 7.92/8, no `mx@c`/cleartext — re-confirmed). So buyer-safe = stock `liveui.multimap` (UI+scale only) + OUR stock `live.remote~`/`live.modulate~`.
- **RECOMMENDATION (diagnosis pass):** the fully-owned hand-built mapper in `7ebe7d15` (8× live.map + live.remote~ + live.modulate~, native grab, Min/Max/±/depth, persistence, instant recompute) ALREADY DELIVERS the working "LFO-style modulation + SendsFollower" the experiment was chasing — it IS the same native `live.map` primitives the stock multimap uses, just with our own render. The MM-Test/stock-multimap route's only upside = pixel-exact native UI, at cost of (a) unconfirmed `patchers/` searchpath for custom devices, (b) a maintained top-level multimap fork, (c) Ableton renaming risk across Live versions. NOT worth abandoning `7ebe7d15`. If founder wants the exact native look: first do the cheap Live drop-test of the reference stub; only if it loads, pursue the top-level-fork route.

## MM Test — STUB REJECTION DIAGNOSED + clean reference test built (2026-06-18, drop-test pass 2)
- **Founder dropped the no-embed stub `694a6f85` (3471 B) → SAME `CreateDevice error 6: Device file broken`.** Re-read as "container/device parse failure, NOT a bpatcher-frame-resolve failure" (a frame miss would load the device with empty/red bpatcher, not reject the whole device).
- **ROOT CAUSE = the stub was never a valid M4L device, independent of the bpatcher.** Diffed its patcher vs known-good `7ebe7d15`. The stub MISSES every device-level marker M4L writes: **no `parameters`** dict, **no `project` block** (so **no `amxdtype`** — the audio/midi/instrument type tag; `7ebe7d15` has `project.amxdtype = 1633771873 = 0x61616161 = 'aaaa' = audio effect`, the same `aaaa` seen at header offset 8), **no `plugin~`/`plugout~`** audio passthrough, no `platform_compatibility`/`minimum_live_version`/`minimum_max_version`/`latency`/`is_mpe`/`openrect`/`openrectmode`/`oscreceiveudpport`/`autosave`/`saved_attribute_attributes`. PLUS it carries `subpatcher_template` (a plain-editor maxpat marker a device must NOT have) and `appversion 8.6.0` (built on a stale/foreign skeleton; real device = 9.1.4). → It's a bare maxpat wrapped in an `ampf` container, not a device. This is WHY static invariants passed (they check the CONTAINER: ptch/mx@c/JSON-parse) while Live rejected it (Live validates DEVICE semantics: parameters + project.amxdtype + audio I/O). Lesson: container-valid ≠ device-valid; a hand-built `.amxd` MUST copy the device-env keys (`project`+amxdtype, `parameters`, `plugin~`/`plugout~`, appversion 9.x) from a known-good device.
- **CLEAN DECISIVE TEST BUILT:** `~/Music/Ableton/User Library/Max Devices/MM Ref Test.amxd` (`5a3b7de1`, 5120 B, NEW name to dodge Live's path cache). Constructed from scratch with the FULL device env copied byte-faithful from `7ebe7d15`: `project` (incl. `amxdtype` aaaa), `parameters {}`, `appversion 9.1.4`, `saved_attribute_attributes`, `plugin~→plugout~` stereo passthrough, `live.thisdevice`, `live.dial` (Source param) → `sig~` → **bpatcher `name:"liveui.multimap.maxpat"` BY REFERENCE (no inline patcher, no embed)** → `print MMPARAM` off outlet 1. openrect `[0,0,332,169]`. Header skeleton from SF with recomputed ptch(LE@1C)/mx@c(BE@2C); fixed regions 0x00..0x1C & 0x20..0x2C byte-identical to SF; first byte `{`, last `\x00`. Validated: `ptch==fs−0x20` ✓, `mx@c==len(JSON)+1` (unfrozen, no dlst tail) ✓, JSON re-parses py `json.loads` + `jq -e .` ✓, has parameters+project+amxdtype+plugin~/plugout~, no `subpatcher_template`, bpatcher has `name` + NO `patcher`/`embed`. 7 box / 5 line. Did NOT touch `SendsFollower.amxd` (`7ebe7d15`) nor the old `SendsFollower MM Test.amxd`.
- ⚠️ **PREDICTABILITY CAVEAT (statics burned us once):** this test has a fully-valid DEVICE env now (copied from a loading device), so it isolates the ONE remaining variable = does a custom device's bpatcher resolve `liveui.multimap.maxpat` from the package `patchers/` dir. Two NEW risk factors found inside the multimap itself that statics can't predict: (1) it nests **8× bpatcher `LiveUI.Map.maxpat`** (CamelCase) while the on-disk sibling is `liveui.map.maxpat` (lowercase) — resolves on case-insensitive macOS FS but is fragile; (2) it carries its OWN **44 parameters** (`obj-16::obj-34` …) that must merge into the device's namespace on load. So even with a valid skeleton, by-reference load can still fail at the nested-resolve/param-merge stage. Needs the hardware drop-test to answer.
- **STILL: this is a UI-strategy test ONLY.** Even if it loads, stock multimap does NOT write to mapped params (no remote~/modulate~ inside) — the founder's actual feature (existing sender-fed encoder gets a black multi-map frame, map to any param of any device, per-slot Min/Max) is ALREADY fully built & owned in `7ebe7d15` via the same native `live.map` primitives. The reference route's only payoff = pixel-exact native render, at the cost of a maintained top-level-multimap fork (to surface per-slot signal+id) + Ableton-rename risk + the (still-unproven) custom-device `patchers/` searchpath.

## Distribution note
Manifest `https://fadercraft.com/api/sends-follower.json` + Gumroad NOT touched by me (out of scope) — deploy is a separate explicit step. Device works offline (fallback URL = library.gumroad.com) until manifest is live.
