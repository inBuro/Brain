# m4l-master — память

Дайджест. Горячие факты + карта детальных файлов. Детали — в отдельных файлах, читай по ссылке.

## Карта памяти
- [amxd-format.md](amxd-format.md) — проверенный рецепт распаковки/пересборки `.amxd`, раскладка байт, модель Max patcher JSON, Live API практики, веб-источники.
- [xl-performance.md](xl-performance.md) — факты по флагману: пути, объекты секции CC47, карта режимов CC30/CC47, текущее состояние (один срез, без журнала версий).
- [feedback-iterative-no-versionlog.md](feedback-iterative-no-versionlog.md) — при итеративной отладке НЕ вести журнал версий/wiki на каждую правку; обязателен только дата-бэкап + краткий current-state. Развёрнутая дока — на финализации/по просьбе.
- [lcxl3-daw-protocol.md](lcxl3-daw-protocol.md) — DAW-mode протокол LCXL3 + decompiled Live12 remote-script (URL structure-void); CC30 select / CC31 report, DAW-порт, relative-энкодеры. Источник истины по нативному протоколу контроллера.
- [lcxl2-mk2-feasibility.md](lcxl2-mk2-feasibility.md) — осуществимость порта Control XL на **MK2**: абсолютные потенциометры (жёсткий блокер бесшовности), 8 user-слотов, native «Template changed» SysEx `77h` (mode-detect проще MK3), отличия `.syx`. Читать при вопросах о поддержке MK2/MK1.
- [controlxl-project-map.md](controlxl-project-map.md) — 6 device-слотов Control XL + инвариант байт-в-байт, эталон md5, zip-деливераблы, процедура правки. ⚠️ Правки теперь в User Library (слот 2); пропагация дальше — только по команде. Читать при любой правке/пропагации.
- [sends-reader.md](sends-reader.md) — **SendsReader.amxd** — форк Sends Follower с ИНВЕРТИРОВАННОЙ семантикой (стоит на ОБЫЧНОМ треке, читает ОДИН send-кноб СВОЕГО трека → 8-слотовый маппер). **v1.3 (2026-06-18, CURRENT):** переключатель Sum/Max УДАЛЁН СОВСЕМ (был холостой с v1.2 — один send, агрегировать нечего). Чистое удаление, БЕЗ перекомпоновки. Из патча убраны **5 box + 4 line**: `agg_mode`(live.tab), `agg_prepend`(`prepend aggmode`), `agg_loadbang`(loadbang), `agg_delay`(`delay 300` @pat y648 — НЕ SF-наследие `obj-6 delay 300` @y1120, оно цело), `mode_label`(comment «Mode»); линии `agg_mode→agg_prepend`, `agg_prepend→obj-46[0]`, `agg_loadbang→agg_delay`, `agg_delay→agg_mode`. obj-46(js) inlet0 сохранил 4 фида (mode_prepend/obj-35/sr_restprep/sr_init), 0 висячих. JS: вырезаны `AGG_SUM/AGG_MAX`, `aggMode`, `aggmode()`, doc-блок про агрегацию, строка `aggmode <0|1>` в списке сообщений, idle-коммент в `bang()`; `bang()`+persist/restore (id+name, без «all») + observers НЕ тронуты. Левая колонка: send_menu `[5,2,46,14]` → version_link `[54,4,70,14]` → dial `[5,16,116,116]` → status_label `[5,151,118,15]`; пустая полоса y132..151 оставлена пустой (ничего не двигал). md5 `9c0386ab…`, 118474 B (size unchanged, Path A pad 2840), **182/190**, JS `sends_reader.js` `0716dff3…` 16993B (node --check clean), `sr_version_check.js` DEVICE_VERSION `1.3` (`dbb3dda0…` 3102B). Архив `…-v1.3.amxd`, pre-edit `…-193718-v1.2-preedit.amxd`. ⚠️ JSON начинается с offset **32** (после ptch), конец = первый `\x00`@EOF; `48` из amxd-format = frozen mx@c layout, НЕ этот UNFROZEN девайс. (v1.2 был тот же меню-без-All + холостой Sum/Max 1:1 как SF, md5 `74a7d1e6…` 187/194 JS `acaeed47…` 18640B; v1.1 меню `A B C … All` + реальный агрегат `840340c4…` 20953B; v1.0 183/190 `2f78a8d0…`.) openrect `[0,0,328,169]` (MAX y+h=166, x+w=320). **UNFROZEN** (внешние `sends_reader.js`+`sr_version_check.js` рядом; repack = `d[:0x20]`+JSON(от 0x20)+`\x00`@EOF, fix ptch). Переиспользовано без правок: 8-слот live.map маппер, диал, шина `---max_send`+downstream, version-check, init+33ms поллер, detectHost (return/master→N/A value 0), чтение `<track> mixer_device sends M value`, анти-feedback `warn 1` при выборе своего же send, буквенное меню (один send). SF-девайс трогать НЕЛЬЗЯ (визуальный референс; Live сам его пересохраняет в рантайме — это НЕ моя правка). Правка в User Library, архив+raw `~/Brain/Sends Reader/`, wiki `~/Brain/Sends Reader/wiki/`. Открыто: FREEZE перед раздачей, hw-тест, deploy манифеста (`latest:"1.3"`). Читать перед любой правкой SendsReader.
- [sends-follower.md](sends-follower.md) — SendsFollower.amxd: пути, dlst dire-структура, version-check + Follow Mode (Peak/Total) + self-healing return-index (observers) + 8-СЛОТОВЫЙ маппер. **± PER-STATE + INSTANT RANGE RECOMPUTE 2026-06-18 (CURRENT md5 `7ebe7d15`, 342759 B, 444/594, frozen, PURE PATCHER):** (1) `bipol_N` показывает ОДНУ полярность по state: `text="+"`(off/state0=positive depth), `texton="−"`(on/state1=negative, U+2212) — не «±» оба; форма/teal/borders целы, `automation`/`on` не трогал; все 8; attr-only. (2) Правка границ применяется МГНОВЕННО без движения follow: ОДИН общий `followf`(`f`, хранит follow от obj-7 в ПРАВЫЙ inlet тихо) + per-slot ×4 `t b l` (32) на min/max/depth/± — сначала ставит bound/k (outlet1=`l`→math), затем бангает followf (outlet0=`b`)→`followf[0]` веером в 16 `scale_N[0]`+`modscale_N[0]`→пересчёт→sig/modsig→remote~/modulate~ СРАЗУ. Работает при СТАТИЧНОМ follow (followf держит last, default 0=floor) и в ОБОИХ режимах. Обычный follow цел (obj-7 гонит левые inlets напрямую; followf пассивный store, без double-fire). idtrig/`_persistence`/LFO/Peak-Total/observers целы. +33 box(1+32)/+81 line. openrect без изм., новые объекты БЕЗ presentation (x≥1500), max(y+h)=166. JS/svg/version byte-identical. Path B: JSON 294030→314387 (dL+20357), of32 svg 314404/vc 314939/sf 318045, mxc=342287, ptch=342727=fs−0x20, нет trailing mdat. Архив `…194608-bipol-instant-frozen.amxd`, pre-edit `…194608-preedit-bipol-instant.amxd`. ⚠️ CRAFT: `scale`/`*` НЕ переоутпутят при смене ТОЛЬКО правого inlet → для применения на лету при статичном источнике храни источник в `f`(правый inlet) и ребангай `f`(левый) после установки bound; порядок через `t b l`. **(прежняя 3 COLOR EDITS md5 `308ada9c`, 322402 B, 411/513, frozen, COLOR-ONLY — box/line без изм.):** (1) Mode-тоггл `map_mode_0..7` ВСЕГДА залит two-color: Remote(off)=ОРАНЖ `bgcolor`/`activebgcolor` `[1,0.7098,0.196,1]`, Mod(on)=ГОЛУБОЙ `bgoncolor`(ДОБАВЛЕН)/`activebgoncolor` #03C3D5, текст ТЁМНЫЙ `[0.117,0.117,0.117,1]` оба (textcolor/textoffcolor/textoncolor/activetextcolor/activetextoncolor)+регистр saved_attribute_attributes. (2) Имя в Map ЧИТАЕМОЕ — **компромисс: ОРАНЖ текст в ОБОИХ состояниях, БЕЗ оранж заливки.** ⚠️ ROOT: Max рисует live.* в ВЕРХНЕМ слое поверх plain-объектов → оранж ЗАЛИВКА live.text map_btn (mapped, α1) перекрывала `mapcom`-comment → тёмный текст тонул=невидимо. live.text-label мёртв + comment не ляжет поверх live.text-заливки → референс «тёмный текст на оранж заливке» НЕДОСТИЖИМ. Фикс: mapped color-msg (obj-54/59/64/69/74/79/84/89) bg α 1→0 (рамка без заливки) + comment ОРАНЖ оба (comdark_N перенаправлен dark→orange, mapcom default orange). Имя/«Map» всегда оранж на тёмном LCD=видно. (3) Min/Max `slot_min/max_N` textcolor teal→ОРАНЖ; depth_N оставлен teal. Path B: JSON 288878→294030 (dL+5152), of32 svg 294047/vc 294582/sends_follower 297688, mxc=321738, ptch=322370=fs−0x20, нет trailing mdat. JS `53bdbfbd`/`a5d905fc` byte-identical. Архив `…193524-3color-frozen.amxd`, pre-edit `…193239-preedit-3color`. ⚠️ CRAFT: live.* рендерятся ВЫШЕ comment/panel независимо от box-index → comment поверх live.text-кнопки требует кнопку БЕЗ заливки (frame-only). **(прежняя NAME-VIA-COMMENT + MODE FONT md5 `d3b83238`, 317250 B, 411/513):** имя цели в Map-кнопке наконец рисуется (4-я попытка) — **live.text ЯРЛЫК БРОШЕН** (`set`/`text`/`texton`/`tosymbol`+`text $1` — ВСЕ 3 прежних провалились на железе). Решение: на каждый слот ПОВЕРХ кнопки лёг **Max `comment`-оверлей** `mapcom_N` (presentation_rect == map_btn, `ignoreclick 1`=клик проходит к live.text-рамке под ним, center, font 9, default «Map»); маршрут `livemap_N[2](name)→namesub_N(substitute <none> Map)→nameset_N(prepend set)→mapcom_N` (comment `set` ОБНОВЛЯЕТ текст, многословные ок). Доказательство что имя приходит: `livemap out2` веером кормит И `xcmp_N`(X-видимость, работает) И `namesub_N`(comment). `map_btn_N` text/texton ОПУСТОШЕНЫ (рамка-фрейм, fill-цепочка цела). Цвет comment динамический по состоянию через тот же `sel 0 1`: mapped→тёмный, unmapped→оранжевый (`comdark_N`/`comorng_N` msg `textcolor …`). Удалены мёртвые `tosymbol_N`+`namemsg_N`. Mode-тоггл шрифт 11→9 (numbox/метки НЕ трогал). +16 box/+24 line. openrect `[0,0,332,169]`, max(y+h)=166. JS `53bdbfbd` byte-identical (node-check clean). Pre-edit `f236f42d` архив `…191851-preedit-name-comment-font`, frozen `…192423-name-comment-font-frozen`. ⚠️ **DEFINITIVE CRAFT: live.text-ярлык в рантайме = ТУПИК; для произвольного текста над кнопкой клади `comment`-оверлей с `ignoreclick 1` + `prepend set`, а live.text оставляй пустой цветной рамкой.** **(прежняя 2 REAL-BUG FIXES + FONT md5 `f236f42d`, 306283 B, 395/489:** (BUG#1 имя в Map-кнопке НАКОНЕЦ видно) `prepend text`/`prepend texton` ОДНИХ не хватало (на железе пусто) → повторил стоковый `p SetName` точь-в-точь: `livemap_N[2]→namesub_N(substitute <none> Map)→tosymbol_N(атомы→1 символ, спейсы целы)→namemsg_N(message `text $1, texton $1`)→map_btn_N`; корень = нужен ОДИН символ («Fade In» ломал prepend) + `$1`-форма (перерисовка); старые prepend-объекты удалены. (BUG#2 Mod/Remote-нога включается на map-захвате БЕЗ тоггла Mode) корень: `set <id>` в message-register `idreg_N` ХРАНИТ молча, не выдаёт → ногу id не получал пока register не забангают (бангал только Mode-свитч); фикс `idtrig_N`(`t b l`) после `livemap_N[1]`: out1(`l`,первым)→set register, out0(`b`,вторым)→bang→gate→активная нога СРАЗУ; load через `_persistence`. Шрифт: live.text 9→11, numbox 8→10, метки 11 (высоты не росли, max y+h=166). +8 box/+8 line, openrect без изм. ⚠️ REUSABLE: live.text рантайм-ярлык = `tosymbol`+`text $1, texton $1` (НЕ голый prepend text на списке); captured-id в `set`-register НАДО банговать иначе нога мертва. Pre-edit `c35eb20b` архив `…171903-preedit-2bugs-font`, frozen `…172059-2bugs-font-frozen`. **(прежняя 4 VISUAL EDITS `c35eb20b` 301300B 387/481:** (1) имя через `prepend text`+`prepend texton` [НА ЖЕЛЕЗЕ НЕ РАБОТАЛО, фикс выше] (live.text игнорит `set` — был баг `nameset_N prepend set`; теперь стоково обновляет off/on-ярлык) + fill-состояние (`xcmp_N`→`colsel_N sel 0 1`: mapped→orange α1 + dark text, unmapped→orange α0 outline + orange text, default «Map»); (2) Mode `map_mode_N activebgoncolor`→teal #03C3D5 `[0.011765,0.764706,0.835294,1]`; (3) `bipol_N`→квадрат 15×15 глиф «±» (text+texton), teal (enum +/- под капотом цел); (4) `slot_min/max/depth`→`parameter_unitstyle:5`(«%»)+прозрачный фон (bgcolor/activebgcolor/bordercolor/tricolor/tribordercolor/activetricolor α0)+teal цифры. +32 box/+56 line. ⚠️ **Live re-save вшил СТАРЫЙ `sends_follower.js` `29ff530b` (5808B, без observers/map) → RESTORED canon `53bdbfbd` (24242B)**; всегда проверять md5 встроенного JS после Live-сейва + `prepend text/texton` для рантайм-ярлыка live.text (НЕ `set`). openrect `[0,0,332,169]` (без изм.), max(y+h)=166. Юзер удалил 4 хедер-коммента (hdr_mode/hdr_range/sub_min/sub_max) + поднял ряды (y29→y5.9) — СОХРАНЕНО. Frozen `…170149-4visual-frozen.amxd`, pre-edit `…165808-preedit-4visual-ondisk.amxd`. **Прежняя MAP UI RECON (md5 `8a9203dd`, 246661 B, 359/425):** name-в-кнопке Map (live.map name→`substitute <none> Map`→`prepend set`→map_btn, стоковый механизм) + чёрная LCD-подложка (`panel` themecolor.live_lcd_bg, FIRST box=behind) + Mode=Remote↔Mod enum live.text toggle (стоковый Abl.MapUi obj-2, teal) с условным Range (Remote→Min/Max, Mod→±/depth, show/hide через thispatcher `script show/hide`) + ИСТИННАЯ Mod-модуляция через `live.modulate~` (портативный external, аддитивный signed offset, не overwrite как live.remote~); id по режиму через `gate 2 1` (line order=1 control до order=2 re-emit). JS НЕ тронут (`53bdbfbd`). ⚠️ База была UNFROZEN (Live re-save: JSON с 0x20, нет dlst — парсить JS=0x20). User-coords сохранены (version_link/mode_label/follow_mode подвинуты юзером). Архив `…164207-mapui-recon-frozen.amxd`. **Прошлая MAP FIX 2026-06-18: arm через `selected_parameter`-observer НЕ РАБОТАЛ (ненадёжный hook — клики по mixer-парам не обновляют его) → заменён нативным `live.map @strict 1` (стоковый LFO Map-объект из `liveui.map.maxpat`): Map→`prepend mapping`→live.map→клик любого парам→out1 `id n`→remote[1] (без live.path), out2 name→`zl compare <none>`→`hidden $1`→X (показ только при маппинге), out3 state→кнопка; `_persistence 1` = нативный reload-persist (убрал pattr/restorepath/live.path/sr_N); JS НЕ тронут (slot-функции теперь dead code, obj-46 out1 не подключён).** UI per founder: без «%», без столбца Parameter, X условный (hidden:1 по умолчанию), порядок Map(130)|X(174)|Mode(192)|Min(240)|Max(282); openrect `[0,0,328,169]`, max(y+h)=166≤167. Девайс **frozen + самодостаточен** (md5 `fb0d2975…`, 144944 B, **190/193**): `sends_follower.js` (`53bdbfbd…`, 24242 B) + `sf_version_check.js` (`a5d905fc`, 3106 B) + `multimap-unmap.svg` (535 B) вшиты. ⚠️ для «map любого Live-парама с лица M4L-девайса» используй `live.map`, НЕ `selected_parameter`-observer. ⚠️ Path-B prefix = `d[:48]`; `mx@c=(dlst_pos+16)−0x30`; нет trailing `mdat`-чанка (файл кончается на dlst). Правка в User Library `~/Music/Ableton/User Library/Max Devices/SendsFollower.amxd`, архив `~/Brain/Sends Follower/raw/archive/`. Проектная wiki = `~/Brain/Sends Follower/wiki/` (docs English, Vale). Читать перед любой правкой SendsFollower.

## Конвенции (железно)
- **Где редактировать Control XL (РАЗВОРОТ 2026-06-06 (8)):** основной редактируемый артефакт = **User Library** `.amxd` (`~/Music/Ableton/User Library/Max Devices/Control XL.amxd`). Прежнее правило «edit canon in project repo, User Library pristine/deploy-only» — **ОТМЕНЕНО**. Пропагация дальше (репо-эталон, бандл-слоты 3–6, zip) — **только по явной команде** «копируй дальше»/«пропагируй». Детали: [controlxl-project-map.md](controlxl-project-map.md), [xl-performance.md](xl-performance.md).
  - **Все 6 слотов Control XL = `28840e394da60839f71c33c39e0922ec`** (212123 B, 271 box / 413 line) с 2026-06-10 (IV): вернули диагностический `maxApi.post` (статус апдейт-детекта + `url=` в Max Console) в `version_check.js` (vc 2933→3100, Путь B по хвосту, JSON-патчер не тронут); оба зипа пересобраны. Прежние `edd4bf55` (III, click-fix v2, без post) / `211ed6f8` (без post, mdat-вариант) — заменены. Сохранены server-controlled update URL + ФИКС клика «Update ready» (v2 silent-store: `vlink_store`=message + `vlink_prepend`=`prepend set`, оба источника url + клик в inlet 0, Console чистый). «New Version» открывает `url` из `version.json` (fallback `library.gumroad.com`). ⚠️ dist-раскладка: Router-слот = `Control XL.amxd` в КОРНЕ dist-проекта. Грабли zip: `zip` падает молча при записи в `~/Brain` (sandbox) → собирать `zip -r -X` в `/tmp`, потом `cp`; `ditto --sequesterRsrc` плодит `__MACOSX`/`._*` junk — НЕ использовать. Механизм/бэкапы — [controlxl-project-map.md](controlxl-project-map.md), [xl-performance.md](xl-performance.md).
  - ⚠️ **Browser Load (CC51)** — ОТЛОЖЕН (нет `browser` в M4L LiveAPI → только Python remote-script). **Hardware feedback** (явный midiout / port-selector) — ОТЛОЖЕН в следующую версию как KNOWN BUG: фидбэк гибнет, если после девайса в цепи стоит audio-девайс. WIP-бэкапы 154534/160053/161926 в archive — материал для следующей версии. Детали обеих фич — [xl-performance.md](xl-performance.md).
- **Архив до правки.** Текущий девайс → `raw/archive/<Name>.YYYY-MM-DD.amxd` (если занято — `-HHMMSS`). Для XL_Performance это `~/Brain/Fadercraft/raw/archive/`. **Pre-edit backup обязателен ВСЕГДА.** Никогда не перезаписывать архив.
- **Правка на месте.** Меняем сам `.amxd`, не шлём куски кода, не просим руками патчить в Max.
- **Валидация перед подменой.** Re-parse JSON, счётчики boxes/lines, хвост байт-в-байт, инварианты размеров. Не сошлось — откат к архиву.
- **Перезагрузка в Live.** Live кеширует — убрать/добавить девайс заново; Max-редактор закрывать без сохранения.
- **⚠️ КООРДИНАТЫ ПОЛЬЗОВАТЕЛЯ НЕПРИКОСНОВЕННЫ (правило founder 2026-06-18).** Если видишь, что координаты/позиции элементов (`presentation_rect`, patching-позиции) внутри девайса изменились **после твоей последней сборки** — это сделал ПОЛЬЗОВАТЕЛЬ руками из эстетики, и **эта координата — верная**. Возвращать элементы на исходные (твои прежние) места **ЗАПРЕЩЕНО**. Любую правку базируй на ТЕКУЩЕМ on-disk файле (с его правками), сохраняй его позиции; меняй только то, что прямо просили (стиль/функцию/новые элементы). Отличай его осознанные правки координат от авто-сериализации Live (без смены позиций). Это про любой девайс, не только SendsFollower. Дублируется в памяти ассистента как `feedback_dont_touch_user_edited_shapes`.

## Где что лежит
- Девайсы: `/Users/Kirill/Music/Ableton/User Library/Max Devices/` (`Control XL.amxd`, `SendsFollower.amxd`).
- Архив: `~/Brain/Fadercraft/raw/archive/` (не рядом с девайсом).
- `.syx` кастом-моды (канон): `~/Projects/Claude/Fadercraft/custom-modes/` (`1.syx`…`15.syx`).
- Wiki проекта (логика, форматы): `~/Brain/Fadercraft/wiki/concepts/` и `…/entities/`.

## Быстрые факты формата
- `.amxd` = контейнер `ampf`: бинарный заголовок → JSON-патчер (с офсета **48**, до первого `\x00`) → встроенные JS/файлы → метачанки с checksum (`mdat`, Live её игнорит при загрузке).
- Два поля размера зависят от длины JSON: `ptch` (u32 **LE** @`0x1C`) и `mx@c`-data (u32 **BE** @`0x2C`). При изменении JSON оба += ΔL. Инвариант: `ptch == filesize − 0x20`.
- Хвост (suffix от `\x00`) при правке сохранять **байт-в-байт**.

## Текущее состояние домена
XL_Performance: 2026-06-02 добавлен round-trip для кастом-мода 15 (QUE/prelisten). Подробности и карта объектов — в [xl-performance.md](xl-performance.md).

⚠️ **Файл девайса переименован 2026-06-02:** `XL_Performance.amxd` → **`Control XL.amxd`** (везде: канон в User Library, бандлы в `dist/`, ссылки в `.als`). **Внутреннее имя Max-патча всё ещё «XL_Performance»** — переименован только файл. Если нужно переименовать и внутренний патч (чтобы на дорожке Live отображалось «Control XL») — отдельная правка. При работе с девайсом теперь это `Control XL.amxd`.

## TODO для следующей сборки девайса
- **Лейбл «Bank fx» → «Bank»** (UI-надпись в MIXER-секции; на сайте-мокапе уже «Bank», приводим девайс к нему). Запрошено пользователем 2026-06-02.
